# LDA 统一实施规划 v1（产品级基准库 → 系统类型 → 创新超市）

> 整合三轮讨论（2026-08-27）：产品级基准库（已落地 v0.8.32）+ 系统类型 + 创新超市。
> 统一逻辑：**用"已锚定的确定性"去覆盖"还没被验证的地带"**。

## 一、战略主线（一句话）

贯穿三轮讨论的统一逻辑是"用已锚定的确定性覆盖未验证地带"，由两条正交轴构成系统级：

- **轴 1 · 物理域**（`domain`）：光子 `photon` / 量子 `quantum` / 混合 `hybrid`
- **轴 2 · 功能原型 / 系统类型**：链路、WDM 解复用、量子保真度链、混合巨型系统……

所有层共享同一条死标量红线：**LLM 只生成候选，不进判决路径**；PASS/FAIL 由死标量比对决定。

## 二、三层结构

### Layer 0 · 产品级基准对照库（已完成 v0.8.32）
- `lda/lda_l2/golden_product_benchmarks.py` + `run_golden_product_smoke.py`
- 5 条标杆覆盖 5 个 loss 引擎（MMI/Grating/Crossing/Y-branch/SiN-PL），每条带**出处 + 死标量 tol + LDA 独立复现**
- CI core 65→66；三端（本地/Gitee/GitHub）+ 生产（115.191.20.92）全同步，HEAD `9bd3d41`

### Layer 1 · 提案编译器系统类型（Phase 1，本规划，目标 v0.8.33）
- `proposal_compiler.py` 增 `SYSTEM_TYPES` 注册表：`link`(默认) + `wdm_demux` + `quantum_fidelity`
- `link` 走原闭环（零回归）；`wdm_demux` / `quantum_fidelity` **直接复用** `wdm_system.design_wdm_advanced` / `multiqubit_fidelity.design_multiqubit_fidelity` 已验证闭环
- `design_pipeline(req, system_type="link")` 增 `system_type` 参数，向后兼容
- 新增 `run_system_types_smoke.py`，CI core 66→67
- 诚实边界：wdm/quantum 复用已验证闭环，自带 B4 / D-46×D-47 死标量判决，零新物理

### Layer 2 · 创新超市货架注册表（Phase 2，本规划，目标 v0.8.34）
- `lda/lda_l2/innovation_market.py`：`ShelfItem` 注册表
  - 字段：`id` / `title` / `target_app` / `signal_ref`（公开 roadmap / PDK / 标准草案，可溯源）/ `domain` / `composition`（已锚定基元 id 列表 + `system_type` 引用）/ `default_geom` / `honest_tier=前瞻预研` / `ci_status`
- 每个货架 = 预填的 `system_type` 实例（组合已锚定基元 + 公开信号驱动）
- 判决仍走 `screen_proposal` 死标量引擎：基元锚 + 系统预算（S1/S4）不破
- `run_innovation_market_smoke.py` 三守护：① 可分解到已锚定基元；② 可行域预算非负；③ `honest_tier` 必填 = 前瞻预研
- `docs/innovation_market.md` 可浏览目录（"创新超市"页面）
- CI core +1，版本 bump 0.8.34

## 三、红线下护栏（全层统一，不可破）

1. **LLM 只生成候选拓扑/默认参数，绝不写判决逻辑**
2. **判决 = 基元锚（物理/实证）+ 系统预算（S1/S4）不破**
3. **创新超市货架必带 `honest_tier=前瞻预研`，CI 绝不输出"已流片验证"**
4. **信号源须可溯源**（roadmap 文献 / 标准草案），标注动机出处
5. **落点全程 A/B 阶段，不碰 C 闸门**（无需外部资源）

## 四、版本与同步纪律

- 每个 Phase 一个 minor bump（0.8.33 / 0.8.34）
- **计数守护**：README 顶行「当前账本：…CI core N 条」↔ `len(CORE_SMOKES)` 严格一致（v0.8.30 加固，防历史链污染）
- 三端（本地/Gitee/GitHub）+ 生产（`/api/health` 校验 version）全同步

## 五、实施顺序与依赖

1. **Phase 1（系统类型）先落地** —— wdm/quantum 直接复用已验证闭环，几乎零新物理，复利密度最高
2. **Phase 2（创新超市）依赖 Phase 1** —— 货架 `composition` 引用 Phase 1 的 `system_type` 名
3. 目录页 / 诚实边界 / 记忆固化贯穿始终

## 六、首批货架候选（Phase 2，待杜先生拍板）

- CPO 共封装光学多通道 WDM 预设计（复用 `wdm_demux`）
- 量子芯片间互联模板（复用 `quantum_fidelity`）
- （可选）片上激光源集成 / 传感前端（陀螺·气体）预设计

## 七、风险与边界

- **范围蠕变**：禁止 AI 发明新物理；任何含未锚定基元的货架 CI 直接拒
- **信号质量**：roadmap ≠ 现实，目录须注明"前瞻非承诺"
- **对外文案**：公众号/B 素材须写"前瞻预研，非流片验证"

## 八、已确认收口事实（截至 2026-08-27）

- v0.8.32 全链路（三端+生产）已同步，HEAD `9bd3d41`
- CI core 66 条；生产 `/api/health` 报 `version=0.8.32`（benchmarks=45, pdks=5, layers_built=8）
- 阶段路线图：A 毕 · B 进行中（生态播种）· C 三信号锁（全空，维持原策略）
