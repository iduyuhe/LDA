# LDA 独立率冲刺 · 路径 B-11 扩基报告

- **日期**：2026-09-17
- **版本**：v0.9.91（✅ **已于 2026-09-17 部署生产**；本行属部署后回填订正，原记录为「本地 · 未部署」）
- **批次**：Batch B-11（B185-B200，16 道严格独立锚）
- **冲刺腿**：腿①「扩基加锚」——纯内部确定性扩基，继续稀释 terminal Tier-1 权重，**缓抬独立率天花板**
- **前置闸门**：**同源体检**（批次第一道闸；本批**继续实际 grep 全仓**核验占用）
- **结论**：独立率 **89.6%（181/202）→ 90.4%（197/218）**；天花板 **94.1% → 94.5%**；零物理判据改动、零 tol 放宽、零判决行为改动、零新增 CI smoke（CI core 仍 178 条）。

---

## 1. 目标与依据

用户指令（2026-09-17）：「**选 B-11（腿①续加锚稀释 terminal，仍最稳）**」。

- terminal Tier-1 = **12 道**（B17/B18/S1-S6/S9-S12），按设计永不升，独立率上限 = (N−12)/N。
- 加 16 道 ⇒ (218−12)/218 = **94.4954% ≈ 94.5%**（B-10 为 (202−12)/202 = 94.1%）。
- 关键纪律：**only 真 strict 才抬指标**。新锚若落成 `self_certified`，会被棘轮 `MAX_SELF_CERTIFIED=18`（只减不增）直接拦死。故本批 16 道全部按「精确闭式/特殊函数 golden × 方法学不同源真实数值法」范式构造，逐道过 C1–C5 + 判据 D。

---

## 2. 🔴 同源体检（批次第一道闸）

### 2.1 闸门定义

**新族 golden 必须与既有 202 道锚的「方程 / 本征值问题 / 数值特殊函数」不同源**，否则只是旧锚换名字（分母虚胖、方法学零增量）。三条同源红线：

1. **数值同源**——同一特殊函数零点/取值（Bessel J₀ 零点、Airy 零点、球 Bessel 零点、`erfc`（B30）、`ellipe/ellipk/fresnel/mathieu_a/mathieu_b`（B-10）已被占用）；
2. **结构同源**——同一算子的换语境（Landau 能级 ≡ 1D 平移谐振子）；
3. **golden 本身是截断近似式**——残差被 golden 误差主导 ⇒ 判据 D 不成立。

### 2.2 机械盘点（**实际 grep 全仓**，非凭记忆）

对既有锚代码库执行占用扫描（`lda/**/*.py`，词边界精确式）：

| 检索式 | 命中 | 结论 |
|---|---|---|
| `voigt\|wofz\|faddeeva` | **No matches**（B-11 前） | Voigt / Faddeeva **未占用** |
| `special\.zeta` | **No matches**（B-11 前） | Riemann ζ **未占用** |
| `kepler` | **No matches**（B-11 前） | 中心力轨道 ODE **未占用** |
| `view_factor\|角系数` | **No matches**（B-11 前） | 辐射传热角系数 **未占用** |
| `mathieu` | 仅 B-10（v0.9.90） | 已占用（故本批不用） |

⇒ 本批四族与既有 202 道**均不同源**（特殊函数类 / 方程类 / 物理域三个维度全新增）。

### 2.3 被否候选（体检记录）

| 候选族 | 处置 | 依据 |
|---|---|---|
| **Airy 函数 / 三角势阱**（复现 B-6 族） | ❌ 否 | **数值同源**：Airy 零点已被 B-6（B115-B117）占用。 |
| **关联 Legendre 方程**（复现 B-6 族） | ❌ 否 | **数值同源**：已被 B105-B110 占用。 |
| **1D 波动方程 leapfrog 时间推进** | ❌ 否 | **结构同源**：与主权 `fdtd1d` 同族（同为 Yee/leapfrog）。 |
| **1D 稳态传导 + 三对角 FDM** | ❌ 否 | **结构同源**：与 B29（热光鳍稳态椭圆 ODE）撞。 |
| **光栅 / 单缝衍射 FFT** | ❌ 否 | **数值同源**：FFT 谱族已被 B14/B15 占用。 |
| **Kelvin ber/bei 函数** | ❌ 否 | **数值同源**：属 Bessel 族（B-5 圆波导已占）。 |
| **开普勒方程数值求根 E−e·sinE=M** | ❌ 否 | **红③**：golden 若取截断级数即违约；且该式为**代数恒等**改写 ⇒ 判据 D 不响应。⇒ 改**轨道时间积分**（真 ODE 数值法）。 |

### 2.4 通过体检的四族

| 族 | 新意在何处（与既有 202 道均不同源） |
|---|---|
| **A. 量子统计积分与 ζ 函数**（玻色/费米/德拜矩） | **全新特殊函数类**——`Γ(s)ζ(s)`（+ 费米 `(1−2^{1−s})` 因子）在全仓**零占用**；物理为量子统计态密度矩。 |
| **B. 经典二体轨道（Kepler）** | **全新方程类**——中心力轨道 ODE（`r̈ = −μr̂/r²`）在全仓**零占用**；既有锚无任何时间域 ODE 积分。 |
| **C. 辐射传热角系数（view factor）** | **全新物理域**——辐射换热几何因子在全仓**零占用**；既有锚无任何传热辐射类物理量。 |
| **D. Voigt 谱线轮廓** | **全新特殊函数类**——`voigt_profile`（= Re[w(z)]/(σ√(2π))，Faddeeva/wofz）在全仓**零占用**；与 B30 的实自变量 `erfc` 不同（复自变量卷积、物理为光谱线型）。 |

---

## 3. 设计范式（同 B-1..B-10）

每道锚 = 一个**确定性解析闭式/特殊函数 golden** 对拍 一个**方法学不同源的真实数值法**：

- 残差 = **离散化误差**（持久、随参数变化、判据 D 响应、反向扰动 FAIL）；
- **不是**代数恒等（B28 血案 / B-10 Mathieu-FG 血案）、**不是**纯数值误差沉底（B10 血案）；
- 数值核纯 numpy/scipy（C 级自主，零商业依赖）：`lda/lda_harness/_batch_b11_numeric.py`（纯 LF · 31177 B）。

🔴 **本批前置纪律（承接 B-5..B-10 血案 + 本批新血案见 §6）**

1. 全批**弃用** `scipy.linalg.eigh(subset_by_index=...)`（B-6 off-by-one 血案）——本批不涉及大矩阵本征，族 C 走**分块张量积**避免内存墙。
2. **弃用 ARPACK `eigsh`**（B-9 血案）。
3. **`default_params` 键集必须 ⊆ golden 形参集**（B-7 血案）——本批并进一步要求「**零响应键必须退出 `default_params`**」（本批血案 8）。
4. **数值求积的 `n` 必须落在离散误差主导区**（B-10 血案 3）——本批 3 道因定档撞噪声地板被下调（血案 9/11）。
5. **观测取守恒量会使残差落机器精度地板**（本批血案 1）——残差必须由**非守恒量**（如近心点 r_p、线心值）承载。
6. **逐锚 tol 收紧**（本批血案 10）——族 C/D 指标动态范围仅 ~0.07–0.29，统一 tol=0.01 会淹没 10% 反向扰动信号。

---

## 4. 四族物理锚（B185-B200）

| 锚 | 物理对象 | golden（解析闭式/特殊函数） | 候选（数值法） | tol |
|---|---|---|---|---|
| B185 | 玻色积分矩 s=4 | `Γ(4)ζ(4)=π⁴/15` | x→t=x/(1+x) 换元后 `[0,1]` **复合 Simpson**（n=64） | 0.01 |
| B186 | 玻色积分矩 s=3 | `Γ(3)ζ(3)=2ζ(3)` | 同上（n=64） | 0.01 |
| B187 | 费米积分矩 s=3 | `(1−2^{1−s})Γ(s)ζ(s)=(3/2)ζ(3)` | 同上（n=64） | 0.01 |
| B188 | 德拜积分矩 s=5 | `Γ(5)ζ(4)=4π⁴/15` | 同上（n=64） | 0.01 |
| B189 | 椭圆轨道周期（a=1, e=0.6） | `T=2π√(a³/μ)`（μ=1） | **RK4 时间积分** + 三次 Hermite 穿越定位 + 二分细化（dt=0.008） | 0.01 |
| B190 | 近心点半径（a=1, e=0.6） | `r_p=a(1−e)=0.4` | 同上（观测非守恒量 r_p） | 0.01 |
| B191 | 近心点速率（a=1, e=0.6） | `v_p=√(μ(2/r_p−1/a))=2` | 同上 | 0.01 |
| B192 | 椭圆轨道周期（a=2, e=0.3） | `T=2π√(a³/μ)=17.7715…` | 同上（dt=0.008） | 0.01 |
| B193 | 平行同轴等尺寸矩形角系数（a=b=c=1） | 解析闭式 `F₁₂`（含对数/反正切） | `F₁₂=(1/A₁)∫∫cosθ₁cosθ₂/(πR²)dA₂dA₁` 的 **4D 张量积复合 Simpson** | 0.001 |
| B194 | 平行同轴矩形角系数（a=1, b=2, c=1） | 解析闭式 | 同上 | 0.001 |
| B195 | 垂直共边矩形角系数（A=B=1） | 解析闭式 | **4D 分块累加**复合 Simpson（n=96，逐片 (n+1)³ + `einsum`） | 0.005 |
| B196 | 垂直共边矩形角系数（A=2, B=1） | 解析闭式 | 同上 | 0.005 |
| B197 | Voigt 线心值（σ=1, γ=1, x=0） | `scipy.special.voigt_profile` 机器精度 | 卷积定义式 + **解析 sinh 换元** `τ=x+γ·sinh u` + 复合 Simpson（n=48） | 0.002 |
| B198 | Voigt 远翼值（σ=1, γ=0.5, x=2） | 同上 | 同上（n=24） | 0.002 |
| B199 | 洛伦兹主导 Voigt 线心（σ=0.5, γ=2, x=0） | 同上 | 同上（n=32） | 0.001 |
| B200 | Voigt 深远翼（σ=2, γ=0.5, x=3） | 同上 | 同上（n=64） | 0.001 |

**接线四件套**（同 B-5..B-10）：

1. `lda/lda_harness/benchmarks.py`：import + B185-B200 字典 + `BENCHMARK_ORDER` 追加（169976 → 180337 chars）；
2. `lda/lda_harness/golden.py`：import + `_GOLDEN_DISPATCH` + `_PHYSICAL_LAW`（**双表同登铁律**；39051 → 40133 chars）；
3. `lda/lda_harness/verification_adapters.py`：`_get_batch_b11()` 双路兜底 + 16× `@_register_candidate`（body 一律 `float()` 包裹；145663 → 151530 chars）；
4. `lda/lda_harness/_batch_b11_numeric.py`：数值核（纯 numpy/scipy）。

---

## 5. 验证证据（真实 harness 路径）

走 `build_harness_specs` + `BENCHMARK_CANDIDATES` + `IndependentCandidateRouter`（即对外 `verified` 所指同源路径）逐道核对四项：

| 锚 | golden | cand | \|cand−golden\| | metric | tol | 余量×（tol/残差） |
|---|---|---|---|---|---|---|
| B185 | 6.493939402 | 6.493933495 | 5.908e-06 | bose_moment | 0.01 | 1692.8 |
| B186 | 2.404113806 | 2.404110276 | 3.530e-06 | bose_moment | 0.01 | 2832.8 |
| B187 | 1.803085355 | 1.803081830 | 3.525e-06 | fermi_moment | 0.01 | 2837.0 |
| B188 | 25.975757609 | 25.976385341 | 6.277e-04 | debye_moment | 0.01 | 15.9 |
| B189 | 6.283185307 | 6.283185241 | 6.619e-08 | orbit_period | 0.01 | 151086.7 |
| B190 | 0.400000000 | 0.399999994 | 5.516e-09 | r_peri | 0.01 | 1812990.4 |
| B191 | 2.000000000 | 1.999999993 | 7.117e-09 | v_peri | 0.01 | 1405147.2 |
| B192 | 17.771531753 | 17.771531753 | 8.782e-12 | orbit_period | 0.01 | 1138652818.4 |
| B193 | 0.199824896 | 0.199825680 | 7.843e-07 | view_factor | 0.001 | 1275.1 |
| B194 | 0.285875385 | 0.285878538 | 3.153e-06 | view_factor | 0.001 | 317.1 |
| B195 | 0.200043776 | 0.198245085 | 1.799e-03 | view_factor | 0.005 | **2.8** |
| B196 | 0.116426301 | 0.114728114 | 1.698e-03 | view_factor | 0.005 | **2.9** |
| B197 | 0.208709281 | 0.208709281 | 1.259e-10 | voigt_profile | 0.002 | 15883349.4 |
| B198 | 0.082424083 | 0.082424083 | 6.900e-10 | voigt_profile | 0.002 | 2898398.1 |
| B199 | 0.150657586 | 0.150657585 | 1.230e-09 | voigt_profile | 0.001 | 813300.3 |
| B200 | 0.067732553 | 0.067732555 | 1.879e-09 | voigt_profile | 0.001 | 532078.5 |

**16/16 全部 `strict_independent`，diff 全非零，全部 within tol**。worst-case 残差 = B196 的 **1.698e-03**，留 **2.9× 余量**（B-4..B-10 各批最小余量依次 2×、5×、16×、20×、67×、54×、252×，本批 **2.8–2.9× 为历批最低**，见 §6 血案 6/10 口径）。

`describe_trichotomy()` = **{strict 197, degraded 3, stub 18} = 218**；`build_harness_specs()` 返回 **218 specs**；模板内建 `_self_test()` **ALL_OK=True**（16/16 主表 + 判据 D **16/16 全单调**）。

**反向标定（`candidate_responds`，双向 ±10%）**：`ALL_REVERSE=True`——16/16 候选均非自证桩；注册键双向信号 ≥1.4×tol。🔴 **如实登记 3 个 sub-tol 弱键**（不掩盖）：`B196.c` 0.89/1.00×、`B198.gamma` 0.74/0.84×、`B200.gamma` 0.11/0.14×；另 `B195.b/c`、`B196.b` 属「薄键」（>tol 但 <1.5×）。

**极限自检（模块内建）**：
- 族 A：A1 组（玻色 s→0 极限）差 **7.105e-15**；A2 比值 **2.701178033**；A3 = **0.75**（费米/玻色 s=3 比值预测 `(3/2)/2 = 0.75`）✓。
- 族 B：**B1 精确命中 2π**（e=0 圆轨道退化）。
- 族 C：C1 平行 0.199824896 / 垂直共边 0.200043776（均 > 0 且 ≤1）✓。
- 族 D：**D1 γ→0 退化 ⇒ 0.398942277**（= 高斯 `1/√(2π)`）✓。

---

## 6. 诚实边界与血案（本批 **11 条**）

1. **🔴 观测取守恒量 ⇒ 残差落机器精度地板（族 B 首次落地即踩）**：B190 初版观测「能量」或「角动量」——二者是**守恒量**，RK4 在一阶精度下**逐位守恒** ⇒ 残差恒 ~1e-16，判据 D 误判为「代数恒等」。**处置**：改观测 **近心点半径 r_p = a(1−e)**（非守恒量，残差 5.5e-9，5 个数量级差异）。
2. **🔴 线性插值定位穿越 ⇒ 非单调（族 B）**：初版用**线性插值**在采样点间求 r 极小 ⇒ 定位误差 O(h²)，且**残差随 dt 加密非单调**（插值误差与积分误差符号相消）。**处置**：改**三次 Hermite 插值**（用位置 + 速度两个端点条件）+ **80 步二分**细化，定位精度 O(h⁴)，收敛链干净单调。
3. **🔴 4D 求积在共边角点 0/0 ⇒ nan（族 C）**：垂直共边几何中 `R²=x²+(y₂−y₁)²+z²` 在**共边角点**（x=z=0 且 y₂=y₁）为 **0**，`num/R⁴` 数值除零 ⇒ nan 污染全积分。**处置**：`np.divide(..., where=R2>0, out=np.zeros_like(R2))`——该点被积函数极限为 0（分子 x·z 也是二阶零），置零是**正确极限**而非掩盖。
4. **🔴 洛伦兹尖峰欠采样 ⇒ 残差非单调（族 D）**：B199（σ=0.5, γ=2，洛伦兹主导）的线心峰宽 ~γ=2，等距 Simpson 在峰区**严重欠采样** ⇒ 残差随 n 反复。**处置**：改**解析换元** `τ = x + γ·sinh u`（把 Cauchy 峰展平为宽缓被积函数），`u∈[−asinh(12σ/γ), +asinh(12σ/γ)]` ⇒ 单调收敛、O(h⁴)。
5. **🔴 4D `(n+1)⁴` 内存墙（族 C）**：4D 张量积 n=96 ⇒ `(97)⁴ ≈ 8.9e7` 双精度点 ≈ **710 MB**，n=128 达 **2.2 GB** ⇒ 触碰内存墙。**处置**：改**分块累加**（`for i in range(n+1)` 逐片 `(n+1)³` + `np.einsum('ij,ijk,k->', WY, I, w)`）——数值**恒等**（差 ≤1e-16），n=96 每片仅 **7 MB**、**0.9 s**。
6. **🔴 垂直共边被积函数在共边发散 ⇒ O(n⁻¹) 固有慢收敛（族 C，诚实边界）**：垂直共边几何的 `cosθ₁cosθ₂/(πR²)` 在共边角点**对数发散** ⇒ Simpson 收敛率从 O(h⁴) **退化为 O(n⁻¹)**。这是**几何固有性质**，非实现缺陷。**处置**：接受慢收敛、定档 n=96（残差 1.8e-3），并把该族 tol 收紧到 0.005（见血案 10）——**不掩盖退化率**，在报告明示。
7. **🔴 积分窗不足 ⇒ 参数扰动下候选「崩」（族 B）**：`_T_MAX=30` 只够 a=2 的 **1.5 个周期** ⇒ 反向扰动 ×1.1（a=2.2）时**找不到完整周期** ⇒ 候选抛 `RuntimeError`。判据 C5 要求「扰动下有**可测响应**」，而崩溃属「有响应」（判据放行），但**这是脆弱的放行**。**处置**：`_T_MAX` **30 → 90**（≥4 个周期余量），使扰动下候选**正常返回数值**而非崩溃。
8. **🔴 零响应键 ⇒ 必须退出 `default_params`（族 B/D）**：`candidate_responds` 对**每个** `default_params` 键双向扰动；若某键物理上**不影响**该锚指标（如 Kepler 第三定律 T 与偏心率 e **无关**、Voigt 线心值对 x **恒等** 0），则该键**永远无响应** ⇒ 判据 C3/C5 信号被稀释。**处置**：`ecc`（B189/B192）、`x`（B197/B199）从 `_DEFAULT_PARAMS` **撤出**（仍保留为 golden/候选形参默认值），杜绝「零响应键」这种伪信号。
9. **🔴 定档撞噪声地板（族 B/D）**：B192 在 dt=0.002 时残差 **~2e-13**（已落双精度地板）、B199 在 n=64 时末值 **2.78e-17** ⇒ 判据 D 窗口「粗端 > 1e-13」不成立。**处置**：B192 定档 **dt=0.008**（残差 8.78e-12）、B199 定档 **n=32**（残差 1.23e-9），使判据 D 扫描窗落在离散误差主导区。
10. **🔴 族 C/D 指标动态范围窄 ⇒ 统一 tol=0.01 会淹没反向信号（本批首次全局收紧）**：角系数 F₁₂∈(0,1)、Voigt 峰归一值 ∈(0.07,0.21)，**10% 参数扰动的绝对信号仅 ~0.07–0.29×tol** ⇒ C5 判据「min 反向信号 > tol」**不成立**（零判别力）。**处置**：引入 **`_TOL_BY_BID`** 逐锚收紧 —— B185-B192=0.01、B193/B194=0.001、B195/B196=0.005、B197/B198=0.002、B199/B200=0.001。🔴 这是**收紧**不是放宽（放宽才违规）。
11. **🔴 基线残差 >1e-12 的 D 判据红灯门槛（族 D）**：B197 原定 n=64 时残差 **1.68e-13** ⇒ 会新踩 `run_d_criterion_smoke` ③「**全部 strict 锚基线残差必须 > 1e-12**」红灯（唯一豁免 `noise_exempt={"B10"}`）。**处置**：定档 **n=64→48**（残差 1.26e-10，>1e-12 且仍有 15883× 余量）。

**其他诚实边界**：
- 族 C 垂直共边几何为 **O(n⁻¹)** 慢收敛（血案 6）⇒ 残差 1.7e-3 主要由被积函数共边奇性主导，**非实现缺陷**；本批余量 2.8× 为历批最低，后续如需提高余量须换求积策略（如自适应/变量替换消奇），**不在本批范围**。
- 族 A 只验证 `s=3/4/5` 单档整数（量子统计矩典型工作点），未扫连续 s 及 ζ 临界带。
- 族 B 只验证 **2 组 (a,e)**，未扫高偏心率（e→1）近抛物区（RK4 在近心点 h 有效步长急剧缩小，属已知刚性问题）。
- 族 D 只验证 **4 组 (σ,γ,x)**，未扫 σ≫γ 或 γ≫σ 极端比（Voigt → 纯高斯/纯洛伦兹极限）。

---

## 7. 计数护栏（七道全绿）

| 护栏 | 结果 |
|---|---|
| `run_p0_count_guard_sync_smoke.py`（README / CONTRIBUTING / ledger docstring 三面同步） | OK（6 tests） |
| `run_count_consistency_smoke.py`（引擎/包/题库/CI core） | OK（11 tests） |
| `run_three_class_consistency_smoke.py`（README ≡ harness ≡ 端点） | 4/4 PASS（197/3/18，和=218） |
| `run_self_certified_lock_smoke.py`（棘轮 ≤18 + 锁原因） | 4/4 PASS（`{t2_blocked:6, terminal_tier1:12}`，棘轮 =18 守） |
| `run_maturity_baseline_smoke.py`（VMM 底线 6 条） | PASS（M5 口径 197/3/18，和=218；低置信 12） |
| `run_webui_verification_ledger_smoke.py`（端点 + 前端接线） | 15 PASS / 0 FAIL |
| `run_statistical_anchor_smoke.py`（题库计数 + 统计一致性） | 34 PASS / 0 FAIL（题库已加 `range(185,201)`） |

---

## 7.1 🔴 诚实披露：三道 core smoke 的**既有红灯**（承接 B-9/B-10，**非 B-11 引入** · 本批实测复现）

B-9/B-10 报告 §7.1 已登记的三条既有红灯为**基线 HEAD 即红**。本批**实测复跑逐条复现**，确认与 B-11 无关：

| core smoke | 本批实测 | 是否含 B185-B200 | 判定 |
|---|---|---|---|
| `run_d_criterion_smoke.py` ③ 基线残差 > 1e-12 | ❌ **9 PASS / 1 FAIL** —— 失败清单 = **B65/B66/B67/B68/B73/B74/B79/B81/B82/B83/B84/B87/B88（13 道，全为 B-4 切片转移矩阵锚）**，残差 0.0~1.74e-13 贴地板 | **无**（16 道新锚残差 8.8e-12~1.8e-3 均 > 1e-12；B197 因此特意从 n=64 降档到 n=48，见 §6 血案 11） | **既有**（失败清单**逐字等于** B-10 的 13 道） |
| `run_benchmark_falsifiability_smoke.py` ⑨ 路径② JSON 序列化 | ❌ **12/13 PASS** —— `TypeError: Object of type bool is not JSON serializable`（**正向：独立候选 197 道全 PASS**，含 B185-B200 十六道，`d/tol` 最大 B195=0.36；**反向：30 项 10% 扰动全 FAIL✅**；仅 ⑨ 路径②序列化崩） | **无**（16 道 golden 均返回 Python `float`，已实测确认） | **既有** |
| `run_coverage_deadzone_closure_smoke.py` B5/B6 仍为自证桩 | ❌ **31 PASS / 2 FAIL** —— B5/B6 `class=strict`（v0.9.81 已升 strict，期望值陈旧） | **无**（16 道新锚已实测 `strict_independent`） | **既有** |

**结论：B-11 零新回归**（判据 D 失败清单逐字等于 B-9/B-10 的 13 道；且 16 道新锚残差最小 8.8e-12 > 1e-12 门槛，**唯一贴门者 B197 已主动降档规避**）。本批**未修**（超范围，且会触碰 B-4/B5/B6 锚的 golden/参数，违反「不做超范围改动」纪律）；建议单开「core smoke 既有红灯清算」专项统一修复（三条各自独立、互不耦合）。

⚠️ 因此 README/CONTRIBUTING 中「CI core 178 条」为**条数账本**（条数不变、由计数护栏守护）；其**全绿性**在上述三条红灯上**不成立**——此为**如实披露**，不作绿。

---

## 8. 账本变更

| 项 | 变更前 | 变更后 |
|---|---|---|
| 版本 | v0.9.90 | **v0.9.91** |
| 严格独立 | 181 | **197** |
| 降级量级参考 | 3 | 3 |
| 自证桩 | 18 | 18（棘轮 ≤18 保持 · 本批无毕业） |
| 题数 N | 202 | **218** |
| 独立率 | 89.6% | **90.4%** |
| 天花板 | 94.1% | **94.5%** |
| B 题数 | 179（B1-B184） | **195（B1-B200）** |
| CI core | 178 | 178（不变） |

**同步文件**（单进程原子替换，逐项 `count==1` 断言，EOL 保真）：`README.md`（当前版本行 + B-10 行转「上一版」+ 4 处计数）、`CONTRIBUTING.md`（3 处）、`pyproject.toml`（版本）、`run_count_consistency_smoke.py`（6 处）、`run_statistical_anchor_smoke.py`（3 处）、`run_webui_verification_ledger_smoke.py`（1 处 docstring）、`run_maturity_baseline_smoke.py`（2 处 M5 docstring）、`lda_webui/app.py` + `cs_agent.py`（对外产品描述陈旧计数「50 道验证锚」→「200+ 道验证锚」）。**棘轮不动**（本批无毕业）。

---

## 9. 提交与推送

- commit：`014d383`（14 files changed, 1467 insertions(+), 25 deletions(-)；新增报告 + `_batch_b11_numeric.py`）
- push：`scripts/sync_push.py` —— **gitee 直连 exit=0**（`18b5f4d..014d383  main -> main`）+ **github 直连被重置（exit=128）→ SOCKS5 兜底 exit=0**（`18b5f4d..014d383  main -> main`）；三端 HEAD 一致 = `014d383`。
- **部署**：✅ **已于 2026-09-17 部署生产**（**写本报告时用户指令为「选 B-11」、未含部署授权；实际部署是在此后用户单独授权时执行的**，故本行属**部署后回填订正**）。
  - **独立确证**（2026-09-17 B-12 部署时实测生产前态）：生产 HEAD = `57e8422`（= B-11 报告回填提交）、`lda-design` = **0.9.91**（非 0.9.90），且 `git pull` 显示 `57e8422..2f879a1` ⇒ **B-11 代码与包元数据均已在生产**。
  - **当次部署记录**：`git pull 69434b1..57e8422` **15 文件 +1478 / −27**；`dist/store.json` 未动（2072 B）；restart rc=0；**HEAD_MATCH=True**；health `version 0.9.91 / benchmarks 218`；`/api/shelf` 75；ledger `strict 197 / degraded 3 / stub 18 / total 218`；`ci_core` 178。

---

## 10. 下一步候选

- **腿①续（B-12）**：仍最稳——继续选**已过同源体检**的新物理族。候选方向（须先体检，勿直接开工）：
  - 1D 双势阱隧道分裂（超越方程 / 半经典分裂式）；
  - 球壳量子点（球 Bessel + Neumann **交叉积**零点 —— 与 B141-B144 圆柱 Bessel 交叉积区分）；
  - 指数势 V₀e^{−x/a} 束缚态（Bessel 函数谱）；
  - 有限深 2D 方阱（tan/cot 二维匹配 —— 与 B16/B145-B148 的 1D/3D 区分）；
  - 电磁波导衰减常数（新 transcendental 族 —— 注意与 B-5 圆/矩形波导截止**不同量**）；
  - 🔴 **前置「同源体检」（含 `grep` 全仓占用扫描 + 三红线逐条）已为批次第一道闸**，未过不得开工。
- **腿②degraded 3→strict**：纯内部，存量已明确（E10 余量 ~28×最有望先回）。
- **腿③T2 解锁 6 道**：外部 KPI 阻塞（E1/E3-E7），需 MPW 实测回流。
- **建议专项（本次新增）**：**core smoke 既有红灯清算**（§7.1 三条，互不耦合，可一次性清完）。
