# LDA 反向悬赏 · 共建实证信任墙

> 把「AI 写的光子 / 量子求解器」钉在事实地基上——靠可被事实打脸，而非自我宣称。

## 为什么要有悬赏

LDA 的核心主张是：**底层核心求解器由 AI agent 递归自举开发，但 LLM 永不进入判决路径**。
要让这套主张被信任，不能靠自我宣称，要靠「可被事实打脸」。

我们设了一道**雷③信任墙**：纯 AI 互证是循环论证。破解之道是把
「真实世界测量 + 开放对抗题」作为事实地基。这就是本悬赏要征集的东西。

## 征集什么

### 1. 实测语料（corpus）—— 让求解器对照真实
任何你能提供的真实器件测量数据，例如：

- 波导有效折射率 `n_eff`、弯曲损耗、交叉串扰、MMI/Y-branch 分光比
- 环形谐振器 `FSR` / `Q`、光栅耦合器效率、锥度传输效率
- 超导量子比特参数（T1 / T2 / 频率 / 耦合）、版图寄生提取 ……

**硬性要求**：必须带**可追溯引用**（论文 / 数据表 / 测试报告）。无引用不予收录。

### 2. 对抗题（adversarial）—— 让求解器翻车
你心里「AI 求解器八成算错」的场景，例如：

- 强受限小弯曲半径（辐射损耗 + 模式失配）
- 紧间隙波导交叉串扰（常被高估隔离度）
- 绝热失效区的极短锥度（T→1 假象）
- 异质集成模场失配耦合

提交请说清**物理陷阱**。一旦某版求解器在你的题上翻车，我们会把它写进 release notes 作为诚实记录。

## 提交通道

- **GitHub**：`New Issue → 实测语料提交` / `对抗基准题提交`（结构化模板已就绪）
- **Gitee**：同名 Issue 模板
- 字段与 `lda/lda_harness/seed_empirical.json` 完全对齐，提交即入库候选

## 奖励机制

| 类别 | 奖励 |
|---|---|
| 每条被收录的实测语料 | 贡献者署名进 AUTHORS + 仓库 Hall of Fame |
| 每道被收录的对抗题 | 同上，并永久留在对抗题库 |
| 让当前求解器在对抗题上翻车的题 | **额外悬赏**：写入 release notes「被打脸记录」，贡献者获「破壁者」徽章 |
| 持续贡献者 | 进入 LDA 共建者名录，优先参与阶段 2 双引擎协作 |

> 诚实说明：当前为开源社区共建阶段，实物 / 现金奖励视后续 sponsors 与社区基金而定；
> 但「破壁者」荣誉与署名是确定的、即时的。我们更看重事实，而非营销。

## 红线（征集与验收都守）

- **主权求解器**：核心算法自研，不借 GPL / Meep / Tidy3D 源码。
- **LLM 不进判决路径**：你提交的数据 / 题只作为 golden 或题库，PASS/FAIL 由死代码标量比对决定。
- **可验证**：每条语料 / 题都配确定性裁判或物理定律锚，杜绝纯 AI 互证。

## 评审流程（D-62 语料评审流 · D-95~D-98 生态链闭环）

1. 提交 Issue（结构化模板，附 citation=可追溯来源——无引用不予收录）
2. **经社区评审流落库**：`submit_measurement`（确定性校验：数值有限 / σ≥0 / citation 必填 / 防重）→ `review_measurement`（**具名人工评审，LLM 不进判决路径**）→ `land_measurement`（写入 `lda/lda_pdk/empirical_contributions.json`，harness E1-E3 实证锚题实时生效）——WebUI 面板 57「实证大数据锚」或 `POST /api/ecosystem/measurement` 均可提交
3. AI-dev 自举写核（`solver_writer`）尝试求解，确定性裁判（`run_harness.py`） + 实证锚验收（比对=|candidate−measured|≤σ 死标量）
4. 翻车则记入 release notes；通过后该能力标注「已被 X 题实证锚定」

---

## 名誉榜与计分（2026-09-09 增设）

现金奖励**暂缓**（视 sponsors / 社区基金而定）；当前阶段以**名誉榜**为唯一激励，即时、确定、可证伪。

- **台账**：`bounty_leaderboard.md` —— 每笔贡献一行：类型 + 锚判定 + 得分 + 锚ID/引用。
- **计分**（死规则，任何人不得自改）：

  | 类型 | 锚判定 | 得分 |
  |---|---|---|
  | 实测语料 `corpus` | `landed`（收录） | +1 |
  | 对抗题 `adversarial` | `landed`（收录） | +2 |
  | 真实缺陷 `defect` | `confirmed`（锚确认翻车） | +10 |
  | 真实缺陷 `defect` | `rejected`（锚未确认） | 0 |

- **反作弊**：`defect/confirmed` 行**必须**在「备注」引用锚 ID（如 `S12` / `B29`），否则不计分——杜绝自证桩式自嗨。
- **判定权**：一律由死标量锚决定，LLM 只出攻击题、不判对错（红线纪律）。
- **CI 护栏**：`lda/run_bounty_ledger_smoke.py` 校验台账结构 + 得分一致性 + 锚引用，并计算实时排名，已进 `CORE_SMOKES`。

## 活体对抗裁决记录（红队 fuzz 常态化 · 信任墙自动造血）

本悬赏不止于「等外部投稿」——LDA 内置红队对 52 锚持续做参数扰动攻击，并**由死标量锚自动裁决**，把结果沉淀为可复核的信任墙记录。纪律与上面一致：**LLM 不进判决路径，红队只出题、锚判卷**。

流水线（全在 CI core 内，非一次性）：

1. `run_redteam_anchor_fuzz_smoke.py`：对 52 锚做 ±30% 规则式扰动攻击，独立候选 vs golden 死标量判卷，落盘发散点到 `redteam_anchor_fuzz_defects_pending.json`。
2. `run_redteam_adjudication.py`：对每处发散点**死标量重判**，按扰动幅度分类——
   - `expected_extreme_perturbation`（±30% 极值，落在闭式 golden 有效域外）→ 预期物理分叉，**非缺陷**，不喊狼来；
   - `in_domain_divergence`（域内扰动仍 err>tol）→ **真疑点，升级本墙人工/锚复核**，不自动判 bug。
3. `scripts/redteam_anchor_fuzz_cron.py`：周期性看门狗（同 `redteam_cron.py` 范式），跑 fuzz+裁决，按域内疑点数定级 OK/CRITICAL，写趋势日志 `redteam_anchor_fuzz_trend.jsonl`。

**最新一轮裁决（2026-09-10，自动生成，详见 `redteam_adjudication_report.md`）**：

- 发散点总数 **18** → 预期极值分叉 **11** · 域内疑点 **7**。
- 7 个域内疑点（扰动仅 15% 仍超出 tol，**未 confirmed，待人工/锚复核**）：

  | 锚 | 最大扰动 | err | tol |
  |---|---|---|---|
  | B24 | 0.15 | 3.68e-05 | 3e-05 |
  | B24 | 0.15 | 7.54e-05 | 3e-05 |
  | B26 | 0.15 | 5.68e-03 | 1e-04 |
  | B26 | 0.15 | 4.34e-02 | 1e-04 |
  | B27 | 0.15 | 4.39e+01 | 30.0 |
  | B27 | 0.15 | 4.43e+01 | 30.0 |
  | S8  | 0.15 | 6.53e-01 | 0.2  |

> 诚实边界：域内疑点 ≠ 已确认缺陷。多数可能是闭式 golden 仅在特定工作点成立的近似局限；但**它们必须被眼睛看到、可被任何人复核**，而非淹没在「全绿」里。欢迎在 Issue 认领任一疑点做独立求解器对照——若证实求解器真翻车，按上表 `defect/confirmed` 计 **+10** 并授「破壁者」。

加入我们，把「AI 写的光子 / 量子求解器」钉在事实地基上。
