# P1-1 自证桩升级冲刺 70% — 可行性审计与边界判定

> 审计日期：2026-09-16 · 基于实测 harness 报告 + `benchmarks.py` `_VMM_OVERRIDES` 终审锁
> 结论先行：**按当前用户指令清单（B16 重审留桩 / B17-B19 / S1-S13 方法学独立候选），70% 目标数学上不可达；清单对 22 道自证桩的净增量为 0。**

---

## 0. 基线（实测）

跑 `run_harness.py`（CI 解释器 3.13.14）得：

| 类别 | 数量 | 说明 |
|---|---|---|
| 总数 | 56 | B1–B33 / E1–E10 / S1–S13 |
| 严格独立候选 | 31 | 计入 `summary.verified` |
| 降级量级参考 | 3 | B21 / E9 / E10（不进死标量判决列）|
| 自证桩 (ReferenceCandidate) | 22 | candidate≡golden，误差恒 0，零验证价值 |

当前独立率 = **31 / 56 = 55.4%**（与账本一致）。

**70% 目标所需**：0.70 × 56 = 39.2 → 需 **40 道严格独立** → 从 31 起需 **+9**。

---

## 1. B16 重审结论：留桩（KEEP STUB）✅ 已复核并打戳

| 复核项 | 结果 |
|---|---|
| golden 数值 | `b16_mmi_length(2.8, 2.4, 1.55) = 27.314 µm`（docstring 声称 ~27.3µm ≈ 实测 27µm，**<1% ✓**）|
| 可用求解器对象 | `lda_solver/mmi_eme.py` = **对称平板波导 (symmetric slab)** 2D-EIM 本征模展开 |
| B16 真实器件 | **脊形 (rib) MMI** → 建模对象不同一 |
| 余量 | slab-EME ~24µm vs golden 27µm，diff ~13% > tol 3.0µm，余量 ~1.3×（远低于本项目 100–1000× 严格标准）|

**判定**：沿用「B21 教训」——独立求解器必须建模同一物理对象，symmetric-slab EME ≠ rib MMI，故不作为 B16 合法独立候选。**B16 如实留自证桩，不可假绿。**

已向 `benchmarks.py` B16 note 追加 `【v0.9.80·2026-09-16 P1-1 重审】` 打戳，记录复核结论。

---

## 2. 用户目标清单可行性判定（C1–C5 + 判据 D）

| 锚 | metric | 判定 | 障碍（判据）| 项目既有分类（`_VMM_OVERRIDES`）|
|---|---|---|---|---|
| **B17** | I_c = 2e·E_J/ℏ | ❌ 不可接 | 定义式同义反复（C1 循环 / C4 参数由 golden 反算）| `self_authored_closed_form`「本就不升」|
| **B18** | F_P = 4g²/(κγ) | ❌ 不可接 | regime 越界：默认 g≫κ 强耦合区，坏腔/失谐极限公式失效 | `self_authored_closed_form`「本就不升」|
| **B19** | max\|T(λ)\| ≤ 1 | ✅ **已严格独立** | 已完成（`link_passivity` 候选，lda_chain 真跑全链路）| `strict_independent`（**不在 22 桩内**）|
| **S1** | margin_dB | ❌ 不可接 | 黄金 = 两数相减（纯算术），独立候选=同义反复（虚报）| `self_authored_closed_form`「本就不升」|
| **S2** | margin_GHz | ❌ 不可接 | 纯算术（100−50=50）| 同上 |
| **S3** | OSNR_dB | ❌ 不可接 | 纯算术（ASE 解析）| 同上 |
| **S4** | margin (∏fᵢ) | ❌ 不可接 | 连乘闭合式 | 同上 |
| **S5** | margin_dB | ❌ 不可接 | 纯算术 | 同上 |
| **S6** | margin_dB | ❌ 不可接 | 纯算术 | 同上 |
| **S7** | margin_p5_dB | ✅ **已严格独立** | 已完成（`gauss_p5_margin` 闭式 ↔ MC）| `strict_independent` |
| **S8** | OSNR_p5_dB | ✅ **已严格独立** | 已完成（`gauss_p5_osnr`）| `strict_independent` |
| **S9** | LVS verdict | ❌ 不可接 | 结构性：golden = 被测引擎自身判决 | `self_authored_closed_form`「本就不升」|
| **S10** | 多层 LVS verdict | ❌ 不可接 | 结构性（同上）| 同上 |
| **S11** | 千器件 verdict | ❌ 不可接 | golden=引擎判决；无连续数值参数可扰（C3/C4）| 同上 |
| **S12** | 阵列分布 verdict | ❌ 不可接 | metric=verdict(0/1) 不连续；扰动只生采样噪声（C3）| 同上 |
| **S13** | yield(0~1) | ✅ **已严格独立** | 已完成（`yield_analytic` 解析 ↔ MC 双算法互证）| `strict_independent` |

**关键结论**：

- 用户清单里 **B19 / S7 / S8 / S13 共 4 题早已是严格独立**——它们**不在 22 道自证桩内**，重新"接"是重复劳动，净增量 0。
- 其余 **B17 / B18 / S1–S6 / S9–S12 共 11 题已被项目自身终审锁为「本就不升」**（定义式 / 纯算术 / 结构性），任何"独立候选"都只能是同义反复或循环论证 = **虚报**，违反 P0 红线（LLM 不进判决路径、诚实边界、不可假绿）。
- **清单对 22 桩的净增量 = 0。**

---

## 3. 70% 数学上不可达（按当前清单）

- 需 +9 严格独立，但清单可贡献 **0**。
- 22 桩中**唯一非「本就不升」类是 B5 / B6 / B7（design_rule_anchor）**，但其升级路径写明「待 Meep/Tidy3D 场级 ORACLE（B 级借今踢后）」——即需新增场求解器，项目已推迟：

  | 锚 | 当前 golden 来源 | 接独立候选的可行性 |
  |---|---|---|
  | B5 | 冻结 numpy 重叠积分 3.4 dB | 可写第二独立方法（BPM/模态）做 S13 式双算互证 — **可行** |
  | B6 | 设计守则锚 0.5（圆整目标）| 仿真对特定几何、目标为共识值 → 弱交叉，**不建议硬接** |
  | B7 | 冻结 numpy 2D-FDTD −19.7 dB | 可写第二独立方法（模态匹配/BPM）— **可行** |

- 即便为 B5/B6/B7 全写第二独立求解器，最多 **+3 → 34 严格独立 = 60.7%**，仍低于 70%。
- E1/E3/E4/E5/E6/E7（6 道）路径=「T2 实测数据集升 Tier-3」——需外部实证语料，且 E1/E3 循环论证、E4–E7 geometry 不足（技能已判归档），当前不可接。

**真实可达上限 ≈ 34/56 = 60.7%**（仅当完成 B5/B7 场级 ORACLE；若含弱交叉的 B6 则为 60.7%，B5/B7 不含 B6 为 58.9%）。否则维持 **31/56 = 55.4%**。

---

## 4. 诚实建议（供重新定调 P1-1）

70% 经此清单**不可达**。三条出路，请择一：

- **(A) 重新定标（推荐）**：把 P1-1 目标改为「固化并显式标注 terminal Tier-1」——将 B16/B17/B18/S1–S6/S9–S12 永久留桩写入路线图与 README 账本，独立率维持 55.4% 但**如实**；把"冲刺 70%"改为"冲刺真实可达的 ~60%（仅 B5/B7 场级 ORACLE）"。这消除账本上的虚高预期。
- **(B) 新增锚扩基**：若 70% 是硬承诺，唯一正路是**新增** N 道带真独立候选的锚（如更多谱拟合族 / FDTD 族），把分母从 56 做大——但这是"加锚"不是"升桩"，需新物理内容，工作量独立于本任务。
- **(C) 启动 B5/B7 场级 ORACLE**：为 B5/B7 各写第二个独立 numpy 求解器（BPM/模态展开），做 S13 式双算法互证，升格至严格独立（+2 → 33/56 = 58.9%）。距 70% 仍差 7 道，需叠加 (B)。

---

## 5. 本次已落地动作

1. **B16 重审数值复核**：golden(2.8,2.4,1.55)=27.314µm ✓；`mmi_eme` 确认建模 symmetric slab（非 rib）→ 留桩判定不变；已在 `benchmarks.py` 打 `v0.9.80·2026-09-16` 戳。
2. **可行性矩阵**：逐题给出 C1–C5 + 判据 D 判定，对照项目既有 `_VMM_OVERRIDES` 终审锁。
3. **未改动任何源码/账本计数**（避免无依据硬接导致假绿）；本报告为纯审计交付。

> 待用户定调（A/B/C）后，再决定下一步是否进入 B5/B7 接线或新增锚工程。
