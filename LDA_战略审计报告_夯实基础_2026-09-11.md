# LDA 战略审计报告（先夯实基础）

> 版本：v0.9.67 基线 · 审计日：2026-09-11
> 背景：愿景/战略/实施策略已升格为正式文档。按杜先生要求，在启动愿景相关后续工作**之前**，先对现有基础做真实盘点、代码审计、功能审计，找薄弱点、补短板。
> 方法：所有数字均**实跑受控 Python 直跑获取**（当前 shell 环境 coreutils 缺失，故绕开 `cd/wc/ls` 等，直接用解释器 walk/import/运行 smoke），不靠记忆复述。

---

## 0. 审计目的与范围

- **目的**：战略已清晰（光子/量子计算芯片愿景），但"地基是否扎实"决定后续能否稳。先把现有底层凿实。
- **范围**：① 现有基础真实盘点（账本复核）② 代码审计（结构/规模/语法/占位）③ 功能审计（验证机械 + 各工作流能力）④ 薄弱点识别与夯实优先级。
- **不在范围**：启动愿景文档里的"四层系统/光子计算 MVP"等新功能——那是夯实完成后的事。

---

## 1. 真实盘点（账本复核 · 实跑）

### 1.1 代码规模
| 指标 | 数值 |
|---|---|
| `.py` 文件总数（排除 reports/__pycache__） | **426** |
| 总行数 | **92,387** |
| 其中根目录 `run_*.py` 脚本/演示 | 190（测试+演示面） |
| 引擎/库代码（排除根脚本） | **236 文件 / 68,000 行** |

**引擎库子包规模（行数）**：
```
lda_agent      12,813   ← AI 递归自举核心（最大、最复杂）
lda_solver     12,130   ← 求解器（FDTD/EME/模式/FDTD2D…）
lda_l2         12,119   ← 版图/GDS/DRC/LVS/ tapeout
lda_harness    10,294   ← 验证机械（锚/候选/VMM/ORACLE）
lda_webui       8,061   ← WebUI + API
lda_pdk         2,512   ← PDK/主权依赖/封装容差
lda_design      2,278
lda_ir          1,432   ← IR 桥接/逆设计
lda_chain       1,400
lda_layout      1,344
lda_l1            678
lda_data          546
lda_qeda          428   ← 量子 QEDA 栈
lda_l3            199   ← 生产规划（已重建，假绿已除）
lda_license        66
```

### 1.2 验证账本（实跑权威逻辑，与 README 宣称一致 ✓）
- **53 锚**：严格独立 **28** / 降级量级参考 **1** / 自证桩 **24**（合计 53，与 v0.9.67 账本一致）
- B 类实际 **31**（B1–B30 + B33，B31/B32 跳号）· E 类 9 · S 类 13
- **CORE_SMOKES 注册 = 157 条**（已确认在 `run_ci_regression.CORE_SMOKES`）
- 22 引擎 + 11 包 = 33 类端到端；光子 15 + 量子 7

### 1.3 本轮已修的真实基础失真
- **计数一致性核心守卫漂移**：`run_count_consistency_smoke.py`（在 157 条 CORE 内）原硬编码 `B 题=30 / 末位 B30`，但 v0.9.67 新增 B33 后 B 实际=31，导致该守卫**全新运行即 RED** → README 宣称"CI core 157 全绿"**不可从干净运行复现**（假绿风险）。
- **已修并提交**（`8453d65`，带 Signed-off-by）：B 计数 30→31、末位 B30→B33、文档注释 52→53 题；重跑 11/11 OK，守卫重新反映代码事实、可自动拦截未来同类漂移。

---

## 2. 代码审计

### 2.1 语法健康
- **426 个 .py 文件 `py_compile` 全过，0 语法错误** ✓ —— 全仓可解析，无破损模块。

### 2.2 占位 / 未实现扫描
- `NotImplementedError` 命中 **10 处**：经验证上下文，**全部为意图明确的抽象方法 / 未接路线 / 只读客户端 stub**（如 `FoundryPDKClient` 未激活、IR 逆设计未接路线），**非缺陷**，属"设计留口"。
- `TODO/FIXME`：**0 处**。
- 含"占位/stub/未实现"字样注释 **41 处**：多为 smoke 中已记录的延迟项（合法分阶段交付），无隐藏缺口。

### 2.3 导入健康
- 核心导入链无破坏：`lda_harness.harness` / `golden` / `real_machine_oracle` 均可干净 import（此前 ORACLE 契约桩接入未污染既有链）。

### 2.4 主权与安全
- DCO 闸门就位（提交须 `Signed-off-by`），本轮修复即带签名提交。
- 敏感/临时文件已 `.gitignore` 排除（`.secrets/`、redteam 缺陷数据、`.cache/`、`*.log` 等），不会误公开。

---

## 3. 功能审计（验证机械 + 工作流能力）

### 3.1 验证机械（核心 · 实跑取真证据）
| 验证项 | 结果 |
|---|---|
| `run_harness.py`（53 锚三分类 + 判决回路） | **PASS**（严格 28 / 降级 1 / 自证 24，53/53 闭合） |
| `run_maturity_baseline_smoke.py`（VMM 底线 M1–M5） | **PASS** |
| `run_real_machine_oracle_contract_smoke.py`（ORACLE C1–C5 + 闸门） | **PASS**（零真实数据不假绿） |
| `run_count_consistency_smoke.py`（计数一致性，修复后） | **11/11 OK** |
| `quickverify` 快验（环境/版本/语法/harness） | 环境+版本+语法 OK，harness PASS；计数守卫原 FAIL 现已修 |

### 3.2 各工作流能力（对照全能力盘点文档）
| 工作流 | 真实能力状态 | 审计结论 |
|---|---|---|
| **A 无源光子** | 28 严格独立锚底座，GC-*48 全 PASS | 扎实 ✓ |
| **B 光电接口** | 31 B 锚（含 B15/B28/B29/B30/B33 共 5 道严格独立有源） | 扎实 ✓ |
| **C 量子 QEDA** | 量子侧独立候选 + QKD 信息论锚 | 扎实 ✓ |
| **D 电学内核（T1 解锁）** | 框架/契约就位，N(x) 求解可研发（合成数据） | 管道就绪，验证受限 |
| **E 有源 A 档 / F 有源 B 档真求解** | 框架就位，真值受限（T2 锁死） | 管道就绪，验证受限 |
| **G 激光增益类** | 锁死（红线：增益介质 + A 级） | 按红线维持禁区 |

### 3.3 结论
- **验证机械健康可用**，地基（器件/PDK 级 EDA）扎实，53 锚与账本一致。
- 工作流 A/B/C 已做实；D/E/F 管道就位仅验证受限（符合"系统研发不受限/验证受限"纪律）；G 锁死。

---

## 4. 薄弱点清单（按优先级）

### 🔴 P0（已在本轮修）
- 计数一致性核心守卫漂移 → 已修 `8453d65`，复验绿。**教训固化**：新增锚必须同步更新计数守卫，现靠该守卫自身拦截，未来自动防回潮。

### 🟠 P1（建议近期夯实，优先于新功能）
1. **24/53 自证桩（45%）升级** —— 方法学独立的"可被外部验货"占比仅 53%（28/53）。这是验证地基最实质的短板。路径：延续 P0 接线程序，把 B11/B16/B17/B18/S1–S13 等自证桩逐步接独立候选（判据 D 双对照 + 反向 FAIL）。**优先级高于任何新功能。**
2. **`lda_agent` 层专项代码评审** —— 12.8k 行 / 53 文件，是全仓最大、最复杂的 AI 递归自举核心。建议做一次独立评审：导入健康、异常路径、死代码、测试覆盖。
3. **完整 157 core 烟感回归** —— 本轮仅抽样重跑（harness/maturity/ORACLE/计数守卫）；部分烟感重/慢/需网络。建议触发一次**完整 CI core 回归**，确认整体 157 全绿可复现（含本修复后）。

### 🟡 P2（观察/追踪项，非缺陷）
- 10 处 `NotImplementedError` + 41 处占位注释：均为意图明确的设计留口（含 `FoundryPDKClient`、IR 未接路线），**非缺陷**。建议建"留口清单"集中追踪，防长期腐化。
- 根目录 190 个 `run_*.py`：测试/演示面大，需确保均在 CI 覆盖或 `NON_CORE_SMOKES` 豁免登记（已有机制，建议季度复核）。

---

## 5. 夯实路线图（先底层，再启动愿景）

**阶段 0 · 夯实基础（当前 → 完成）**
- P1① 自证桩升级（目标：严格独立占比 53% → 70%+）
- P1② `lda_agent` 专项评审
- P1③ 触发完整 CI core 回归，确认 157 全绿可复现
- 状态门禁：阶段 0 未完成前，**不启动**愿景文档里的"四层系统 / 光子计算 MVP"等新功能

**阶段 1 · 底层稳固后（按愿景战略文档）**
- 铺设四层系统管道（架构综合 / 编译映射 / 控制校准 / 协同仿真），仍严守"系统研发不受限 / 验证受限"纪律
- 光子路线优先（外置激光可跑通，不破红线）

---

## 6. 结论

1. **地基扎实**：0 语法错误、验证机械健康、53 锚与账本一致、A/B/C 工作流已做实。
2. **发现并修复 1 处真实基础失真**（计数一致性守卫），消除了"README 绿但干净运行 RED"的假绿风险——这正是"底层没夯实"的典型，已闭环。
3. **最大待夯实项** = 24 自证桩升级 + `lda_agent` 专项评审；二者优先于新功能。
4. **建议**：先完成阶段 0（P1①②③），再启动愿景相关后续工作。这正契合"先把现有底层做好，再启动后续"的原则。

> 注：本轮修复（`8453d65`）已在本机提交（带 DCO 签名），因沙箱网络限制尚未推送远端；需在本机 `git push gitee main && git push github main`。
