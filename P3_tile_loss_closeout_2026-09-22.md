# P3 · 16×16 tiling + 几何寄生协同损耗预算（定天花板）

**日期**：2026-09-22 ｜ **模块**：`examples/budget_p3_tile_loss.py` + `lda/lda_layout/mesh_pnr.py`（`build_mesh_pnr`） + `lda/lda_l2/parasitic_rc.py`
**路线**：B+C 自研（主权光电接口 + light-EIC 光子核）｜ **来源**：战略路线图 P0–P4 表 P3
**红线**：A 级永不外借 · C 级自主零依赖 · LLM 不进判决路径 · 零外部求解器依赖。

---

## 一、裁定（结论先行）

P3 完成两件此前未做的新工作，闭合战略路线图 P3：

1. **16×16 tiling（瓦片化）**：把「独立 16×16 酉瓦片」经波导级联成 T 瓦片链
   （块分解架构：大酉 = 多块级联，块间走互连波导）。单个 16×16 瓦片复用 P1-B
   已验证的主权 P&R（`build_mesh_pnr`，DFT(16)），实测 **layout_fidelity=1.0 / DRC PASS /
   LVS ACCEPT / footprint≈0.255 mm²**，GDS 落盘为 `examples/mesh16x16_tile_p3.gds`。
2. **几何寄生协同损耗预算**：对瓦片 GDS 跑主权 `parasitic_rc` 提取全版图 R/C，
   把衬底电容折算为**光学衬底泄漏代理损耗**，与 D1 总线损耗、瓦片间互连路由损耗
   **协同求和**，扫描 T 定出规模天花板（定天花板）。

**天花板（实测，最大 N=16T）**：

| 场景 | α_prop/α_tap | 需均衡可工作 (≤30dB) | 免均衡接收 (≤20dB) |
|------|--------------|----------------------|--------------------|
| A 优化 SOI+绝热 MMI | 1.0 / 0.020 | **N≤512** | **N≤256** |
| B 标准 SOI+方向耦合器 | 2.0 / 0.050 | **N≤256** | **N≤128** |
| C 保守/未优化 | 3.0 / 0.100 | **N≤128** | **N≤64** |

→ 与 D1 定性结论一致（N≤128 安全区确认），并因「瓦片化 + 寄生」维度把天花板刻画为
**可工作 vs 免均衡两条线**：优化工艺 + EO-VOA 幅度均衡下 512 模式仍可行；免均衡接收
上限 64–256 模式，随工艺急剧收紧。

---

## 二、瓦片 + 寄生协同模型（不纸糊楼）

总损耗（dB）= T×瓦片内部损耗 + (T−1)×瓦片间互连路由损耗 + T×寄生衬底代理损耗。

- **瓦片内部损耗** = D1 律 `IL = α_prop·L_bus + n_tap·α_tap`，取 N=16：
  L_bus(16)=35.6×16≈561 µm，n_tap=1.3×15。场景 A/B/C 分别为 0.447 / 1.089 / 2.121 dB。
- **瓦片间互连路由损耗** = `α_prop · L_tile_x`，L_tile_x=561 µm（瓦片 x 跨度），
  每多一级联边界增一段。此模型**不重复计 D1 单融合总线**——D1 假设单一融合总线，
  tiling 保留独立瓦片 + 显式互连开销，故 tiling 总损耗略高于同规模融合单总线（如实）。
- **寄生衬底代理损耗** = `C_SUB_LOSS_DB_PER_FF · C_total · T`，
  C_SUB_LOSS_DB_PER_FF=1e-4（几何级透明系数，亚 dB/瓦片量级，**非 foundry 签核值**）。
  瓦片实测 `C_total=3193.9 fF / R_series=10.2 MΩ / 152 元素` → **0.319 dB/瓦片**。
  该项是「损耗预算协同仿真（几何寄生工具）」的实体贡献：随 T 线性放大，在 T≥32 时
  已成主导项之一（10.2 dB @ T=32），故寄生维度**真实参与**天花板判定，非装饰。

---

## 三、三场景实测明细（接受闸实跑）

```
scn    T     N   IL_bus  IL_link   IL_par   IL_tot  eq≤30  rx≤20
A      1    16    0.447    0.000    0.319    0.766      Y      Y
A      8   128    3.576    0.393    2.555    6.524      Y      Y
A     16   256    7.151    0.842    5.110   13.103      Y      Y
A     32   512   14.303    1.739   10.221   26.263      Y      N
A     64  1024   28.605    3.535   20.441   52.581      N      N
B      8   128    8.711    0.786    2.555   12.052      Y      Y
B     16   256   17.423    1.683    5.110   24.216      Y      N
B     32   512   34.845    3.479   10.221   48.545      N      N
C      8   128   16.967    1.178    2.555   20.700      Y      N
C     16   256   33.934    2.525    5.110   41.569      N      N
```
（完整 8 档 T 见 `examples/_p3_tile_loss_out.txt`）

**关键观察**：寄生代理项在 T=32 已占 10.2 dB（≈总线 14.3 dB 的 71%），证明几何寄生
维度对大规模天花板有**实质性**压顶作用——这正是 P3 相对 D1（纯解析损耗）的新增洞察。

---

## 四、与系列报告联动

- **D1（总线损耗）**：P3 复用 D1 损耗律并扩展为「瓦片链 + 互连 + 寄生」三维；确认 D1
  的 N≤128 安全区（场景 A 免均衡上限 ≥256 ≥128，场景 B/C 更紧，与 D1 一致）。
- **D3（规模上探）**：P3 把 D3「256/512 需幅度均衡」落为**可工作 vs 免均衡两条天花板线**，
  并量化寄生维度的额外压顶（T≥32 寄生占 10+ dB）。
- **P2（EO-VOA 硬件选型）**：P3 的「需均衡可工作 ≤30dB」天花板直接吃 P2 的 EO-VOA
  级 30 dB 动态范围——P2 选型的 30 dB 正是 P3 场景 A 能撑到 N=512 的前提。
- **P1-B（规模爬升）**：P3 的 16×16 瓦片即 P1-B 已验证的 16×16 主权干净 GDS，P3 在其上
  加 tiling + 寄生协同，不重复造轮子。
- **收官**：P0 + P1(A/B/续/C) + D1/D2/D3 + 幅度均衡 PDK 框架 + P2 硬件选型 + **P3 tiling
  + 寄生协同定天花板** 全链路闭合，主权光子计算主链路完整且天花板可量化。

---

## 五、诚实边界（防纸糊楼）

1. **寄生代理系数为几何级**：`C_SUB_LOSS_DB_PER_FF=1e-4` 是透明几何映射（衬底电容→
   光学泄漏代理），非 foundry 签核值；真值由真实 PDK 圆片表征回填（截面损耗、衬底耦合）。
2. **瓦片互连长度取瓦片 x 跨度（561 µm）**：为直线互连基准，未含拐弯/扇出路由开销；
   实际版图互连更长 ⇒ 天花板更紧（保守方向的误差，不虚报乐观）。
3. **瓦片内部损耗用 D1 律**：与 D1 同参数区间，未含弯曲/模场失配/偏振漂移（D1 §五已述
   不推翻线性随 N 结论）。
4. **天花板阈值口径**：P3 用「总损耗 vs 接收余量 20dB / 均衡范围 30dB」；D1 用「路径方差
   IL_var ~3 dB→均衡区」。二者互补：D1 看幅度失衡、P3 看总链路损耗，结论同向。
5. **不阻塞主权 P&R**：本设计为损耗预算层，不修改酉分解 / 版级保真度 / DRC / LVS 判决
   （16×16 瓦片实测 = 1.0 / PASS / ACCEPT）。

---

## 六、交付物

| 文件 | 内容 |
|------|------|
| `examples/budget_p3_tile_loss.py` | P3 接受闸：16×16 瓦片构建 + 寄生协同损耗预算 + 定天花板，EXIT=0 [PASS] |
| `examples/mesh16x16_tile_p3.gds` | 16×16 主权干净瓦片 GDS（LVS ACCEPT） |
| `examples/_p3_tile_loss_out.txt` | 三场景 8 档 T 原始数值 |
| `P3_tile_loss_closeout_2026-09-22.md` | 本收官报告（消除战略路线图 P3 pending） |

入口复算：`PYTHONPATH=D:/agent_LDA/lda python examples/budget_p3_tile_loss.py`（系统 Python 含 numpy）。
