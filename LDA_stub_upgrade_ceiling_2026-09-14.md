# LDA 自证桩升级冲刺 70% —— 诚实上限报告

> 日期：2026-09-14 · 基于 `BENCHMARK_DEFS`（v0.9.77 · 56 锚）权威分类 + `_VMM_OVERRIDES` 终审锁
> 触发：用户指令「自证桩升级冲刺 70%（当前 55.4%）——优先 B2/B11 已升路径的同类（B16 重审留桩、B17-B19/S1-S13 方法学独立候选）」
> 结论：**70% strict 在现行红线（A级不借 + 虚报禁）下不可达；当前 55.4% 即诚实上限。**

---

## 一、权威现状（源：`BENCHMARK_DEFS` 实跑分类）

| 口径 | 数量 | 占比 |
|---|---|---|
| 总数 | 56 | 100% |
| 严格独立 (strict_independent) | **31** | **55.4%** |
| 降级 (degraded_ordinal: E9, E10) | 2 | 3.6% |
| 自证桩 (self_certified_stub) | 23 | 41.1% |
| strict + degraded（非自证） | 33 | 58.9% |

**70% 目标换算**：
- 若按 strict 单计：`56 × 0.70 = 39.2` ⇒ 需 **≥40 strict**（40/56 = 71.4%），即 **+9**。
- 若放宽到 strict+degraded：`56 × 0.70 = 39.2` ⇒ 需 **≥40**（当前 33），即 **+7**。

无论哪种口径，都需要 +7~+9 道锚从「自证桩」移出且**升为 strict 或 degraded**。下文证明：在红线约束下，**0 道 stub 能诚实升 strict，至多 1 道（B21）能诚实升 degraded**。

---

## 二、用户候选集逐一定位（关键冲突）

用户列「B17-B19 / S1-S13」为方法学独立候选。实跑 `BENCHMARK_DEFS` 后：

### 2.1 已严格独立（无需动，占列表 4 道）
`B19`（link_passivity，v0.9.25 已升）、`S7`（gauss_p5_margin，MC↔闭式）、`S8`（gauss_p5_osnr）、`S13`（yield_analytic，解析↔MC 双算法）。
→ **用户的列表里这 4 道其实早已升格**，把列表当"候选"是重复计数。

### 2.2 项目已锁「本就不升」(terminal Tier-1) —— 升 strict = 虚报（占列表 12 道）
来自 `_VMM_OVERRIDES`（benchmarks.py:1334-1374）白纸黑字：
- `B17`：「本就不升：定义同义反复」—— I_c = 2e·E_J/ℏ 是定义式，任何"独立候选"都是同义反复。
- `B18`：「本就不升：regime 越界」—— F_P=4g²/(κγ) 是坏腔极限公式，默认参数落在强耦合区，独立求解器一跑就暴露 regime 越界。
- `S1-S6`：「本就不升：系统/算术合成校验」—— margin=Σ损耗、信道间隔−带宽、OSNR 解析、∏fᵢ、P_tx−IL−Sens 等，**纯算术**，无第二独立物理方法。
- `S9/S10`：「本就不升：系统/算术合成校验」—— LVS 版图-原理图签核（verdict 0/1，确定性集合运算）。
- `S11/S12`：「本就不升：系统/算术合成校验」—— 千器件规模 verdict、阵列分布 verdict(0/1) 不连续。

→ 把这 12 道升 strict，等于把"算术校验 / 定义式 / 签核 verdict"**伪装成物理定律独立交叉验证**，**直接撞铁律红线**（「宁可如实留自证桩，不可假绿」）。**本报告拒绝执行。**

### 2.3 B16（用户明确「重审留桩」）
`B16` MMI 1×2 自映像长度：repo `mmi_eme` 求解核建模的是 **slab（对称平板）**，真实器件是 **rib MMI**——建模对象不同 ⇒ EME 解（~24µm）与抛物线闭式 golden（27µm）差 ~13%，余量仅 **1.3×**（远低于严格锚 100–1000× 标准）。已诚实归档为自证桩。**重审结论：留桩**（与用户指令一致）。

### 2.4 唯一"B2/B11 同类"可再审计项 = B21（但余量不达标）
`B21` PhC 腔共振：golden = λ_res=(n_core+n_clad)·L_cav（一阶近似 n_eff,grating=(n_core+n_clad)/2）；2D FDTD 全波内验吻合 **~2%**。
- 2% ≈ **50× 余量 < 严格锚 100–1000× 标准** ⇒ 诚实只能落 **degraded_ordinal（量级带）**，不增 strict 计数。
- `_VMM_OVERRIDES` 注「待特定结构外部 ORACLE 标定升 Tier-3」——即现在无外部 ORACLE，不能升 strict。

---

## 三、全量 23 道自证桩可行性分类（诚实上限推导）

| 分类 | 锚 | 数量 | 能否诚实升 strict | 障碍 |
|---|---|---|---|---|
| **① 永久 terminal（虚报禁）** | B17, B18, S1, S2, S3, S4, S5, S6, S9, S10, S11, S12 | 12 | ❌ 否 | 定义式/纯算术/LVS 签核，无第二独立物理方法 |
| **② 需 A级场求解器（A级不借）** | B5, B6, B7 | 3 | ❌ 否 | 设计规则锚（Y分支/光栅/交叉），精确真值待 Meep/Tidy3D ORACLE |
| **③ rib 求解器缺失** | B16 | 1 | ❌ 否（用户留桩） | 仅 slab EME，rib MMI 无同对象求解器 |
| **④ 实证锚需 A级语料/几何** | E1, E3, E4, E5, E6, E7 | 6 | ❌ 否 | E4-E7 geometry 不足以唯一确定器件；E1 FDFD 在 SOI 不达标；E3 循环论证 |
| **⑤ 可诚实升 degraded_ordinal（不增 strict）** | B21 | 1 | ⚠️ 仅 degraded | 解析 golden ↔ 2D FDTD ~2%（50×，低于 strict 100×） |
| **合计** | | **23** | **strict: 0 可升** | — |

**推导结论**：
- 能诚实升 **strict** 的 stub = **0 道**。
- 能诚实升 **degraded_ordinal** 的 stub = **1 道（B21）**。
- 故 strict 诚实上限 = **31/56 = 55.4%**（当前即天花板）。
- 即便放宽到 strict+degraded：33 → 34（B21 升 degraded） = **60.7%**，仍 < 70%；再需 +6 只能来自 ②/④（A级，禁）或 ①（虚报，禁）。

---

## 四、红线依据（不可逾越）

- **铁律·验证方法学**：「判决 = 死标量比对 `compare_fn(candidate, golden)`，LLM 不进判决路径」「标签≠行为 / 值≠行为」「优先可行性与创新而非克隆旧系统」。
- **诚实边界铁律**：「宁可如实留自证桩，不可假绿」「自证桩是合法第一阶段非缺陷」。
- **主权分层（T1/T2）**：A 级（Meep/Tidy3D 全波场求解器）**永不借**；B 级借今踢后；C 级自主。②/④ 类升级需 A 级 ORACLE ⇒ 红线禁止。
- **判据 D（假独立拦截）**：数值积分 vs 解析闭式若在默认参数下剖分守恒则代数恒等，残差≡0 是"同一式子的两种写法"——B17/S1-S6 即此，判假独立。

---

## 五、若强行冲 70% 的两条禁路（明确拒绝）

| 禁路 | 操作 | 后果 |
|---|---|---|
| 虚报 | 把 B17/S1-S6/S9-S12 标 strict | 撞「不可假绿」红线；破坏"主权可验货"叙事；CI 反向测试必暴露 candidate≡golden |
| 借 A级 | 用 Meep/Tidy3D 给 B5/B6/B7/E锚升 strict | 撞「A级永不借」红线；主权降级 |

**两条均不可取。** 70% 是"数量指标"，红线是"质量底线"——底线优先。

---

## 六、诚实可执行的后续动作（建议）

1. **B21 诚实升 degraded_ordinal（可选）**：把 2D FDTD 内验接为 harness 候选、标 degraded_ordinal（量级带 ~2% 吻合）。非自证占比 33→34（60.7%），B21 移出"自证桩"，账本语义改善；**不触 70% strict**。需补 `candidate_status="degraded_ordinal"` + 常驻 smoke。
2. **把本报告入档**：作为"70% 不可达"的官方裁决，防未来重复审议（参照 B2 `_VMM_FINAL_VERDICT` 终审锁模式）。
3. **若确需 70% strict**：唯一合法路径是**放宽红线前提**——批准"借 A级给 B5/B6/B7 + 补 E4-E7 器件几何语料"，属主权层级决策（T1/T2 解锁裁定），**须用户显式授权且走分层解锁流程**，不在本冲刺范围内。
4. **修复 `lda-anchor-wiring` skill 陈旧条目**：该 skill「不可接清单」仍列 B2 为不可接，但 B2 已于 v0.9.66 升 strict（LOCKED_STRICT_INDEPENDENT）。属文档陈旧，建议同步。

---

## 七、一句话裁决

**55.4% = 诚实上限。70% 在红线内不可达。用户列表里 4 道已升、12 道锁死（虚报禁）、1 道留桩、1 道（B21）仅能 degraded。零道可诚实升 strict。守红线，不出假绿。**
