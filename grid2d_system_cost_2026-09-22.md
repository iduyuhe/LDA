# grid2d 系统级代价专题（D1 损耗 + D2 热串扰 + D3 规模）

> 合并 P1-B 续 2D 压实网格闭合报告 §八 三项待裁定（D1/D2/D3）。
> D1 闭环：`D1_bus_loss_budget_2026-09-22.md`；D2 闭环：`D2_thermal_crosstalk_budget_2026-09-22.md`。
> 日期：2026-09-22。结论：**D1 硬件级(∝N)、D2 控制级(N无关)、D3 绑定 D1 的 PDK**。

## §一 两种代价的本质差异（一句话）

| 维度 | D1 总线传播损耗 | D2 同列抽头热串扰 |
|---|---|---|
| 随 N 缩放 | **∝ N**（L_bus≈35.6·N µm，抽头数∝N） | **N 无关**（d_min=rail_pitch=4µm 恒定） |
| 层级 | 硬件（波导/耦合器插入损耗） | 控制/标定（热耦合矩阵 Γ） |
| 校正手段 | 幅度均衡 PDK（P2） | 原位标定 `φ_cmd=Γ⁻¹φ_tgt` |
| 阻断性 | N≥256 需硬件否则保真塌 | 非阻断（静态全可标定） |
| 最坏裸值 | 均值≤3dB@128 | 未标定 0.08–0.16 rad/单邻 |

## §二 D1 复算要点（详 `D1_bus_loss_budget_2026-09-22.md`）

真实 grid2d 几何（U=DFT(N)）：L_bus ≈ 35.6·N µm；col_pitch 饱和 ~35.5µm；每轨抽头≈N。
路径间损耗方差由**抽头耦合器插入损耗主导**（var_tap ≈ 9–10× var_prop）。

| N | α_prop=2,α_tap=.05 | 结论 |
|---|---|---|
| 4 | IL_var=0.17 dB | 免均衡 |
| 16 | IL_var=1.03 dB | 免均衡 |
| 128 | IL_var=9.11 dB | 需 α_tap≤.02 才免均衡 |
| 256 | IL_var=18.3 dB | 需绝热 MMI(α_tap≤.01)+评估幅度均衡 |

## §三 D2 复算要点（详 `D2_thermal_crosstalk_budget_2026-09-22.md`）

d_min_D-D=4µm、d_min_int=8µm（确定值）。未标定单邻动态误差 0.08–0.16 rad（全 N FAIL）；
但 Γ 固定对角占优 ⇒ **一次标定全补偿静态项**，残余仅时漂（周期再标定）。
深槽隔离（c0 .002→.001）在 N≥128 为推荐项，收紧漂移/标定负担，非强制。

## §四 合并规模裁决表

| N | 面积(grid2d) | D1 损耗 | D2 热串扰 | 综合门控条件 |
|---|---|---|---|---|
| 4 | 0.06 mm² | 免均衡 | 标定即可 | 工作 |
| 16 | 0.36 mm² | 免均衡 | 标定即可 | 工作 |
| 128 | 2.34 mm² | α_tap≤.02 免均衡 | 深槽+周期标定 | 工作（推荐隔离） |
| 256 | ~9.3 mm²* | 需绝热 MMI+评估幅度均衡 | 深槽+气包+闭环 | **绑定幅度均衡 PDK** |
| 512* | ~37 mm²* | 必须幅度均衡 | 深槽+气包+闭环 | 必须幅度均衡 PDK（P2） |

（*512 由线性外推：面积∝N·col_pitch；精确分解 O(N⁴) 超时，外推不影响门控判定。）

## §五 D3 方向（待裁定，非阻塞）

D3 = 256/512 规模再上探。判定：**D3 不独立存在，须与 D1 的幅度均衡 PDK 绑定排期**。
- grid2d 列数≈N、几何∝N，规模上探**仅增列**，主权 P&R 算法不变（O(N) 列分配已证）。
- 真正的规模天花板由 D1（幅度均衡）而非 D2（标定）决定；D2 在 N=512 仍 N 无关、可标定。
- 建议：D3 排期 = 「幅度均衡 PDK 就绪」之后；此前 128 为演示锚点上限。

## §六 诚实边界与待钉 PDK 参数

- 几何（rail_pitch/col_pitch/d_min）为确定值；损耗/串扰系数为经验区间，须 PDK 实测钉死：
  - `α_prop`（波导传播，dB/cm）、`α_tap`（抽头耦合器插入，dB/tap）
  - 热耦合矩阵 `Γ`（或 `c0`）、时漂率（决定再标定间隔）
- 两项代价均属 **PDK/标定层**，不破坏 grid2d 主权 P&R（酉分解/版级保真/DRC/LVS/驱动映射已证无关）。
- 幅度均衡（D1 缓解）与热标定（D2 缓解）均需原位光源+探测器+闭环驱动 —— 系统级外围，非版图内核。

## §七 交付物
- `D1_bus_loss_budget_2026-09-22.md` · `examples/budget_bus_loss_d1.py` · `examples/_d1_bus_loss_out.txt`
- `D2_thermal_crosstalk_budget_2026-09-22.md` · `examples/budget_thermal_d2.py` · `examples/_d2_thermal_out.txt`
- 本专题 + 上游 `P1B_2d_grid_closeout_2026-09-22.md`
