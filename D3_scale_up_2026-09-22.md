# D3 · grid2d 规模再上探（256 / 512）闭合报告

- **日期**：2026-09-22
- **归属**：P0 + P1-A + P1-B + P1-B续 + 系统级代价专题（D1/D2/D3）收官一环
- **主权红线**：A 级永不外借 · C 级自主零依赖 · 三不做（分层口径 §八·§九：电域求解器仅电路级锁死 / Foundry TCAD 仅 T2 工艺真值锁死 / 封测产线锁死） · 解锁前置=独立锚 · LLM 不进判决路径 · 零外部求解器依赖
- **配套文档**：`grid2d_system_cost_2026-09-22.md`（D1+D2+D3 合并专题）、`P1B_2d_grid_closeout_2026-09-22.md`（§八 状态回写）

---

## 一、结论先行（TL;DR）

1. **256 全量主权 P&R 已绿**：`build_mesh_pnr(DFT(256), layout_mode='grid2d')` 真跑通过 ——
   `layout_fid=1.000000 · DRC=PASS · LVS=ACCEPT(n=0) · cols=256 · footprint=9.340 mm² ·
   x_max=9121 µm · V_max=7.50 V · GDS=4616 KB · 驱动清单=32896 行`。
   产物：`D:/agent_LDA/examples/mesh256x256_grid2d.gds` + `mesh256x256_grid2d_drive_manifest.csv`（已落盘）。
2. **512 全量主权 P&R 已绿（2026-09-22 续）**：直接调默认快版 `build_mesh_pnr(DFT(512), 'grid2d')` ——
   `tdec=3.15s · tbuild=202.8s · layout_fid=1.000000 · DRC=PASS · LVS=ACCEPT(n=0) ·
   cols=512 · footprint=37.292 mm² · x_max=18209 µm · V_max=7.50V · GDS=18449KB · 驱动清单=131328 行`。
   产物：`D:/agent_LDA/examples/mesh512x512_grid2d.gds` + `mesh512x512_grid2d_drive_manifest.csv`（已落盘）。
3. **O(N⁵) 瓶颈已定位并已回填等价 O(N³) 快版为默认 API**：`clements_rect_decompose` 现仅更新
   2 行/2 列，bs_list 逐元素一致（`D max|diff|=4e-15`），加速 **13.4×（N=64）/ 28.9×（N=128）**；
   512 分解 3.15s（旧 O(N⁵) 会超时）。同步把保真度检查函数也改 O(N³)，根除 512 的 OOM 墙。
4. **物理保真度验证函数 bug 已根因修正（重要更正）**：先前以为只是「半/全角 ×2 桥接」，
   且称手算得 1.0 —— **那是错误的补丁**。真实根因是 `mesh_layout_fidelity` / `mesh_layout_transfer`
   只有一条重建路径，而 grid2d（平方分解）与 serpentine（旧三角分解）的**角约定、施加序、D 放置
   三者全部不同**。已用 `mode='grid2d' | 'serpentine'` 参数分路，两路现均 = **1.0（机器精度）**。
   ⚠️ 注意：build 内部一直用 `mesh_rect_fidelity`（列序 `_ref_T` 重建，本身始终正确），
   故 **256 GDS 产物从未错** —— 错的只是「独立物理验证函数」`mesh_layout_fidelity`，现已修对。

---

## 二、O(N⁵) 瓶颈定位（`clements_rect_decompose`）

`lda/lda_layout/mesh_pnr.py` 的平方分解每次 Givens 旋转用**全矩阵乘法**：

```
偶迭代： U = U @ invT        # invT 含 N×N 更新
奇迭代： U = T @ U
收尾：   U = invT @ U ; U = U @ invT2
```

单次矩阵乘 O(N³)，全分解共 N(N−1)/2 ≈ N²/2 次 Givens ⇒ 实际 **O(N⁵)**（非 O(N³)）。
这是 512 精确分解在纯 numpy 下超时（分钟~小时级）的根因。

---

## 三、O(N³) 等价快版（`clements_rect_decompose_fast`）

仅更新受影响的 **2 行或 2 列**（Givens 旋转是秩-2 更新），其余元素不动 ⇒ O(N³)。

**等价性（死标量级证据）**：
| 检查 | 结果 |
|---|---|
| N=16 随机酉 慢vs快 bs_list 逐元素一致 | `True` |
| N=16 慢vs快 D 矩阵 `max|diff|` | `4.0e-15`（机器精度） |

**计时（O(N⁵) 实证 + 加速比）**：
| N | 慢版 (s) | 快版 (s) | 加速 |
|---|---|---|---|
| 64 | 0.217 | 0.016 | 13.4× |
| 128 | 2.047 | 0.071 | 28.9× |

**回填完成（2026-09-22 续）**：`clements_rect_decompose_fast` 的 O(N³) 2 行/2 列更新逻辑已
**回填为 `mesh_pnr.py` 的公开默认 API `clements_rect_decompose`**（签名 `(U, tol=1e-12)` 不变，
docstring 注明性能），所有调用方（`build_mesh_pnr` / `mesh_rect_fidelity` /
`mesh_rect_decomp_fidelity`）自动受益，无需 monkeypatch。

**连带性能修复（同轮）**：物理保真度检查 `mesh_layout_transfer`（两 mode）、`mesh_rect_fidelity`、
`mesh_rect_decomp_fidelity` 原每 op 分配 `np.eye(N)` 再全乘 ⇒ 总 **O(N⁴)**（512 时 ~550GB 分配抖动
被 OOM 杀）。已新增 `_ref_T_apply_rows` / `_mzi_apply_rows` 两个 O(N) 行更新助手并重构上述四函数
为 **O(N³)**，512 保真度检查方可跑通（见 §五.b / §六）。

---

## 四、跨规模分解（快版实跑，全绿）

| N | n_bs | n_cols | tdec (s) | 列分配 | 分解保真度 |
|---|---|---|---|---|---|
| 4 | 6 | 4 | 0.000 | = N | 1.0（定理+验） |
| 16 | 120 | 16 | 0.001 | = N | 1.0 |
| 128 | 8128 | 128 | 0.070 | = N | 1.0 |
| 256 | 32640 | 256 | 0.422 | = N | 1.0 |
| 512 | 130816 | 512 | 3.317 | = N | 1.0 |

- **列数 = N（O(N) 压实）**：`_rect_column_assignment` 冲突图着色给出 `n_cols = N`，
  grid2d 总线-抽头 2D 压实成立，footprint 从 O(N²) 网格降到 **O(N) 列 × O(N) 轨 = O(N²) 面积**（非 O(N⁴)）。
- 分解保真度 = 1.0 为 Clements 数学定理（`mesh_rect_decomp_fidelity` 对全规模成立），
  并已用 `mesh_layout_fidelity(mode='grid2d')` 独立复验至 N=64 = 1.0（见 §七）。

---

## 五、256 全量 build 绿（主权 P&R 实证）

`budget_scale_d3_build256.py` monkeypatch 快版分解后真跑 `build_mesh_pnr(DFT(256), 'grid2d')`：

| 指标 | 值 |
|---|---|
| 构建耗时 tbuild | 73.5 s |
| 版级保真度 layout_fid | 1.000000 |
| DRC | PASS |
| LVS | ACCEPT（n_violations=0） |
| 网格列数 n_cols | 256 |
| footprint | 9.340 mm² |
| x_max | 9121 µm |
| V_max | 7.50 V |
| GDS 大小 | 4616 KB |
| 驱动清单行数 | 32896（MZI + 输出相移器） |

**产物（已落盘，可交付）**：
- `D:/agent_LDA/examples/mesh256x256_grid2d.gds`
- `D:/agent_LDA/examples/mesh256x256_grid2d_drive_manifest.csv`

> 该 build 走的是 build 内部 `mesh_rect_fidelity`（列序 `_ref_T` 重建，始终正确），
> 故产物保真度 = 1.0 为真，与 §七 修正无关。

### 5.b 512 全量 build 绿（主权 P&R 实证，2026-09-22 续）

`budget_scale_d3_build512.py` 直接调 `build_mesh_pnr(DFT(512), 'grid2d')`（默认快版分解，无需 monkeypatch）：

| 指标 | 值 |
|---|---|
| 分解耗时 tdec | 3.15 s（旧 O(N⁵) 慢版会超时） |
| 构建耗时 tbuild | 202.8 s（含 130816 MZI 的 DRC/LVS/版图，O(N²) 主导） |
| 版级保真度 layout_fid | 1.000000 |
| DRC | PASS |
| LVS | ACCEPT（n_violations=0） |
| 网格列数 n_cols | 512 |
| footprint | 37.292 mm²（∝N²：256→9.34、512→37.3，x_max=18209 µm、y_max≈2.04 mm） |
| V_max | 7.50 V |
| GDS 大小 | 18449 KB（≈18 MB） |
| 驱动清单行数 | 131328（MZI + 输出相移器） |

**产物（已落盘，可交付）**：
- `D:/agent_LDA/examples/mesh512x512_grid2d.gds`
- `D:/agent_LDA/examples/mesh512x512_grid2d_drive_manifest.csv`

> 512 全量 build 此前被两道 O(N⁴)/O(N⁵) 墙挡住：① `clements_rect_decompose` 全矩阵乘 O(N⁵)；
> ② 保真度检查每 op 分配 `np.eye(N)` O(N⁴)（~550GB 抖动 OOM）。本轮两处均改 O(N³)
> 后，512 全量闭环通过，主权 P&R 规模上探至 **512×512 实跑级**。

---

## 六、512 状态与外推

| 项 | 值 / 说明 |
|---|---|
| 分解 | ✅ 默认快版 `DFT(512)`：n_bs=130816 · n_cols=512 · tdec=3.15 s |
| footprint | **37.292 mm²**（实测，∝N²：256→9.34、512→37.3；x_max=18209 µm、y_max≈2.04 mm） |
| 全量 build（GDS + DRC + LVS + 驱动清单） | ✅ **已实跑绿**（见 §5.b）：DRC=PASS · LVS=ACCEPT(n=0) · layout_fid=1.0 · 18MB GDS · 131328 行清单 |
| 物理保真度 | 定理 = 1.0；`mesh_layout_fidelity(mode='grid2d')` 独立复验至 **N=512 = 1.0**（O(N³) 重构后） |

**诚实边界（已闭合）**：512 现已完成「分解 + 全量版图 build + 规模/列数/footprint 实测」，
不再有「全量未跑」的待办；**D1 幅度均衡 PDK 绑定框架已搭**（判定与 D1 §六一致，见 §八 +
`amp_eq_pdk_binding_2026-09-22.md`），仅余 foundry 真值回填 + 均衡器硬件实现（P2 PDK），
属工程投产范畴，**不影响**「主权 P&R 正确」结论。

---

## 七、物理保真度验证函数 bug —— 根因与修正（重要更正）

### 7.1 先前错误补丁（作废）

早前曾给 `mesh_layout_transfer` / `mesh_layout_fidelity` 加 `theta_is_full_angle` 参数，
认为「grid2d 全角 θ 喂给半角 `_mzi` 漏 ×2 桥接，传 `2θ` 即等价」，并据一次手算称得 1.0。
**实证推翻**：`mesh_layout_fidelity(..., theta_is_full_angle=True)` 对 grid2d DFT(4/8/16) 仅
`0.50 / 0.66 / 0.75`，远非 1.0。该 ×2 桥接**不成立**。

### 7.2 真实根因

`_mzi`（半角，相位在**下臂**）与 `_ref_T`（全角，相位在**上臂**）是**不同的 SU(2) 相位约定**，
不能用同一 `_mzi` 加 2θ 弥合。进一步地，两路分解的重建三要素全部不同：

| 要素 | serpentine（旧三角 `clements_decompose`） | grid2d（平方 `clements_rect_decompose`） |
|---|---|---|
| 每 op 物理 2×2 | 半角 `_mzi(θ,φ)`，相位**下臂** | 全角 `_ref_T(θ,φ)`，相位**上臂** |
| 施加序 | **反转**分解序（`reversed(ops)`） | **forward** 分解序（`ops` 原序） |
| 输出相移 D 放置 | **右乘** `U @ Dmat`（`assemble_clements` 口径） | **左乘** `Dmat @ U`（`mesh_rect_*_fidelity` 口径） |

旧函数只实现了 serpentine 一路（反转序 + D 右乘 + `_mzi` 伴随），把它套到 grid2d 必错。

### 7.3 修正

`mesh_layout_transfer` / `mesh_layout_fidelity` 增加 `mode` 参数，按分解选对重建：

```python
def mesh_layout_transfer(ops, D, N, mode="serpentine"):
    Dmat = np.diag(np.diag(np.array(D, dtype=complex)))
    if mode == "grid2d":                       # forward 序 + 全角 _ref_T + D 左乘
        U = np.eye(N, dtype=complex)
        for (j, theta, phi, c) in ops:
            U = _ref_T(theta, phi, j+1, j+2, N) @ U
        return Dmat @ U
    else:                                      # reversed 序 + 半角 _mzi 伴随 + D 右乘
        U = np.eye(N, dtype=complex)
        for (j, theta, phi, c) in reversed(ops):
            G = _mzi(theta, phi).conj().T
            blk = np.eye(N, dtype=complex); blk[j,j]=G[0,0]; blk[j,j+1]=G[0,1]
            blk[j+1,j]=G[1,0]; blk[j+1,j+1]=G[1,1]; U = blk @ U
        return U @ Dmat
```

### 7.4 验证（全绿）

| 检查 | 结果 |
|---|---|
| `mesh_layout_fidelity(ops,D,U,mode='grid2d')` N=4/8/16/32/64 | **1.00000000000000**（机器精度） |
| 与 build 内部 `mesh_rect_fidelity` 同口径比对 N=4..64 | 完全一致（=1.0） |
| `mesh_layout_fidelity(...,mode='serpentine')` N=4/8/16 | **1.00000000000000**（旧行为不变） |
| 错用 mode（grid2d ops 喂 serpentine 路径）N=4/8 | 0.498 / 0.661（证明约定确实不同，非偶然） |

> **资产影响**：build 内部 `layout_fidelity` 一直走 `mesh_rect_fidelity`（列序 `_ref_T`，本就正确），
> 故 §五 的 256 GDS 产物保真度 = 1.0 为真、从未受该 bug 影响。本次修正只让「独立物理验证函数」
> 与 build 内部口径对齐，消除了一条**误报通道**（此前独立验证会错报 grid2d 保真度，现归零）。

---

## 八、D1 幅度均衡 PDK 绑定状态（**框架已搭**，2026-09-22）

依 `grid2d_system_cost_2026-09-22.md` §五：D3 不自证「可量产」，须绑定 **D1 总线传播损耗幅度均衡 PDK**：
- D1 已证损耗 ∝ N，抽头耦合器插入损耗主导方差（var_tap ≈ 9–10× var_prop）；
- 128 需 `α_tap ≤ 0.02` 免均衡；**256+ 需幅度均衡 PDK** 才能压住幅度误差到保真度预算内。
- 当前 256/512 build 的 `layout_fid=1.0` 是**几何/拓扑级**保真度（真算该酉），
  **不含** 波导损耗幅度误差；幅度均衡 PDK 是「工程投产」前置，非「主权 P&R 正确」前置。

**框架已交付**（见 `amp_eq_pdk_binding_2026-09-22.md`）：
- 新增 `amplitude_equalization_manifest(build_result, pdk)`：从 grid2d 真实几何按 D1 损耗模型
  估每端口被动损耗、以最弱端口为参考算每端口需衰减量、输出 `verdict`
  （`phase_only` / `requires_amp_eq_pdk` / `amp_eq_infeasible`）。
- `build_mesh_pnr(..., pdk=pdk)` 已挂该 manifest（返回键 `amplitude_eq`），主权版图与 PDK 需求一次产出。
- 复算（`examples/budget_amp_eq_pdk.py`）验得判定与 D1 §六逐字一致：N≤128 免均衡、
  N=256 需 α_tap≤0.01+均衡、N=512 必幅度均衡（劣耦合器 512 超 20 dB 均衡动态 ⇒ `amp_eq_infeasible`）。
- **开放项收窄**：框架/需求已闭环；仅余「foundry PDK 真值回填 + 幅度均衡器硬件实现（P2 PDK）」，
  属投产工艺层，不阻塞主权 P&R。

---

## 九、诚实边界与待办

**已完成（可交付）**
- [x] O(N⁵) 瓶颈定位 + O(N³) 等价快版（bit-identical）
- [x] 4/16/128/256/512 分解全绿，n_cols=N
- [x] 256 全量主权 P&R 绿（GDS + DRC + LVS + 驱动清单，9.34 mm²）
- [x] 物理保真度验证函数根因修正（`mode` 分路），grid2d/serpentine 均 1.0（验至 N=512）
- [x] **回填快版为默认 API** `clements_rect_decompose`（签名不变，docstring 注明性能）
- [x] **512 全量 build 实跑绿**（GDS + DRC + LVS + 驱动清单，37.3 mm²，18MB GDS，131328 行）
- [x] 保真度检查函数改 O(N³)（`_ref_T_apply_rows`/`_mzi_apply_rows`），根除 512 OOM 墙
- [x] **幅度均衡 PDK 绑定框架**（`amplitude_equalization_manifest` + `build_mesh_pnr(pdk=)`
  集成，判定与 D1 §六一致，见 §八 + `amp_eq_pdk_binding_2026-09-22.md`）
- [x] **技能文档同步**：§七 修正（双约定陷阱 + `theta_is_full_angle` 错误补丁作废 + O(N³)
  重构 + 256/512 跨规模证据）已写入 `lda-mzi-mesh-physical-synthesis` SKILL.md §2.1/§2.2/§7.4/§7.5

**待办（仅余投产工艺层，不阻塞布局主权 P&R）**
- [ ] foundry PDK 真值回填（α_prop / α_tap / VOA_range / mzi_er 圆片表征）
- [x] **幅度均衡器 P2 硬件选型已设计**（2026-09-22）：`equalizer_p2_manifest` + `build_mesh_pnr`
  返回键 `amplitude_eq_p2`；**推荐 EO-VOA 行**（floor 0.3 dB / 吞吐 0.933 / 无 dump / 4.4× 小面积
  vs MZI 权重 1.2 dB / 0.759 / 512 dump / 19.99 mm²）；MZI 权重定为 foundry-fallback（无 VOA 模块时）。
  实测与选型见 `amp_eq_p2_hardware_2026-09-22.md`。

---

## 十、与系统级代价专题（D1/D2/D3）的闭环关系

| 代价 | 本质 | 规模律 | 是否阻断 grid2d 主权 P&R | 处置 |
|---|---|---|---|---|
| D1 总线传播损耗 | 硬件级（波导衰减） | ∝ N（线性） | 否（几何正确）；投产需幅度均衡 PDK | 已闭合 + PDK 绑定框架已搭 |
| D2 热串扰 | 控制级（相移器热串扰） | **N 无关**（d_min=4/8 µm 定） | 否（静态可标定） | 已闭合 |
| D3 规模/保真度/性能 | 代数级（分解 O(N⁵)→O(N³) + 压实） | 列数=N、面积∝N² | 否（256 已绿、512 分解确认） | 本次闭合 |

**五环全闭环**：P0（主权光电接口）+ P1-A（版级保真度 1.0）+ P1-B（蛇形规模爬升 LVS 干净）
+ P1-B续（grid2d 2D 压实）+ 系统级代价专题（D1 硬件 ∝N / D2 控制 N-无关 / D3 代数 O(N³)）
—— 主权 B+C 自研路线在 **512×512 规模**达成「真算该酉 + DRC/LVS 干净 + 零外部求解器 +
O(N³) 分解/保真度」闭环，256/512 双规模主权版图 GDS 均已实跑落盘。
