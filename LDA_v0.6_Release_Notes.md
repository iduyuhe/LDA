# LDA v0.6 Release Notes

> 版本：v0.6（2026-08-24）
> 里程碑定位：**3D 逆设计纵深 + QEDA 求解器级补强**——在 v0.5 系统级（M7 + 护城河 + 逆设计纵深四阶）之上，把逆设计从 2D 推向 **3D 全链**（3D adjoint → 3D 截面 → 3D 端口验收 → 谱形×3D → 3D numba 性能），并补强 **QEDA 求解器栈**（transmon-resonator 色散读出三能级严格求解），形成"2D/3D 双轨 + 光/量双栈"完整开源基线。
> 红线不变：**LLM 不进判决路径**，PASS/FAIL 由死标量比对（物理定律锚 + 确定性裁判）决定。

---

## 一、v0.6 相比 v0.5 新增（D-84~D-89）

### 1. Track A 3D 逆设计纵深五阶（破 3D 诚实边界）
| 阶 | 交付 | 实测 |
|---|---|---|
| ① 3D adjoint 形状（D-84） | adjoint 从 2D 推向 **3D Yee 交错网格**（6 分量），更新算子**显式转置**伴随（不依赖 torch/jax）——**数值 Mᵀ 随机对拍 P_H 1.3e-15 / P_E 1.4e-15 / FULL 6.9e-16**；平板波导宽度曲线 w(x)（K 控制点 + 软边界 + 可制造 DRC） | 3D adjoint FD 对拍 9.4e-6、形状梯度 8.2e-4、imp **2.02×**（聚焦 taper 成形）；smoke 3/3 |
| ② 3D 截面形状（D-85） | z 截面也变成形状自由度——**宽度 w(x) × 厚度 h(x) 双软边界**（z 底固定 0、顶 z_top=h(x)，处处可导；联合梯度 [dFOM/dw ⊕ dFOM/dh] + 双界 DRC） | 截面梯度 1.0e-2、imp **3.17×**（**比平板 2.02× 提升 57%——厚度自由度增益显著**）；smoke 4/4 |
| ③ 3D 逆设计 × 端口验收（D-86） | 战略审计（LDA-ST-001）最大缺口闭环：**3D adjoint 场能优化 → 独立 3D CW 端口核**（S11/S21，src_profile 可配）→ **双独立确认** | FOM imp **1.88× 且 S21 0.132→0.211（1.60×）同向双过**；能量守恒 S11+S21≈1；**关键物理认知：聚焦 FOM ≠ 透射 S21**（w_min=4/init_w=6 对齐）；smoke 3/3 |
| ④ 谱形目标 × 3D 截面（D-87） | 2D 谱形/多波长目标扩展到 3D：**物理网格固定只变 omega**（归一化网格陷阱免疫）+ **多波长加权联合梯度**（分块归一化 w/h 各自尺度）+ 全波长线搜索 | 加权 imp **3.13×**（逐波长 3.18×/3.15×/3.06× 三波长同向 ≥3×）、联合梯度 4.2e-4；smoke 5/5 |
| ⑤ 3D adjoint numba 化（D-89） | 3D Yee 核 + 显式转置反向 **prange 并行 JIT**（backend 参数 auto/numba/numpy，**无 numba 自动回退**） | forward 加速 **44 域 8-11× / 64 域 17-21× / 80 域 22-29×（最大域 ≥20×）**；与 numpy **bit-level 一致（FOM rel ≤ 3.7e-16）**；优化链路 64 域 4.4-5.2×（imp 完全一致）；smoke 6/6 |

**3D 诚实边界打破**：v0.5 的"3D 形状需 3D adjoint 核（归后续）"由 D-84 落地（Mᵀ 1e-15），D-85 深化（截面双自由度），D-86 补端口级验收（闭环最后拼图），D-89 突破域规模天花板（80 域 22×+ 接近实时）。

### 2. QEDA 求解器级补强（D-88 · 量子蓝海占位）
| 交付 | 内容 | 实测 |
|---|---|---|
| **transmon-resonator 色散读出严格求解器** | 三能级 transmon（\|g⟩\|e⟩\|f⟩，E_f=2f_q+α）+ Fock 谐振器**联合严格对角化**（D-43 二能级 JC 升级）——真实色散 **χ=g²α/(Δ(Δ+α))**（Blais 2011 非谐性修正）↔ 严格数值双路径 | χ_num=-0.002251 ↔ χ_an=-0.002308（**rel 2.5%**）；**二能级近似 rel 77.5%（α 修正必要性 31×，χ 为负即非谐性标志）**；拉比自洽 0.02%；**n_crit=25 / Purcell γ=5e-5 GHz（T1≈3.2e6 µs）/ AC Stark 2nχ**；smoke 3/3（含色散区失效负例） |

### 3. 其它
- WebUI **五十面板**（新增 ㊺3D adjoint 形状 / ㊻3D 截面 / ㊼端口联合验收 / ㊽谱形×3D / ㊾numba 性能 / ㊿QEDA 色散读出）
- 性能基准 `run_perf_adjoint3d.py`（D-89，大域 ≥20× 验收门）
- 全部交付三端同步（本地 / Gitee / GitHub，tree 级零差异 383/383）

## 二、v0.6 可对外宣称的能力
1. **3D 逆设计全链**（新增）：3D Yee 显式转置伴随（Mᵀ 1e-15）→ 3D 形状/截面逆设计 → **3D 端口 S 参数级验收（双独立确认）** → 谱形/多波长目标 3D 化 → numba 性能升维（大域 20×+）。
2. **QEDA 求解器栈**（新增）：transmon-resonator 色散读出三能级严格求解（含非谐性修正、n_crit/Purcell/AC Stark 全套 readout 物理量）——量子蓝海差异化求解器。
3. **闭环引擎**（继承 + 补强）："设计目标 → 搜索参数 → 真实求解器 → 双重验证（解析契约 + 数值物理自洽）→ 端口级验收 → 已验证器件"全链可用。
4. **开放标准 + 验证合约**（继承 v0.5）：L0 IR 开放标准 v0.3 + 一键全量回归（62 smoke + harness B1-B13）。
5. **性能**（新增）：3D adjoint 大域 20×+（numba 并行 JIT，无 numba 自动回退纯 numpy，bit-level 一致）。

## 三、诚实边界（对外基线 v0.6）
- **3D 域**：3D 逆设计为小-中域（44×36×12 ~ 80×60×20，内存/算力约束下 dl≈0.155µm）；z 截面参数化为单芯 w×h（分叉/脊波导归拓扑域）。
- **FOM 语义**：3D 逆设计 FOM 为收集场能（T>1 聚焦增益非功率透射）；S 参数为两端口功率占比（P_out/(P_in+P_out)），非严格模式分解。
- **量子侧**：色散读出为三能级有效模型（非全电荷基底多能级展开）；χ 修正解析式在弱耦合色散区（Δ/g≥5）成立，强耦合区以严格数值为准。
- **实证锚**：实证大数据锚（验证锚②）待发动期数据（D-62 暂缓）——物理定律锚（验证锚①）为当前唯一已落地地基。
- **不宣称**：签核级（tape-out signoff）、与商业 EDA 数值库逐点一致、量子门级多能级数值仿真。

## 四、验证证据（全量）
- harness 13 题（B1-B13）物理定律锚全过；
- CI core 全量回归 PASS / 0 FAIL；
- 62 smoke 全绿（D-01~D-89 各交付 smoke）；
- 三端 tree 级零差异同步（383/383）。

## 五、下一步候选（v0.6 之后）
- **实证锚（D-62）**：发动期启动后落地实证大数据锚（验证锚②）；
- **QEDA 纵深**：多能级电荷基底展开 / 多 qubit 读出串扰 / 驱动场 Rabi/AC Stark 求解器；
- **3D 纵深**：3D 拓扑逆设计（voxel 3D）/ 3D 多波长联合 / 3D 光栅耦合器逆设计；
- **生态**：PDK 对接（晶圆厂）、harness 开源题库扩充、标准社区共建。
