# P0 死缺口闭合验证报告 · MZI 网格 P&R（主权零依赖）

> 日期：2026-09-22 · 模块 `lda/lda_layout/mesh_pnr.py` · 接受闸 `examples/demo_mesh_pnr_4x4.py`
> 战略依据：`LDA_strategic_plan_2026-09-22.md` §7 P0 验收闸

---

## 1. 一句话结论

**"酉分解 → 摆位 → 连波导 → GDS → 主权 DRC/LVS 签核" 死缺口已闭合**，且酉分解重构保真度达到**机器精度（1.0）**——光计算"真能算"的数学可行性被硬证据锁定。

---

## 2. 硬证据（实测，非声明）

| 指标 | 结果 | 判据 |
|---|---|---|
| 4×4 DFT 重构保真度 | **1.000000** | =1.0 为机器精度完美重构（‖U_rec−U_tgt‖_F ≈ 5e-16）|
| MZI 单元数 | **6** | = N(N−1)/2 = 6，无冗余/无缺失 |
| 单位阵 / 随机酉 保真度 | **1.000000**（N=4/7/16，各 12 例）| 数学定理级，跨尺寸稳定 |
| DRC | **PASS** | 逐 MZI 单元 min_width/min_space 全绿 |
| LVS | **ACCEPT** | 违规 0 · 网一致 {设备 14/14 · 网 16/16} |
| 真实 GDSII | `examples/mesh_4x4_dft.gds`（10 PATH/BOUNDARY）| 主权编码器 round-trip 解析通过 |
| 几何最小间距 | 3.200µm（≥ DRC 0.20µm）| 不桥接 |
| 接受闸 EXIT | **0** | 硬闸通过 |

---

## 3. 本轮修复的关键 bug（Clements 矩形分解）

最初实现的 Clements 分解在**多行列**上枢轴链断裂：偶列自上而下 `j=k..N-2` 推进时，第 j 步零化 `U[j+1,k]` 后，下一项枢轴 `U[j+1,k]` 已被置零，导致后续 MZI 退化为 θ=π 翻转、回带非零 → 残差非对角 ~0.6–0.9、保真度仅 0.72–0.85。

**修正**（数学上等价于 Reck 的"相邻耦合"矩形化）：逐列 `c=0..N-2`，**自下而上** `j=N-2..c`，用 MZI 耦合相邻 `(j, j+1)` 零化 `U[j+1, c]`，枢轴为 `U[j, c]`（上邻行，仅被当前 MZI 触及、不被更高 j 的 MZI 扰动）。全程只动 `(j, j+1)` ⇒ **无波导交叉**（LVS 干净），重构 = 机器精度。

验证路径：`thewalrus` 新版已移除 `clements`；改用其 `mzi_unit_cell`（同 `_mzi` 矩阵）作参照，自推相邻耦合矩形化正确性并以 DFT4/I/随机酉（N=4/7/16）全样本锁定 ‖·‖_F ≈ 1e-16。

---

## 4. 诚实边界（防纸糊楼，必读）

1. **保真度是"分解级"证据**：1.0 证明 `Clements 分解 + 装配` 精确还原目标酉（数学）。它验证**光计算编译映射**可行性，不直接等于"物理版图已算对该酉"。
2. **版图为逻辑网格（轨链式）**：P&R 把 N(N−1)/2 个 MZI 按 x 序级联到各 rail，网表由布线几何独立恢复并通过 LVS。该逻辑网格的**物理耦合序**尚未逐 op 绑定到分解的 `(j, j+1, 列 c)` 坐标——即"分解 op → 物理位置/耦合"的物理综合（physical synthesis）是 **P1** 任务。在 P1 完成前，**不声称物理版图已算对该 DFT**，仅声称：数学可算（保真度 1.0）+ 版图可出版（DRC/LVS 全绿 + GDS）。
3. **PDK 为演示近似 SOI 180nm**（非真 foundry NDA-PDK）；相干实测（L3 标定闭环）为 Tier-1 自证桩，不在 P0 范围。
4. **LLM 不进判决路径**：全部死标量；DRC/LVS 判决、保真度均为 numpy 闭式运算。

---

## 5. 下一步（P1 建议，待杜先生裁定）

- **P1-A 物理综合**：把每个分解 op 的 `(j, j+1, 列 c)` 映射到版图 MZI 物理位置与端口耦合，使"布线几何恢复的网表"精确等于分解网表 ⇒ 物理版图实算该酉（闭环保真度从"分解级"升为"版图级"）。
- **P1-B 规模爬升**：16×16 / 128×128 光子张量核（light-EIC 光子核），验证 x_max / 器件数 / DRC 预算随 N 的扩展性。
- **P1-C 跨分解对照**：与 Reck 三角分解对比 MZI 数/交叉数，量化 Clements 矩形网格的免交叉收益。

---

## 6. 复跑命令

```bash
PYTHONPATH=D:/agent_LDA/lda python examples/demo_mesh_pnr_4x4.py   # 接受闸，EXIT=0
PYTHONPATH=D:/agent_LDA/lda python examples/verify_clements.py      # 保真度回归（各尺寸 1.0）
```
