# P1-E · T2 外部实测通道解锁方案（E1/E3-E7 六道卡口锚）

> 口径来源：`lda/lda_harness/benchmarks.py` 实测推导（v0.9.81：84 锚 / 严格独立 **62** / 降级 **3** / 自证桩 **19**）。
> 配套：`P1-1_self_certified_discipline.md`（常态化纪律）· `ALIGNMENT_REPORT.md` §S8（战略）· `docs/verification_maturity_model.md`（成熟度模型）。
> 本文件只写「**要拿到什么数据、用什么判据、卡在哪**」，不替代 T2 立项决策。

---

## 0. 一句话结论

**T2 阻塞是业务 KPI 阻塞，不是技术阻塞。**

六道锚的 golden **全部是「某个特定已流片器件」的公开实测值**（AMF / Sridaran-Bhave / Zhang PTL / Chack-Hassan / 光子学报），
任何**不接触真实器件或真实工艺参数**的求解器都无法在 tol 内复现——这不是算力问题，是**信息缺失**问题。

⇒ **纯技术侧的天花板已到 75.0%（63/84）**；下一跳只能靠 MPW 流片 + 委托封测**实测回流**（`S8 · T2 真值链`，目标「≥5 自证桩换真锚」，当前 **0 轮 MPW**）。

---

## 1. 六道锚现状（代码真值，非记忆）

| 锚 | 语料 id | 物理量 | golden | tol | 现分级 | 阻塞类型 |
|---|---|---|---|---|---|---|
| **E1** | E-SOI-NG-220 | n_g | 4.18 ± 0.05 | 0.10 | 自证桩 | T2（对象不对齐 + 标量精度） |
| **E3** | E-TBOX-FSR-TM | FSR_nm | 10.44 | 0.1 | 自证桩 | T2（同 E1 型 + tol 极紧） |
| **E4** | E-SOI-CROSS-IL | 插入损耗 dB | 0.18 ± 0.03 | 0.1 | 自证桩 | T2（需器件版图 + 3D 全波） |
| **E5** | E-MMI-1X2-EL | 过量损耗 dB | 0.05 | 0.1 | 自证桩 | T2（判据窗口为零，双路线已证否） |
| **E6** | E-SIN-PL-800 | 传播损耗 dB/cm | 0.087 ± 0.01 | 0.05 | 自证桩 | T2（粗糙度散射需工艺计量） |
| **E7** | E-SOI-CROSS-XT | 串扰 dB | −41 ± 2 | 5.0 | 自证桩 | T2（同 E4） |

> 🔴 **口径订正（v0.9.81）**：E 簇 10 道**全部是光子 PDA 锚**（SOI/SiN/TBOX/MMI/CROSS/GRATING）。
> 此前 `benchmarks._VMM_OVERRIDES` 与两处文档把 E1/E3-E7 的升级路径写成「**量子 QEDA** 实证锚」系**标签≠行为**错配
> （量子锚实际在 B 系列「单量子比特门保真度 / χ 色散位移 / 量子门保真度预算」与 S 系列）。
> 已于 v0.9.81 全仓订正为「光子 PDA 实证锚」（`benchmarks.py` 6 处 + 纪律文档 2 处 + 台账 8 处）。

---

## 2. 逐锚「为什么非 T2 不可」（证据在代码/血案里）

| 锚 | 已试过的非 T2 路线 | 为何失败（实测） | 结论 |
|---|---|---|---|
| **E1** | 标量亥姆霍兹 FDFD 直波导 n_g；半矢量本征模 | ①标量 FDFD 对 SOI 高反差不足：直波导 n_g=3.74 vs 实测 4.18（差 **~10%**），sub-cell averaging + 网格 dl=24→64 均无效（R16 **已实测证伪**）；②**对象不对齐**：golden 4.18 是**环/弯曲**器件的 n_g（FSR 反演），FDFD 解**直**波导，弯曲天然更受限 ⇒ n_g 高 ~0.46；③半矢量是约束变分，SOI 高对比度 β² **系统性偏高 +0.0276** ⇒ 明确「不得用于 SOI」 | 需**弯曲/环** n_g 独立来源或实测 |
| **E3** | 同 E1（可复用 E10 的 `ring_fsr_independent_ng` 路） | 薄埋氧 SOI（n_g=4.92）约束**更极端**；且 tol 仅 **0.1 nm** ⇒ 反算要求 **Δn_g ≤ 0.047**（ΔFSR≈2.12·Δn_g），远严于 E10 的 tol=0.6 nm | 至多 degraded，升 strict 需实测 n_g |
| **E4** | 2D FDTD / EME / CMT | 低损 crossing 是**已设计器件**（多模干涉/浅刻蚀优化），需其**精确版图** + 3D 全波；`seed_empirical.json` 的 geometry 只有通用 w/h/n/wl，**无法复现该器件**。且 B7 血案已证 2D 下「cross 口」度量不良定义 | 需器件版图 or 实测回流 |
| **E5** | ①2D-EIM 本征模展开（`mmi_eme.py`）②2D TEz 全场 FDTD（`fdtd2d_mmi.py`） | 两条**方法学独立**路线都判 **~4 dB = golden 的 70×**；且两法**互相分歧本身**（0.23–0.99 dB）就 > tol ⇒ 判据窗口为零。FDTD 二阶数值色散要压到 0.03 dB 需 dl≈0.0093 µm ≈ **5e11 网格点步 / 2.6 h 每次 ⇒ 不可用作门禁** | 实测已判「只能做假绿」，保持自证桩 |
| **E6** | 无（未建散射模型） | <0.1 dB/cm 由**侧壁粗糙度散射 + 材料吸收**主导，需粗糙度统计量（RMS σ、相关长度 Lc）——**来自工艺计量，不是求解器能凭空给出的** | 需工艺计量或 cut-back 实测 |
| **E7** | 同 E4 | 同 E4 | 需器件版图 or 实测回流 |

---

## 3. 所需 T2 实测数据 / 计量清单（按锚）

| 锚 | 所需实测输入（最小集） | 来源形态 | 缺失即不能升的原因 |
|---|---|---|---|
| **E1** | 500×220 nm² SOI **弯曲/环**波导 n_g（或同 wafer **直**波导 n_g 实测） | foundry PDK / 实测 n_g 数据表 | 求解器精度不足 + 对象不对齐，二者皆由实测一次性解决 |
| **E3** | 薄埋氧 SOI 环器件 n_g（精度 **Δn_g ≤ 0.047**） | 实测 FSR / 群延迟 | tol 0.1 nm 对 n_g 精度要求远超现有求解器能力 |
| **E4** | crossing **器件版图**（或 foundry 实测 IL） | 版图 GDS + 实测 / 3D 全波 ORACLE | 无版图无法建同源模型；无实测无法定 ORACLE |
| **E5** | 实测**不确定性 < 0.05 dB** 的过量损耗（现测 ±0.05 = tol ⇒ 窗口为零） | 高精度封测 / 统计样本 | 判据窗口为零 ⇒ 任何候选都不可证伪，只能假绿 |
| **E6** | 侧壁粗糙度 **RMS σ + 相关长度 Lc** + 材料吸收 | 工艺计量（AFM/SEM）/ cut-back 实测 | 无粗糙度统计量则散射损耗不可算 |
| **E7** | crossing 器件版图（或 foundry 实测 XT） | 同 E4 | 同 E4 |

---

## 4. MPW 路径与成本（foundry/MPW 现实 KPI）

- **轮次**：目标 **3–4 轮 MPW** + 委托封测实测回流（`LDA_1000万投资决策备忘录` B 档：真值+接口 700–1200 万）。
- **成本量级（公开行情，非报价）**：MPW shuttle **$8k–95k/座**（IMEC/CEA-Leti 200mm 偏低，TSMC/GF 300mm 偏高）；**封装测试占近八成** ⇒ 建议**先委托测试、后自建**（自建耦合/测试能力需几百万至上千万）。
- **平台候选（P0，`design_alliance_resource_planning_report` 已盘点）**：
  - **重庆·CUMEC**：180/130nm 硅光 PDK + 300nm 氮化硅 PDK + 三维集成 PDK，**MPW 流片周期 ~120 天** → 最直接对应 E1/E4/E5/E7（SOI/SiN）。
  - **上海交大 CHIPX**：国内首条光子芯片中试线（110nm CMOS 兼容，已出 6 英寸晶圆）。
  - **武汉·中国光谷 / 九峰山实验室**：8 英寸硅光薄膜铌酸锂晶圆、化合物半导体中试。
  - **海外参照**：**AMF（Advanced Micro Foundry, 新加坡）** —— E1 语料（E-SOI-NG-220）的原始实测来源方。
- **时序约束**：MPW 一轮 ≈120 天 ⇒ 3–4 轮 ≈ **1–1.5 年**；封测排期另计（高端耦合设备交期 5–9 个月）。

---

## 5. 验收条件（升 strict 的判据，**先写死再接数据**）

数据到位后，每道锚的新候选必须同时满足（与既有纪律一致）：

1. **方法学不同源**：候选与 golden 不同源（不得由 golden 反算，见 E10 的 C4 防火墙）。
2. **死标量通过**：`|candidate − golden| ≤ tol`，且 tol 不得为「变绿」而放宽（放宽 = 拟合实测 = 循环自证）。
3. **判据 D 通过**：扰动参数 `|diff| ≠ 0`（非代数恒等）。
4. **诚实标注**：候选带 `provenance` + 剩余不确定度归因；不得把几何不对齐伪造成已验证精度。

---

## 6. 红线（不得违反）

- **不得为变绿放宽 tol 去拟合实测**（拟合=循环自证，见 E6/E9 教训）。
- **不得用仿真值/解析反算值冒充实测 golden**（D-63 / D-66 血案）。
- **LLM 永不进判决路径**；PASS/FAIL 由死标量定。
- **`self_certified` 计数只减不增**（`P1-1` 纪律）；新增自证桩必须带锁定原因。

---

## 7. 与 S8「≥5 自证桩换真锚」目标的映射

- 目标 ≥5，本清单 **6 道** ⇒ 满足 S8 目标口径。
- **达成即账本刷新**：严格独立 **63 → 69**，自证桩 **18 → 12**，独立率 **75.0% → 82.1%**（69/84）。
- 即使只换掉其中 **3 道**，独立率也达 **77.4%**（65/84），跨过 75% 台阶。

---

## 8. 诚实边界

- 本文所列「为何非 T2 不可」**全部来自代码/血案实测**（E5 双路线 4 dB、E1 R16 证伪、B7 2D 度量不良定义等），非推断；未穷举的路线（如自建弯曲本征求解器）**成功率未评估**，属 T2 之外的**备选研发题**，不影响「T2 是主路径」的结论。
- 成本/时序为**公开行情量级估算**，非报价；正式预算须以询价为准。
- 本方案**不构成 T2 立项决策**——是否启动 foundry/MPW 窗口由项目负责人（杜先生）定调。
