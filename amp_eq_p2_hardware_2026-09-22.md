# 幅度均衡器 P2 硬件设计（VOA vs MZI 幅度调节）

**日期**：2026-09-22 ｜ **模块**：`lda/lda_layout/mesh_pnr.py`（`equalizer_p2_manifest`）
**路线**：B+C 自研（主权光电接口 + light-EIC 光子核）｜ **来源**：D3 收官用户指令「推进幅度均衡器的 P2 硬件设计」
**红线**：A 级永不外借 · C 级自主零依赖 · LLM 不进判决路径 · 零外部求解器依赖。

---

## 一、裁定（结论先行）

grid2d 总线 + 抽头损耗随 N 线性增长（D1），幅度失衡须由**每输出端口可变衰减器**补偿
（弱端口为参考，强端口衰减回参考功率 ⇒ 恢复酉性、重构保真度不塌）。两候选硬件：

- **VOA（Variable Optical Attenuator）**：纯吸收式幅度元件，挂在每输出波导上。
- **MZI 幅度调节器**：把末端「输出相移 D」换成 2×2 MZI，用一臂偏置做可变分束，
  多余臂须终止（dump 端口）。

**推荐 = EO-VOA 行（电光吸收式，非热 VOA）**，理由（§二/§三 实测）：
1. **吞吐更优**：VOA 最小插损 floor 0.3 dB vs MZI 1.2 dB ⇒ 均衡后吞吐系数 0.933 vs 0.759
   （512 规模差 **17.4%**，总线 IL 已 12–56 dB，floor 每省 0.9 dB 都关键）。
2. **面积更小**：VOA 单元 ~250 µm × 轨距 ⇒ 512 端口均衡行 4.54 mm²；MZI 权重
   ~1100 µm × 轨距（含 dump 臂）⇒ **19.99 mm²（4.4×）**。
3. **无 dump 端口**：MZI 权重须给多余臂做**终止吸收器** ⇒ 512 个悬空波导须 LVS 标
   `loss port`（禁悬空），且占 ~2× 面积；VOA 无此负担。
4. **控制线更少**：VOA 每端口 1 线（512）vs MZI 每端口 2 线（1024，atten+phase）。
5. **免热串扰（联动 D2）**：热 VOA 增 N 个加热器 ⇒ 复用 D2 同列抽头隔离预算；**EO-VOA
   （载流子耗尽吸收）零热负载**，彻底规避 D2 热串扰，是主权流片首选。
6. **无相位扰动**：VOA 纯吸收，输出相位恒定 ⇒ 网格保持「干净酉」（P1-A 真算该酉不变）；
   MZI 权重在直通臂引入残余相位，若输出馈相干级联须额外正交偏置。

**MZI 权重的唯一优势 = 工艺复用**（与网格同 MZI+相移模块，无新工艺、单标定流），
故定为 **foundry-fallback**：仅当 PDK 无 VOA 模块时采用，并承受 1.2 dB floor + dump 端口代价。

---

## 二、VOA vs MZI 技术对比（主权流片视角）

| 维度 | VOA（推荐 EO） | MZI 幅度调节器 | 胜方 |
|------|----------------|----------------|------|
| 工艺兼容性 | 需 VOA 模块（EO 吸收 / 热吸收） | 复用网格 MZI+相移，**零新模块** | MZI |
| 动态范围 | 0–30 dB（易超 20） | 受消光比限 ~18 dB | VOA |
| 最小插损 floor | **0.3 dB** | 1.2 dB（耦合器+dump） | VOA |
| 相位扰动 | 无（纯吸收） | 直通臂残余相位 | VOA |
| dump 端口 | 无 | 每端口 1（须终止/LVS 标 loss） | VOA |
| 面积（512 端口） | **4.54 mm²** | 19.99 mm²（4.4×） | VOA |
| 控制线（512） | 512（1/端口） | 1024（2/端口） | VOA |
| 速度 | EO 纳秒级 | 热 µs–ms / EO 纳秒 | 平（EO 同速） |
| 热串扰（联动 D2） | EO 零热；热 VOA 复用 D2 预算 | 热 MZI 同网格热负载 | EO-VOA |
| 标定轴 | 1（幅度） | 2（幅度+相位） | VOA |
| 吞吐系数（512B） | **0.933** | 0.759 | VOA |

---

## 三、实测选型（D1 三场景 × N=128/256/512，auto 模式）

`equalizer_p2_manifest(amp_eq, pdk)`，pdk 采用 **VOA 级动态范围 30 dB**
（supersedes D1 §六 保守 20 dB 假设，见 §五）。

```
   N scn 幅度均衡 verdict         max_att  P2 tech  floor  thru    area     ctrl  dump  pwr
 128  A  phase_only                 1.28   voa      0.3  0.933   1.13mm²  128    0    0mW
 256  A  requires_amp_eq_pdk        2.56   voa      0.3  0.933   2.27mm²  256    0    0mW
 512  A  requires_amp_eq_pdk        5.12   voa      0.3  0.933   4.54mm²  512    0    0mW
 128  B  requires_amp_eq_pdk        3.20   voa      0.3  0.933   1.13mm²  128    0    0mW
 256  B  requires_amp_eq_pdk        6.40   voa      0.3  0.933   2.27mm²  256    0    0mW
 512  B  requires_amp_eq_pdk       12.80   voa      0.3  0.933   4.54mm²  512    0    0mW
 128  C  requires_amp_eq_pdk        6.40   voa      0.3  0.933   1.13mm²  128    0    0mW
 256  C  requires_amp_eq_pdk       12.80   voa      0.3  0.933   2.27mm²  256    0    0mW
 512  C  requires_amp_eq_pdk       25.60   voa      0.3  0.933   4.54mm²  512    0    0mW   ← 20→30dB 救回
```

**强制对比（N=512 场景 B，max_att=12.80 dB）**

```
tech   floor  thru    area_mm2  ctrl  dump  power  drive
voa     0.3   0.933    4.54     512     0     0mW  3.0V(EO)
mzi     1.2   0.759   19.99    1024   512     0mW  3.75V(MZI arm)
```

auto 在全 9 例均选 **VOA**（floor 更低 ⇒ 吞吐更优、无 dump、面积小）。512C 在 20 dB 保守
假设下原判 `amp_eq_infeasible`（25.6>20），升级 VOA 级 30 dB 后救回 `requires_amp_eq_pdk`
（残余阻断项变为绝对总线损耗 56 dB 的**增益预算**问题，非失衡）。

---

## 四、推荐架构（EO-VOA 均衡行）

- **位置**：每输出波导末端（网格 `D` 相移器之后），与 `D` 串接；弱端口 VOA 置最小设置
  （floor 0.3 dB），强端口按 `att_db = IL_ref − IL_k` 设置衰减。
- **控制**：每端口 1 路驱动（EO 反向偏置电压 0–3 V，载流子耗尽吸收）；N 端口 = N 控制线，
  与网格 MZI 驱动阵列分列（避免串扰）。
- **标定**：一次性静态标定（与 D2 热串扰标定同流程）——测每端口 IL、算 att、写 VOA 偏置；
  幅度感知闭环（光电探测器读输出功率）可在线微调，属 P2 标定固件。
- **LVS**：VOA 无 dump 端口 ⇒ 不触发「loss port」规则；纯吸收端用金属/掺杂终端闭合网表。
- **热预算**：EO-VOA 零静态热 ⇒ 不增加 D2 同列抽头热串扰；若改热 VOA，须复用 D2 深槽隔离
  预算（N≥128 推荐项）。

---

## 五、与系列报告联动

- **D1（总线损耗）**：本设计把 D1 §六「幅度均衡」杠杆落为**具体硬件**；并将均衡器动态范围
  从 20 dB（保守）升级为 VOA 级 30 dB，使 512C 由「动态不足」转为「均衡可行」（残余 =
  绝对损耗增益预算，见 §三）。
- **D2（热串扰）**：推荐 **EO-VOA** 正是为规避「热 VOA 增 N 加热器」对 D2 同列隔离预算的
  冲击；若用热 VOA，须与 D2 深槽隔离一并排期。
- **D3（规模上探）**：本设计闭合 D3 §九「幅度均衡 PDK 绑定」的 P2 硬件层——256/512 主权
  版图已绿，投产只需在输出行加 EO-VOA（面积增量 4.5–20 mm² 量级，远小于网格本体 9–37 mm²）。
- **收官**：P0+P1-A+P1-B+P1-B续+系统级代价(D1/D2/D3)+幅度均衡 PDK 框架 + P2 硬件选型
  全链路闭合，主权光子计算「编译→版图→系统级代价→投产 PDK 需求→P2 硬件选型」主链路完整。

---

## 六、诚实边界（防纸糊楼）

1. **L3 参数化估算**：VOA floor / MZI ER / 面积 / 驱动电压取公开代表值（SOI EO 吸收、
   载流子耗尽）；真值须 **foundry PDK 圆片表征**回填（VOA_range / mzi_er / 实际面积）。
2. **面积估算粗**：VOA 单元按 `轨距 × 250 µm`、MZI 按 `轨距 × 1100 µm` 线性估，未含
   焊盘/驱动走线环绕 ⇒ 实际版图面积更大；结论（VOA 远小于 MZI）定性稳健。
3. **吞吐系数 = 10^(−floor/10)**：仅算均衡器 floor 对参考路径的均匀附加损失；总线本身
   IL（D1 模型）另计，不在此抵消。
4. **不阻塞主权 P&R**：本设计为投产工艺层选型，不修改网格酉分解 / 版级保真度 / DRC / LVS
   判决（已对 256/512 实测 = 1.0 / PASS / ACCEPT）。
5. **foundry fallback**：无 VOA 模块时退 MZI 权重（承受 1.2 dB floor + 512 dump 端口 +
   2× 控制线），由 `equalizer_tech='mzi'` 显式选择。
