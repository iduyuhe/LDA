# LDA 项目进展总览（截至 2026-08-27 · v0.8.31）

> 目的：为后续整体工作规划奠定扎实、可核验的事实基础。所有硬指标均来自代码/生产实测，非记忆推断。

---

## 一、一句话定位

**LDA（Lightwave Design Agent）** 是面向**光子芯片（PDA）+ 量子芯片（QEDA）**的开源、Agent-native EDA 等价物。核心范式只有一句话：

> **AI 写内核，确定性裁判验收。**

生成侧由 AI agent 提方案、写代码、递归自举开发底层求解器；判决侧由死标量比对决定 PASS/FAIL——物理定律与真实测量数据才是最终裁判，LLM 不进判决路径。

---

## 二、战略与护城河

| 维度 | 结论 |
|------|------|
| 核心战略 | 开源优先 · Agent 原生 · 底层核心由 AI 自写（递归自举：用 agent 造产品，也用 agent 造求解器）|
| 护城河所在 | **标准 + 生态 + PDK 供给**（带可信基准套件的开放内核），不是某段求解器代码 |
| 竞争窗口 | 巨头（Synopsys / Ansys / Cadence）因商业模式自噬，**结构性不能做开放内核** → LDA 有持久窗口 |
| 差异化 | 跨光子+量子统一 IR + 开放生态 + AI-dev 自举 |
| 主权边界 | A 级永不借（商业 EDA / NDA-PDK）；B 级借今踢后（MIT/BSD 工具 fork 主权副本）；C 级第一天自主 |

---

## 三、验证红线（不可逾越）

唯一硬约束：**裁判最终判定须落「非 AI ground」**。

- **① 物理定律锚**：解析解 / 麦克斯韦确定性计算（零依赖自研 FDTD 校验）
- **② 实证大数据锚**：跨多源真实测量语料（E1-E7 语料均有 DOI 可溯源）
- **红线**：LLM 不进判决路径，PASS/FAIL 由死标量比对决定
- **五类锚判决**：物理 / 实证 / 系统 / 统计 / LVS（含 S10 多层 + S11 规模）

判决体系两套并存（勿混）：
- `device_library.py` verify_spec：**真交叉验证**（候选数值法 ↔ 闭式 ORACLE，如 B2/B9/B14/B21 等）
- `verification_adapters.py` harness 展示层：默认 `_harness_reference_candidate` 为**自证桩**（占位语义，非真实引擎判决路径，属补强项）

**验证源独立性（事实校准）**：B 锚 27 题中约 **16/27 同源自证** + **11/27 真交叉验证**；真正独立源验证 = **E1-E7 实证锚 + S7/S8 统计锚**。

---

## 四、架构分层

```
L0 开放 IR/DSL（光子+量子统一中间表示）   ← C 级自主
L1 智能体协议层（MCP/A2A 风格）            ← C 级自主
L2 开放 PDK/器件本体 Registry（社区共建）  ← B 级（fork 主权副本）
L3 求解器后端（AI 可重写）                 ← C 级自主
        护城河 = 标准 + 生态 + PDK 供给
```

---

## 五、当前硬账本（已实测校验）

| 指标 | 数值 | 校验来源（实测）|
|------|------|------|
| 引擎 | **22**（光子 15 + 量子 7）| `lda/lda_design/design_package.py` ENGINE_DOMAIN |
| 端到端类 | **33**（22 引擎 + 11 包）| design_package PACKAGE_KINDS |
| 验证锚题 | **45**（B1-B27=27 + E1-E7=7 + S1-S11=11）| 生产 `/api/health` benchmarks=45 |
| CI core 门禁 | **65** | `lda/run_ci_regression.py` CORE_SMOKES len=65 |
| 版本 | **0.8.31** | pyproject + 生产 health |
| 三端同步 | 本地 / Gitee / GitHub + 生产 | HEAD `4cb46f6`（main）|
| 生产 PDK / 层栈 | 5 PDK / 8 层 | 生产 health |

引擎构成：光子 15 = 8 设计量（Waveguide/BraggMirror/RingResonator/MziInterferometer/PhCCavity/Mmi1x2/GratingCoupler2/DirectionalCoupler2）+ 5 loss（YbranchLoss/GratingEff/Crossing/MmiEl/SinPl）+ 2 有源双出口（PhaseShifter/MziModulator）；量子 7 = Transmon/ReadoutResonator/Fluxonium/TunableCoupler/可调 Transmon/读出配对/CZ 门。

---

## 六、主权技术闭环（v0.8.31 收口）

**设计侧主权闭环已达成**：

```
设计 → 仿真(FDTD 自研) → 版图(GDS) → 主权几何 DRC/LVS → 工艺角(SS/TT/FF) → 几何寄生估算
```

- **FDTD 自研**：1D/2D/3D 通过物理定律锚校验，零依赖 numpy；numba-CPU JIT≈20×；PyTorch 可切 GPU/CPU（消费卡 fp64 阉割≈持平 numba-cpu，生产默认 numba-cpu）
- **版图 7 差距全部闭合** + 千器件演示闭环（`run_chip_scale_demo.py` 1000 器件 GDS/DRC/LVS 双闸 0.99s）
- **几何级 RC 寄生**（v0.8.31 新增）：主权零依赖、诚实标注「非 foundry 工艺级 deck」；主权几何护栏（max_r=1kΩ / max_c=1pF），不进签核硬门

> ⚠️ **诚实边界**：以上 = **设计侧主权闭环**，≠ **全物理闭环**。真实 PDK 工艺级 deck、封装测试、晶圆实测回流 → 结构性锁在发动期（C 阶段），非代码问题。

---

## 七、阶段路线图与闸门

```
A 器件级深化 ✅ ──▶ B 生态播种 🔶 ──[三信号闸门]──▶ C 发动期 🔒
```

| 阶段 | 状态 | 说明 |
|------|------|------|
| **A 器件级深化** | ✅ 已毕 | 设计→验证闭环引擎家族（22 引擎/33 类/45 锚/65 CI），四锚 A-D + LVS 双闸 |
| **B 生态播种** | 🔶 进行中 | B1 仓库资产/共建入口 ✅；B2 分享物料 ✅；B3 技术叙事首篇 ✅（草稿已入库待发布）|
| **（闸门）** | — | C 解锁三信号**全空** → 不解锁 |
| **C 发动期** | 🔒 锁 | 院校/退休专家/晶圆厂 PDK 外联、真实流片、封装测试 |

**C 解锁三信号（任一触发即评估切换）**：
1. 外部 ORACLE（fork Meep 等 B 级）真跑通并进入判决路径
2. 发动期资源（院校 · 晶圆厂 PDK）到位
3. 原策略遇瓶颈

**决策纪律（8-27 拍板）**：杜先生决定「维持原策略继续推进」，本次仅思想碰撞（速度/质量/实用张力 → 独立源 / 外部 ORACLE / 单点 PDK 纵深 / 开发者钩子 / 速度卫生）。影响半径分析结论：**增量加固非推倒重来**——22 引擎 / 45 锚 / LVS / 千器件 / 四端同步全保住，仅把自证桩换独立源对拍、宽度补一条纵深。

---

## 八、当前状态与同步纪律

- **版本** v0.8.31，**HEAD** `4cb46f6`（main）
- **三端**：本地 / Gitee / GitHub 一致；**生产** `115.191.20.92` 已部署，health 报 `0.8.31`
- **推送纪律**：headless 下用 `ctypes CredReadW` 取 Windows 凭据管理器令牌**内联**推送（禁用 credential.helper，防 helper-selector 弹 GUI 被 kill 静默失败）；GitHub 直连 443 常封 → `git -c http.proxy=socks5://127.0.0.1:7890`（libcurl 支持）
- **生产部署**：paramiko SSH → `git pull` → `pip install --force-reinstall --no-deps .` → `systemctl restart lda-webui` → `/api/health` 校验 version
- **公众号**：B3 草稿已入箱（媒体号「工业5点0产业生态联盟」），media_id 已建，**待杜先生后台审核发布**

---

## 九、未决 / 风险 / 诚实边界（规划须正视）

1. **真缺口（结构性卡死发动期）**：foundry 工艺级 deck、封装测试、晶圆实测回流——需外部资源，代码侧无解。
2. **验证源独立性**：B 锚 16/27 同源自证 + 11/27 真交叉验证；真正独立源 = E1-E7 实证 + S7/S8 统计；harness 展示层默认自证桩待填真实候选（**补强项，非红线破**）。
3. **策略加固候选项（待时机）**：①每新锚配独立源 ②外部 ORACLE 进判决 ③单点 PDK 纵深 ④开发者钩子 `lda check <gdsfactory_component>` ⑤速度卫生——均增量，不推倒重来。
4. **git 噪声**：`lda/reports/` 生成物在 regen 后变脏（本次 `git status` 即有 20+ 报告文件被改）。建议收紧 gitignore 或明确「生成物不入库」纪律，避免污染提交。

---

## 十、为后续规划奠定的基础

**可立即推进（不依赖 C 信号）：**
- B 出口人工动作：发知乎稿（`LDA_知乎稿_B3_开源EDA护城河.md`）、办分享会填 `LDA_阶段B反馈记录.md`
- B4+ 候选议题（共建机制深化、Good First Issue 认领流转）
- A 级红线守护下的增量加固（独立源对拍、ORACLE 试点）

**需杜先生拍板：**
- C 三信号任一触发时的切换评估流程
- 策略加固五项（第三节候选项）的优先级与启动时机

**不建议：**
- 在 C 三信号未触发前，提前发动期动作（院校/晶圆厂外联、真实流片、封装测试）

---

*本总览为后续工作规划的事实基线。所有硬指标于 2026-08-27 经代码与生产实测校验。*
