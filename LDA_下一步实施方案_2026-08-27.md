# LDA 原方向收口与下一步实施方案

> 2026-08-27 · 在"找回方向"讨论后定稿
> 目的：纠正主线状态判断 + 给出原 v2 计划走完之后、**发动期之前**仍可执行的最后一段工作方案

---

## 〇、关键纠正（先讲清，避免方向误判）

此前一度判断"我们停在 Merge-3 之前、主线偏离"。**经代码级核实，该判断错误。**

| 里程碑 | 真实状态 | 证据 |
|---|---|---|
| Merge-0 Phase 0 试金石 | ✅ 已完成 | S1 功率预算锚等 |
| Merge-1 相移器+loss+角扫 | ✅ 已完成 | `lda_pdk/corner_performance.py`（v0.8.13） |
| Merge-2 系统锚 5+ + 探测器 | ✅ 已完成 | `active_models.py` detector 三件套 + S2-S6 |
| **Merge-3 层级 IR + model_class** | ✅ **已完成（v0.8.16, 6cec96d）** | `lda_chain/registry.py` + `lda_chain/link_model.py`（add_subsystem/flatten） |
| Phase 3/4 统计锚 + 提案编译器 + LLM 生成 | ✅ 已完成（v0.8.17-21） | S7/S8 + 提案编译器 |
| 第二梯队 版图 7 差距 | ✅ 已完成（v0.8.22-28b） | LVS/多层/千器件 |

**结论**：原 v2 开发规划的**全部可执行部分已走完**。当前 v0.8.28b = 四里程碑 + 系统级探索 + 版图闭环的终点。之前几轮（tape-out 可行性 / 方法再平衡 / 收口审计）均是**分析讨论，非代码支线**，未偏离主线。

---

## 一、原 v2 计划剩余项（全部属发动期，已定延后）

v2 规划"第三梯队 = 总决策点"之后的三件事，均依赖外部信号：

1. **go/no-go 专投区** → 已实际 go（Phase 3/4 已落地），无需再决策
2. **版图深化** → 7 差距已闭环，后续仅真实 PDK 层表（发动期）
3. **P3 生态/签核** → **延后至发动期**（院校/晶圆厂外联纪律）

因此"找回方向"后，我们能立刻做的，是**发动期之前最后一段价值兑现**——把已验证能力**包装成对外可用、可被独立验证**的形态。这是原方向（P3 生态的前置准备）的自然延伸，不切换策略。

---

## 二、真·下一步可执行工作（3 块，均非发动期）

### ① Merge-3 收口（CI 棘轮诚实化）—— 成本 0.5 天
**问题**：`run_model_class_smoke.py` / `run_hierarchy_smoke.py`（v0.8.16 产物）只进了 `run_ci_regression`，**未进 `run_ci_industrial_smoke`（core 棘轮）**。即 model_class 与层级 IR 的守护强度弱于其他资产——违反"每阶段收口必进 CI 棘轮"的纪律。
**方案**：把这两个 smoke 并入 `run_ci_industrial_smoke` 的 core 集；`model_class` 当前全标 `L0-解析`（诚实占位），在 registry 注释明确"升 L1 标定需 fork Meep/foundry PDK，发动期"。
**复用**：现有 smoke 脚本零改写，仅改 CI 注册行。
**门禁**：CI core 计数 61→63 全 PASS；model_class/hierarchy 断言不降级。
**风险**：极低（纯注册，无新逻辑）。

### ② 开发者 CLI 钩子（实用价值兑现）—— 成本 2-3 天 · 优先级最高
**问题（真实缺口）**：grep `entry_points / console_scripts / __main__ / cli.py` **全空**——项目目前**没有任何命令行入口**，外部人只能靠 WebUI 或读源码调用。这是开源 EDA "10 分钟跑通" 的硬伤，也是之前"方法再平衡"讨论里"开发者钩子 > 功能计数"主张的落地。
**方案**：新建 `lda_cli.py`（+ `pyproject.toml` `[project.scripts]` 注册 `lda` 命令），暴露**已有能力**为三条子命令，不引入任何外部依赖（主权纪律）：
- `lda design <kind> --target <v>` → 调 `design_engine.design()` 跑网格搜索 + 双验证，输出候选参数 + 误差 + 验收。
- `lda check <gds.json|ir.json>` → 调 `chip_layout_export.export_chip_gds()` 输出 DRC + LVS 双闸报告（复用 v0.8.24 已验证能力）。
- `lda report` → 输出当前 22 引擎 / 45 锚 / 模型精度等级（model_class）一览（复用 registry）。
**复用**：`design_engine` / `chip_layout_export` / `registry` 全部已有，CLI 仅是薄壳。
**门禁**：CLI 三条命令各 1 个 smoke 入 core（CI 63→66）；陌生人按 README 10 分钟跑通 `lda design` + `lda check`。
**风险**：低（薄壳 + 已有能力）；需确认 `chip_layout_export` 的输入序列化格式与 CLI 参数对齐。
**价值**：把"锚全过"的抽象主张变成"丢一个设计进来，LDA 一条命令告诉你 DRC/LVS/验收"的可感知入口——开源增长引擎的真正启动点。

### ③ 对外物料准备（P3 前置，非正式生态启动）—— 成本 1-2 天
**问题**：P3 生态正式启动延后至发动期，但"准备素材"属系统开发现在可做（战略想法候选项已记）。
**方案**：
- 把 `lda/lda_harness` 的对照能力固化为 `lda benchmark --report`：设计包 vs 解析锚/实证锚/跨源 死标量对照，生成对照报告（院校说服素材飞轮雏形）。
- 更新 README "Quick Start" + 一页纸，把 CLI 钩子写进去。
**复用**：对照逻辑已有（empirical_anchor / verification_spec）；README 模板已有。
**门禁**：对照报告 smoke 入 core；README Quick Start 可复现。
**风险**：低；诚实边界标注"原理验证级、非 tape-out 级"不变。

---

## 三、负面清单（纪律不变）

❌ 不提前 Phase 3/4（已落地，不再碰）· ❌ 有源不物理级建模 · ❌ 不做封装测试 · ❌ 不为赶进度堆 demo · ❌ 发动期外联不提前 · ❌ 每周期主线 ≤2 条 · ❌ 不引入 A 级商业依赖（CLI 纯主权壳）。

---

## 四、决策建议（执行顺序）

1. **先做 ② CLI 钩子**——实用价值兑现最大、纯主权、零新依赖，且是 P3 生态启动的必备前置。
2. **并行 ① Merge-3 CI 收口**——成本极低，顺手把棘轮补诚实。
3. **③ 对外物料**——CLI 落地后紧接着补 README + 对照报告。

三块做完，原方向在发动期之前的可执行部分**彻底收口**，剩下的 P3 生态/真实 PDK/硅校准全部等发动期信号——届时再翻出 `LDA_阶段收口审计` 里的"策略切换影响半径"底稿评估是否升级验证严格度。

---

## 五、一句话

原 v2 计划没偏、没停、已走完；我们处在一个"能力齐备但对外不可达"的收口点。下一步不是写新引擎，而是**把已验证的 22 引擎 / 45 锚 / DRC-LVS 包装成开发者一条命令能用的形态**——这才是原方向里 P3 生态启动前最后、也最有杠杆的一步。
