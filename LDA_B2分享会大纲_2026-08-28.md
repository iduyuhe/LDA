# LDA 阶段 B2 · 试点内部分享会大纲与 5 分钟 Demo 脚本

> 日期：2026-08-28 · 配套版本 v0.8.44（三端+生产全同步）
> 配套物料：`LDA_一页纸_概览.md` · `docs/lda_scale_performance_whitepaper.md` · `examples/demo_tapeout_closed_loop.py`
> 目标听众：试点企业工程师/管理层（光模块/光互连方向优先）
> 总时长：30 分钟（可拆 20 分钟精简版）

---

## 一、分享目标（3 句话）

1. **听懂 LDA 是什么**：开源、主权、Agent-native 的光子+量子芯片设计软件——AI 写代码、物理定律判对错。
2. **看见它跑起来**：5 分钟 live demo，从版图几何一路跑到 DRC/LVS/工艺角/几何寄生，全主权闭环。
3. **知道下一步**：试点场景怎么接（设计→仿真→版图→签核→寄生全程主权），社区怎么共建（认领 issue / 提交器件 / 反馈痛点）。

---

## 二、议程（30 分钟）

| 时间 | 环节 | 内容 | 素材 |
|---|---|---|---|
| 0-5min | 开场 | 一句话定位 + 为什么做（商业 EDA 主权风险 / 开源 Agent-native 是第三条路） | 一页纸 |
| 5-15min | 能力纵览 | 22 引擎 / 46 锚 / 20 GC 对标 / 20 创新超市 / CI 69 条全绿；**规模能力：32k 器件全链 0.93s**（近线性） | 一页纸 + 白皮书 |
| 15-25min | **5 分钟 Live Demo** | 主权闭环五段（见下节） | demo 脚本 |
| 25-28min | 试点场景讨论 | 光模块设计链路怎么用 LDA 闭环；痛点收集 | — |
| 28-30min | 收尾 | 认领任务 / 反馈记录 / 下一步 | B 反馈模板 |

---

## 三、5 分钟 Live Demo 脚本（可照读）

### Step 1 · 版图几何 → 主权 DRC（1 分钟）

```bash
cd lda
python examples/demo_tapeout_closed_loop.py
```

预期输出：
```
DRC 通过        : True
工艺角全过      : True
LVS 判决        : SKIP
几何寄生 R/C    : 81.50 Ω / 1.75 fF (passed=True)
签核 accepted   : True
```

**讲稿**：这一个命令跑完「设计→版图→DRC→工艺角→LVS→几何寄生」六段主权闭环，全程零外部依赖——不是调用商业 EDA，是 LDA 自研的求解器 + 主权几何规则。

### Step 2 · 导入任意 GDS 做主权 DRC 快查（1 分钟）

```bash
lda check --gds examples/sample_layout.gds --out reports
```

**讲稿**：任何 GDSII（含 gdsfactory 导出的）都能导入做主权几何 DRC 快查——先筛后深查，不阻塞设计流。

### Step 3 · 设计闭环：给目标 → 出已验证候选（1 分钟）

```bash
lda design RingResonator --target 9.0 --top-k 3
lda report --out reports --quick
```

**讲稿**：`lda design` 让 agent 递归自举设计，`lda report` 输出设计包 vs 解析锚/实证锚/ORACLE 死标量对照——每个候选都有物理定律背书。

### Step 4 · 规模震撼点（1 分钟）

```bash
python -c "from lda_harness.scale_anchor import run_scale_pipeline; r=run_scale_pipeline(32000); print(r['verdict'], r['time_total_s'], 's')"
```

预期：`ACCEPT 0.93s`（32k 器件构建+放置+布线+LVS 亚秒）。

**讲稿**：万级规模的签核从 O(n²) 压到近线性——这是从「不可迭代」到「亚秒迭代」的质变（详见白皮书）。

### Step 5 · 收尾（1 分钟）

**讲稿**：demo 里的每一行都是死标量判决（LLM 不进判决路径）；真实 PDK/流片属发动期，当前为等效对标 + 主权近似。

---

## 四、试点场景建议（光模块方向）

| 场景 | LDA 闭环 | 预期收益 |
|---|---|---|
| 光模块链路预设计 | 设计→仿真→版图→DRC/LVS→几何寄生 | 免流片先验设计可行性 |
| 器件库对标 | 20 GC 整芯片对标（CPO/DR4/LR8/QKD 等） | 规格→设计→复现死标量对照 |
| 版图签核快查 | `lda check --gds` 主权几何 DRC | 早期筛错、减少流片返工 |

> 诚实边界：LDA 对标为等效验证非流片；真实工艺参数需发动期经社区/晶圆厂接入。

---

## 五、反馈收集模板（分享会后填）

```markdown
## LDA B2 分享反馈（{企业/日期}）
### 痛点（当前设计流程最痛的是什么？）
1. 
### 误用点（哪里最容易理解偏差？）
1. 
### 想要的能力（缺什么最影响你试用？）
1. 
### 试点意愿（愿不愿意用 LDA 跑一个真实器件？）
- [ ] 愿意（器件：______）
- [ ] 观望
- [ ] 暂不
```

---

## 六、物料清单（分享会前确认）

- [ ] `LDA_一页纸_概览.md`（已更新 46 锚）
- [ ] `docs/lda_scale_performance_whitepaper.md`（性能纵深叙事）
- [ ] `examples/demo_tapeout_closed_loop.py`（已实测可跑）
- [ ] 本大纲
- [ ] （可选）录屏/线上会议链接
