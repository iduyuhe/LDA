# LDA 封装/测试闭环与晶圆厂数字孪生协同 · 讨论稿

> 状态：**讨论稿（2026-08-27）** —— 尚未动手实施，仅作为"创造条件"的对标清单。
> 主线：把"设计→验证数据层"的闭环，按同一套红线（物理定律锚、LLM 不进判决路径）向"制造/量测回流"延伸。
> 落点原则：**测试数据应"流进来"（实测回流），而非 LDA"跳出去做测试"**。

---

## 1. 背景与当前定位

LDA 当前已打通三层战略闭环（均落在 A/B 阶段，不碰 C 闸门）：

- **Layer 0 · 产品级基准库**（v0.8.32）：`golden_product_benchmarks.py`，器件级 `GP-*` 5 项全 PASS。
- **Layer 1 · 系统类型注册表**（v0.8.33）：`proposal_compiler.SYSTEM_TYPES`（link / wdm_demux / quantum_fidelity）。
- **Layer 2 · 创新超市货架**（v0.8.34–0.8.35）：`innovation_market.py`，`ShelfItem` 5 货架。
- **Layer 3 · 整芯片级对标**（v0.8.36–0.8.37）：`golden_product_benchmarks.py` 增 `GC-*`，4 项全 PASS（CPO 8ch / 量子控制 / 传感前端 / 商用量子控制）。

整芯片级对标已把"器件→芯片"的诚实断层用死标量钉死，但**芯片→封装→测试→良率**这一段目前仍是负面清单（图 1 红叉 ⑧）。

---

## 2. 为什么 ⑧ 封装/测试短期不做（结构原因）

| 挑战 | 具体含义 | 对 LDA 红线的影响 |
|---|---|---|
| **设备与校准黑箱** | 探针台、光纤对准、VNA/OSA/误码仪有厂商协议与校准模型 | 测试原数据进 LDA 前须经**校准剥离**，否则把设备商误差当"实测金值" |
| **夹具非标准** | 光子芯片无统一 probe card 标准，耦合损耗差异大 | 须分离"夹具损耗"与"芯片性能"，否则实证锚被污染 |
| **光口对准可重复性** | grating 角度/位置敏感，同芯片多次测试波动大 | 需**测试协议锚**（SOP + 统计去噪），否则无法形成 stable golden |
| **数据归属与 NDA** | 代工厂/封测厂数据常带 NDA | 与 B 级"借今踢后"同理：作**私有租户 silo 锚**，默认不进公开 `golden_product_benchmarks` |
| **成本与周期** | MPW + 封装 + 测试 易数月 + 数十万 | 须有"测试失败也产生价值"的闭环设计，否则不可持续 |
| **封装引入寄生** | 打线/凸点/TGV/光纤阵列带来额外 RC/热/应力，与版图几何级 RC 不同量级 | 需新增**封装级寄生模型**（当前仅有版图几何级 `parasitic_rc.py`） |

**结论**：⑧ 不是"算法难"，而是"接口主权 + 数据合规"问题。LDA 的护城河是**设计→验证数据层**，不是封测设备。

---

## 3. LDA 待补的 6 个模块缺口（若未来启动 ⑧）

> 关键定位：LDA 不造测试设备，只补"设计侧 → 硅上验证"之间的**接口与标准层**。

| # | 模块 | 作用 | 诚实边界 |
|---|---|---|---|
| 1 | **封装级寄生与热模型** | 打线/凸点/TGV 的 RLC、光纤阵列耦合容差、热阻网络、应力折射率漂移 | 复用 `parasitic_rc.py` 思路，层级升到封装；不建模材料分子级物理 |
| 2 | **测试接口 / 探针模型** | grating/fiber array 耦合损耗、探针卡几何作**参数化黑箱**，与芯片性能分离 | 黑箱参数由实测校准，不进判决逻辑 |
| 3 | **校准剥离层** | 原始测试值减设备校准误差 → "芯片真值" | ⑨ 回流格式强制字段，否则数据有毒 |
| 4 | **硅级基准库** | `golden_product_benchmarks` → `silicon_proven_benchmarks`，golden 来自 LDA 实测硅 | 带 die/wafer/设备/校准版本元数据 |
| 5 | **封装级 DRC/LVS** | 基板/interposer 的 DRC/LVS，复用 `lvs.py` 框架换规则库 | 不同规则 deck，逻辑同芯片级 |
| 6 | **闭环校正引擎** | 用实测良率反修正 `engine_*_loss` 经验拟合（"AI 写内核"反馈环） | 只动经验系数，不动物理定律锚 |

**红线不变**：测试设备内部模型不做（设备商的事）；LDA 只做"消费测试数据的标准层"。

---

## 4. 晶圆厂数字孪生的 8 条接口要求

您在孪生项目里**按这 8 条设计数据出口**，将来对接 LDA 即插即用、不用返工：

1. **格式合规回流 schema** —— 输出 JSON/CSV，字段对齐 LDA ⑨ 回流规范（device_id / 几何参数 / 实测指标 / 测试条件 / 设备号 / 校准版本 / 时间戳）。**第一前提**，否则下游解析不了。
2. **工艺波动分布** —— 给 per-wafer/per-die 分布（CD、膜厚、侧壁角），非单点名义值。LDA 据此把 `SS/TT/FF` 工艺角从"假设"升级为"数据驱动"。
3. **量测数据 CD-SEM / 薄膜 / 散射仪** —— 能关联到具体设计单元（MMI、Y-branch、ring），LDA 才能做几何-性能回归（强化 E 语料）。
4. **校准溯源元数据** —— 每个值带校准版本/设备号。**没有它，数据就是毒药**（误差混进金值）。
5. **匿名化 / NDA 隔离** —— 真实厂数据须可匿名化，或走 LDA **私有租户 silo 锚**（不进公开 golden，只供您自己迭代）。
6. **设备模型抽象** —— 把设备行为建模出来，使"虚拟测试"替代部分实体测试（未来省成本关键）。
7. **热 / 力耦合场** —— 给封装热阻与应力场，供模块 1（封装寄生与热）消费。
8. **闭环反馈 API** —— 标准接口：LDA 推设计意图 → 孪生仿真 → 返回预测 yield/寄生 → LDA 校正引擎。即"协同仿真"。

---

## 5. 时机与分阶段路线

| 阶段 | LDA 侧动作 | 数字孪生侧动作 | 触发条件 |
|---|---|---|---|
| **现在（A/B）** | 维持 ⑧ 负面清单；继续扩 ⑩ 预研/创新超市；完善 ⑨ 回流接口规范 | 按 §4 八条设计数据出口（哪怕今天不接 LDA） | 您已有/将有可能的数据 |
| **发动期（C 信号任一）** | 补 §3 模块 1/2/4/5；`golden` → `silicon_proven` | 提供 NDA 合规匿名化数据集；共建测试 SOP | 稳定测试数据 + 资源投入 |
| **生态期** | 与封测厂/设备商共建数据接口标准；开源测试协议锚 | 孪生覆盖封装测试全链 | 行业联盟/标准组织推动 |

**最佳轻动作（现在零成本、未来高复利）**：在孪生项目里**直接按 §4 schema 设计数据出口**，回流通道待 C 信号触发即现成。
**最佳协同切入点**：用孪生的工艺波动 + 量测数据，先把 `tapeout_pipeline` 工艺角从假设升级为数据驱动——这不碰 ⑧，纯 A/B，立刻提升设计侧可信度。

---

## 6. 红线与诚实边界（全程不可破）

- LLM 只生成候选拓扑/默认参数，**绝不写判决逻辑**；判决 = 基元锚（物理/实证）+ 系统预算（S1/S4）不破。
- 测试数据走 ⑨ 回流，**必须带校准溯源元数据**，否则拒收。
- 真实厂数据默认走**私有租户 silo 锚**，不进公开 `golden_product_benchmarks`（除非获授权去 NDA）。
- 封装寄生/热模型只到"工程可用"层级，不宣称材料级物理精确。
- 落点全程 A/B 阶段，不触发 C 闸门。

---

## 7. 待确认事项（待杜先生定）

1. 晶圆厂数字孪生当前是否已具备 §4 中任意一条数据出口？优先打通哪条？
2. 是否先用"工艺角数据驱动升级"作为首个轻协同（不碰 ⑧、纯 A/B）？
3. 未来若启动 ⑧，§3 六模块中优先补哪几个（建议 1+4 先行）？

---

_本稿为讨论用，未产生代码改动。落地时须另起实施规划，并遵守上述红线与 CI 护栏。_
