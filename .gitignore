# ---- Python ----
__pycache__/
*.pyc
*.pyo
lda_cuda_venv/
*.egg-info/
.pytest_cache/
dist/
build/

# ---- 日志 / 临时 ----
*.log
_*_run*.log
_diag_*.log
_val_*.log

# ---- 诊断脚本（本仓库不发布，留在本地工作区）----
diag_*.py
test_wg_quick.py
# v0.9.111（2026-09-19 全面审计 F-10）：根目录历史诊断 / 构建脚本与散落产物。
# 均为各次分析或发布物料生成的一次性产物，非产品代码、无模块 import 它们、
# 也不在 CI smoke 清单内（verify_lvs_cross_equiv.py 仅被 run_ci_regression.py
# 注释引述为历史证据，实跑的是 run_lvs_cross_equiv_smoke.py）。
# 与上面 diag_*.py 同纪律：留在本地工作区、不随仓库发布；历史提交仍可追溯。
assess_1m_circuit.py
assess_1m_hierarchy_poc.py
assess_1m_layout.py
assess_1m_route_e2e.py
assess_p00_cpo250k_verify.py
assess_p01_hierarchy_250k.py
assess_p02_lvs_baseline.py
assess_p02_lvs_profile.py
build_rdplan_output.py
build_resource_map.py
build_wechat_draft.py
decrypt_redteam.py
md2docx_batch.py
spike_scale_100k_v3.py
verify_gds_library_equiv.py
verify_lvs_cross_equiv.py
spike_v3_out.txt
nav-index.png
reports_verification_ledger_sample.json

# ---- 私有记忆 ----
.workbuddy/

# ---- 生态共建社区贡献库（真实提交数据，不进版本库） ----
lda/lda_pdk/contributions.json
lda/lda_pdk/landed.json
lda/lda_pdk/empirical_proposals.json
lda/lda_pdk/empirical_contributions.json
lda/reports/patches/
# ---- CLI / 飞轮 运行生成物（阻塞生产 git pull，绝不入库）----
lda/reports/cli_smoke/
lda/reports/crosscheck_history/

# ---- IDE ----
.idea/
.vscode/
*.swp
*.pid
*.pid

# ---- 部署密钥（绝不提交真实 .env）----
.env
.env.local

# ---- 外部 ORACLE 隔离环境配置（含本机绝对路径，由 scripts/setup_oracle_env.sh 生成）----
.oracle_env

# ---- 红队自动攻击趋势数据（运行时生成，绝不入库）----
lda/lda_webui/data/

# ---- 临时 / 诊断 / 缓存（本仓库不发布，防误提交）----
.cache/
_githelper.py
_gitprobe.txt
_pstest.txt
ci_core_report.json
lda/ci_core_report.json
# v0.9.111：scripts/ci_core_batched.py 的默认聚合报告（分批跑法产物，不入库）
_ci_core_batched_report.json

# ---- 红队待处理缺陷原始数据（含漏洞信息，加密保管于 .secrets/，绝不入版本库）----
lda/redteam_anchor_fuzz_defects_pending.json

# ---- 本地加密保管目录（密钥+密文，不进版本库）----
.secrets/

# ---- IP 材料「写而不发」（不得入库，防误公开；见 IRONLAWS §16.6）----
# 交底书 / 论文稿 / 申请信息单一旦入库并 push，即构成「自己公开发表」⇒ 新颖性不可恢复。
# 用户 2026-09-23 裁定：材料照写，但不申请、不发表、不阻塞平台推进。
LDA_专利申请交底书_*.md
LDA_IP申请信息单_*.md
LDA_paper_*.md

# ---- DEVSIM 主权镜像冷备（B 级 Apache-2.0，本地离线副本，不进版本库）----
vendor/devsim_mirror/
vendor/devsim_wheels/
_devsim_clone.log
_devsim_probe.txt
_devsim_wheels.txt

# ---- Node 资产构建（对外物料 DOCX/PPTX 生成器，见 package.json）----
node_modules/
npm-debug.log*
.pnpm-store/

# ---- G12-H 外部 ORACLE 镜像 + 诊断稿（本地工作区，不入库、不进 pyflakes 扫描面）----
# 依据：与上面「DEVSIM 主权镜像冷备」「外部 ORACLE 隔离环境」同纪律。
# `_modesolver_src/` 是公开论文（Fallahkhair, JLT 26(11) 1423-1431 (2008)）的
# **第三方参考实现**，本仓只用它做**外部 oracle 标量对照**（一次性脚本），
# 生产模块 `lda/lda_solver/full_vector_mode_solver.py` **不 import 它**
# （该不变量由 `run_full_vector_mode_smoke.py` 判据⑩ 红线守卫机器化）。
# 下面 `fv_*.py` / `g12h_*.py` 是 G12 四条失败路径 + G12-H 的**一次性诊断稿**，
# 无模块 import 它们、不在 CI smoke 清单内；证据链见 `G12_nedelec_findings.md`。
_modesolver_src/
_fh2008.pdf
_fh2008.txt
_A_ref.npz
# `fv_*` 同时覆盖诊断稿（.py）与其运行输出（.txt）—— 整族均为一次性本地产物。
fv_*
g12h_*
_pf_all.txt

