# LDA 认证版技术边界论证（D-08）

> **版本**：v1.0 · 2026-08-20
> **定位**：阶段3 任务 3.1（认证/商业化技术前提）的技术侧先行论证
> **目标**：审计内核依赖许可证，定义**闭源认证版**的组件边界与 SLA 兜底框架，
> 在不触碰「主权求解器」红线（阶段1 雷②）的前提下，给出可商业化的技术边界。

---

## 1. 依赖许可证审计（零 GPL 确认）

认证版能否安全闭源再分发，**前提**是内核不依赖任何 copyleft（GPL/LGPL/AGPL）组件。
对 `lda/` 全量 `import` 审计结果如下：

| 组件 | 用途 | 许可证 | 是否核心 | 闭源再分发 |
|---|---|---|---|---|
| **numpy** | 数值数组 / 线性代数（所有求解器） | **BSD-3** | 核心 | ✅ 可 |
| **scipy** | 稀疏线性代数（FDFD ORACLE：`oracle_mode`/`oracle_coupler`） | **BSD-3** | 核心（ORACLE） | ✅ 可 |
| **torch** | GPU FDTD 加速（`fdtd3d_torch` / coupler 实时 FDTD） | **BSD-3** | 可选（CPU 回退 numpy） | ✅ 可 |
| **miepython** | B1 完整 Mie 级数 ORACLE（替代 Rayleigh 极限） | **MIT** | 可选（函数内 import，缺失降级） | ✅ 可 |
| **sax** | B4 电路级确定性 ORACLE | **Apache-2.0** | 可选（函数内 import，缺失降级） | ✅ 可 |
| 标准库 `math` | 核心 harness golden（B1–B11 基准） | Python 许可 | 核心 | ✅ 可 |

**结论**：内核全部依赖 BSD / MIT / Apache-2.0 宽松许可，**无任何 GPL/LGPL/AGPL 传染性依赖**。
闭源认证版可在不违反任何第三方许可的前提下，对自有扩展部分闭源再分发。

> 纪律重申（话术文档红线）：LDA 主权求解器**不以 GPL 组件为底座**；上述可选 ORACLE
> 即使缺失也仅降级（核心 golden 用纯 `math` 实现，见 `golden.py`），绝不阻塞核心运行。

---

## 2. 主权三级分类（边界定义基础）

LDA 把资产分为三级，认证版边界必须守住这一分类（阶段1 雷②）：

| 级别 | 资产 | 许可证策略 | 认证版可否闭源 |
|---|---|---|---|
| **L1 主权求解器** | FDTD / FDFD / Mie / TMM 内核 + 确定性裁判 | 必须保持开源 | ❌ **永不闭源**（红线） |
| **L2 验证层 / 资产** | 统一契约（D-04）/ 实证语料库（D-06/D-10）/ L0 IR（D-05）/ WebUI（D-07） | 保持开源 | ❌ 保持开源（协作地基） |
| **L3 外部验证层** | 晶圆厂 PDK / 真实实测（D-09） | PDK 属晶圆厂资产，LDA 借真值不借代码 | N/A（外部） |
| **C 认证扩展** | 企业 SLA / 私有 PDK adapter / 托管部署 / 合规审计日志 | **可闭源** | ✅ 仅此级闭源 |

**边界铁律**：闭源部分（C 级）只能**包裹 / 扩展** L1/L2，绝不允许替换或混淆主权求解器内核。
认证版交付的仍是「同一套可验证内核 + 商业服务外壳」，而非另一套求解器。

---

## 3. 闭源认证版组件边界

```
┌───────────────────────────── 认证版（闭源 C 级外壳） ─────────────────────────────┐
│  · 企业 SLA 与可用性承诺                                                       │
│  · 私有 PDK adapter 注册（不公开晶圆厂工艺细节，D-09）                          │
│  · 托管部署 / 多租户隔离                                                        │
│  · 合规审计日志（每次判决留痕，满足可验证追溯）                                 │
│  · 商业支持通道                                                                │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │ 仅调用，不替换
┌──────────────────── L2 开源（验证层 / 资产，AGPL 或当前宽松） ────────────────────┐
│  D-04 统一验证契约 · D-06/D-10 实证语料库 · D-05 L0 IR · D-07 WebUI · 基准 B1–B11  │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │ 调用
┌──────────────────── L1 开源（主权求解器，永不闭源） ──────────────────────────────┐
│  FDTD(1D/2D/3D) · FDFD 超模法 · Mie · TMM · 确定性裁判（LLM 不进判决路径）         │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- **保持开源的部分**：L1 + L2（求解器内核、验证层、语料库、IR、WebUI）。这是信任地基，
  闭源会自毁「可验证」立身之本。
- **可闭源的部分**：C 级外壳（SLA、私有部署、合规日志、商业支持）。它产生商业价值，
  但不改变求解结果——同一输入，开源版与认证版产出一致（认证版额外保证可用性）。

---

## 4. SLA 兜底框架

认证版相对开源版的**增量保证**（不夸大求解精度，只兜「可用 / 可复现 / 可追责」）：

| 维度 | 开源版 | 认证版（SLA 兜底） |
|---|---|---|
| **求解可复现性** | 尽力而为（确定性内核，但无承诺） | ✅ 承诺：同一输入误差界一致（确定性裁判加固） |
| **回归防护** | CI 全量回归（D-04 统一契约 18/18） | ✅ 承诺：每次发布跑全量回归，失败阻断发布 |
| **私有 PDK 接入** | 不提供 | ✅ 提供：私有 adapter 注册 + 保密处理（D-09） |
| **可用性** | 社区 best-effort | ✅ 承诺：托管服务可用性 SLA（如 99.5%） |
| **安全响应** | 社区披露 | ✅ 承诺：漏洞响应时效（如 72h 内确认） |
| **合规追溯** | 无 | ✅ 提供：每次判决留痕审计日志 |

> 关键：**SLA 兜的是「工程可靠性与责任」，不是「求解精度无限高」**。求解精度由主权
> 内核的数学保证（误差界 / 收敛性）与实证锚（D-06）共同约束，认证版不对此额外承诺，
> 只保证这套保证机制**持续可用、可追责**。

---

## 5. 与路线图阶段3 任务 3.1 的对齐

路线图阶段3 任务 3.1 = 「认证 / 商业化版本边界定义」。本文件即该任务的技术产出：
- ✅ 依赖审计确认零 GPL，闭源可行；
- ✅ 三级分类 + C 级闭源边界铁律，守住主权红线；
- ✅ SLA 兜底框架明确认证版相对开源版的**真实增量**（不含夸大精度承诺）。

**下一步**：D-09 PDK 接入规范（把 L3 外部验证层落到具体接入流程）将作为认证版「私有 PDK adapter」
的数据契约前置。

---

*本文件与《LDA_晶圆厂PDK对接首封话术与路线图.md》《LDA_统一验证契约VerificationSpec说明.md》
《LDA_L0_IR_v0.2_增补说明.md》配套。技术侧先行，商业条款由杜先生与法务确认。*
