# LDA 名誉榜 · 对抗贡献台账

> 本榜仅以「锚确认的真实缺陷」与「被收录的语料 / 对抗题」为计分来源。
> **现金奖励暂缓**（视 sponsors / 社区基金而定）；当前以名誉榜为唯一激励——即时、确定、可证伪。
> 判定一律由死标量锚决定，LLM 只出攻击题、不判对错（红线纪律）。

## 计分规则（死规则，任何人不得自改）

| 类型 | 锚判定 | 得分 |
|---|---|---|
| 实测语料 `corpus` | `landed`（收录） | +1 |
| 对抗题 `adversarial` | `landed`（收录） | +2 |
| 真实缺陷 `defect` | `confirmed`（锚确认翻车） | +10 |
| 真实缺陷 `defect` | `rejected`（锚未确认） | 0 |

## 反作弊纪律

- `defect / confirmed` 行**必须**在「备注」栏引用锚 ID（如 `S12` / `B29`），否则不计分——杜绝自证桩式自嗨。
- 得分与（类型, 锚判定）组合严格绑定，台账里写 `+100` 直接判非法。
- 排名由 `lda/run_bounty_ledger_smoke.py` 计算，进 `CORE_SMOKES`，防台账漂绿 / 刷分。

## 交易台账

| 提交人 | 类型 | 内容摘要 | 锚判定 | 得分 | 备注(锚ID/引用) | 日期 |
|---|---|---|---|---|---|---|
| （示例）杜玉河 | defect | 小尺寸调制器相位误差被锚抓出 | confirmed | 10 | S12 | 2026-09-09 |
| （示例）杜玉河 | corpus | 波导 n_eff 实测数据集 | landed | 1 | DOI:10.1364/OE.000000 | 2026-09-09 |

> 以上为机制演示行（标注「示例」）。真实提交由社区 Issue 落库后手工补录此表，CI 护栏持续校验。

## 实时排名

由 `lda/run_bounty_ledger_smoke.py` 在 CI 中计算并打印；本地运行 `python lda/run_bounty_ledger_smoke.py` 亦可查看。
