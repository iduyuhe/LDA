# LDA · 内部设计能力优化规划与实施路径（2026-09-23）

> **原则（杜先生定调）**：**先内后外 · 先设计后验证**。
> **性质**：规划 + 实施路径 + 计划。**不改任何锚、不动棘轮常数、不发版**。
> **范围**：**只含内部可完成项**。外部动作（PDK 准入 / 流片 / NDA / 资金）由杜先生另线推进，本计划只在其外围标注接口，不安排、不替代。
> **基线**：v0.9.127（`d4b2c44`）· 469 锚（strict 448 / degraded 3 / stub 18）= 95.52% · 天花板 97.44% · CI core **194**。
> **上游**：`LDA_design_capability_roadmap_2026-09-23.md`（差距评估）· `LDA_photonic_chip_internal_summary_2026-09-23.md` §4（U 系列）· `docs/verification_maturity_model.md`（VMM）
> **取证**：三路并行代码级实测（设计能力 / 验证能力 / 操作能力）+ 主控复核。凡路径与行号均经本机实测复核。

---

## §0 一句话结论

**U 系列的「零外部依赖」清单已经清空（U1/U3–U8/U10 全部落地并进 CI core），所以这份计划不是补旧账，而是往上走一层 —— 补的是「把能力变成可交付物」这一层。**

实测结果很集中：**平台有大量「能算对」的能力，但没有任何一条路径把它们变成「能交出去的东西」。**

- 设计能力（Design-Scout）：U1–U8/U10 全部 D3 级；剩余内部可推进量全在 G 系列，其中 **G4（几何回提）、G8（双向互操作）零物理锚、判据最干净**。
- 验证能力（Verify-Scout）：**横向交叉验证网基本不存在**（仅 DC coupler 一格，2D、容差 40%）；外部 oracle 全为空表。
- 操作能力（Ops-Scout）：🔴 **平台最核心的「★ 设计闭环端到端」面板产物只有 JSON 设计包，没有 GDS**；GDS/签核在另一个面板且**不可下载** —— **UI 走不到「可交付文件」那一步**。

⇒ **本计划的主线不是"再写功能"，而是"把已有的 D3 能力，变成 D4 可交付物"。**

---

## §1 准入判据（本计划只收什么）

每一条进入本计划的项，必须**同时**满足 4 条：

| # | 判据 | 含义 |
|---|---|---|
| **A** | **零外部依赖** | 不依赖 NDA / 流片 / 现金 / 第三方 / 商用求解器 / 网络 |
| **B** | **不破红线** | A 级永不借 · B 级借今踢后 · C 自主 · 三不做分层口径 · **LLM 不进判决路径** |
| **C** | **可机器验收** | 有死标量判据 + **反向护栏**（「必 raise」须配「合法必过」） |
| **D** | **有门禁归属** | 新增 smoke 必须登记进 `lda/run_ci_regression.py:36` `CORE_SMOKES`，或至少被 `NON_CORE_SMOKES`（`:821`）覆盖门禁兜底 |

**明确不收**（外部，杜先生已安排）：

| 不收的项 | 原因 |
|---|---|
| U12 真 PDK 注入 / U13 损耗真值回填 / U14 物理验证 | NDA / 流片 / 现金 |
| A1 发函、foundry 沟通、预算 | 人在办 |
| 真 DRC deck 对齐 | 需 foundry 交付 |
| 任何"硅片回流"类验收判据 | 无硅片 |

> 🔴 **本计划的所有验收判据都必须能在当前仓库上、离线、确定性地跑出来。**

---

## §2 标尺：DMM —— 把 VMM 从「锚成熟度」扩到「设计能力成熟度」

### 2.1 VMM 现状，与它的盲区

`docs/verification_maturity_model.md` 是我们自己确立的验证成熟度阶梯（v0.9.62 / 2026-09-09）：

| 层级 | 名称 | 判定 |
|---|---|---|
| Tier-1 | 自证 | 无有效独立候选；`candidate ≡ golden` 自洽 |
| Tier-2 | 降级量级 | 有候选但粗糙度超死标量判决列 |
| Tier-3 | 严格独立 | 有方法学独立的候选且经死标量比对通过 |
| **Tier-4** | **红队存活** | 经独立红队攻击 + 死标量判卷仍 PASS —— **规划中，未做** |

配 6 类 provenance（`provenance.py:86 classify_citation` A/B/X 分级，仅 A 级进判决）与「**必要验证 vs 不必要验证**」的资源投放纪律（VMM §4.1）。

🔴 **盲区**：VMM 只在回答「**某个物理量算得对不对**」。它**不回答**「**平台能设计什么、这一项设计能力在第几级、怎么升级**」。
⇒ 这正是杜先生说的「平台的操作与设计能力的验证」缺的那一块。

### 2.2 DMM（Design Maturity Model）· 五级定义

**沿用 VMM 的全部纪律**（诚实标注 / 每项必写升级路径 / 不得越级谎报），只换被测对象：从「锚」换成「设计能力」。

| 级 | 名称 | 判定标准（机器可判） | 升级触发 |
|---|---|---|---|
| **D0** | **不存在** | 全仓无对应代码路径 | — |
| **D1** | **能跑** | 有可执行入口，能产出**某种**产物（含 JSON/CSV） | 产物确定性化 + 进 CI |
| **D2** | **可复现** | 同输入 → 确定性同输出（`deterministic.py` 口径）+ 登记进 `CORE_SMOKES` | 加异源验证 + 反向护栏 |
| **D3** | **可自证** | 有**内部异源验证**（≥2 方法/求解器互验）+ 判据 D 严格单调 + 反向护栏全红 | **产物是可交付物**（GDS + 签核报告） |
| **D4** | **可交付** | **单命令产出**：GDS + DRC/LVS 签核报告 + **几何↔原理图双向一致**（G4） | 过真 foundry deck |
| **D5** | **可外签** | 通过某家 foundry 真实 deck（对方签认）/ 有流片实测回流 | — **外部，不在本计划** |

> 🔴 **D4 是内部能达到的上限**；D5 起必须外部。**本计划的目标就是把 D3 推成 D4。**

### 2.3 现有能力的 DMM 打分表（本计划的核心产出）

| 能力 | 现状级 | 卡在哪 |
|---|---|---|
| 器件级设计搜索（`lda design`，`cli.py:50`） | **D1** | 只往 stdout 打印最优候选，**不落文件、不出 GDS** |
| 链路装配 → GDS + DRC/LVS（`lda check`，`cli.py:112`） | **D3** | 要**手写链路 JSON**；**几何不回提 ⇒ 够不到 D4** |
| MZI 网格 P&R（`lda_layout/mesh_pnr.py`） | **D3** | 产物 GDS ✓，但无单命令入口 + 无几何回提 |
| WDM 共享网格（U1，`wdm_shared_mesh_pnr.py`） | **D3** | 同上 |
| 编译器前端（U5，`compiler_frontend.py`） | **D3** | 产物是驱动清单，**非 GDS** |
| 微环 κ_c 标定（U3）/ 权重库（U4） | **D3** | 参数化产物，**无版图落地** |
| 损耗感知编译（U7） | **D3** | 口径精确化 ✓，编译层自由度 = 0 已证 |
| 良率/容错（U6） | **D3** | 条件式（缺 foundry 良率真值） |
| 非易失权重后端（U8） | **D3** | 条件式（文献锚 ≠ 实测） |
| 校准协议（U10） | **D3** | 恒 `BLOCKED_NO_ANCHOR`（本项目锚 = 空集） |
| **WebUI 设计闭环端到端**（`index.html:182-208` → `/api/design_outcome`） | **D1** | 🔴 **产物仅 JSON 设计包，无 GDS**；与签核面板**不联动** |
| WebUI 流片签核（`index.html:407` → `/api/tapeout`） | **D2** | 后端内存生成 `gds_bytes` 跑 DRC/LVS/寄生，**不暴露 GDS 下载** |
| SPICE 网表导出（`spice_netlist.py:161/177`） | **D1** | **只导出，不求解**（无瞬态/眼图/BER） |
| 双向互操作（`gdsfactory_bridge.py:49`） | **D1** | **单向** gf→LDA，且 `:92 "nets": []` **互连不解析** |
| 系统级功耗 / 面积-性能联合优化（G20） | **D0** | **全仓无 TOPS/W、无能耗/MAC、无 tradeoff 计算** |
| 跨求解器交叉验证 | **D0–D1** | 仅 DC coupler 一格（FDFD 超模 ↔ 2D FDTD），**2D、容差 40%** |
| 全矢量模式求解（G12） | **D0 → D3** | 起点：仅 2D 半矢量；`semivec_mode_solver.py:47-55` 已自证 collocated 不可行。<br>**2026-09-24 更新**：**G12-H** ✅ v0.9.135（2D 三维矢量场 · H-field staggered + PML，消除 SOI +0.0276）· **G12-A** ✅ 任意 2D 截面入口 + 门禁 13→20 判据 + 探针 6/6 · **G12-B**（真 3D 空间）⏳ 未做 |
| 任意 2D 截面本征模（G12-A） | **D3** | 2026-09-24 新增：`neff_2d(eps, h, wl, pol)` 收**任意 ε 场**（含脊形样例 `eps_rib`）；退化一致性 |Δ|≤1e-9、slab 单调、抹 slab 必变、网格收敛 |Δ|=1.3e-3 均已机器化 |
| 真 PML（G11） | **D0–D1** | 梯度二次型海绵层，代码自述「无 Mur-ABC、无 CPML」 |
| 时域色散/各向异性/非线性（G13） | **D0** | FDTD 内 ε 为标量实 n²；**χ²/χ³ 全仓缺失** |
| 任意几何 3D 网格（G16） | **D0–D1** | `voxel_field.py:30` 仅矩形掩模 |

**🔴 结论（这张表就是「平台操作与设计能力」的体检结论）**：

1. **D4 = 0 项。** 平台有 **13 项 D3**（"算得对"），但**没有一条路径把它们变成"交得出去的东西"**。
2. 断点高度集中，只有两处：
   - **①「设计包 → 可交付 GDS」不贯通**（WebUI 两块不联动、GDS 不可下载、CLI 无单命令）；
   - **②「几何 ↔ 原理图」不闭环**（G4：器件参数全部来自原理图声明，版图侧无几何回提 ⇒ 画错尺寸查不出）。
3. **这两处都是纯内部可解的，且合起来正好就是 D4 的定义。** ⇒ 这就是 P1。

---

## §3 内部能力现状三张账（实测）

### 3.1 设计能力账 —— 已完成项（不需再做）

U 系列「零外部依赖」清单**已全部落地**并登记进 CI core：

| 项 | 模块 | 判据数 | CI core 登记行 |
|---|---|---|---|
| U1 WDM × 2D 联合布局 | `lda_layout/wdm_shared_mesh_pnr.py` | 23 | `run_ci_regression.py:647` |
| U5 编译器/AI 框架前端 | `lda_l2/compiler_frontend.py` | 69 | `:667` |
| U3 微环 κ_c FDTD 回填 | `lda_l2/ring_kappa_calib.py` | 63 | `:696` |
| U4 微环权重库 | `lda_l2/ring_weight_bank.py` | 75 | `:707` |
| U7 损耗感知编译 | `lda_l2/loss_aware_compile.py` | 67 | `:717` |
| U6 良率/容错映射 | `lda_l2/yield_fault_tolerance.py` | 69 | `:733` |
| U8 非易失权重后端 | `lda_l2/nonvolatile_weight_backend.py` | 139 | `:760` |
| U10 校准固件协议 | `lda_l2/calibration_protocol.py` | 152 | `:796` |

**未做（按需）**：U2（K 提升，`wdm_mesh_pnr.py:394` 默认 K=4）· U9（**全仓无 tiling 模块**，仅文档）· U11（`cpo_engines.py:33` EIC 按黑箱）。

### 3.2 验证能力账（Verify-Scout 实测）

| 手段 | 现状 | 说明 |
|---|---|---|
| 自洽性 / 守恒 / 正交归一 | **已支持** | `mmi_eme` 能量守恒 + ΦᵀΦ = I |
| 收敛性（判据 D 严格单调） | **已支持** | `run_d_criterion_smoke` 448/448 |
| 反向测试 / 突变探针 | **已支持** | falsifiability 13/13；新 smoke 12/12 探针全红 |
| 双算法互证（同锚两法） | **已支持** | B28/B31/B32/S13 等 smoke |
| 文献实测复现（**唯一免费外部真值**） | **已支持但规模小** | 30 条 corpus + 4 对抗题，约 20 篇文献；实测对照仅 **5 条** |
| **跨求解器一致性** | 🔴 **半支持（最大缺口）** | 仅 DC coupler（`fdtd2d_coupler.py` ↔ FDFD 超模），**2D、容差 40%** |
| 与外部真值（Meep/Tidy3D/pyEPR/SAX） | **无** | `oracle_pyepr:15` / `oracle_sax:11` / `oracle_tidy3d:19` / `oracle_field:35` 全默认 `None`；`real_machine_oracle.py:313 REAL_MACHINE_ORACLE` **空表** |
| Tier-4 红队存活 | **规划中未做** | `run_redteam_anchor_fuzz_smoke.py` 已建，4 题入库**无自动判分** |

**关键定性**：现有的 448 道 strict 是「**解析闭式 golden × 数值候选**」的**方法学独立**（跨方法），**不是跨求解器**。二者不同 —— 前者证「我算得对」，后者证「我的求解器彼此不共谋」。

### 3.3 操作能力账（Ops-Scout 实测，路径经主控复核）

| # | 缺口 | 影响动作 | 难度 |
|---|---|---|---|
| 1 | 🔴 **设计包（JSON）与 GDS/签核不联动、GDS 不可下载** | UI 走不到流片文件 | 中 |
| 2 | 无「一句话目标 → GDS + 签核」**单命令** | 端到端门槛高 | 中 |
| 3 | README:338/350 引用 `examples/cli_check_example.json` —— **实为 `lda/examples/cli_check_example.json`**（少一层目录） | 照抄首条命令即失败 | **小** |
| 4 | `examples/README.md:19` 写 `lda design --kind X --params '{...}'`，**实为 `lda design <kind> --target <float>`** | 照抄即报错 | **小** |
| 5 | WebUI 重计算需登录（`routes.py:339-365 _heavy_guard` 覆盖 `/api/design_outcome` `/api/tapeout` `/api/verify`），**入口无提示** | 匿名首点即 401 | **小** |
| 6 | 无零基础入门 / 快速上手文档 | 新用户无引导 | 中 |
| 7 | 无 API 参考（57 端点）；`LDA_D-13_WebUI内网部署说明.md:73-88`（**位于仓库根**）清单过期（仍写 B1-B11/七面板/无鉴权） | 二次开发只能读源码 | 中 |
| 8 | 环境变量分散无集中文档（`LDA_ADMIN_TOKEN` / `LDA_WEBUI_PORT` / `DEPLOY_MODE` / `LDA_CS_LLM_*` / `LDA_STORE_EMAIL_PASS` / `LDA_LICENSE_KEY` / `LDA_DB_URL`） | 部署踩坑 | **小** |
| 9 | 解释器口径冲突：`examples/README.md:42` 要 3.14.3，`CONTRIBUTING.md:25` 要 3.13 venv | 环境搭不起来 | **小** |
| 10 | 无 `tests/`、无 pytest（约 200 个 `run_*smoke*.py`） | 外部贡献者习惯不匹配 | 中 |

> 🔴 **第 1 条是旗舰级问题**：平台最核心的「★ 设计闭环端到端」面板（`index.html:182-208`）→ `POST /api/design_outcome`（`app.py:1506`）→ 目标→搜索→双重验证→**统一 DesignPackage(JSON) + 下载**。**产物仅 JSON，无 GDS。**

---

## §4 目标：内部可度量指标 M1–M8

把「从能做到能做好」翻译成数字。**每项都能在当前仓库上离线测出来。**

| # | 指标 | 定义 | 当前 | 目标 |
|---|---|---|---|---|
| **M1** | **端到端单命令率** | 「一句话目标」→ GDS + 签核报告所需的**命令数** | **无**（须手写链路 JSON + 分两步） | **1 条命令** |
| **M2** | **D4 能力数** | DMM 达 D4 的能力计数 | **0** | **≥3** |
| **M3** | **交叉验证矩阵覆盖** | 有 ≥2 自研求解器互验的「器件 × 物理量」格数 | **1**（DC，2D，40% tol） | **≥6** |
| **M4** | **几何回提覆盖率** | LVS 中由版图几何回提（而非原理图声明）的器件参数比例 | **0%** | **100%**（13 类器件） |
| **M5** | **零基础走通成本** | clone → 第一个 GDS 的步骤数 | 路径断（#3/#4） | **≤5 步 + 1 篇教程** |
| **M6** | **A 级实证锚数** | `empirical_bank` 入库数 / 实测对照数 | 30 / 5 | **≥60 / ≥12** |
| **M7** | **CI 预算** | core 条数 / 全量耗时 | 194 / ~105 min | 条目增，**耗时不超预算** |
| **M8** | **API 文档覆盖** | 57 个 REST 端点有参考的比例 | **0%** | **≥80%** |

---

## §5 实施路径（三主线 · 六批次）

### 5.1 批次总览

| 批 | 名称 | 主线 | 目标指标 | 外部依赖 | 建议序 |
|---|---|---|---|---|---|
| **P1** | **让设计落地** | 设计 + 操作 | M1 · M2 · M4 | 无 | **1** |
| **P2** | **让别人能用** | 操作 | M5 · M8 | 无（验收需 1 名非项目成员，**内部可组织**） | **2** |
| **P3** | **横向交叉验证网** | 自证 | M3 | 无 | **3**（可与 P1 并行） |
| **P4** | **物理深度补齐** | 设计 | —（加锚计） | 无 | 4 |
| **P5** | **工程化与生态** | 操作 + 自证 | M6 · M7 | 无 | 5（可与 P4 并行） |
| **P6** | **系统级 / 计算架构** | 设计 | — | 无（**受红线强约束**） | 6 |

**依赖图**：

```
P1 让设计落地 ──┬──► P2 让别人能用（P2 的走通验收依赖 P1 的单命令）
                ├──► P4 物理深度（P4 产物要经 P1 链路才可交付）
                └──► P6 系统级（产物口径统一）
P3 横向交叉验证网 ── 独立，可并行
P5 工程化 ── 独立，可并行
P4: G16 任意几何网格 ──► G13 色散/各向异性/非线性
P1: T1.3 G4 ──► P4 各项的签核判据（几何可信后用）
```

---

### 5.2 **P1 · 让设计落地**（最高优先 · 先设计的第一刀）

**为什么是它**：它同时是「设计能力」（设计能不能变成文件）与「操作能力」（别人能不能拿到东西）的交点；且**零物理锚、零外部依赖、判据最干净**。

| 任务 | 具体改动 | 验收判据 |
|---|---|---|
| **T1.1 设计包 → GDS 贯通** | `lda_webui/app.py:1506`（`/api/design_outcome`）与 `:1622`（`/api/tapeout`）联动：设计包产出后可直接送签核；新增 GDS 下载端点（复用 `chip_layout_export.export_chip_gds`）；`index.html:182-208` 面板挂「→ 生成 GDS 并签核」按钮 | ① UI 一次点击产出 **可下载 .gds + DRC/LVS 报告** ② 报告与 `/api/tapeout` 同源（不得双口径） |
| **T1.2 端到端单命令** | 新增 `lda build <goal.json> --out <dir>`：目标 → 设计包 → 版图 → GDS → DRC/LVS 报告（复用 `cli.py:112 cmd_check` 链路与 `:85 _build_link`）；修 README:338/350 与 `examples/README.md:19` 的路径/签名；补 `lda/examples/` 下一句话目标的示例 JSON | ① 干净 clone 下**1 条命令**产出 `.gds` + 签核报告 ② 无参数时给可用指引而非 traceback |
| **T1.3 G4 器件参数几何回提** | `lda_l2/lvs.py:419-420` 现在 `kind_of = {c.id: c.kind for c in link.ir.components}`、`inst = {k: kind_of.get(k,"?") ...}` —— **kind 与 params 全来自原理图**。新增 `extract_layout_params()`：用 `gds_export.geometry_desc:198` 反推多边形 → `gds_drc._poly_width:98` 提宽度 → `placement.device_bbox:146` 提包围盒，与 IR `params` 逐字段比对（Waveguide `length`、DC `Lc`、Ring `R`、MMI `L_mmi`）；新增违规类 **`device_param_mismatch`**，注入 `run_lvs:439` 与 `run_lvs_multilayer:784` | ① **反向护栏**：故意画错尺寸 ⇒ 必报 `device_param_mismatch`（造两反例：改长度、改半径）② **合法必过**：正确版图 ⇒ 零违规 ③ 既有 LVS 判据**零影响**（对照跑新旧两版）④ 判据数 +N，进 `CORE_SMOKES` |

**P1 出口判据**：**M1 = 1 条命令 · M2 ≥ 1 · M4 > 0**（即 DMM 首次出现 D4）。

---

### 5.3 **P2 · 让别人能用**（操作能力）

| 任务 | 具体改动 | 验收判据 |
|---|---|---|
| **T2.1 修文档致命错** | README:338/350 路径改 `lda/examples/cli_check_example.json`；`examples/README.md:19` 改 `lda design RingAddDrop --target <float>`；`examples/README.md:42` 与 `CONTRIBUTING.md:25` 解释器口径统一 | 逐条按文档照抄，**命令全部成功** |
| **T2.2 零基础快速上手** | 新增《5 分钟从 clone 到第一个 GDS》，含 P1 单命令 + 截图；补 WebUI 匿名 401 的入口提示（`routes.py:339-365`） | 文档步骤数 **≤5**；每步有可复制命令与预期输出 |
| **T2.3 API 参考 + 修过期清单** | 从 `routes.py:1598-1743` 路由表**自动生成** 57 端点参考；作废并重写 `LDA_D-13_WebUI内网部署说明.md:73-88`（位于**仓库根**）的过期清单 | M8 ≥ 80%；参考由脚本生成（不手写，避免再漂移） |
| **T2.4 环境变量集中文档** | 汇集 7 个变量（`LDA_ADMIN_TOKEN` / `LDA_WEBUI_PORT` / `DEPLOY_MODE` / `LDA_CS_LLM_BASE_URL`+`_API_KEY`+`_MODEL` / `LDA_STORE_EMAIL_PASS` / `LDA_LICENSE_KEY` / `LDA_DB_URL`） | 单页可得全部变量 + 默认值 + 影响 |
| **T2.5 外部走通验证** | 🔴 **由一名非项目成员**（内部可组织：非核心同事 / 或杜先生本人无协助走一遍）从 clone 到 GDS | **零协助走通**，卡点全部记录并回灌 T2.1/T2.2 |

**P2 出口判据**：**M5 ≤5 步 · M8 ≥80% · 一名外部使用者零协助走通。**

---

### 5.4 **P3 · 横向交叉验证网**（自证最大缺口 · 可并行）

**为什么是它**：这是 469 锚纪律里**唯一成体系的空白**，且 **100% 内部可做** —— 因为不借商用求解器，但**自研求解器彼此可以交叉**。DAC 2026 的结论（"autonomy scales with the quality of your verifier"）正指向这里。

| 任务 | 具体改动 | 验收判据 |
|---|---|---|
| **T3.1 建交叉验证矩阵** | 新增 `run_cross_solver_matrix_smoke.py`：同一器件同一物理量，**≥2 个自研求解器**互验。已知可落地子集（全部实测过存在）：<br>· DC 定向耦合器：FDFD 超模（`oracle_coupler.py:29`）↔ 2D FDTD（`fdtd2d_coupler.py`）<br>· Bragg：闭式 λ_B ↔ 反周期 Bloch 广义本征值（`bragg_solver.py`）<br>· EME taper：绝热极限 ↔ EME（`eme_taper.py`，现余量 4.65e-5）<br>· Transmon：Koch 解析 ↔ 严格对角化<br>· Ring：解析 FSR ↔ FDTD | ① 矩阵 **≥6 格**（器件 × 物理量）② 每格给出误差并**如实报出**（FAIL 也登记，不藏）③ 每格判据 D 严格单调 |
| **T3.2 收紧 DC 的 40% 容差** | 现有 DC 交叉验证容差 40%（2D）+ 4.775e-16 的 `numba vs numpy` 属**同算法换后端**，不算独立 ⇒ 明确标注并排除出矩阵 | ① 容差收紧到判据 D 口径 ② 同后端对照**不列入**独立格（防虚假繁荣） |
| **T3.3 对抗题自动判分** | 4 道入库对抗题（`empirical_bank.py:244`）升级为**自动判分**，迈向 VMM Tier-4 | 判分脚本可跑；错答必不通过 |

**P3 出口判据**：**M3 ≥6**（且每格有判据 D）。

---

### 5.5 **P4 · 物理深度补齐**（设计能力 · 按需求驱动，不全做）

**排序依据**：影响面 × 前置关系。**不追求"把 Lumerical 功能补齐"**，只补**挡住我们自己的设计能力**的那几项。

| 序 | 任务 | 接缝（实测） | 工作量 | 验收判据 |
|---|---|---|---|---|
| 1 | **G11 真 PML** | `fdtd2d.py:42 _sponge_1d` / `fdtd3d.py:42` 同构 ⇒ 新增 CPML 吸收层；接缝 = 两处 `_sponge_1d` + `_run_planewave` 的 sigma 装配 | 中 | ① **回反射系数机器可证下降** ② 判据 D 严格单调 ③ 既有 FDTD 判据零影响 |
| 2 | **G16 任意几何 3D 网格** | `voxel_field.py:30 LayoutLayer` / `voxelize_rectangular` 仅矩形（x0/x1/y0/y1/z0/z1）⇒ 支持任意多边形/曲线栅格化 | 中 | ① 非矩形几何能被正确体素化 ② 是 G13/G14 的**前置**（先做） |
| 3 | **G13 时域色散 / 各向异性 / 非线性** | FDTD 内 `eps` 为标量实 n²；Sellmeier 只在**频域**模式求解；**χ²/χ³ 全仓缺失** ⇒ 改造 `fdtd2d/fdtd3d` 的 eps 装配，引入 ADE 或各向异性张量 | 大 | ① 色散介质中脉冲传播与频域解一致 ② 稳定性有判据 ③ 非线性项有**解析极限**可验（如弱场退化到线性） |
| 4 | **G12 全矢量模式求解**<br>① **G12-H**（2D · H-field staggered）✅ **v0.9.135 已结算**<br>② **G12-A**（任意 2D 截面入口）✅ **已实现并通过门禁（待发版）**<br>③ **G12-B**（真 3D 空间 · 传播方向也离散）⏳ **未做** | `semivec_mode_solver.py:209 neff_2d` / `:264 neff_strip` 仅 2D 半矢量；**`:47-55` 已自证 collocated 网格上数学不成立** ⇒ 必须走 **staggered/Yee 或 Nédélec FE**，属**新增模块** | 大 | ① 与 2D 半矢量在低对比度（SiN）退化为一致 ② SOI 绝对 n_eff 相对 Lumerical FDE 的 **+0.0276 偏差须收敛**（这是本项目自己已钉死的判据） |

> 🔴 **§5.5 第 4 项标题口径订正（2026-09-24 · 此前被误读，勿再犯）**：本表原有的「**3D** 全矢量」指的是**三维矢量场**（Hx/Hy/Hz 三个分量都解、由散度约束代数消 Hz），**不是**三维空间几何。
> 该含义下 G12-H **已达成**（三个分量都在算子里，Hz 由 ∇·(εH)=0 消掉）。
> 真正**尚未做**的是两件事：
> - **G12-A「任意 2D 截面」**：此前全部 13 条判据只覆盖 `neff_strip`（居中矩形）便捷封装 ⇒ **任意形状（脊形/多层/非矩形）没有合法入口**，能力边界被封装卡死。现已补 `neff_2d(eps, h, wl, pol)` + `solve_modes_eps(eps, xv, yv, ...)` + `eps_rib()` + `core_vertex_mask()`，门禁判据 **13 → 20**（新增 ⑪–⑰），突变探针 **6/6 全红**。
> - **G12-B「真 3D 空间」**：γ（传播常数方向）也进网格 ⇒ 规模从 N=(nx+1)(ny+1) 升到 ~(nx·ny·nz)，属**独立量级**的工程，单独立项。

**本批不做**：**G14**（RCWA/FEM/BPM/3D-EME —— 每个都是独立求解器）、**G15**（多物理耦合 —— 耦合刚度与收敛风险高）。理由：投入产出比低于 P1–P3，且不挡当前设计能力。

---

### 5.6 **P5 · 工程化与生态**（最省力 · 可并行）

| 任务 | 具体改动 | 验收判据 |
|---|---|---|
| **T5.1 `tests/` + pytest** | 新增 `tests/` 与 `conftest.py`；把 `run_*smoke*.py` 的判据**暴露为 pytest 用例**；`pyproject.toml:59-64` 保留 `run_ci_regression.py` 为**主入口**（双入口并存，不替换） | ① `pytest` 能收集并全绿 ② **CI 主链路口径不变**（`CORE_SMOKES` 194 不动） |
| **T5.2 `pip install -e .` 实测** | `pyproject.toml` 已有 `[project.scripts] lda`（:57）与 16 个 `lda_*` 命名空间（:59-64 附近）⇒ 实跑安装并验证 | `pip install -e .` → `lda --help` 可用；`lda build`（P1 新增）可用 |
| **T5.3 A 级实证锚扩容** | `empirical_bank` 现 30 条 / ~20 篇文献 / 实测对照仅 5 条 ⇒ 扩容；**只收 A 级**（`provenance.py:86` 门禁） | **M6 ≥60 条 / ≥12 对照**；每条带 citation + source_url + σ + geometry |

**关于两条历史 FAIL（已结清，不列为任务）**：`E-SIN-NG-1200`（差 0.117）与 `E-GRATING-EFF`（差 0.10）经查是**故意合成的演示候选**（`lda_harness/run_empirical_bank.py:42` 注释即写明「演示实证锚抓偏离」），脚本**从不 `sys.exit`** ⇒ 属「检测力自证」，**不构成 CI 判决**。且 `E-SIN-NG-1200` 在 `seed_empirical.json:674` 明文「**不作为任何锚题的 golden**」「口径不自洽（λ²/(FSR·L) = 2.3305 ≠ 2.2834）⇒ 禁止拿它做精度判定」；`E-GRATING-EFF` 已升格为锚题 **E8**（`benchmark_defs/part1.py:1504-1519`）判 `strict_independent`。⇒ **非静默 FAIL、非真实缺陷，只在 §9 记一笔。**

---

### 5.7 **P6 · 系统级 / 计算架构**（L6 唯一大空白 · 红线强约束）

**背景**：L6（计算架构 / 编译）是全球**没有公认标准工具**的层 —— 是定义权机会。但系统级这一半是空的（G20：全仓**无 TOPS/W、无能耗/MAC、无面积-性能 tradeoff**）。

🔴 **红线约束（必须写进设计定稿）**：`cpo_engines.py:26` 与 `golden_product_benchmarks.py:941` **已经明确拒绝判决 pJ/bit**（能效由电域 SerDes 主导，LDA 无锚）。⇒ **G20 只能做「光域口径」**。

| 任务 | 具体改动 | 验收判据 |
|---|---|---|
| **T6.1 G20 光域 Pareto** | 新增「光域 MAC 吞吐 / 面积 / 光损耗-精度」的联合权衡计算；口径 = **光域**，**严禁**输出能效（pJ/bit、TOPS/W 中依赖电域的部分） | ① 给出 N × K × 损耗 × 面积的 **Pareto 表** + 锚 ② **零能效数字**（沿用既有拒绝，不新增越界项）③ 反向护栏：人为放宽损耗 ⇒ Pareto 面必须移动 |
| **T6.2 U2 K 提升（K=4 → 8/16）** | `wdm_mesh_pnr.py:394` 默认 K=4 ⇒ 重算微环 R 与 FSR（现 FSR 51.67 nm，K=16 需 ≳12.8 nm，余量充足） | FSR/信道间隔比 ≥1.5 · 环间串扰 ≤ −20 dB（设计预算）· 保真度 1.0 保持 |
| **T6.3 U9 tiling 扩档** | **全仓无 tiling 模块**（仅文档）⇒ 需新增；每扩一档**必须重跑 D1/D2/P3 三预算** | 每档的天花板数字随档更新；旧档数字不得复用 |
| **T6.4 U11 EIC 行为级** | `cpo_engines.py:33` EIC 现为黑箱 ⇒ 做 per-MZI 驱动 / TIA 阵列**行为模型** | 只到行为级；**不碰晶体管级**（红线） |

**P6 出口判据**：光域 Pareto 表 + 锚；**零越界数字**。

---

## §6 里程碑（每条都是机器可判）

| # | 里程碑 | 判据 | 归属 |
|---|---|---|---|
| **M-1** | **设计可交付** | `M1 = 1 条命令` · `M2 ≥ 1` · `M4 > 0`（DMM 首次出现 D4） | P1 |
| **M-2** | **外部可用** | `M5 ≤ 5 步` · `M8 ≥ 80%` · 1 名非项目成员零协助走通 | P2 |
| **M-3** | **自证成网** | `M3 ≥ 6` 格，每格判据 D 严格单调 | P3 |
| **M-4** | **物理补深** | 真 PML 回反射机器可证下降 + 判据 D；非矩形几何可体素化 | P4 |
| **M-5** | **工程化就绪** | `pytest` 可收集且全绿 · `pip install -e .` 后 `lda --help` 可用 · `M6 ≥60` | P5 |
| **M-6** | **系统级有口径** | 光域 Pareto 表 + 锚，**零能效数字** | P6 |

> **注意**：以上里程碑**没有一条依赖外部条件** —— 这是本计划成立的证明。

---

## §7 与既有纪律的接口（施工必读）

**每一批都必须遵守的既有铁律（违反即返工）**：

| 项 | 要求 | 位置 |
|---|---|---|
| **加锚四件套** | `benchmarks.py`（`BENCHMARK_DEFS` + `ORDER`）→ `golden.py`（import + **`_GOLDEN_DISPATCH` 与 `_PHYSICAL_LAW` 双表同登**）→ `verification_adapters.py`（`_adapter_pN` + `_register_candidate`）→ `_batch_bN_numeric.py` | `golden.py:777` / `:987`；`_adapter_core.py:39/41` |
| **账本 7 处** | README 顶行 + README「当前账本」三分类 + CONTRIBUTING 顶行 + `pyproject.toml` version + `run_count_consistency_smoke` + `run_statistical_anchor_smoke` + `run_webui_verification_ledger_smoke` | `README.md:7` |
| **CI core 登记** | 新 smoke 必须登记 `lda/run_ci_regression.py:36 CORE_SMOKES`；漏登由 `NON_CORE_SMOKES:821` 覆盖门禁兜底 | — |
| **自证桩锁** | `MAX_SELF_CERTIFIED = 18`，**只减不增** | `run_self_certified_lock_smoke.py:31` |
| **超时预算** | 新 smoke 预算 ≥3× **跨轮**上界，硬闸 ≥2×；**只改上限，不改判据** | 预算棘轮核心门禁 |
| **护栏自证** | 新护栏必须造**两反例**（畸形 + 空集）+ 配「**合法必过**」；有效性须跑**新旧两版**对照 | — |
| **非确定性消灭** | 秒级 id + 防重守卫 = 同秒假红 ⇒ **源头消除**，不许 wall-clock 进 detail | `deterministic.py:47` |
| **动 WebUI ⇒ 进面** | 动 `lda_webui/**` 则 `run_webui_api_smoke` 必进本批验收面 | P1/P2 强约束 |
| **解释器与路径** | 唯 `…/envs/default/Scripts/python.exe`；`PYTHONPATH=D:/agent_LDA/lda`；Bash shim 坏（`ls/head/tail` 缺）⇒ 用 Python 绝对路径 + `subprocess.run([...], cwd=)` | — |
| **EOL** | 文本编辑须**字节级**判（`open(p,"rb")`）；`autocrlf=true` ⇒ index 恒 LF | — |
| **判据 D** | `1e-13 < 粗端残差 < tol` 且**严格单调**；禁超收敛地板；**tol 只收紧** | — |

**CI 预算提示**：全量 194 条约 **105 min**（v0.9.119 实测 183 条 = 6278.5 s）。P1–P6 会持续加条目 ⇒ 必须用 `scripts/ci_core_batched.py` 分批跑，并守住「**不超预算**」（M7）。

---

## §8 不做清单（防范围蔓延）

| 不做 | 理由 |
|---|---|
| U12 / U13 / U14（真 PDK / 损耗真值 / 物理验证） | **外部**（NDA / 流片 / 现金）—— 杜先生另线推进 |
| 真 foundry DRC deck 对齐 | 需 foundry 交付 ⇒ D5，不在本计划 |
| 电路级 EIC 晶体管级（DAC/ADC/SerDes/DSP/BER） | **红线**：电路级电域锁死；且是 500 亿管无底洞 |
| foundry TCAD **T2 工艺真值** / 封测产线 | **红线**：三不做分层后仍锁死 |
| 让 LLM 进判决路径 | **红线**：判决落非 AI ground |
| 追「千 TOPS / 对标 GPU / pJ/bit 能效」 | **无实测锚** ⇒ 一说出口就无法自证，违背 T1 |
| 在无物理锚时宣称「校准已解锁 / 权重已物理化」 | **假绿比不解锁更危险** |
| 为好看而放宽 tol / 改判据 | **铁律**：tol 只收紧 |
| G14（RCWA/FEM/BPM/3D-EME）、G15（多物理耦合） | 投入产出比低于 P1–P3；每个都是独立求解器 |
| 自造 PDK 生态 | 时间上不可能赢 gdsfactory 的 50+ PDK（见 roadmap 判断 3） |

---

## §9 诚实边界

1. **本计划的所有判据都是内部判据**：它们能证明「我们的设计与工具自洽、可复现、可交叉」，**不能证明「芯片做出来是对的」**。后者只能由阶段 0（真实流片）回答 —— 那是外部线。
2. **DMM 是本次**新提出**的内部标尺**（VMM 的姊妹模型），**未经任何外部采纳或评议**。它的一致性靠与 VMM 共用纪律保证，不靠外部背书。
3. **§2.3 打分表是「代码级」判断**，不是「实验级」：判级依据是代码路径、CI 成员登记与既有判据文件，**未做逐项数值复算**。
4. **D4 的定义刻意避开外部**：D4 = 「单命令产出 GDS + 签核报告 + 几何↔原理图一致」。**它不等于「foundry 能收」** —— 那需要真 deck（D5）。
5. **G4 的改造有残余风险**：几何反推在多形态器件（如变宽波导、锥形）上可能不唯一 ⇒ 必须靠「**反向护栏 + 合法必过**」双向证明，且需明确**哪些参数不可回提**（如实标注，不硬凑）。
6. **两条历史 FAIL 已结清但如实记录**：`E-SIN-NG-1200`（差 0.117）与 `E-GRATING-EFF`（差 0.10）是**故意合成的演示候选**（`run_empirical_bank.py:42/45`），脚本从不 `sys.exit` ⇒ 不构成 CI 判决。**这是设计意图，不是缺陷**；但对外引用 empirical 数字时**不得引这两条**。
7. **侦察路径已复核**：三路侦察报告中若干路径与仓库实测不符（`cli_check_example.json` 实在 `lda/examples/`；`empirical_anchor_report.md` 实在 `lda/lda_harness/reports/`；`LDA_D-13_...md` 在**仓库根**）。**本文凡路径均以本机实测为准。**
8. **时间与人力未估**：本计划按「批次 + 机器判据」组织，**刻意不排时间表**（项目惯例：不设到达时间）。
9. **一句话**：**我们缺的已经不是能力（D3 有 13 项），而是「把能力变成可交付物」的那一道工序（D4 = 0 项）—— 而这道工序正好完全在内部。**

---

## §10 待杜先生裁决（4 点）

| # | 决策 | 影响 | 我的建议 |
|---|---|---|---|
| **Q1** | **P1 是否按「T1.1 设计包→GDS + T1.2 单命令 + T1.3 G4 几何回提」三件一起做** | 决定 DMM 能否一次跨到 D4 | 建议**三件一起** —— 少任何一件，D4 的定义就残缺（GDS 有了但画错查不出，等于交付不可信） |
| **Q2** | **P3（交叉验证网）是否与 P1 并行** | 决定自证短板的闭合速度 | 建议**并行** —— P3 与 P1 零文件耦合，且它是与 DAC 2026 行业共识对齐最直接的一步 |
| **Q3** | **P4 是否接受「只补 G11/G16/G13/G12，明确放弃 G14/G15」** | 决定物理深度的边界 | 建议**接受** —— G14/G15 不挡当前设计能力，且每个都是独立求解器级投入 |
| **Q4** | **DMM 是否对外公开**（作为 VMM 的姊妹模型） | 决定「平台设计能力」能否成为可对外讲的资产 | 建议**先内部用满 P1–P3，待 D4 ≥3 项后在阶段 1 对外** —— 空表对外讲没有力量，「D4 = 0 → ≥3」的过程才有力量 |

> **补充**：P6 的 G20 建议**暂缓到 P1–P3 之后**再定口径 —— 因为它的红线边界（光域 vs 电域）需要在 P1 的签核链路与 P3 的验证网都就位后，才有可能说清「哪些数字是光域的、可引用的」。
