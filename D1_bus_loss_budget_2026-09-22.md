# D1 总线传播损耗预算（随 N 线性增长）

**日期**：2026-09-22 ｜ **模块**：`lda/lda_layout/mesh_pnr.py`（`layout_mode="grid2d"`）
**路线**：B+C 自研（主权光电接口 + light-EIC 光子核）｜ **来源**：P1-B 续闭合报告 §八待裁定 D1
**红线**：A 级永不外借 · C 级自主零依赖 · LLM 不进判决路径 · 零外部求解器依赖。

---

## 一、裁定（结论先行）

grid2d 把 MZI 压实为 O(N) 列、每列共享 x **抽头**、每根 rail 是一条**连续长直总线**
（光自左向右穿过所有列）。总线波导传播损耗与每列抽头插入损耗都随 N **线性增长**。

- **布局正确性不受影响**：酉分解（机器精度）、版级保真度、DRC、LVS 网表一致、驱动映射
  全部与「如何摆位」无关（P1-B 续已证）；损耗是**系统插入损耗指标**，属 PDK/标定层，
  **不破坏 grid2d 主权 P&R**。
- **真正风险 = 路径间损耗方差（IL_var），且由「每抽头耦合器插入损耗」主导**，随 N 线性增长。
  相位校准**无法补偿幅度失衡** ⇒ 方差过大 → 有效传输矩阵偏离酉 → 重构保真度塌方。
- **规模结论**：
  - **N ≤ 128**：α_prop ≤ 2 dB/cm + α_tap ≤ 0.02 dB ⇒ 均值损耗 ≤ 3 dB、方差 ≤ ~3.3 dB，
    在数 dB 接收余量内可工作（相位校准 + 轻微幅度感知）。
  - **N ≥ 256**：朴素耦合器（α_tap ≈ 0.05 dB）下方差达 **18 dB（256）/ ~37 dB（512 外推）**
    ⇒ **必须**配超低位损耗耦合器（α_tap ≤ 0.01 dB，绝热 MMI/方向耦合器）**和/或**逐路幅度均衡
    （可变衰减器 / 幅度感知标定），否则 fidelity 不可接受。

---

## 二、模型与真实几何（取自实际分解，非假设）

从 `clements_rect_decompose`（Clements 平方分解）+ `_rect_column_assignment` 抽取
**真实** grid2d 几何（同一 U=DFT(N) 即 P1-B 续验证所用）：

| 量 | 4 | 16 | 128 | 256 | 512* |
|----|---|----|-----|-----|------|
| 全宽总线长 L_bus (µm) | 155.3 | 561.1 | 4554.1 | 9120.3 | ~18227* |
| 列距 col_pitch (µm) | 30.33 | 32.95 | 35.31 | 35.49 | ~35.5* |
| 列数 n_cols | 4 | 16 | 128 | 256 | 512* |
| 每轨平均抽头数 ⟨deg⟩ | 3.0 | 15.0 | 127.0 | 255.0 | ~511* |

\* 512 为线性外推：L_bus ≈ 35.6·N µm（256 斜率 9120/256=35.6），col_pitch 已饱和，
⟨deg⟩ ≈ N−1。512×512 精确分解 O(N⁴) 超时，外推足够支撑预算判定。

**损耗模型（dB）**：
```
IL_path = α_prop · L_path + n_tap · α_tap
```
- `α_prop` = 波导传播损耗（dB/cm），SOI 条波导典型 1–3 dB/cm；
- `α_tap` = 每个方向耦合器抽头插入损耗（dB），方向耦合器 ~0.05–0.1 dB、绝热 MMI ~0.01–0.02 dB；
- 全宽路径：L_path = L_bus，n_tap ≈ ⟨deg⟩·1.3（含轨切换余量）；
- 最短路径（相邻 I/O，仅 1 列）：L_path = col_pitch，n_tap = 1；
- **IL_var = IL_full − IL_short**（相位校准无法补偿的幅度失衡上界，保守取 max−min）。

---

## 三、三场景预算（mean / worst / 路径方差，dB）

### 场景 A：α_prop=1.0 dB/cm，α_tap=0.020 dB（优化 SOI + 绝热 MMI）
| N | IL_mean | IL_worst | IL_var |
|---|---------|----------|--------|
| 4 | 0.08 | 0.09 | 0.07 |
| 16 | 0.36 | 0.45 | 0.42 |
| 128 | 3.00 | 3.76 | 3.73 |
| 256 | 6.01 | 7.54 | 7.52 |
| 512* | ~12.0 | ~15.1 | ~15.1 |

### 场景 B：α_prop=2.0 dB/cm，α_tap=0.050 dB（标准 SOI + 方向耦合器）
| N | IL_mean | IL_worst | IL_var |
|---|---------|----------|--------|
| 4 | 0.18 | 0.23 | 0.17 |
| 16 | 0.86 | 1.09 | 1.03 |
| 128 | 7.26 | 9.17 | 9.11 |
| 256 | 14.57 | 18.40 | 18.34 |
| 512* | ~29.2 | ~36.8 | ~36.7 |

### 场景 C：α_prop=3.0 dB/cm，α_tap=0.100 dB（保守/未优化）
| N | IL_mean | IL_worst | IL_var |
|---|---------|----------|--------|
| 4 | 0.35 | 0.44 | 0.33 |
| 16 | 1.67 | 2.12 | 2.01 |
| 128 | 14.07 | 17.88 | 17.77 |
| 256 | 28.24 | 35.89 | 35.78 |
| 512* | ~56 | ~71 | ~71 |

---

## 四、方差拆解（核心洞察：抽头主导）

场景 B（α_prop=2.0, α_tap=0.05）拆解 max−min 路径损耗方差：

| N | var_tap (dB) | var_prop (dB) | var_total (dB) |
|---|--------------|---------------|----------------|
| 4 | 0.15 | 0.02 | 0.17 |
| 16 | 0.93 | 0.11 | 1.03 |
| 128 | 8.21 | 0.90 | 9.11 |
| 256 | 16.53 | 1.82 | 18.34 |

→ **抽头损耗贡献传播损耗的 ~9–10×**。把 α_tap 从 0.05 降到 0.01 dB 即可把 128 方差从
9.1 dB 压到 ~1.8 dB；但 N=256 即便 α_tap=0.01 仍 ≈ 3.7 dB（抽头主导、随 N 线性）。

**对 unitary 保真度的含义**：相位校准（Vπ·L 映射）只能修正相位，不能修正幅度失衡。
IL_var 即「最差路径 vs 最短路径」的幅度差上界；该差越大，有效传输矩阵 T = A·U
（A=逐路幅度对角）越偏离酉 ⇒ 编程酉矩阵的重构保真度越低。经验上 IL_var > ~3 dB 即
进入「需幅度均衡」区（否则 fidelity 跌破 0.99 量级，具体取决于变换本身）。

---

## 五、诚实边界（防纸糊楼）

1. **本预算为参数化估算（L3 级），非流片实测**：α_prop / α_tap 取公开区间代表值；
   真值须由 foundry PDK + 圆片表征（弯曲损耗、截面损耗、耦合器实测 IL）回填。
2. **未含项**：弯曲损耗（总线为直波导，本布局弯曲极少 ⇒ 此项小，已优于蛇形）、
   模场失配、偏振/温度漂移、热串扰（见 D2）、截面散射（随 wg 尺寸）。这些不推翻
   「线性随 N」结论，只微调常数。
3. **方差口径保守**：取 max−min 全宽 vs 最短路径；实际 I/O 对分布居中，真实 std 小于此，
   但量级与「抽头主导、∝N」结论不变。
4. **grid2d 不引入额外损耗机制** vs 蛇形——蛇形每 MZI 唯一 x、总线更短但折叠回折多、
   弯曲损耗更高；二者总损耗同量级。grid2d 的优势是面积，代价是长直总线（线性传播），
   已计入本预算。

---

## 六、缓解与路线图（非阻塞）

| 杠杆 | 作用 | 建议 |
|------|------|------|
| 波导传播 α_prop | 降低均值/方差中的传播项 | 维持优化 SOI ≤ 2 dB/cm（低损耗刻蚀/包层） |
| **每抽头 α_tap** | **降低方差主项（∝N）** | 指定绝热 MMI / 低损耗方向耦合器 **α_tap ≤ 0.01 dB** |
| 幅度均衡 | 补偿残余路径失衡 | P2 PDK：逐 MZI 可变衰减器 / 幅度感知标定（V→幅度闭环） |
| 等长路由 | 压路径长度方差 | 与压实目标冲突（会牺牲面积），仅局部用 |
| 接收余量 | 吸收均值损耗 | 链路预算留 ≥ 20 dB（EDFA/ detector），均值 ≤ 10 dB 安全 |

**规模建议**：
- **N ≤ 128**：直接可流片（α_prop≤2 + α_tap≤0.02，方差 ≤3.3 dB，相位校准够用）。
- **N = 256**：需 α_tap ≤ 0.01 dB（绝热耦合器）+ 评估幅度均衡；方差仍 ~3.7 dB（优耦合器）。
- **N = 512**：必须幅度均衡（即便优耦合器方差 ~8.5 dB）→ 列为 P2 PDK 项，不阻塞布局。

---

## 七、交付物

| 文件 | 内容 |
|------|------|
| `D1_bus_loss_budget_2026-09-22.md` | 本预算报告 |
| `examples/budget_bus_loss_d1.py` | 从实际分解抽取几何 + 参数化损耗模型（numpy-only） |
| `examples/_d1_bus_loss_out.txt` | 三场景 + 方差拆解原始数值 |

入口复算：`python examples/budget_bus_loss_d1.py`（需 `D:/agent_LDA/lda` 在 sys.path 使
顶层 `lda_agent` 可导入；本机已验证 `envs\default\Scripts\python.exe`）。

---

## 八、与待裁定项联动

- **D2 热串扰**：同列抽头相位器并行驱动 → 热串扰随列数增长，与 D1 损耗同属「随 N 增长」
  的系统级代价，建议合并为「grid2d 系统级代价」专题（损耗+热+标定）。
- **D3 规模再上探（256/512）**：本预算给出硬性边界——512 不靠幅度均衡不可行，
  故 D3 上探须与幅度均衡 PDK 绑定排期。
- **收官建议**：D1 不阻塞 P1-B 续收官（布局正确）；作为「主权光子计算主链路」对外演示时，
  标注 N≤128 为当前免幅度均衡安全区，N≥256 需 PDK 升级。
