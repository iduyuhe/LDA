# LDA 技术白皮书

> 文档编号：LDA-TW-001
> 版本：v1.0（内部技术宪章草稿）
> 编制日期：2026-08-14
> 编制依据：《LDA 前期战略讨论纪要（全量）》《LDA 主权化搬迁清单与操作步骤》
> 密级：内部 · 暂不对外

---

## 摘要

本白皮书定义 LDA 的技术架构与设计哲学。LDA 不是又一个光/量子芯片 EDA 工具，而是一个**开源、Agent 原生、由 AI 主体直接开发底层内核**的设计自动化平台。其创新不在"又写了一个求解器"，而在三件事：**（1）定义跨光子+量子统一的开放中间表示 L0；（2）定义开放智能体协议 L1；（3）用 agent 自举的方式长出可验证的求解核 L3，并把研发过程本身也 agent 化。** 巨头（Cadence/Synopsys）因商业模式自噬不会做开放内核；现有开源（gdsfactory）只覆盖布局层、未占标准层——LDA 建在两者都没占的那一层。

---

## 1. 设计哲学（四条不可妥协）

1. **开源优先**：内核 MIT/Apache 开放，靠社区共建 PDK 与基准，闭源拿不到生态数据。
2. **Agent 原生（AI-maximalist）**：设计流程与研发流程都由多智能体闭环驱动；在法律法规风控下尽可能用 AI，不自我设限。
3. **自主可控**：对外国受控关键依赖零容忍（A 级永借、B 级主权化、C 级自主）。
4. **验证底线**：AI 写手 + AI 裁判可最大化，但裁判最终判定必须落到「物理定律 + 实证大数据」非 AI 真值锚——这是数学约束，不是保守。

---

## 2. 系统总体架构（L0–L4）

```
┌─────────────────────────────────────────────┐
│ L4  应用与生态（用户层 / 社区 / 商业认证版）   │
├─────────────────────────────────────────────┤
│ L3  求解器后端（FDTD/FEM/EME/模式/量子哈密顿） │
│      ← AI-dev 自举生成 / 插件调 gdsfactory·Meep│
├─────────────────────────────────────────────┤
│ L2  开放 PDK / 器件本体 Registry（社区共建）    │
├─────────────────────────────────────────────┤
│ L1  智能体协议层（Interpreter·Designer·Layout· │
│      Verifier·Solver-agent 开放接口 MCP/A2A）  │
├─────────────────────────────────────────────┤
│ L0  开放 IR/DSL（光子+量子统一中间表示）        │
└─────────────────────────────────────────────┘
```

**分层原则**：L0/L1 是"标准层"（我们的护城河），L2 是"供给层"（社区共建），L3 是"能力层"（AI 自举 + 插件），L4 是"价值层"。gdsfactory、Meep、Lumerical、Qiskit Metal 都落在 L3 实现层——我们是它们的**中立编排者**，不是重造它们的竞争者。

---

## 3. 开放中间表示 L0（核心创新点之一）

### 3.1 设计原则
- **跨域统一**：同一 IR 表达光子器件与量子器件的设计意图，使"电子-光子-量子协同设计"成为可能（巨头未认真做的空白）。
- **机器可读 + 人类可读**：YAML/JSON 风格的 DSL，兼作"自然语言→设计"的落点（PhIDO 仅用薄私有 YAML 且光子-only，L0 要补齐统一与开放）。
- **可验证溯源**：IR 节点携带来源（人类标注 / agent 生成 / 仿真反推）、版本、约束标签，便于 Verifier agent 追责。

### 3.2 与 gdsfactory 的关系（关键澄清）
gdsfactory 是 L3 实现层最成熟的"版图/验证框架"，其 Python API 是 L0 IR 的一个**下游序列化目标**之一。L0 不重写 gdsfactory 的版图能力，而是定义"意图→多种后端（gdsfactory/Meep/Qiskit Metal）"的统一语言，让 gdsfactory 成为被编排的插件。

---

## 4. 智能体协议层 L1

### 4.1 角色定义（开放标准接口，MCP/A2A 风格）
| Agent | 职责 | 输入 | 输出 |
|---|---|---|---|
| **Interpreter** | 自然语言/需求 → 结构化意图 | 用户描述 | L0 IR 草案 |
| **Designer** | 意图 → 参数化电路/器件方案 | L0 IR | 候选设计 |
| **Layout** | 设计 → 版图（调 gdsfactory 等） | 候选设计 | GDSII/OASIS |
| **Solver-agent** | 设计 → 物理场/参数（自举或插件） | 候选设计 | 场分布/S 参数/n_eff |
| **Verifier** | 比对真值、判对错、出修复 brief | 候选 + ORACLE | pass/fail + 修复指令 |

### 4.2 协议要点
- 所有 agent 间通过 L0 IR 和标准消息 schema 通信，可独立替换实现。
- 裁判路径（Verifier→ORACLE）必须是确定性死代码，LLM 不进判决。

---

## 5. 开放 PDK / 器件本体 Registry L2

- **跨晶圆厂**：NOEIC(武汉)/CUMEC(重庆)/SITRI(上海) 等硅光 PDK、超导量子工艺 PDK，社区共建、统一 schema。
- **器件本体**：标准化器件参数（波导截面、耦合器、量子比特几何）的语义本体，支撑 Interpreter 的意图解析与 Designer 的方案检索。
- **主权要点**：PDK 是生态护城河，须对接本土晶圆厂打"自主可控"牌。

---

## 6. 求解器后端 L3（AI-dev 自举）

### 6.1 自举回路
```
SolverWriter(ai写) → Candidate → Benchmark(裁判) → Verifier(agent判对错)
       ↑                                              │
       └────────── 不达标退回重写 ←──────────────────┘
                              ↓ 达标
                       进入开放内核
```
- **更深一层**：用 L0/L1 的 agent 框架去开发 L3 求解器本身 = agent 造 agent，递归自举。

### 6.2 验证裁判架构（物理定律 + 实证大数据锚）
- **① 物理定律锚**：解析解（Mie 散射、已知模指数）、麦克斯韦确定性计算——方程的必然，非某人意见。
- **② 实证大数据锚**：跨多源真实流片/测量语料，众人贡献、越用越厚，非 legacy 遗留。
- **Meep 降级为 ① 的一种实现**（MVP 期 ORACLE），非唯一裁判；后期由 AI 写的 FDTD 核取代。
- **底线**：无任何外部 ground 的纯 AI 互证 = 循环论证（两个 AI 互相点头，错也被确认）。此线不可破。

### 6.3 确定性比对脚本（唯一真裁判）
- `benchmark_harness.py` 跑候选求解器 → 取场分布/S 参数/模式有效折射率 → 对齐 ORACLE 黄金参考 → 算标量误差（相对 L2 误差、Δn_eff、透射系数误差、能量守恒残差、收敛阶）→ 对照**写死的阈值常数**判定。
- LLM（Verifier）**只读取 JSON 摘要指标 + 自动 diff 叙述**，决定"哪个指标挂了、写针对性修复 brief"。LLM 从头到尾不碰场矩阵、不碰数字。

---

## 7. 主权依赖政策（A/B/C 分级）

| 级 | 含义 | 实例 | 处置 |
|---|---|---|---|
| **A** | 永不借、自己搞 | Lumerical/Ansys、Synopsys、Cadence、Siemens、GDSFactory+ 商业 NDA-PDK | 求解器由 AI 自举，本就无关 |
| **B** | 借今踢后 | gdsfactory(MIT)、Meep(GPL-2.0)、KLayout(GPL-3.0)、MPB(GPL-2.0)、SAX(Apache-2.0)、**Tidy3D(GPL 前端+美属云 ORACLE B6)** | fork 主权副本（已建 Gitee i4hub 仓、gdsfactory 已入库）；功能层后期 AI 重写；**Tidy3D 仅外部 ORACLE 校验、零硬编码、可离线回退** |
| **C** | 第一天自主 | L0 IR/DSL、L1 agent 协议、L3 AI 求解核、物理定律锚 | 无外部依赖 |

### 7.1 GPL 传染性红线（关键）
- **MIT（gdsfactory）/ Apache-2.0（SAX）**：可 vendor 进闭源商业代码（Apache 须保留 NOTICE）。
- **GPL-2.0/3.0（Meep/MPB/KLayout）**：**红线**——仅作独立外部进程调用（subprocess/CLI/本地 socket），不静态链接/直接 import 进闭源代码。
- 结论：GPL 组件只进"研发工具链/外部调用"，不进"产品研发代码"。动手写 L3 前定死。

### 7.2 本地为根、Gitee 为门面
- **本地（自有服务器）= 主权根**（root of truth）+ 离线冷备；**Gitee/GitCode = 公开门面**（社区入口）。双轨并存。
- 已完成：Gitee(i4hub) 建 5 仓、gdsfactory 入 Gitee 并落地本地 `D:/agent_LDA/.lda_mirror/gdsfactory.git`；sax/mpb/meep/klayout 须杜先生本地补（本 Agent 沙盒对 github.com 间歇性不可达）。

### 7.3 外部 ORACLE B6 = Tidy3D 主权应对（2026-08-15 杜先生确认）
**本质**：Tidy3D（Flexcompute，美国公司，2016 在美创立，受 EAR/ITAR 合规约束）是超快 FDTD 全电磁 3D 求解器，等价于直接数值求解麦克斯韦方程组的真 3D Maxwell 仿真。其前端 Python 包为 GPL 开源（可 fork 主权副本），但**真跑仿真必须上 Flexcompute 美国云服务器、账号买 credit**——计算在美属云、受出口管制。

**主权定性**：典型 **B 级（借今踢后）**——fork 开源前端做主权分发，但**绝不把云算力当 L3 核心依赖**；仅作「外部 ORACLE B6」真 3D 验证锚（且必须兜底到物理定律锚 / Meep）。

**是否卡脖子**：属于，但**不是命门**。LDA 核心求解（B11 解析近似 + Meep/B9 物理定律锚）已自立；Tidy3D 仅为「真 3D 高精度兜底校验」，断供仅致该段验证精度降级，不影响主流程。

**四原则（已落地代码纪律）**：
1. **不依赖**：核心求解零硬编码 Tidy3D，无 API Key 也能离线跑通（当前沙盒即此态，B6 走「设计守则锚」兜底——优先 Meep 后解析近似）。
2. **战术可借**：合法凭证到位后，作为外部 ORACLE B6 接真 3D 校准，提升验证精度（仅外部调用，绝不 import 核心）。
3. **被限回退**：断供即回退物理定律锚 / Meep，流程不崩，无致命风险。
4. **长期替代**：后期用国产 TCAD（如国内硅光代工自用仿真链）或 AI 自写 FDTD 核接替，把 B 级彻底踢掉。
- **代码现状**：`lda/lda_harness/oracle_tidy3d.py` 仅读取凭证与提交/回取，核心零依赖；缺 `TIDY3D_API_KEY` 时自动降级到 Meep/解析锚，符合主权纪律。

### 7.4 「AI 自写 FDTD 核踢掉 B 级」可行性论证（2026-08-15 杜先生立项）
**结论：技术可行，且 L2-A 的 1D、2D 与 3D 维度均已实跑通过校验（非仅立项）；L2-B 性能升维两步均已推进——第一步 Numba-CPU JIT 加速（fdtd3d_numba.py，约 20×、5/5 PASS）已交付；第二步 PyTorch 可切换 GPU/CPU 张量化后端（fdtd3d_torch.py）亦已交付：复用 numpy 几何构造、六分量改写为切片式 curl 算子，与 numpy/Numba 版逐位等价、selfcheck 5/5 PASS（CPU 实测验证），`device='cuda'` 一行即激活 GPU；**GPU 已于 2026-08-16 在 RTX 5060 Ti 上装 CUDA 轮子（torch 2.11.0+cu128, CUDA 12.8）实跑激活**：cuda selfcheck 4/4 PASS（偏差与 numpy/Numba 逐位一致）+ cuda-cpu fp64 跨设备 bit-equivalence（max_rel=0.00e+00）；greens N=120 cuda 19.41s（vs numba-cpu 1.03×、vs torch-cpu 5.30×，消费卡 fp64 阉割、收益在显存非 flops，与研判一致）。功能型 3D 主权核已具生产级 CPU 性能（numba-cpu 43.1× 最快），GPU/CUDA 生产级超大网格为后续升维项（显存受限场景）。**

**可行性分级（按代码量与风险）**：
- **L2-A 自写 1D/2D FDTD（含吸收边界 PML）**——**1D 已完成并自研通过物理定律锚校验（2026-08-15 晚）**。实现于 `lda/lda_solver/fdtd1d.py`（C 级自主、机器优先接口、零外部依赖仅 numpy、梯度海绵吸收层、参考跑归一化绝对标度）；交叉校验 `run_fdtd_selfcheck.py`（ORACLE = tmm.py 传输矩阵解析解）**4/4 PASS**：匹配介质 T≡1.0000、单界面空气→玻璃 T=0.958、FP 标准具条纹、布拉格光栅禁带中心≈2.46µm 全部复现（公差 0.02–0.10 内）。**这意味着 1D 透射谱已无需借 Meep 即得，是"踢掉 B 级求解器"路径首个可运行实证。** **2D 升维已交付（2026-08-15 晚）**：`lda/lda_solver/fdtd2d.py`（TEz 模式 Yee 网格 Ez/Hx/Hy + 2D 梯度海绵吸收层 + 参考跑归一化，零依赖 numpy），配套 `run_fdtd2d_selfcheck.py`（双 ORACLE：① tmm.py 传输矩阵解析解覆盖"分层膜 y 方向平移不变→退化为一维"极限；② 点源柱面波 |Ez|·√r 常数作为真·二维校验并同时验证四向海绵无回反射）**5/5 PASS**：匹配介质 T≡1.0000（Δ=0.0000）、单界面空气→玻璃 T=0.958（maxΔ=0.0022）、FP 标准具 n=2.5 d=2.0µm 条纹（maxΔ=0.0135）、布拉格光栅禁带中心≈2.46µm（maxΔ=0.0019）、点源柱面波 |Ez|·√r 常数（max_rel_dev=0.0134）全部复现。关键修复：2D 海绵须厚于 2λ（沿用 1D 经验——1.25λ 会让单界面用例在波长间出现被海绵回反射驻波污染的残留偏差，呈 0.88/0.88/0.96/1.03 波长振荡）；分层膜用例启用 y 方向 PBC 退化为一维（仅 x 方向海绵）、点源用例用四向海绵。**2D 透射谱已实现"踢掉 B 级求解器依赖（Meep），离线自研得出"——主权 B 级替代路径在 1D 之后于 2D 维度再次实证落地；**3D 维度（fdtd3d.py 全 Yee 六分量 + 参考跑归一化，零依赖 numpy）亦已随 L2-A 自研通过物理定律锚校验（selfcheck 5/5 PASS：匹配介质 T≡1.0000（maxΔ=0.0000）、单界面空气→玻璃 T=0.958（maxΔ=0.0019）、FP 标准具 n=2.5 d=2.0µm 条纹（maxΔ=0.0170）、布拉格光栅禁带中心≈2.46µm（maxΔ=0.0090）、点源球面波 |Ez|·r 常数（max_rel_dev=0.0101）全部复现，公差 0.02–0.20 内）——"踢掉 B 级求解器（Meep）"路径已在 1D/2D/3D 全三维实证落地。**
- **L2-B 3D 性能升维（GPU 加速，对标 Tidy3D）**——**功能型 3D 主权核（fdtd3d.py，零依赖 numpy）已随 L2-A 落地（selfcheck 5/5 PASS）**。**第一步 Numba-CPU JIT 加速（fdtd3d_numba.py）已交付**：将逐字节等价的六分量更新融合为 `@njit(parallel=True)` 核，同精度 selfcheck 5/5、较纯 numpy 版约 **20× 加速（16m19s→0.8m）**，功能型 3D 主权核已具生产级 CPU 性能。**第二步 PyTorch 可切换 GPU/CPU 张量化后端（fdtd3d_torch.py）已交付**：把六分量更新改写为切片式 curl 算子（复用 numpy 几何构造 `_build_interior/_sponge_1d/_grid_constants/_avg_sigma`，保证逐字节等价），`device='cuda'/'cpu'` 一行切换、算法完全相同；CPU 上 selfcheck 5/5 与 numpy/Numba 逐位一致（A=0.0000/B=0.0019/C=0.0170/D=0.0090/E=0.0101）。CPU 实测 torch-cpu(≈104s) 慢于 numba-cpu(≈20s)（每步多张量算子派发开销），其性能价值在向 GPU 迁移后释放——RTX 5060 Ti 级 GPU 物理存在，仅缺 CUDA 轮子（本沙箱 pytorch CDN 限速、缓存轮子损坏），装上轮子后即 `device='cuda'` 激活。**工程重、算力门槛高的是"生产级 3D 超大网格"**——仍由 AI 团队自举（自配算力），不外包旧体系。外部人力（高校计算物理团队 / 退休专家）**仅定位于验证层**：提供真实流片/测量语料做"实证大数据锚"、对 AI 裁判做人工抽检与背书（延续全局验证锚纪律），**绝不承接求解器开发**。仅当 AI 确证做不了（如缺物理实测设施、需特定实测数据集）才外协——此为少数例外。先借 Tidy3D/Meep 当 ORACLE 校准，自配算力（装 CUDA 轮子）到位后攻生产级 3D 超大网格。
- **非 FDTD 的 B 级（gdsfactory 版图 / KLayout / SAX 频域）**——**版图层不踢**（MIT 已 fork 即满足主权，属最不急；AI 重写版图层 ROI 低）；SAX 频域可自写但非瓶颈。**严格说"彻底踢掉 B 级"目标应收窄为"踢掉 B 级求解器依赖（Meep/Tidy3D）；版图/频域类借今即可"**——1D FDTD 已兑现此收窄目标的第一步。

**前置条件（走通前必须满足）**：
1. 物理定律锚题库（B1–B11）已建且全绿——自写核的"裁判"必须在自写之前就独立存在（已满足）。
2. Meep ORACLE 在线可用——自写核每一版都须经 Meep 真场比对（**1D 自研核已以 tmm.py 物理定律锚交叉校验通过，进一步降低对 Meep 的依赖**）。
3. 算力：1D/2D CPU 即可；3D 已用 Numba-CPU 取得约 43.1× 加速（覆盖验证类与生产级中小网格）；PyTorch 张量化后端已就绪（CPU 验证 5/5、`device='cuda'` 一行激活，2026-08-16 已在 RTX 5060 Ti 实跑验收）；GPU/CUDA 用于生产级超大网格（显存受限场景，消费卡 fp64 收益有限、纯算力≈持平 numba-cpu）。

**风险与边界**：
- 自写核的 bug 风险——必须由"非 AI 物理定律锚 + Meep 双 ORACLE"交叉校验，AI 写手 + AI 裁判不能自证（沿用全局验证锚纪律）。1D 核已用该纪律实跑通过。
- 性能：自写核短期不及 Tidy3D GPU 百倍加速；**仅在"真 3D 高性能"维度暂时落后，不阻塞主流程**（1D 维度已自立）。

**立项标记**：
- ✅ **第一攻关项 · 已交付（1D + 2D + 3D + Numba 加速 + PyTorch 可切换后端）**：**L2-A 自写 1D + 2D + 3D FDTD 核（梯度海绵 + 参考跑归一化），均由 agent 自举实现并经物理定律锚校验通过（1D selfcheck 4/4 PASS、2D selfcheck 5/5 PASS、3D selfcheck 5/5 PASS）**，已实现"1D/2D/3D 全三维踢掉 Meep 求解依赖"。**L2-B 性能升维两步已推进**：第一步 Numba-CPU JIT 加速（fdtd3d_numba.py）已交付——与纯 numpy 版逐字节等价、同精度 selfcheck 5/5、约 20× 加速（16m19s→0.8m）；第二步 PyTorch 可切换 GPU/CPU 张量化后端（fdtd3d_torch.py）已交付——复用 numpy 几何构造、与 numpy/Numba 版逐位等价、selfcheck 5/5 PASS（CPU 实测验证），`device='cuda'` 一行激活 GPU（本沙箱 CDN 限速致 CUDA 轮子未装、GPU 实测待部署机）。功能型 3D 主权核已具生产级 CPU 性能。
- 🚩 标记待攻关（开发由 AI 团队自举）：**"L2-B 3D 生产级超大网格 GPU/CUDA 升维（PyTorch 张量化后端已就绪、GPU 已于 2026-08-16 在 RTX 5060 Ti 激活验收，攻生产级超大网格显存受限场景）"** 与 **"国产 TCAD 接替"** 仍为后续 AI 自研项；高校/退休专家在此**仅作验证层协作**（实测数据 + AI 裁判人工抽检背书），不阻塞当前路线。仅当 AI 确证做不了才外协。
- ⚠️ 收窄目标：原"把 B 级彻底踢掉"改为 **"踢掉 B 级求解器依赖（Meep/Tidy3D）；版图/频域类 B 级维持 fork 主权化即可"**——1D 已实跑兑现第一步，避免为'自主'盲目造已可复用轮子（违反量力原则）。

---

## 8. MVP 定义与技术路线

### 8.1 MVP 范围（光子优先）
- 单点垂直场景（如某类波导耦合器/分束器）打通：**自然语言 → L0 IR → gdsfactory 版图 → 本地 Meep ORACLE 算标准考题 → 与解析解比对通过**。
- 交付：AI-dev 自举闭环 demo + 第一个可信基准考题集。

### 8.2 技术路线
1. 先定 L0 IR 草案（光子子集）→ 2. 起 L1 最小 agent 集（Interpreter/Designer/Layout/Verifier）→ 3. 接 gdsfactory + Meep 跑通端到端 → 4. SolverWriter agent 试写最小 FDTD 核并用 Meep ORACLE 校验 → 5. 攒基准套件 + 反向悬赏。

---

## 9. 开放标准与生态治理

- **治理结构**：技术委员会（退休 EDA 专家 + 高校博导）+ 社区 RFC 流程定 L0/L1 标准；PDK Registry 由晶圆厂 + 社区共治。
- **许可策略**：内核 MIT/Apache；GPL 组件外部调用；商业「认证版」闭源 + SLA 担保（红帽模式）。
- **信任基建**：公开对抗性基准套件 + 反向悬赏失败榜 + 第三方复现预注册。

---

## 10. 已决议事项（2026-08-14 · 杜先生授权 AI 决策）

> **决策授权**：杜先生原则——不走回头路（=该打好的基础一定要打好、不为出成果做表面；但基础工作在能力圈内、超出的借外力、能绕的绕开）、按科学工程原理、技术/工程决策全权委托 AI；杜先生只定方向与想法。
> 以下两项原"待定"议题，由 AI 基于前期论证（见可行性报告 §4.3、技术白皮书 §3–§8）代为拍板并**固化**。

- [x] **光子优先、量子后上（已决议）**。依据科学工程原理：① 光子侧基准题丰沛（解析解 / Meep 可直接当 ORACLE），信任墙更易跨；② 量子侧基准稀缺、误差更致命、保守子领域更长期不信任；③ 光子器件产业受 AI 数据中心 800G/1.6T 拉动高增长（2025 增速 37%+），有现实产业牵引。量子侧物理深度高，留待信用攒足后攻坚（阶段 2 之后）。
- [x] **先单点垂直场景、后统一（已决议）**。依据 MVP 最小可行原则：先竖切一个垂直场景（如波导分束器 / 耦合器）跑通"设计→仿真→版图→签核"闭环，L0 IR 先做光子子集；统一（跨光子+量子）是阶段 2 之后的事。贪全则闭环难成、信用难建。

**仍待阶段细化（非方向性待定，是执行细节）**：
- [ ] L0 IR 的首个序列化格式（YAML/JSON/专有 DSL）与命名空间——阶段 1 细化。
- [ ] 实证大数据锚的冷启动语料来源（开放题库 + 众包实测）——阶段 1 细化。
- [ ] 顾问委员会架构与首批退休专家招募话术——阶段 2 启动。

---

## 11. 基础工作量力而行原则（执行总纲 · 2026-08-14 晚澄清）

"不走回头路"的真实含义：**该打好的基础一定要打好，不为出成果做表面；但基础工作要在能力圈内，超出的借外力、能绕的绕开。** 据此将工程推进按三层划分。

**为什么采取这种原则——技术复利（杜先生 2026-08-14 晚洞察）**：扎实打基础不是"慢"，而是提前投资一笔会生息的资产。能力圈内的真地基（L0 IR / 验证 harness / 主权骨架 / 标准基线）一旦建好，上层每加一个求解器、PDK、agent 都**复用同一底座**，边际成本递减——越往后做，单位成本越低。反之，若跳过地基赶进度，欠的功课不会消失，只会在下一阶段（统一跨光子+量子、商业化、被断供）**集中暴雷**，那时补的成本远高于前期投入（且往往伴随信任崩塌、项目中断等不可逆损失）。这与软件工程"技术债务"隐喻互为反面：投机取巧是借高利贷，扎实基建是存复利。它与"量力借力、能绕则绕"不矛盾——**在护城河相关的真地基上投资复利，在非护城河的可复用件上借力/绕开省时间，是同一工程哲学的两面。**

### 11.1 必须扎实打好的地基（能力圈内，不可替代）
这些是我们的护城河核心，必须自己做、不取巧、不外包：
- **L0 统一 IR/DSL**——光子+量子统一的机器可读中间表示（建在 gdsfactory 私有 YAML 之上、巨头没占的真空格）。
- **L1 agent 协议层**——Interpreter/Designer/Layout/Verifier/Solver-agent 的开放接口标准（MCP/A2A 风格）。
- **主权依赖策略落地**——B 级 fork/镜像/冷备、SBOM、GPL 红线（已启动，gdsfactory 入 Gitee+本地根）。
- **验证裁判的确定性 harness 架构**——把"裁判"从 LLM 手里拿回确定性数值比对脚本，LLM 仅解释摘要指标。
- **标准 / 文档基线**——本白皮书、宪章、基准套件规范（已产出 4 份战略文档）。

### 11.2 该借外力（超出能力圈，主动借 —— 这不是偷懒，是科学分工）
- **真值校验**：物理求解器内核的"对错"借 Meep / 解析解当 ORACLE，不自己造标准答案。
- **PDK 对接**：借本土晶圆厂（NOEIC/CUMEC/SITRI）的工艺与 PDK，不生造工艺。
- **量子侧深度物理**：借高校（中科大/上交）与本源/量旋，光子侧先跑通后再攻坚。
- **人力**：写代码借高校学生（有时间有热情），定标准/把质量关借退休 EDA 老炮与光电研究员（有资源有情怀有时间）。

### 11.3 能绕过去就绕（不硬刚、不盲目造轮子）
- **不重写 gdsfactory 布局层**：MIT 已 fork 即满足主权，属最不急项；重写它是浪费。
- **不碰 A 级美系商业工具**（Lumerical/Ansys/Synopsys/Cadence）：直接不用，绕开壁垒，本就是"自己搞"的 thesis。
- **不自建商业级 FDTD**：先借 Meep 当验证锚，后期用 AI 写核取代（B 级"暂借终将踢掉"路径）。

### 11.4 红线
- **不为赶进度做表面**：如只堆 demo 不建真地基（确定性 harness、L0 IR 草案）即算表面。
- **也不为"自主"盲目造轮子**：已可合法复用的（gdsfactory 布局层）不必重造——那是另一种不科学。

### 11.5 复利 vs 债务：地基项的回报对照

| 真地基（前期扎实投入 → 技术复利） | 若跳过（欠债 → 下一阶段暴雷，补课成本指数级） |
|---|---|
| **L0 统一 IR/DSL**：定一次，所有布局/仿真/验证/agent 复用，扩展边际成本≈0 | 阶段 2 统一时发现光/量子两套表示不兼容，重写 IR + 全部 adapter |
| **验证裁判确定性 harness**：建一次，所有求解器（光子/量子）复用同一验证闭环，信任墙可累积 | 阶段 3 别人流片失败，信任崩塌，商业化搁浅；每个新求解器都要单独手验 |
| **主权依赖骨架**（fork/镜像/SBOM/冷备）：现在 1 小时，未来断网/被限/离线构建零成本 | 阶段 1 中途 GitHub 被限，研发停摆，紧急重写 + 项目中断 |
| **标准 / 文档基线**：一次写清，每次招募学生/退休专家/合作方都用同一语言，沟通成本递减 | 每个新人/合作方重新培训，生态碎片化、分叉，标准话语权流失 |

---

## 12. 系统设计哲学：为人结果负责 vs 为人操作设计（杜先生 2026-08-14 晚界定）

LDA 与所有现有 EDA/PDA/QEDA 工具的根本区别，不在功能清单，而在**设计的第一性假设**：

### 12.1 系统目的与分工
- **目的**：产出**可用的设计结果（design outcome）**，不是一个"帮人设计"的辅助软件。
- **人机协作分工**：智能体（agent）负责**操作执行**——调用内核、跑仿真、生成版图、跑验证、迭代；人负责**决策**——定方向、验收结果、承担结果责任。人是"结果责任人"，不是"操作工"。

### 12.2 旧系统的根本假设（对 LDA 是错的）
Synopsys / Cadence / gdsfactory / Meep 等**全部"为人操作而设计"**：GUI、逐步点击交互、给人看的报表、给人逐步调参的 API 粒度。它们的很大价值（易用性、培训、交互体验）建立在"人盯着屏幕一步步操作"上。这与 LDA 的"agent 操作、人只决策"恰好相反。

### 12.3 借用旧系统时的陷阱与正确姿势
- **陷阱**：旧系统里大量"为人操作设计"的东西（交互壳、GUI、给人看的抽象、逐步式/状态依赖式 API、人确认步骤）对 LDA **无用甚至负担**。若直接把 gdsfactory/Meep 的"人操作接口"当 L3 接口用，会把交互粒度、状态依赖、人确认步骤等包袱**污染 agent 编排**。
- **正确姿势**：只借它们的**内核能力**（求解、版图生成引擎、PDK 编译），经 **L1 agent 协议层**把"人操作接口"翻译/适配为"agent 操作接口"——确定性、批处理、可验证、无交互。这同时也强化了 §11.3"不重写布局层、能绕则绕"的深层理由：**不是不重写，而是它的交互壳对我们无用，我们只要程序化内核。**

### 12.4 对 L0 / L1 的定义性影响
- **L0 统一 IR 必须是机器优先的 DSL**——是 agent 之间、agent 与内核之间的"机器语言"，不是给人看的 GUI 配置。这是 L0 存在的根本性理由之一。
- **L1 协议层本质是"人操作壳 → agent 操作接口"的翻译/适配层**——剥离交互包袱、暴露确定性能力，让旧内核能被 agent 无摩擦编排。

### 12.5 红线
- **不要把"为人操作设计"的旧 API 直接当 L3 接口**（会带着交互粒度/状态依赖/人确认步骤污染 agent 编排）；必须经 L1 适配层封装。
- 系统设计每一个环节先问一句：**这是给人操作方便，还是给 agent 出结果？** 凡是前者、且非人决策点的，对 LDA 都是可剥离的壳。

---

*本白皮书与《LDA 可行性分析报告》《LDA 市场竞争与赛道分析》《LDA 发展里程碑与路线图》配套。*
