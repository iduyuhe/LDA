# LDA 光子计算芯片 · 设计阶段内部总结

> 版本：v1.0 · 2026-09-23 · 性质：**内部总结（功能 / 技术 / 市场对比 / 升级判定）** · 范围：**仅设计角度**，不含物理生产
> 依据：P0 / P1-A / P1-B / P1-B续 / P1-C / D1 / D2 / D3 / 幅度均衡(P2) / P3 / **#26 WDM 网格** 十份收官报告 + v0.9.120 实跑硬数字 + 2026-09 公开市场报道交叉核对
> 纪律声明：本文所有「我们」侧数字均来自**接受闸实跑（死标量）**；所有「市场」侧数字均标 **「公开报道／厂商宣称」**，按 T1 铁律**宣称 ≠ 实测**，不作对标基准。
> 🔴 订正（2026-09-23）：本文 §4.1 / §4.5 原写「三不做（电域求解器…）」「不碰电域求解器」，属**旧口径漂移**——§八/§九 已于 2026-09-11 分层解锁：**器件级 PDE 的 T1 内核在红线内**，锁死的只有**电路级 / T2 工艺真值 / 封测产线**。已在原处订正。
> 🔄 更新（2026-09-23 · v0.9.122）：§4.4 执行序第 1 项 **U1（WDM × 2D 联合布局）已落地** —— 见 §4.2 U1 行与 §4.4 第 1 项的状态标注；CI core 186→**187**。**下一项 = U5（编译器前端）**。
> 🔄 更新（2026-09-23 · v0.9.124）：§4.4 执行序第 3 项 **U3（微环 κ_c 的 FDTD 精确回填）已落地** —— 见 §4.2 U3 行与 §4.4 第 3 项；CI core 188→**189**。🔴 **U3 的验收判据「偏差 ≤10%」【未达标】并已诚实暴露**（解析侧 1200–3700% · FDTD 跨网格 30–55/78%）⇒ 交付物是「带不确定度的取数接口 + 不达标的事实」，**不**声称「精确回填」。**下一项 = U4（微环权重库 —— 复用 U3 同一套微环器件模型）**。
> 🔄 更新（2026-09-23 · v0.9.125）：§4.4 执行序第 4 项 **U4（微环权重库）已落地** —— 见 §4.2 U4 行与 §4.4 第 4 项；CI core 189→**190**。**实测结论：两条验收判据「面积 ≤10%」与「权重级数 ≥5-bit」在**指定设计点**上均达标（面积 **1.2171%** · gap 旋钮**档位极差 8.13 bit** · 失谐旋钮 κ=0.0405 ⇒ **5.70 bit**），但**三项硬事实同时被钉死**：① 🔴 **权重动态范围只有 −0.038 dB**（环内损耗上限 a=0.9913）—— 这是本架构的物理天花板；② 🔴 **「高权重」与「波长选择性」是冲突目标**（FWHM 地板 143.7pm ⇒ 5%·2.5nm=125pm 预算**无解**；20% 预算下 w_max 仅 a 的 52%）；③ 🔴 **绝对定标未达标**（解析占位 vs U3 FDTD 的 w 差 **39.5× = 16.0 dB** ⇒ 只有**相对**编程分辨率有意义）。**下一项 = U7（损耗感知编译 —— 把已就位的 D1 路径方差变成编译目标）**。
> 🔄 更新（2026-09-23 · v0.9.127）：§4.4 执行序第 6 项 **U6（良率 / 容错映射）的条件式设计已落地** —— 见 §4.2 U6 行与 §4.4 第 6 项；CI core 191→**192**。**实测结论：三条「不利结论」同时被钉死，且都是机器可证的**：① 🔴 **结构冗余 = 0** —— Clements 矩形网格 `n_dof = N²` 而 `n_params = 2·n_mzi + N = N²`（`n_mzi = N(N-1)/2`）⇒ `redundancy_margin ≡ 0`（N=2/3/4/8/16 实测）⇒ 任一参数被硬冻结即 `N²-1 < N²` ⇒ 对一般 U **精确可达性丢失** ⇒ **软件 / 编译层无法修复故障**（与 U7「编译层对 IL_var 自由度 = 0」**同源**：网格拓扑唯一决定，冗余只能来自硬件）。② 🔴 **硬失效「最优重编译」也救不回** —— `stuck_bar` / `stuck_cross` / `phi_dead` 三模式在每个站点都掉（N=4 bar min **0.659882** · cross min 0.884948 · phi min 0.748389），而冻结该参数后对**其余全部参数**做确定性最小二乘，`f_best` 仍 < 1（**0.660199 / 0.981381 / 0.981383** @ N=4；N=8 bar **0.823776** 触预算未收敛 ⇒ `bound_kind` 如实标「下界」不冒充上界）；**判别性对照**：θ 漂移（参数全自由 `N²/N²`）实测**可**精确重编译回 **0.99999999999999778**（未标定时 naive 0.9950）⇒ **漂移的解法是校准固件（U10），不是冗余**。③ 🔴 **冗余该不该做，由「开关失效率」决定，不是由「MZI 失效率」决定** —— 解析良率（p = p_switch = 1e-3）：N=4 `0.994015 / 0.994009 / 0.991992` · N=8 `0.972375 / 0.972348 / **0.983368**` · N=16 `0.886867 / 0.886761 / **0.956095**`（none / site_spare / dual_mesh）⇒ **站点 1:1 热备在 p_switch ≈ p 时净亏**（盈亏平衡 `p_sw* = 9.9900e-4 < p`，面积 ×2 换 ~0）；**整网格双模冗余交叉点 = N=6**（盈亏平衡 `p_sw* = 1.7017e-3 > p`）⇒ 大 N 才真买器件容差。**额外交付（把无锚宣称换成规格）**：反解「达 99% 网格良率所需器件失效率」⇒ `none` N=4/8/16 = **1.674e-3 / 3.589e-4 / 8.375e-5**、`dual_mesh` N=16 = **2.799e-4** ⇒ 这是**可写进 PDK 规格、将来可实测回填**的硬数字。**另有 N=2 严格闭式锚**（θ≡0 ⇒ 可达集 = 对角子群，`f_best = 1 − sqrt(2N−2·Σabs(U_kk))/(N·√2)` = **0.45843460296921246**，与数值重编译**逐位一致**）与 11 条披露（`YIELD_DISCLOSURE`）。🔴 **本轮两处自纠**：① N=2 闭式**原本漏掉不可消除的非对角项**（偏乐观 0.7067 vs 数值 0.4584），由「闭式 ↔ 数值交叉验证」判据当场抓出；② 突变探针抓出**覆盖缺口** —— `inject_fault` 的 `drift` 分支此前无判据经过（置空也全绿）⇒ 补 `E6`/`E7`。**下一项 = U8（非易失权重后端：PCM / Sb₂Se₃）的条件式设计 —— 只做设计接口 + 参数化预算，不做能力宣称**；其后 **U10（校准固件）只做协议设计**。
> 🔄 更新（2026-09-23 · v0.9.126）：§4.4 执行序第 5 项 **U7（损耗感知编译）已落地** —— 见 §4.2 U7 行与 §4.4 第 5 项；CI core 190→**191**。**实测结论：原验收判据「IL_var 下降 ≥20%」只在「相对 D1 文档口径」上达标（实测 **39.60% / 55.33% / 60.54%** @ N=4/8/16 · 高估倍率 **1.6555× / 2.2387× / 2.5341×**，渐近 **2.6 = 1.3×2**），但三条结构性事实同时被钉死**：① 🔴 **D1 的 `IL_short` 物理不可实现** —— grid2d 每根 rail 是 `gc_in(k) → MZI… → ps_out(k) → gc_out(k)` 的**水平直链**，光自 `x=mesh_x0=10.0µm` **单调**行进 ⇒ 任意 I/O 对必 `L_path ≥ L_bus > col_pitch`；机器化实测 `IL_min_physical / IL_short` = **2.328× / 4.589× / 9.121×**（`infeasible_ratio_min_required = 2.0`）。② 🔴 **编译层对 IL_var 的自由度 = 0**（结构性不变量）—— `deg` 多集恒为 `{N/2, N/2, N…N}`（`deg_span = N/2`），对 U 的**转置 / 共轭 / 共轭转置 / 反序 / 端口置换**共 **10 种合法变换完全不变**（N=8/16 实测 10/10）；且直总线下 `L_bus` 项对**所有端口相同** ⇒ 对 IL_var **零贡献**，per-port `IL_var` 完全由 `n_tap` 决定（`dIL_per_tap = 0.050740000 dB` 三 N 恒定）⇒ **不可能**靠列分配/分解阶段改善方差。③ 🔴 **变长列只降 L_bus、不降 IL_var** —— 逐列自适应列宽使总宽 **121.333→113.583 / 254.653→239.129 / 527.123→501.098 µm**（↓ **6.39% / 6.10% / 4.94%**），但两点 `IL_var` **严格相同**（只带走 `IL_mean` −0.00155/−0.00310/−0.00521 dB）⇒ 面积与损耗方差**不冲突**（原判据假设的「权衡曲线」在方差维上退化）。**额外发现（口径漂移，只记录不改既有实现）**：D1 **文档** §二 `n_tap ≈ ⟨deg⟩·1.3`（含轨切换余量），既有 `mesh_pnr.amplitude_equalization_manifest` 用 `⟨deg⟩·1` **漏 1.3** ⇒ 两者 `IL_var_io` 差 **0.045 / 0.105 / 0.225 dB**。⇒ 交付物 = **口径精确化 + 零自由度证明 + 一张诚实的权衡曲线**，**不**声称「更优布局」。**下一项 = U6（良率容错）/U8（非易失权重）的**条件式**设计，或 U10（校准固件）的**协议**设计**。

> 🔄 更新（2026-09-23 · v0.9.127 · 续）：§4.2 第二梯队 **U8（非易失权重后端）的条件式设计**与 **U10（校准固件）的协议设计**均已落地 —— 见 §4.2 U8/U10 行与 §4.4 第 7/8 项；CI core 192→**193**（U8）· 193→**194**（U10）。**U8 实测结论**：三材料文献锚只认公开值（`Sb₂Se₃` **7bit / 10 年 / 1e8 次 / 0.1 dB·π⁻¹**，后两项标**下界**；`GST` **全 None ⇒ 拒绝发明数字**，唯一可用 = 定性 `crossbar_limit=3`）；线性器件模型（全摆幅 2π 是**假设**）下幅度域动态范围**只有 0.2 dB** ⇒ **PCM 是相位元件、不是衰减元件**；7 bit ⇒ 最大相位误差 **π/128 = 1.40625°**、单点导数口径仅高估 **1.0233×**（与 U4 同向但弱得多）；参照规模 N=16 网格（120 MZI + 16 相移器 = 136 个热相移器）静态 **629.62645351887784 mW** ⇒ PCM 静态 **0**、省量比 **1.0（100%）**，🔴 但**绝对 mW 不可引用**（根因 = **内部口径本身不一致**：B29 **4.6296 mW/mm** vs P2 **60.0 mW/mm** ⇒ **12.960065375898857×**）；retention/endurance 是**下界语义**（10.0001 年即不满足 · 下界不外推），1e5 次/天 × 10 年 ⇒ 需 **3.65e8 次 > 1e8 ⇒ 超耐久**（不利结论如实报出）；🔴 PCM 位深 **7 bit < U4 档位极差 8.1328 bit** ⇒ **优势在功耗、不在分辨率**；**139 判据 · 18/18 突变探针全红 + sha256 还原**。⇒ 接缝：与 U4（位深/动态范围正面对比）· U10（校准）· U7（`α_prop` 跨模块一致）三处**实时咬合**（上游漂移即红）。

> 🔄 更新（2026-09-23 · v0.9.127 · 续）：**U10（校准固件）协议设计**落地 —— **四条不利结论全部机器可证**：① 🔴 **纯功率（强度）读出结构性不可解** —— 单波长功率 Jacobian 秩 = **(N−1)²**（一般位置取等，N=2..8 实测），亏缺 = **2N−1**；`argD`（输出相位）那 N 维块的 Jacobian **恒为 0**（`|U|²` 不含输出相位）⇒ **输出相位永不可观测**。② 🔴 **WDM 不提升秩（否决其作为解法）** —— K = 1/2/4 · 间隔 0→100 nm ⇒ 秩**恒为 (N−1)²**（N=2/3/4/6/8 实测，`rank_gain_per_lambda ≡ 0`）⇒ WDM 标定地基只是**接口**，**不是解法**。③ ✅ **出路 = 相位敏感（复场）读出** —— 单波长即**满秩 N²**；σmin/σmax 相对功率读出改善 **25.6× / 37.7× / 107.1×**（N=4/6/8）⇒ 标定固件**必须**基于相干探测 + 相位参考（LO / 参考臂）。④ 🔴 **秩定律只对「一般位置」成立** —— DFT 退化族：功率秩 **8<9 / 21<25 / 44<49**（N=4/6/8；非退化 N=2/3/5/7 恰好取等）；且 σmin/σmax **不是全序单调**（σmin(4)=1.495e-3 **<** σmin(5)=2.075e-3，如实记录不粉饰）。**方法自证（本轮最关键的自纠）**：早期探针把功率行写成 `2·Re(U ⊙ dU)`（**少共轭**）⇒ 那不是 `|U|²` 的导数；更糟的是 FD 交叉验证**复用了同一个错式** ⇒ 「解析 vs FD 一致」是**循环验证**，反而伪造成「秩 = N²−1、亏缺恒为 1」的**错误结论**。**地面真值改为 FD(|U|²)**（真实可观测量本身）后**完全推翻**：真秩 = (N−1)² · `argD` 块恒 0 · WDM 不提升秩；修正式 `2·Re(conj(U) ⊙ dU)` 与地面真值相对差 **< 1e-6**（N=2..8），而旧式差异 **> 0.1**（判据钉死「修正是实质的，不是洗白」）。**标定状态机 8 态**（`UNCALIBRATED → PROTOCOL_DESIGNED → IDENTIFIABILITY_VERIFIED → ANCHOR_BOUND → CALIBRATED` ＋ `BLOCKED_NO_ANCHOR` / `STALE` / `FAILED` 终态）+ **三条红线机器守卫**（键名扫描 · **锚类别白名单**〔文献/仿真/假设不算物理锚〕· **核心门**「claim=calibrated 而无合格锚 ⇒ raise」）+ **自检 S1..S7**；**本项目拥有的锚 = 空集**（`ANCHORS_AVAILABLE_TO_THIS_PROJECT = ()`，是**诚实边界**不是待办）⇒ 真实终态恒为 **`BLOCKED_NO_ANCHOR`**、`can_claim_calibrated()` 恒 False（功率读出 N=8 更早一步 `FAILED`，属**结构性**不可辨识而非缺锚）；补锚 + 闭环 + 漂移 ⇒ **7/7**（门**可通过**、非恒红）；**152 判据 · 25/25 突变探针全红 + sha256 还原**。⇒ **U 系列「设计层零外部依赖」清单（U1/U3/U4/U5/U6/U7/U8/U10）至此全部落地**，余 **U2（K 提升）/ U9（tiling 扩档）/ U11（EIC 行为级）** 均为「按需 + 重跑预算」型。

---

## 0. 一句话结论

**我们已经拥有「自己设计一台主权光子计算系统」的完整设计闭环**：从酉矩阵 → Clements 分解 → 物理综合 → 2D 压实 P&R → GDS → 主权 DRC/LVS 签核 → 工艺映射（驱动电压清单）→ 系统级代价预算（损耗/热/规模）→ 幅度均衡需求与硬件选型 → tiling 定天花板，**十个环节全部闭合、全部有实跑硬数字**，且新增了**波分复用维（WDM）**。

**我们与市场的差距已从「设计骨架缺失」收敛为「三个物理化缺口」**：波长维（WDM）刚补上设计层，**权重物理化**与**校准固件**仍为设计接口，**真 PDK / 硅片实测**属外部依赖（P4）。
**结论：设计目标已达成；下一步的全部可自主推进项都指向同一件事——把「设计接口」变成「可演示模块」。**

---

## 1. 功能视角：这台芯片「能干什么」

### 1.1 端到端功能链（已全部打通，可复跑）

```
输入：一个酉矩阵 U（默认 DFT(N)，或任意随机酉）
  ↓ ① 酉分解        Clements 矩形分解 → N(N−1)/2 个相邻 MZI op + 对角相位 D
  ↓ ② 物理综合      伴随 MZI (G†) + 反转序摆放 + 输出相移器 D  → 版图级传输 == U
  ↓ ③ 摆位与布线    1D 蛇形展开 / 2D 总线-抽头压实（O(N) 列）
  ↓ ④ GDS 导出      主权 GDSII 编码器（零依赖、DBU=1nm）
  ↓ ⑤ 签核          主权 DRC（可制造性）+ LVS（原理图网表 vs 版图几何独立恢复网表）
  ↓ ⑥ 工艺映射      θ→耦合器长度 Lc（CMT 锚）；φ/D→驱动电压 V（Soref-Bennett Vπ·L 锚）
  ↓ ⑦ 系统代价      总线损耗（∝N）/ 抽头热串扰（N 无关）/ 规模—损耗权衡
  ↓ ⑧ 均衡与选型    幅度均衡需求清单 + EO-VOA vs MZI 硬件选型
  ↓ ⑨ 规模与架构    16×16 瓦片 tiling 定天花板 / Clements vs Reck 交叉对照
  ↓ ⑩ 波长维        K 波长 × N×N 波分复用网格（微环 add-drop 解/复用）
输出：可流片量级的 GDS + 驱动电压清单 CSV + 预算报告 + 验收闸（EXIT=0）
```

### 1.2 功能清单（✅ 已证 / 🟡 设计接口 / ❌ 未做）

| # | 功能 | 状态 | 硬证据 |
|---|---|---|---|
| 1 | 任意酉 → 物理版图且**真算该酉** | ✅ | 版级保真度 **1.000000**（N=4/5/7/16 + 30 例随机酉） |
| 2 | 规模可扩展 | ✅ | **4 / 16 / 128 / 256 / 512** 全绿（grid2d，n_cols=N） |
| 3 | 可签核（DRC/LVS） | ✅ | 逐级 **DRC PASS + LVS ACCEPT（0 违规）**，512 时 16508/16508 网全一致 |
| 4 | 可流片量级面积 | ✅ | 128 → **2.34 mm²**；256 → 9.34 mm²；512 → **37.29 mm²** |
| 5 | 产出驱动电压清单 | ✅ | 128×128 = 8254 路；512×512 = **131328 路** CSV 落盘 |
| 6 | 免交叉架构 | ✅ | Clements 交叉 **0**（Reck 为 C(N,3)：N=128 消除 **341376** 个交叉） |
| 7 | 系统级代价预算（损耗/热/规模） | ✅ | D1（∝N，抽头主导 9–10×）/ D2（d_min=4µm，N 无关）/ D3（256/512 全量绿） |
| 8 | 幅度均衡需求 + 硬件选型 | ✅ | EO-VOA：floor 0.3dB / 吞吐 0.933 / 4.54mm²@512 / 零热负载 |
| 9 | 瓦片化 tiling 定天花板 | ✅ | 场景 A：N≤512（需均衡）/ N≤256（免均衡） |
| 10 | **波分复用维（WDM）** | ✅（设计层，小规模） | K=4×N=4：24 MZI · 保真度 1.0 · DRC PASS · **LVS ACCEPT 0 违规** · 带宽密度 **×4** · 微环锚 R=3.0207µm · FSR 51.67nm |
| 11 | 非易失权重（PCM/Sb₂Se₃） | 🟡 设计接口 | 权重后端接口已留（L2），**未物理表征** |
| 12 | 微环权重库（MRR 替 MZI） | 🟡 设计接口 | 微环 add-drop 器件模型已在 WDM 落地，**权重库未实现** |
| 13 | 相干无-DAC 架构 | 🟡 设计接口 | L3 预留，未实现 |
| 14 | 校准固件（闭环 per-MZI 热/相） | ❌ 未做 | 需**实测锚**方能解锁（T1：仿真 ≠ 解锁） |
| 15 | 编译器/AI 框架前端 | ❌ 未做 | 现有 Reck/Clements 分解器，无 PyTorch/ONNX 前端 |
| 16 | 真 PDK / 硅片 / 封装 / EIC | ❌ 未做 | 属 P2（NDA）/ P4（现金+foundry），**刻意留白** |

### 1.3 关键功能洞察

- **「真算」而非「像算」**：保真度不是分解级自证，而是**物理级联网表前向下行传输 == U_target**（P1-A）。这把「可出版」升为「真算该酉」。
- **两个维度已可用**：空间维（N×N Clements）+ **波长维（Kλ）**，聚合带宽密度 ×K。
- **功能天花板可量化**：不是「能到多大」的模糊说法，而是**两条线** —— 可工作线 / 免均衡线（随工艺场景 A/B/C 收紧）。
- **全程零现金、零外部依赖**（至设计层）：C 级自写、LLM 不进判决路径、全部死标量。

---

## 2. 技术视角：怎么做到的

### 2.1 架构栈（可复利资产栈）

```
L3  前沿架构（相干无-DAC / 全光非线性 / 非易失权重）      🟡 接口预留
────────────────────────────────────────────────────────
L2  轻-EIC 光子核（MZI 网格 + 权重后端可插拔 + WDM 维）    ✅ 设计层就位
────────────────────────────────────────────────────────
L1  网格 P&R + 光电接口网表（Clements 分解 / 物理综合 / GDS / DRC / LVS）
                                                    ✅ 4→512 复用
────────────────────────────────────────────────────────
L0  主权物理模型（耦合模 CMT / 漂移-扩散 / 几何寄生 R&C）  ✅ 所有上层共享
```

### 2.2 四个技术「承力件」

| 承力件 | 机制 | 为什么关键 |
|---|---|---|
| **Clements 矩形分解** | 逐列**自下而上**、只耦合**相邻** `(j,j+1)` 零化 `U[j+1,c]`，枢轴取上邻行 | 唯一同时满足：MZI 数最优 N(N−1)/2 + **零波导交叉** + 重构机器精度。三角 Reck 需 C(N,3) 个交叉，N=128 即 34 万个 |
| **物理综合（伴随 + 反转序）** | 每个 MZI 用 **伴随** `G†=_mzi(θ,φ).conj().T`；摆放取**反转分解序**；末端 N 个相移器实现 D | 这是 P1-A 唯一的坑：用非伴随/正序，保真度掉到 0.56–0.65（实测）。锁定后版级保真度 = 1.0 |
| **冲突图着色列分配** | 节点=MZI，边=共享波导；`color_k = max(末色[j_k],末色[j_k+1])+1` | 把 O(N²) 蛇形展开压成 **O(N) 列**：128 从 146.2 mm² → **2.34 mm²（↓63×）**。两个硬保证：同列不撞端口 + 列序保序 ⇒ 物理传输不变 |
| **微环 add-drop 物理锚（WDM）** | `R_k = m·λ_k/(2π·n_g)`（整数 m 谐振）、`L_couple=2√(2R·gap)`、`k_ring=sin(κ_c·L_couple)` | 波长维的**器件级物理地基**；FSR≈λ/m。实测 R=3.0207µm、FSR=51.67nm > 25nm 信道间隔 ⇒ 不串扰 |

### 2.3 工艺映射（把「数学参数」翻成「可施加量」）

| 映射 | 锚 | 表达式 | 实测 |
|---|---|---|---|
| θ（耦合角）→ Lc（耦合区长度） | CMT 偶/奇模折射率（N_E=2.45 / N_O=2.40） | `Lc = (θ/2)·λ/(π·|Δn|)` | 与几何一致（grid2d 经 `coupler_length_from_theta(2θ)` 与蛇形同锚） |
| φ / D → V（驱动电压） | Soref-Bennett 硅热光 `Vπ·L` | `V = φ·(Vπ·L)/(π·L_arm)`，相位取主值 (−π,π] | Vπ·L=7.5 V·mm（代表生产值）· **V_max = 7.50 V** · 红线预算 250 V·mm（25 V·cm） |

> **绝对电压是闭环标定量**（设 φ → 测输出 → 调 V），不是一次性计算值 —— 这一句决定了「校准固件」是设计层绕不开的下一步。

### 2.4 系统级代价（技术天花板的三根支柱）

| 支柱 | 结论 | 数字 |
|---|---|---|
| **D1 总线损耗** | 均值与方差均 **∝N**；**方差由每抽头耦合器插损主导（≈9–10× 传播项）** | 场景 A（α_prop=1.0/α_tap=0.02）N=128：IL_mean 3.00 dB、**IL_var 3.73 dB**；N=512：IL_var ~15.1 dB |
| **D2 同列抽头热串扰** | **d_min=4µm 恒定、与 N 无关**；Γ 对角占优 ⇒ **一次原位标定可全补偿**，非阻断 | 深槽隔离在 N≥128 为**推荐项**，非强制 |
| **D3 规模上探** | 算法瓶颈 O(N⁵)→**O(N³)** 快版（加速 13.4–28.9×）；256/512 主权 P&R 全绿 | 512：footprint **37.292 mm²**、GDS 18 MB、驱动 131328 行、tdec 3.15s / tbuild 202.8s |

**天花板（P3，含寄生虫协同）**：

| 场景 | α_prop / α_tap | 可工作（≤30dB，带均衡） | 免均衡接收（≤20dB） |
|---|---|---|---|
| A 优化 SOI + 绝热 MMI | 1.0 / 0.020 | **N ≤ 512** | **N ≤ 256** |
| B 标准 SOI + 方向耦合器 | 2.0 / 0.050 | N ≤ 256 | N ≤ 128 |
| C 保守/未优化 | 3.0 / 0.100 | N ≤ 128 | N ≤ 64 |

> 寄生代理项在 T≥32 时已占 10.2 dB（≈总线项的 71%）⇒ 几何寄生维度**真实参与**天花板判定，非装饰。

### 2.5 方法论纪律（这是本项目最硬的部分）

- **判决全死标量**，LLM 不进路径：保真度、DRC、LVS、电压均为 numpy 闭式运算。
- **反向护栏自证**：不断言「应该通过」，而是**造出反例证明判据真会变红** —— WDM 的 D1（断一条下路 net ⇒ LVS REJECT）/ D2（非酉 U ⇒ `clements_decompose` 拒绝）；P1-C 的交叉计数器初版算错（把 Reck 也算成 0 交叉）后重写并**与封闭形式 C(N,3) 零偏差**校验。
- **不变量已证**：分解保真度 / 版级保真度 / DRC / LVS 网表一致性 / 驱动映射 **全部与「怎么摆位」无关** ⇒ 摆位只影响面积与损耗，不影响正确性。这条让「先证正确、再压面积」成为合法路径。
- **常驻门禁**：`run_wdm_mesh_pnr_smoke`（20 判据）等进 **CI core 194 条**；账本 **469 锚（严格 448 / 降级 3 / 自证桩 18）**、独立率 **95.5%**、天花板 **97.4%**、13/13 护栏全绿。
- **两条血案已入库**（防复发）：① **增量构建 × 身份缓存陈旧** —— `placement._port_abs_comp_cache` 按身份只建一次，逐面增量 `add_device` 后端口查不到 ⇒ `port_abs` 静默回落器件原点 ⇒ **几何全对但 LVS 假红 135 违规**；② **平面路由同 x 扇入交叉** —— 解/复用目标端口同 x，直连必判 `short_cross`（12 对），修法 = **L 型 + 次序相反**（demux 反序 / mux 同序）。

### 2.6 诚实边界（必读，防纸糊楼）

1. PDK 为**演示近似 SOI 180nm**，非真 foundry NDA-PDK ⇒ DRC 是**主权示意**，不是签核级。
2. LVS 为**单层波导**网表签核；多层金属/通孔完整 LVS 待真 PDK。
3. 仿真用**抽象 MZI 2×2 传输**，未含波导损耗 / 非理想耦合 / 串扰（属工艺级精度）。
4. 损耗/热/寄生为 **L3 参数化估算**；天花板含**透明几何系数**（`C_SUB_LOSS_DB_PER_FF=1e-4`），非 foundry 值。
5. **0 硅片、0 实测、0 校准闭环** —— 全部结论止于「设计 + 仿真 + 主权签核」。
6. WDM 目前仅 **K=4×N=4 小规模**验证（未做 Kλ × 128/512，也未与 2D 压实联合）。

---

## 3. 对比视角：与当前现实流行产品

### 3.1 市场侧快照（2026-09 公开报道／厂商宣称，需独立基准）

| 厂商 | 代表产品 | 关键宣称 | 备注 |
|---|---|---|---|
| **Lightmatter**（美） | Envise（计算）/ **Passage M1000 / L20**（互连） | **世界首个 16 波长双向 DWDM 链路**；L20 单向 6.4 Tbps、聚合 12.8 Tbps；BiDi 双向同纤；Guide VLSP 单芯片 **128+ 激光器**；路线图 NPO 2027 → CPO 2028 → 中介层 2029+ | **当前重心明显在「互连」而非「计算」**，已进 NVLink Fusion 生态 |
| **Lightelligence 曦智**（中） | **PACE 2** | **128×128 光学矩阵乘**、**40000+ 光子器件**、8-bit、32 TOPS、50W（含激光器）、3D TSV 封装、oMAC 时延 200ps、PCIe Gen4×16、PyTorch/ONNX/TVM | 2026-04 港股 IPO；产品线含 Hummingbird oNOC / Photowave |
| **光本位科技**（中） | **256×256 光子存内计算** | **PCM 存算一体**、单晶粒 **65000+ 光计算单元**、年内二代卡百→**千 TOPS**、玻璃基光计算芯片、太空算力 | 2024-06 已流片商用级 128×128；融资数十亿元、估值近 200 亿；已 10+ 系统规模交付 |
| **Q.ANT**（德） | NPU2 / 薄膜 LiNbO₃ | **原生非线性**光子（diffusion/RNN 上硬件）；2026-05 上 IONOS 商用云 | 走「非线性 + 化合物半导体」路线 |
| **Ayar Labs / Celestial AI / NVIDIA** | TeraPHY / Photonic Fabric / CPO | 光 I/O、共封装光学 | 与 Lightmatter 同属「互连」赛道 |
| **器件研究前沿** | Sb₂Se₃ PCM | 7-bit、retention >10yr、耐久 >1e8 次、~0.1 dB/π；Nature 2026 可编程模式转换器 5-bit | GST 因晶态高损耗，交叉阵列被限制在 3×3 量级 |

### 3.2 差距矩阵（更新版 —— 相较 2026-09-22 版，**#2 波长维已从「设计缺维」降级为「设计层已补、规模待扩」**）

| # | 差距维度 | 市场成熟系统 | 我们当前 | 性质 |
|---|---|---|---|---|
| 1 | 规模（**已建芯片**） | 128×128（曦智）/ 256×256（光本位）**实物流片** | 设计/仿真至 **128×128 压实 2.34mm² / 256 / 512**，未建 | 设计估算 ≠ 实物 |
| 2 | **波长维（WDM/DWDM）** | Lightmatter **16λ DWDM**（**互连层**） | ✅ **设计层已补**：Kλ×N×N 计算网格 + 微环解/复用，K=4 验证 | **用途不同**：市场在互连、我们在计算 |
| 3 | **权重技术** | 非易失 PCM（7-bit/10yr）/ MRR 权重库（面积仅 EO 9.64%）/ 光本位 PCM 存内 | 权重后端=**可插拔接口**（设计层），未物理表征 | **差异化赌注未验证** |
| 4 | **校准固件** | 闭环 per-MZI 热/相校准 + 温补（MZI 路线刚需） | **递延**（需实测锚，T1 铁律锁死） | 待补（需物理锚） |
| 5 | 真 PDK / DRC deck | iSiPP50G / AIM / GF Fotonix 真 deck 签核 | 演示近似 PDK（主权示意） | 需 NDA |
| 6 | EIC 协同（DAC/ADC/SerDes/TIA） | 重-EIC 集成（SerDes/DSP/BER 链路判决） | per-MZI 驱动/TIA 仅行为级 | 主权内：只做行为级（器件级 T1 已解锁；电路级锁死） |
| 7 | 3D/CPO 封装 | Passage 3D 堆叠、CPO/NPO 量产 | CPO 协同网表就位，无物理 3D 封装 | 超 LDA 定义 |
| 8 | **软件栈（AI 框架前端）** | PyTorch/ONNX/TVM + 编译器 + 开发者工具 | Reck/Clements 分解器，**无框架前端** | **设计缺口（可自研）** |
| 9 | 非线性光子 | Q.ANT 原生非线性 | 线性矩阵乘专用 | 路线选择 |
| 10 | 硅片/实测证据 | 已流片 + 基准 | tape-out-ready GDS，**0 硅片** | = P4 |

### 3.3 我们真正的差异点（可与不可讲）

**独有 / 可讲：**
1. **主权全链路**：从物理模型到 GDS 到签核，**零外部求解器、零 GPL、零 Meep/Tidy3D 真值**，可断供自持。
2. **判决可证伪**：物理锚 + 反向护栏 + 死标量。**「我们的 1.0」是可被证伪的 1.0**，而非厂商宣称。
3. **正确性与摆放解耦已证**：先证正确再压面积 —— 这条数学结论（保真度/DRC/LVS 与摆位无关）让我们能用 512 规模做设计探索而无需先解决工艺。
4. **代价可量化到天花板**：不是「能做多大」，而是**「在什么工艺条件下能做多大」**（A/B/C 三场景 × 可工作/免均衡两条线）。
5. **把 WDM 放在计算层**：市场把波分用于互连（更易产品化），我们把它用于**计算面复用**（更难、但正是「带宽密度×K」的来源）。

**慎讲：** 电域限器件级；校准限方法学独立（非实测）。
**禁讲：** 完整 EIC / 已流片 / 能与 XXX 对标能效 / 千 TOPS（**无实测锚，一律不说**）。

### 3.4 一句话对比

> 市场玩家已经走到「**产品化 + 规模交付**」（曦智 IPO、光本位数十亿融资并交付 10+ 系统、Lightmatter 进 NVLink 生态）；
> 我们走到「**设计闭环 + 可证伪的正确性 + 可量化的天花板**」。
> 差距**不在设计方法的先进性**，而在 **① 权重物理化 ② 校准固件 ③ 真 PDK/硅片** 这三处「物理化闸门」。其中 ①② 主权内可做，③ 必须外部。

---

## 4. 下一步升级方向判定（**仅设计角度 · 不触物理生产**）

### 4.1 判定原则（先立规，再列项）

| 原则 | 含义 |
|---|---|
| **红线不动** | A 级永不外借 / C 级自研零依赖 / 三不做**分层口径**（电域求解器 → 仅**电路级**锁死，器件级 PDE 的 T1 内核已解锁；Foundry TCAD → 仅 **T2 工艺真值**锁死；封测产线锁死；T1 输出永不作 ORACLE）/ LLM 不进判决路径 |
| **解锁前置 = 独立锚** | 任何「能力解锁」必须有锚。**「写个仿真器跑通」≠ 解锁**，无锚必假绿（T1 铁律） |
| **先复利、后扩张** | 优先做能复用现有 L0–L2 资产、且**不引入外部依赖**的项 |
| **与已完成链的接缝最少** | 同一处改动能同时服务多个升级项者优先（例：微环器件模型同时服务 WDM 与微环权重库） |

### 4.2 升级项清单（含条件 · 验收 · 风险）

#### 🔴 第一梯队 —— 零外部依赖、主权内、接缝最少（建议先做）

| 项 | 升级方向 | 条件（前置） | 验收判据（设计层） | 风险 |
|---|---|---|---|---|
| **U1 ✅ 已完成（v0.9.122 · 2026-09-23）** | **WDM × 2D 压实联合布局** —— 当前 WDM 是「空间复制 K 份网格 + 微环解复用」（面积 ×K、未压实）；目标：K 个波长**共享同一套物理网格**，波长做并行维 | ① grid2d 的 bus-tap 已就位；② 微环解/复用平面性已验证（WDM 血案②）；③ 需新增**「波长-空间联合列分配」**算法（冲突图着色 + 波长面约束） | Kλ×N×N（K=4,N=16）**单网格**：保真度 1.0 ✅ · DRC PASS ✅ · LVS ACCEPT 0 ✅ · footprint < K×单面 ✅（实测 **0.0359 mm²** vs 朴素 0.1436 mm² = **1/K**）· 120 MZI（朴素 480） | 微环与 MZI 混排的同列端口冲突；LVS 单层网表可能需扩展器件类别 |
| **U2** | **WDM 通道数 K 提升**（K=4 → 8/16，对齐市场 16λ） | ① U1 或至少「K 面共享总线」；② 微环 FSR 需 > K×信道间隔（K=16、50GHz 间隔 ⇒ FSR 需 ≳ 12.8nm，当前 51.67nm **余量充足**，但 R 需重算）；③ κ_c 由 FDTD 标定回填 | K=16：FSR/信道间隔比 ≥ 1.5 · 环间串扰 ≤ −20dB（设计预算）· 保真度 1.0 保持 | 微环半径随 K 收缩或间距压缩 ⇒ 热串扰耦合进 D2 预算 |
| **U3** ✅ **已完成（v0.9.124 · 2026-09-23）** | **微环 FDTD κ_c 精确回填**（当前 κ_c 是几何式 `sin(κ_c·L_couple)` 的解析值） | 主权内可行（自写 2D/3D FDTD 已具备），**但须方法学独立**：FDTD 候选 ≠ 解析 golden | κ_c 相对解析值偏差 ≤ 10%（设计预算）· 反向护栏：扰动 gap ⇒ κ_c 必变 | FDTD 网格收敛性（需判据 D 严格单调）；耗时预算 —— **落地实测：两项风险全部兑现** |
| **U4** ✅ **已完成（v0.9.125 · 2026-09-23）** | **微环权重库（MRR 替 MZI 做幅度控制）** | ① 微环器件模型（U3 同源，**复用同一套**）；② 权重后端接口（L2）已留；③ 需波长相关权重模型（wavelength-division weighting） | 面积对比：≤ EO 调制的 ~10% · 权重级数 ≥ 5-bit（设计预算）· 与 WDM 网格共网格时保真度保持 1.0 —— **实测**：面积 **1.2171%** ✅（对真实 π 臂 1000µm）· gap 旋钮**档位极差 8.1328 bit** ✅（单点口径 12.32 bit **高估 1.52×**，不采用）· 失谐旋钮 κ=0.0405 ⇒ **5.7026 bit** ✅ / κ=0.147 ⇒ **2.91 bit** ❌（如实报出）· 共网格保真度 **1.000000000000000 保持** ✅ | 微环热漂移 ⇒ 与 U6（校准）强耦合；权重分辨率受工艺 —— **落地实测：风险兑现且已量化**（① 权重上限 **a=0.9913 ⇒ 仅 −0.038dB** 动态范围；② FWHM 地板 **143.7pm** 使「高权重」与「选择性」**不可兼得**；③ 绝对定标差 **39.5×=16.0dB**） |
| **U5 ✅ 已完成（v0.9.123 · 2026-09-23）** | **编译器/AI 框架前端**（网络矩阵 → 最近等距 → 酉分解 → 驱动清单） | ① Reck/Clements 分解器已就位 ✅；② 驱动清单 CSV 格式已稳定 ✅（复用 `mesh_drive_manifest` + `scale_up_p1b` 表头）；③ 非酉算子处理 ✅（SVD 谱 + **最近部分等距** Procrustes 闭式解 + 正交补全为 N×N 酉 + 奇异值夹持） | 端到端 ✅ 实测（demo 网络 4×4 → 4×16 → 16×16）：**n_mzi 246 · 驱动器 282 · 最小目标保真度 1.000000000000000 · 端口块误差 0.00e+00**（部分等距可被网格**精确**实现）；近酉层残余 **8.83e-03**；增益层 σ_max 3.72 ⇒ β 0.269、**最优缩放下残余仍 0.951**（诚实暴露不可被动实现） | 实数/非酉映射的近似误差与「诚实标注」 ✅（残余有闭式解 + `residual_at_opt_relative` 指标 + `COMPILER_DISCLOSURE`）；前端外部依赖 ✅ 留在**边界外**（`guard_no_framework_in_judgment` 机器守卫） |
| **U6** ✅ **已完成（v0.9.127 · 2026-09-23）** | **良率/容错映射**（MZI 冗余 + 故障遮蔽） | ① 故障模型需定义（器件级失效率假设，**须标为假设而非实测**）；② LVS/DRC 判决需支持冗余网表 | 注入 f% 故障 ⇒ 遮蔽后保真度 ≥ 目标 · 反向护栏：无冗余必掉 —— **实测（条件式）**：① **结构冗余 = 0 已证**（`n_params = 2·n_mzi + N = N² = n_dof`，N=2/3/4/8/16）；② 三硬模式**每站点都掉**（N=4 min 0.6599/0.8849/0.7484），**最优重编译** `f_best` 仍 < 1（0.6602/0.9814/0.9814；N=8 bar 0.8238 未收敛⇒标「下界」）⇒ **软件不可精确修复**；③ θ 漂移（可标定）实测**可**精确重编译回 0.99999999999999778（双向对照）；④ N=2 严格闭式锚 0.45843460296921246 与数值**逐位一致**；⑤ 良率结论 = **由开关失效率决定**（站点热备净亏 / 双模冗余交叉点 N=6 / N=16 0.887→0.956） | 缺少 foundry 良率真值 ⇒ 结论只能是**条件式**的，须显式标假设 —— **落地实测：风险兑现且已转成规格**（达 99% 网格良率须器件失效率 ≤ **1.674e-3 / 3.589e-4 / 8.375e-5** @ N=4/8/16；`dual_mesh` N=16 放宽至 2.799e-4）+ 11 条披露（`YIELD_DISCLOSURE`）· 零 FDTD/不做 3D |
| **U7** ✅ **已完成（v0.9.126 · 2026-09-23）** | **损耗感知编译** —— 把 D1 的路径方差作为**编译目标**（在酉分解/列分配阶段最小化 IL_var） | ① D1 损耗模型已就位 ✅；② 需在 `_rect_column_assignment` 加入损耗权重（等长路由 vs 面积折中） | 同 N 下 IL_var 下降 ≥ 20%（相对当前列分配）· 保真度/DRC/LVS 不退化 —— **实测：判据只在「相对 D1 文档口径」上达标（↓ **39.60% / 55.33% / 60.54%** @ N=4/8/16），🔴 但**根因是 D1 把 `IL_short` 取得过低（物理不可实现，实测下限比之高 2.328×/4.589×/9.121×）**，而非布局被改进；保真度 **1.000000000000000 保持** ✅ · DRC PASS ✅ · LVS ACCEPT 0 ✅（零锚改动 · 零既有判据影响） | 可能与压实面积目标冲突 —— **实测：在「方差」维上不冲突（编译层自由度 = 0；变长列只降 L_bus 6.39%/6.10%/4.94%，IL_var 严格不变）⇒ 原「权衡曲线」假设退化为：面积优化合法且无方差代价** |

#### 🟡 第二梯队 —— 有前置条件，条件满足即可做

| 项 | 升级方向 | 条件（前置） | 说明 |
|---|---|---|---|
| **U8** ✅ **已完成（v0.9.127 · 2026-09-23）** | **非易失权重后端（PCM / Sb₂Se₃）** | ① 器件级模型（透射率-编程态曲线、多级 5–7bit、retention、endurance）——**公开文献值可作设计预算，但须显式标为「文献锚≠实测」**；② 不碰 foundry 工艺真值（红线） | 收益最大（干掉热调静态功耗，是 MZI 路线第一痛点），但**它是最需要物理锚的一项** ⇒ 「先做设计接口 + 参数化预算」，不做能力宣称 —— **实测（条件式 · 全部机器可证）**：① 三材料文献锚只认公开值（`Sb₂Se₃` **7 bit / retention 10 年 / endurance 1e8 次 / IL 0.1 dB·π⁻¹**，后两项标**下界**；`GST` **四个数值字段全 None ⇒ 拒绝发明数字**，唯一可用 = 定性约束 `crossbar_limit=3`；`ModeConverter2026` 只给 5 bit）；② 线性器件模型（全摆幅 2π 是**假设**常量）⇒ 幅度域动态范围**只有 0.2 dB** ⇒ **PCM 是相位元件、不是衰减元件**（拿它做幅度加权不成立）；③ 7 bit ⇒ `Δφ = 2π/128` · 最大相位误差 **π/128 = 1.40625°**；**单点导数口径高估 1.0233×**（U4 同向但弱得多：U4 = 1.515×）；④ 参照规模 N=16（136 个热相移器）静态 **629.62645351887784 mW** ⇒ PCM 静态 **0** · 省量比 **1.0**，🔴 但**绝对 mW 不可引用**（`absolute_mw_quotable=False`；根因 = **内部口径不一致**：B29 4.6296 vs P2 60.0 mW/mm ⇒ **12.960065375898857×**）；⑤ retention/endurance 是**下界语义**（10.0001 年即不满足 · **不外推**）；1 次/天×10 年 ⇒ 余量 **27397.26×**，🔴 1e5 次/天×10 年 ⇒ **3.65e8 次 > 1e8 ⇒ 超耐久**；⑥ 🔴 PCM **7 bit < U4 8.1328 bit** ⇒ **优势在功耗、不在分辨率**；⑦ **零 foundry 工艺真值**（递归键扫描守卫 · **可满足性已证**）· **GST 的总报告本就不可构造**（raise 而非「生成空报告」）；**139 判据 · 18/18 探针全红 · 13 条披露** |
| **U9** | **tiling 扩规模（32/64 瓦片 + 瓦片间光路由）** | ① P3 tiling 已闭合；② **N≥256 投产须先有幅度均衡 PDK**（D1/D3 判定） | 在设计层可直接扩，但**每扩一档都要重跑 D1/D2/P3 三预算**，否则天花板数字失效 |
| **U10** ✅ **已完成（v0.9.127 · 2026-09-23）** | **校准固件（闭环 per-MZI 热/相）** | 🔴 **必须有物理锚**（真 PDK 工艺角 或 实测标定数据）⇒ **设计层只能做「标定流程与自检协议」的设计，不能宣称已校准** | 这是 MZI 路线的刚需，但**恰恰是最不能靠仿真解锁的一项**（T1 铁律）。设计层可交付：标定协议 + 自检判据 + WDM 标定地基的接口定义 —— **实测（协议设计 · 全部机器可证）**：① 🔴 **纯功率读出结构性不可解**（秩 = **(N−1)²** · 亏缺 **2N−1** · `argD` 块**恒为 0** ⇒ 输出相位**永不可观测**）；② 🔴 **WDM 不提升秩**（K=1/2/4 · 间隔 0→100 nm ⇒ 秩恒 (N−1)² · `rank_gain_per_lambda ≡ 0`）⇒ **否决 WDM 作为解法**；③ ✅ **出路 = 复场（相位敏感）读出**（单波长满秩 N² · σmin/σmax 改善 **25.6× / 37.7× / 107.1×** @ N=4/6/8）；④ 🔴 **秩定律仅对「一般位置」成立**（DFT 退化族 8<9 / 21<25 / 44<49；σmin **非全序单调**）；⑤ **方法自证**推翻早期「**循环验证**」（功率行少共轭 + FD 复用同一错式）的**错误结论**（地面真值改 `FD(|U|²)`，修正式相对差 <1e-6、旧式差异 >0.1）；⑥ 状态机 **8 态** + **三条红线守卫** + **自检 S1..S7**（无锚复场 N=8 ⇒ **3/7**；补锚 + 闭环 + 漂移 ⇒ **7/7**）；⑦ **本项目锚 = 空集**（诚实边界）⇒ 终态恒 **`BLOCKED_NO_ANCHOR`**、`can_claim_calibrated()` 恒 False；**152 判据 · 25/25 探针全红 · 14 条披露 · 四处自纠** |
| **U11** | **EIC 行为级协同**（per-MZI 驱动 / TIA 阵列行为模型） | ① 只做**行为级**、不做晶体管级电路仿真（器件级 PDE 的 T1 内核已解锁〔§九〕，此处属路线取舍）；② CPO 协同网表已就位 | 补齐「光电接口层」的 B 路线设计深度，仍不越红线 |

#### ⚪ 第三梯队 —— 外部依赖，不在「设计」范围

| 项 | 依赖 | 说明 |
|---|---|---|
| U12 真 PDK 注入（iSiPP50G / AIM / GF Fotonix） | **NDA + foundry 准入** | 解锁真 DRC deck 与 SerDes/DSP 前置 |
| U13 损耗真值回填（α_prop / α_tap / VOA_range / mzi_er） | **流片 + 圆片表征** | 替换全部代理系数（含 `C_SUB_LOSS`） |
| U14 物理验证（MPW + 封装 + 光测） | **$10k–50k + 光测实验室** | = P4，唯一能动「0 硅片」的项 |

### 4.3 依赖图（谁卡谁）

```
U3 微环 κ_c 标定 ✅(v0.9.124) ──┐
                   ├──► U4 微环权重库 ✅(v0.9.125) ──┐
U1 WDM×2D 联合 ✅(v0.9.122) ─────┘                    ├──► 「WDM × 存内计算」合体（= 市场主航道）
                   └──► U2 K 提升 ───────┘
U7 损耗感知编译 ✅(v0.9.126) ◄── D1（已就位）+ 结构性零自由度证明
U6 良率容错 ◄── 故障模型（假设级）                ← ✅ 已落地（v0.9.127 · **结构冗余 = 0** 已证 ⇒ 硬失效**软件不可修复**；良率由**开关**失效率决定；`drift 可标定` 与 U10 直接咬合）
U5 编译器前端 ✅(v0.9.123) ◄── 分解器 + 驱动清单(已就位)  ← 已落地（69 判据）
U8 非易失权重 ✅(v0.9.127) ◄── 文献锚（非实测）      ← 已落地（139 判据）· 收益在**功耗**不在分辨率；绝对 mW 不可引用
U10 校准固件 ✅(v0.9.127) ◄── 🔴 物理锚（本项目 = 空集）   ← 已落地（152 判据）· 协议 + S1..S7 已交付；终态恒为 BLOCKED_NO_ANCHOR（**只做设计，不宣称已校准**）
U9 tiling 扩 ◄── 每档重跑 D1/D2/P3 + 均衡 PDK 前置
U12/U13/U14 ◄── NDA / 流片 / 现金                    ← 外部，不在设计范围
```

### 4.4 建议的执行顺序（设计层，零外部依赖优先）

1. **U1（WDM × 2D 联合布局）** —— ✅ **已完成（v0.9.122 · 2026-09-23）**：实现 = `lda/lda_layout/wdm_shared_mesh_pnr.py`（K 波长共享单套网格 · 底座复用 `build_mesh_pnr(grid2d)`）+ `lda/run_wdm_shared_mesh_smoke.py`（**23 判据**）⇒ **CI core 186→187**。实测（K=4×N=16）：**120 MZI**（朴素 480）· footprint **0.0359 mm²**（朴素下界 0.1436 mm² = **1/K**）· 保真度 **1.0** · DRC PASS · LVS ACCEPT 0。🔴 随之确立两条诚实边界：**① 色散**（φ(λ)∝1/λ ⇒ K 波长**并非**完全等价；实测版级 min 0.996515〔θ 无色散〕/ 0.994039〔θ 全色散〕）· **② λ 复用必须落在光接口层**（单层平面上「N 轨 × K 波长各自独立源」= K×N 完全二分交叉 ⇒ **非平面**；v1 能单层签核正因把波长空间化成 K 个不相交面，代价 = 面积 ×K）。—— 理由：这是唯一能同时证明「波长维可用」与「面积可比」的项；且它是 U2/U4 的公共前置；接缝全部在已有代码内（`mesh_pnr` + `wdm_mesh_pnr` + `_rect_column_assignment`）。
2. ~~**U5（编译器前端）**~~ ✅ **已完成（v0.9.123）** —— 理由（当初）：**独立性最高**，与网格链路零耦合，可并行；直接补市场对比表中「软件栈」缺口；验收判据客观（示例网络端到端）。**落地结果**：69 判据常驻门禁（含 7 反例 + 3 合法必过 + 2 独立重算）· 三条诚实边界（只做线性 / 非酉残余不可为零 / 夹持有损）· 前端边界机器化。
3. ~~**U3（微环 κ_c 标定）**~~ ✅ **已完成（v0.9.124 · 2026-09-23）** —— 理由（当初）：微环器件模型是 WDM 与权重库的**共用底座**，做一次服务两项；且对齐「市场主流 = 微环/MRR 权重」。**落地结果**：`lda/lda_l2/ring_kappa_calib.py` + `lda/run_ring_kappa_calib_smoke.py`（**63 判据**）⇒ **CI core 188→189**。实测（gap=0.30µm · λ=1.55µm · dl=16/20/25）：κ_c = **0.026324 / 0.024114 / 0.010492** · 残差 **[0.002210, 0.013622]**（**非单调**）· **跨网格离散度 78.0%** · 解析/FDTD **33.36×**。🔴 **四条诚实边界**：① **判据 D 不成立**（根因 = 阶梯离散的有效 gap 随 dl 非单调 × κ_c 对 gap 指数敏感 ⇒ **结构化**离散误差）⇒ 不声称收敛，只报离散度；② 解析模型是占位上界；③ **10% 预算未达标**（诚实暴露）；④ 2D 无垂直限制 + FDTD 不可在设计时跑。**回填入口 `calibrated_ring_anchor()` 只增不改**（几何与 `wdm_ring_anchor` 逐位一致，既有 P&R/DRC/LVS 判据零影响）。⇒ **U4 直接复用这一套微环模型与取数接口。**
4. ~~**U4（微环权重库）**~~ ✅ **已完成（v0.9.125 · 2026-09-23）** —— 理由（当初）：U3 已把微环器件模型与 κ_c 取数接口就位（**同一套**，无需二次标定）；对齐「市场主流 = 微环/MRR 权重」。**落地结果**：`lda/lda_l2/ring_weight_bank.py` + `lda/run_ring_weight_bank_smoke.py`（**75 判据**）⇒ **CI core 189→190**。实测（R=3.0207µm · α_bend=39.98dB/cm）：闭式往返 **5.551e-15** · a=**0.991300**（权重上限 −0.038dB）· 面积 **1.2171%** ✅ · gap 旋钮 **8.13 bit** ✅ / 单点 12.32 bit（**高估 1.52×**）· 失谐 5.70 bit ✅（κ=0.147 时 2.91 bit ❌）· FWHM 地板 **143.70pm** · 权库可达 w ∈ [**2.4984e-4, 8.0366e-2**]（已签发表）· 共网格保真度 **1.0 保持**。🔴 **八条诚实边界**：① 权重非负（负权重不建模）② **绝对定标未达标（39.5×=16.0dB）** ③ 解析占位在 **gap≲0.288µm 处违反能量守恒**（κ>1）⇒ 一律 raise 不截断 ④ **单点导数骗人** ⑤ 热漂移未校准 ⑥ **权重带宽 = 谐振 FWHM**（Q_i 硬地板）⑦ 只支撑非负权重网络 ⑧ 不跑 FDTD/不做 3D。⚠️ **本轮自纠**：analytic 域界原硬编码 0.205µm（**自身越域**，与披露③矛盾）⇒ 改 `_kappa_one_gap_analytic()` 二分重算（实测穿越点 **0.288047µm**）；两处静默钳位改**显式报出**。
5. ~~**U7（损耗感知编译）**~~ ✅ **已完成（v0.9.126 · 2026-09-23）** —— 理由（当初）：把已就位的 D1 从「预算报告」升级为「编译目标」，零新增外部依赖，但会让天花板数字真正可用。**落地结果**：`lda/lda_l2/loss_aware_compile.py` + `lda/run_loss_aware_compile_smoke.py`（**67 判据** · 0.32s · 12/12 突变探针全红）⇒ **CI core 190→191**（零锚改动 · 零账本变化 · 复用 `mesh_pnr.build_mesh_pnr` / `_rect_column_assignment` / `clements_rect_decompose` 等既有资产）。实测（N=4/8/16 · α_prop=2.0dB/cm · α_tap=0.05dB）：`L_bus` = **145.333 / 278.653 / 551.123 µm** · `col_pitch` = **30.333 / 31.832 / 32.945 µm** · `L_bus/col_pitch` = 4.79/8.75/16.73 · `deg_span = N/2` · `dIL_per_tap` = **0.050740000 dB**（三 N 恒定）· `IL_var` 精确值 vs D1 文档口径 ⇒ 高估 **1.6555×/2.2387×/2.5341×**（渐近 **2.6 = 1.3×2**）· `IL_min_physical/IL_short` = **2.328/4.589/9.121** · DP 可达 `n_tap ∈ [N/2, N]`（N=4 暴力枚举独立重算一致）· 10/10 合法变换下 deg 多集不变 · 变长列总宽 **121.333→113.583 / 254.653→239.129 / 527.123→501.098 µm**。🔴 **九条诚实边界**（写入 `LOSS_AWARE_DISCLOSURE`，9 键机器可检）：① 口径精确化 **≠** 更优布局 ② 参数化非实测 ③ `IL_short` 是**物理下界的证伪**、不是版图收益 ④ **编译层对 IL_var 的真实自由度 = 0** ⑤ 变长列降 `IL_mean` 不降 `IL_var` ⑥ DP 是**几何**可达标、非光子可达 ⑦ 不跑 FDTD、不做 3D ⑧ 原验收判据「≥20%」在**自洽口径下不可达**（已诚实暴露） ⑨ D1 文档 vs manifest 实现的 `⟨deg⟩·1.3` 口径漂移（0.045/0.105/0.225 dB，**只记录不改既有实现**）。
6. ~~**U6（良率容错）**~~ ✅ **已完成（v0.9.127 · 2026-09-23）** —— 理由（当初）：受「缺实测锚」限制，只宜交付**条件式结论 + 参数化接口**，不宜宣称能力。**落地结果**：`lda/lda_l2/yield_fault_tolerance.py` + `lda/run_yield_fault_tolerance_smoke.py`（**69 判据** · 5.4s · **12/12 突变探针全红 + sha256 还原**）⇒ **CI core 191→192**。实测（`_generic_unitary` · 一般位置 · 无 RNG）：自由度账 `n_params = N² = n_dof` ⇒ **结构冗余 = 0**；三硬失效模式扫描**每站点都掉**（N=4 bar min **0.659882** / cross 0.884948 / phi 0.748389）+ **最优重编译** `f_best` **仍 < 1**（**0.660199 / 0.981381 / 0.981383**；N=8 bar 0.823776）⇒ **软件不可精确修复**；θ 漂移（参数全自由）可精确重编译回 **0.99999999999999778**（判别性对照）；**N=2 严格闭式锚** 0.45843460296921246 与数值**逐位一致**；条件式良率 `none/site_spare/dual_mesh` @ p=1e-3 ⇒ N=8 `0.972375/0.972348/0.983368`、N=16 `0.886867/0.886761/0.956095` ⇒ **站点热备净亏**、**双模冗余交叉点 N=6** ⇒ **冗余该不该做由开关失效率决定**；反解 PDK 规格（99% 良率 ⇒ 器件失效率 ≤ **1.674e-3/3.589e-4/8.375e-5** @ N=4/8/16）。🔴 **11 条诚实边界**（写入 `YIELD_DISCLOSURE`）。🔴 **两处自纠**：① N=2 闭式**漏掉不可消除的非对角项**（0.7067 vs 数值 0.4584）由交叉验证抓出；② `inject_fault` 的 `drift` 分支**无判据覆盖**（突变探针抓出）⇒ 补 `E6`/`E7`。⇒ **接缝**：U6 的 `drift 可标定` 与 **U10（校准固件）** 直接咬合；`结构冗余 = 0` 与 **U7（编译层自由度 = 0）** 同源。**U8 的设计接口仍待做**（下一项）。
7. ~~**U8（非易失权重后端）**~~ ✅ **已完成（v0.9.127 · 2026-09-23）** —— 理由（当初）：收益最大（干掉热调静态功耗，是 MZI 路线第一痛点），但**锚最弱** ⇒ 只宜「设计接口 + 参数化预算」，不做能力宣称。**落地结果**：`lda/lda_l2/nonvolatile_weight_backend.py` + `lda/run_nonvolatile_weight_backend_smoke.py`（**139 判据** · 0.31s · **18/18 突变探针全红 + sha256 字节级还原**）⇒ **CI core 192→193**。实测（条件式 · 文献锚作设计预算）：三材料锚（`Sb₂Se₃` **7bit / 10 年 / 1e8 次 / 0.1 dB·π⁻¹**，后两项标**下界**；`GST` **全 None ⇒ 拒绝发明**）· 幅度域动态范围**仅 0.2 dB**（⇒ **相位元件**，非衰减元件）· 7 bit ⇒ 相位误差 **π/128 = 1.40625°**、单点高估 **1.0233×**（U4 同向但弱）· N=16（136 热相移器）静态 **629.62645351887784 mW** ⇒ PCM 静态 **0** · 省量比 **1.0**（🔴 绝对值**不可引用**：B29 vs P2 口径差 **12.960065375898857×**）· retention/endurance **下界语义**（1e5 次/天×10 年 ⇒ **3.65e8 > 1e8 超耐久**）· PCM 位深 **7 bit < U4 8.1328 bit**（**优势在功耗不在分辨率**）。🔴 **13 条诚实边界**（`NVM_DISCLOSURE`）。⇒ **接缝**：与 U4（位深/动态范围正面对比）· U10（校准）· U7（`α_prop` 跨模块一致）**三处实时咬合**（上游漂移即红）。
8. ~~**U10（校准固件）**~~ ✅ **已完成（v0.9.127 · 2026-09-23）** —— 理由（当初）：🔴 **必须有物理锚**（真 PDK 工艺角或实测标定数据）⇒ 设计层**只能做标定流程与自检协议**，**不能宣称已校准**（T1 铁律）。**落地结果**：`lda/lda_l2/calibration_protocol.py` + `lda/run_calibration_protocol_smoke.py`（**152 判据** · 0.62s · **25/25 突变探针全红 + sha256 字节级还原**）⇒ **CI core 193→194**。实测：🔴 **功率读出秩 (N−1)²、亏缺 2N−1、`argD` 恒不可观测**；🔴 **WDM 不提升秩**（**否决**其作解法）；✅ **复场读出满秩 N²**（改善 **25.6×/37.7×/107.1×**）；🔴 **DFT 退化族**（8<9 / 21<25 / 44<49）与 σ **非全序单调**；**方法自证**用 `FD(|U|²)` 地面真值**推翻**早期「循环验证」的错误结论；状态机 **8 态** + **三红线守卫** + **S1..S7 自检**（无锚 3/7 → 补锚+闭环+漂移 7/7，门**可通过**）；**本项目锚 = 空集** ⇒ 终态恒 **`BLOCKED_NO_ANCHOR`**。🔴 **14 条诚实边界**（`CAL_DISCLOSURE`）+ **四处自纠**。⇒ **U 系列「零外部依赖」清单（U1/U3/U4/U5/U6/U7/U8/U10）至此全部落地**。
9. **U2 / U9 / U11** —— 按需（K 提升需重算环半径与 FSR；tiling 扩档需重跑 D1/D2/P3 三预算；EIC 行为级随光电接口层推进）。

### 4.5 明确「不建议做」（防范围蔓延）

| 不做 | 理由 |
|---|---|
| 重-EIC（DAC/ADC/SerDes/DSP/BER 晶体管级） | 主权红线：不碰**电路级**电域（三不做分层后电路级仍锁死；器件级 PDE T1 已解锁）；且是 500 亿管无底洞 |
| 追「千 TOPS / 对标 GPU」类指标 | 无实测锚 ⇒ 一旦说出口就无法自证，违背 T1 |
| 在无物理锚时宣告「校准/权重已解锁」 | 假绿比不解锁更危险 |
| foundry TCAD **T2 工艺真值** / 封测产线 | 三不做（分层后仍锁死；T1 数值内核已解锁） |
| 为好看而放宽 tol / 改判据 | 项目铁律：tol 只收紧 |

---

## 5. 一页判断

| 问题 | 判断 |
|---|---|
| **功能上完成了吗？** | ✅ 设计闭环十环全通（含新增波长维）；❌ 权重物理化 / 校准 / 硅片未做（设计如此） |
| **技术上靠得住吗？** | ✅ 保真度 = 机器精度、签核 0 违规、天花板可量化、判据可证伪；⚠️ PDK 为演示近似、损耗为参数化估算 |
| **与市场比在哪？** | 差距**不在设计方法**，而在 **①权重物理化 ②校准固件 ③真 PDK/硅片**；前两者主权内可做，第三者必须外部 |
| **下一步做什么？** | ~~U1（WDM×2D 联合）~~ ✅ **已完成（v0.9.122）** → ~~U5（编译器前端）~~ ✅ **已完成（v0.9.123）** → ~~U3（微环 κ_c 标定）~~ ✅ **已完成（v0.9.124 · 判据「偏差≤10%」未达标并已诚实暴露）** → ~~U4（微环权重库）~~ ✅ **已完成（v0.9.125 · 面积 1.22% 与位深 8.13bit 达标，但「权重动态范围仅 0.038dB」「高权重×选择性冲突」「绝对定标差 39.5×」三项已钉死）** → ~~U7（损耗感知编译）~~ ✅ **已完成（v0.9.126 · 判据「IL_var ↓≥20%」仅在 D1 文档口径下达标，根因是 D1 的 `IL_short` 物理不可实现 2.33×/4.59×/9.12×；编译层对 IL_var 的自由度 = 0 已证）** → ~~U6（良率容错）~~ ✅ **已完成（v0.9.127 · 结构冗余 = 0（`n_params = N² = n_dof`）已证 ⇒ 硬失效**软件不可精确修复**（f_best < 1 实测）；θ 漂移虽**可标定**但需 U10；冗余良率结论 = **由开关失效率决定**（站点热备净亏 / 双模冗余交叉点 N=6 / N=16 0.887→0.956）；已把「无锚宣称」转成**对 PDK 的规格要求**（99% 网格良率 ⇒ 器件失效率 ≤ 1.674e-3/3.589e-4/8.375e-5））** → ~~U8（非易失权重后端）~~ ✅ **已完成（v0.9.127 · 条件式：文献锚≠实测 · 幅度域仅 0.2dB ⇒ 相位元件 · 绝对 mW 不可引用 · 省量比 1.0 · PCM 7bit < U4 8.13bit ⇒ 优势在功耗）** → ~~U10（校准固件）~~ ✅ **已完成（v0.9.127 · 协议设计：功率读出秩 (N−1)² 结构不可解 · WDM 不提升秩 ⇒ 否决 · 复场读出满秩 N² · 本项目锚 = 空集 ⇒ 终态恒 BLOCKED_NO_ANCHOR，只做设计不宣称已校准）** ← **U 系列「零外部依赖」清单至此全部落地**；余 U2 / U9 / U11 为按需项 |
| **最大风险？** | 不是技术，是**范围蔓延**（滑向重-EIC/封装）与**无锚宣传**（说千 TOPS）。用「主权边界评审 + T1 铁律」锁死即可 |

---

## 附录 A：证据索引（文件 → 硬数字）

| 环节 | 报告 / 模块 | 关键实测 |
|---|---|---|
| P0 网格 P&R | `P0_mesh_pnr_closeout_2026-09-22.md` · `lda/lda_layout/mesh_pnr.py` | 4×4 DFT 保真度 1.000000 · 6 MZI · DRC PASS · LVS ACCEPT(14/14 器件, 16/16 网) |
| P1-A 物理综合 | `P1A_mesh_physical_synthesis_closeout_2026-09-22.md` | 版级保真度 1.0（N=4/5/7 DFT + 随机酉）· 伴随+反转序是关键 |
| P1-B 规模 + 工艺映射 | `P1B_mesh_scale_up_closeout_2026-09-22.md` | 4/16/128 · MZI=N(N−1)/2 · 128 驱动 8254 路 · V_max 7.5V |
| P1-B 续 2D 压实 | `P1B_2d_grid_closeout_2026-09-22.md` | 128: 146.235 → **2.336 mm²**（↓63×）· 列内冲突 0 · 两保真度差 0.00e+00 |
| P1-C 跨分解 | `P1C_cross_decomp_closeout_2026-09-22.md` | Clements 交叉 0 vs Reck C(N,3)；N=128 消除 341376 · 缩放指数 3.276 |
| D1 总线损耗 | `D1_bus_loss_budget_2026-09-22.md` | ∝N · 抽头主导 9–10× · N≤128 安全区 |
| P2 均衡硬件 | `amp_eq_p2_hardware_2026-09-22.md` · `amp_eq_pdk_binding_2026-09-22.md` | EO-VOA floor 0.3dB/thru 0.933/4.54mm²@512/零热 |
| P3 tiling + 寄生 | `P3_tile_loss_closeout_2026-09-22.md` | 寄生 0.319 dB/瓦片 · T≥32 占 10.2 dB · 天花板 A: 512/256 |
| **#26 WDM 网格** | `lda/lda_layout/wdm_mesh_pnr.py` · `run_wdm_mesh_pnr_smoke.py`（v0.9.120 / commit `9ef84ce`） | K=4×N=4: 24 MZI · 保真度 1.0 · LVS ACCEPT 0(44 器件/82 网) · 带宽密度 ×4 · R=3.0207µm · FSR 51.67nm |
| **U6 良率/容错** | `lda/lda_l2/yield_fault_tolerance.py` · `run_yield_fault_tolerance_smoke.py`（v0.9.127） | 结构冗余 = 0（`n_params = N² = n_dof`）· 三硬模式每站点都掉（N=4 min 0.6599/0.8849/0.7484）· `f_best` 仍 <1（0.6602/0.9814/0.9814；N=8 bar 0.8238 未收敛⇒下界）· θ 漂移可精确重编译 0.99999999999999778 · N=2 闭式 0.45843460296921246 · 冗余交叉点 N=6 · 99% 良率 ⇒ 器件失效率 ≤ 1.674e-3/3.589e-4/8.375e-5 |
| **U8 非易失权重** | `lda/lda_l2/nonvolatile_weight_backend.py` · `run_nonvolatile_weight_backend_smoke.py`（v0.9.127） | `Sb₂Se₃` 7bit/10年/1e8次/0.1 dB·π⁻¹（标下界）· `GST` 全 None · 幅度域仅 0.2 dB ⇒ 相位元件 · 相位误差 π/128=1.40625° · 单点高估 1.0233× · N=16 静态 629.62645351887784 mW ⇒ 省量比 1.0 · B29 vs P2 口径差 12.960065375898857× · 1e5次/天×10年 ⇒ 3.65e8 > 1e8 超耐久 |
| **U10 校准固件** | `lda/lda_l2/calibration_protocol.py` · `run_calibration_protocol_smoke.py`（v0.9.127） | 功率秩 (N−1)²（亏缺 2N−1 · `argD` ≡ 0）· WDM 秩恒定（`rank_gain_per_lambda ≡ 0`）· 复场满秩 N²（改善 25.6×/37.7×/107.1×）· DFT 退化族 8<9/21<25/44<49 · σ 间隙 >1e9 · 状态机 8 态 · 锚空集 ⇒ 终态 BLOCKED_NO_ANCHOR |
| 战略与评估 | `LDA_strategic_plan_2026-09-22.md` · `BplusC_series_summary_eval_2026-09-22.md` | 路线图 P0–P4 · 市场差距 10 项 · 资产栈 L0–L3 |
| 全局纪律 | `README.md`（顶行账本） | 469 锚(448/3/18) · 独立率 95.5% · 天花板 97.4% · **CI core 194** · 13/13 护栏 |

## 附录 B：本文引用市场数据的口径

所有市场数字（TOPS / 波长数 / 器件数 / 融资额 / 交付系统数 / 耐久次数等）均来自 **2026-09 公开报道或厂商宣称**，**未经独立基准验证**。本项目对外表述时，一律加「厂商宣称，需独立基准」限定，且**不得用于与自研设计数字做同轴对比**（我们的数字是设计层实跑，市场数字是产品级宣称，**两者不同轴**）。
