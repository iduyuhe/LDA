# LDA 修复工作计划（基于 2026-09-19 全面功能 + 代码审计）

> 来源：`LDA_functional_code_audit_2026-09-19.md`（18 条发现：P1×2 / P2×11 / P3×5）
> 状态：**已入库**（裁决 **R5=①**，2026-09-19 —— 与已入库的 `LDA_functional_code_audit_2026-09-19.md` 构成「审计 → 修复计划」闭环）· 本文所有规模数字均为**本次实跑复测**（2026-09-19 22:xx 起，受跟踪文件口径）
> 🔴 **账本影响声明**：波次 1 / 2 / 4 为**纯代码卫生与工程改造**，`N=469 · 448/3/18 · 95.5% · 天花板 97.4%` **预期零变化**；~~仅波次 3 因触及数值语义，可能触发锚定级复核（届时按 `lda-anchor-wiring` 流程，**只收紧不放宽**）~~。🔴 **2026-09-21 更新：波次 3 已关闭**（F-14 经取证判为审计上下文误读 ⇒ 判定不做）⇒ **本计划实际执行范围全程零账本影响**。

---

## 0. 结论先行

| 分类 | 数量 | 状态 |
|---|---|---|
| 审计发现总数 | 18 | — |
| **已闭环**（v0.9.111 · 已上线） | 14 | ✅ 不再排期（见 §1） |
| **待排期** | 8 → 归为 **4 个波次**（**2026-09-21 更新**：F-14 转「判定不做」⇒ 实为 **7 → 3 个波次**；波次 1 ✅ / 2 ✅ 已闭环 · 3 🚫 已关闭） | 见 §2 |
| 审计未覆盖的补盲工作 | 4 | 见波次 4 |
| 需你裁决 | 5 项（R1–R5） | ✅ **已全部裁决**（R1②/R2②/R3②/R4①/R5① 入库）· 见 §4 与 §0 进度块（R1② 于 2026-09-20 被误提交 `ab85534`，2026-09-21 经裁定已 `git revert` 恢复原状） |
| 判定不做（已证伪 / 无收益） | **7 项**（2026-09-21 新增 F-14） | 见 §5 |

**一句话**：P1 已清零，剩下的全是「代码卫生 / 可维护性 / 一致性」级债务。**唯一有实质风险的是波次 2 —— 它会动判决路径的注册表入口**，因此必须单独发版、单独验证，绝不与机械清理混批。

---

> **📌 收口记录（2026-09-21 · 波次 3 / F-14 改判关闭 · 零代码 · 零发版）**
> - **裁定**：用户 2026-09-21 批准执行「波次 3 / F-14 改判收口」（本文件原报告所提优先级表第 1 项）。
> - **结论**：**T3.1 的前提不成立** —— 审计发现表 F-14 系 **grep 视角的上下文误读**：`solver_writer.py` 中那行 `# BUG` 位于 `_CANDIDATE_V0` **三引号模板字符串内部**，是 demo 的**刻意错误示范**；紧邻的 `_CANDIDATE_V1` 即修复版（含参考跑归一化 + `nL/n0` 阻抗因子）。
> - **六条取证**：① 注释在字符串模板内（L443-539，非真码） ② `_CANDIDATE_V1`（L542-644）即修复版 ③ `build_offline_generator()` docstring 自述「v0（bug）→ v1（修复），证明闭环能判 FAIL→收重写→判 PASS」 ④ **CI core 常驻门禁** `run_solver_writer_sandbox_smoke.py`（`run_ci_regression.py:458`）**判据③ 直接依赖它**（实跑 8 PASS / 0 FAIL） ⑤ 发布说明 `LDA_v0.1_Release_Notes.md:29` 记载 v0 FAIL→v1 PASS（max_err=0.0326） ⑥ **真 ORACLE 实证**：`tmm` + FDTD 实跑 `run_demo` ⇒ `[iter 1] FAIL max_abs_err=0.4109` → `[iter 2] PASS max_abs_err=0.0326`（与发布说明**逐位一致**），跑后 `git status` **零污染**。
> - **处置**：**T3.1 判定不做**（登记 §5）· 波次 3 关闭（§2 标已关闭 · §6 排期表移除）· 核查面零变化。
> - 🔴 **防再犯**：「自述缺陷」类发现**必须先判定注释所在容器**（真码 / 字符串模板（demo、prompt）/ 文档块）—— 三者处置完全不同，而 grep 视角**区分不了**。
> - **账本影响**：**零**（`448/3/18/469` 不变 · 零锚 · 零 tol · 棘轮不动 · CI core 182 不变）⇒ 本次为**纯文档改判**，不新增门禁、**不需重跑全量 CI**。

> **📌 执行进度（2026-09-20 · 待裁 item 1 已处置 · v0.9.116）**
> - **item 1（`run_tapeout_smoke` 秒级 id 假红）裁决 = 修复**（用户 2026-09-20 选定）。
> - **根因 = 两条件叠加（缺一不复现）**：R-a 唯一 id 用**秒级** `int(_t.time())`；R-b 三处提交**不传 `proposals_path`** ⇒ 写仓库内共享库 `lda_pdk/empirical_proposals.json`（`.gitignore` 忽略的社区贡献库）⇒ pending **跨进程持久**。🔴 **方法论血案**：首版证明脚本把 store 改成「每跑新建」**恰好切断 R-b** ⇒ 旧实现 `rc=0`、不复现 —— **复现条件写错 = 证明无效**。
> - **取证**：该共享文件当日积压 **38 条 pending，100% 为 `proposed_by=tapeout-smoke`** 的测试残留（method=simulated，非真实测量）⇒ 既**污染**社区贡献库、又使该 smoke **非 hermetic**（结果依赖历史残留）。
> - **处置**：① `run_tapeout_smoke` hermetic 化（`tempfile.mkdtemp()` + 显式 `proposals_path`）② `run_tapeout_pipeline` 增**可注入** `proposals_path`（末位追加 ⇒ 向后兼容）③ id 改 `uuid4` ④ 弱断言「二选一」收紧为确定性 + **补防重守卫正向断言**（此前仅被脏库残留偶然覆盖）⑤ 删悬空重复调用 ⑥ 移除断言**不存在键** `st.get("citation")` 的假绿地雷。
> - **有效性证明**：**2×2 冻结时钟矩阵** —— 旧/独立 store `rc=0,0`（耦合被切断 ⇒ 不复现）· 旧/共享 store **`rc=0,1`（假红复现 ✅）** · 新/共享 store `rc=0,0`（**耦合已断** ✅）；真机同秒连跑 2 次均 rc=0。
> - **新增护栏**：`run_smoke_isolation_ratchet_smoke.py`（14 判据 · I1/I2/I3 静态 + I4 扫描非空洞 + I6 共享库残留 + I7/I12 反证对照 + I8~I11 四条反向测试 + I13 自食其规则）⇒ **CI core 180→181**。**同码对照**：同套判据跑 HEAD 旧实现 ⇒ 命中 **L81 + L89 + L102 恰 3 处**（与手工定位逐条一致），新实现 0 处。
> - **残留清理**：38 条已清（先备份 · 写回前**格式保真自证**逐字节 27503 B · 写后残留 0）；该文件被 gitignore 忽略 ⇒ 版本库零影响。
> - **账本影响**：**零锚改动**（448/3/18/469）· 棘轮 `MAX_SELF_CERTIFIED=18` / `min_kit_importers=70` 不动 · 零 tol 放宽 · pyflakes **F401 仍 201**。
> - **同族连带项 D（`lda/reports/empirical_d62.json` 报告非确定性）**：同族根因（**wall-clock 非确定性**）的第二个实例 —— 该受跟踪报告**每次重跑字节必变**（实测连跑两次差异恰为秒数 `17:37:14` vs `:15`）。**三条件（缺一不复现）**：D-a 生成器用**裸 `json.dump`**（违 v0.9.75「受跟踪报告必须是输入的确定性函数」）· D-b **未登记**在 `run_report_determinism_smoke.lint_spec`（该表 L149 自述「没登记 = 门禁缺口」⇒ **漏登记 19 天无感**）· D-c `detail` 把含 `landed_at` 的 provenance **整串序列化**进字符串。🔴 **要点**：`deterministic.canon` 只剔**结构化** volatile 键，**抓不到字符串内** ⇒ 必须**源头消除**。**处置**：detail 只取确定性字段（`contributor`/`reviewer`）+ 落盘走 `det.write_json` + 登记白名单 + 删死导入 `import json`；顺带把报告口径从 **48 题**（2026-09-01 起未再生成）更新到当前 **469 题**。**反向证明（同码对照）**：新表 × 旧实现（`git show HEAD:`）⇒ `rc=1` 且红灯**精确指向** `not-using-deterministic=['run_empirical_d62_report.py']`；还原 ⇒ 复绿。**确定性实证**：改造后连跑两次（**强制跨秒**）⇒ 字节一致（len=2008）。**不新增 smoke ⇒ CI core 181 持平**。
> - **本项挖出的新 backlog**：**T6.4** 报告写入者白名单的**精确发现式判据**（实测粗扫 **55 候选 / 30 个不在白名单**，其中多数非真报告写入者 ⇒ 直接做成判据会变「狼来了」被关停 ⇒ 只补最小登记）。
> - **全量 CI core 181 实跑（2026-09-20）**：**`181 PASS / 0 SKIP / 0 FAIL`**（181 条 · 11 批 · **5539.2s**）—— **全绿**，含新护栏 `run_smoke_isolation_ratchet_smoke.py`（批次 11 · 0.2s）。
> - **超时预算处置（第三次同项复发）**：体检报全表 14 项 · 最低 **2.87×**（`run_fdtd2d_mmi` **450/156.79s**，**不在改动面内**）。
>   **先归因**：本项**不 import 账本**、无规模循环 ⇒ 参数固定、**与锚数无关**；同轮对照 `run_d_criterion` 173.51s **低于**上轮 202.79s（**−14%**）⇒ 机器状态**逐项波动** ⇒ 判为**纯抖动**（前四轮 128–135s → 本轮 156.79s，**+16~22%**）。
>   **处置**：450 → **600s**（≈**3.83×**，对齐同族档位）⇒ 吸收 ±27% 抖动。**只改耗时上限**（零判据/零锚/零棘轮改动）⇒ 属**单调放宽** ⇒ 结论仍有效、未重跑。`numstat` 40/4 无整文件重写。
>   **影响面**：全仓引用该表的 **smoke 类文件 = 0 个** ⇒ 零连带变红。
>   **🔴 规则细化**：同项**第三次**踩线（2.99→3.12→3.03→2.97→**2.87×**）⇒ 原规「≥3× **单轮**实测」不严谨（谷值轮配额、峰值轮破线）⇒ 新规 = **3 ×「跨轮实测上界」**（取历次实测**最大值**）；纯抖动型项**一次给足**同族档位。

> **📌 执行进度（2026-09-20 · 策略待裁 G2 已闭环 · v0.9.115）**
> - **G2（v0.9.114 登记的策略待裁项）**：台账**空表**（表头在 · 零数据行）时 `main()` 判 rc=0 并称「结构合法、无刷分、无自证」—— 三个宣称在空表下**空洞为真** ⇒ 等于「**清空整表即漂绿**」。**用户裁定 = 判非法**。
> - **取证**：① 台账是**入库资产**（`git ls-files` 确认 · 含 2 行机制演示行）⇒ 正常永不应为空；② 台账文档**自称「防台账漂绿 / 刷分」** ⇒ 允许空表与自身承诺冲突；③ 全新克隆亦不会遇到（演示行随仓库入库）；④ 全仓无其他 smoke 兜底。
> - **处置**：`validate(rows)` 起始处断言 `rows` 非空；配套删 `main()` 内已不可达的「（暂无记录）」死分支（上游断言后不可达）；契约同步 `bounty_leaderboard.md` + `BOUNTY.md`。
> - **验证**：新增 **R9/R10**（空台账 / 表头后仅空行必拒）⇒ 本 smoke 11 项全绿；🔴 **有效性证明**（同码对照）：同一套新判据跑旧实现（HEAD `89ec353`）⇒ **仅 R9/R10 变红、其余 9 条两版一致**；端到端：旧实现空表 `rc=0` 且打印「结构合法…」（铁证）、新实现正确 raise。
> - **账本影响**：**零锚改动**（448/3/18/469）· **不新增 smoke ⇒ CI core 180 持平** · 棘轮不动 · 零 tol 放宽 · pyflakes F401 仍 201。
> - **全量 CI core 180**：**`180 PASS / 0 SKIP / 0 FAIL` · 5753.7s**；`budget_audit` 14 项 · 最低 **2.96×**（`run_d_criterion` 600/202.79s 与 `run_fdtd2d_mmi` 400/134.82s **均不在本轮改动面内** ⇒ 负载抖动 + 规模增长）⇒ 两项上调 600→**660s** / 400→**450s**（**只改耗时上限**）⇒ 复算最低 **3.14×**。属**单调放宽** ⇒ 结论仍有效、未重跑。

> **📌 执行进度（2026-09-20 · 待裁 item 1 已处置 · v0.9.114）**
> - **item 1（`run_bounty_ledger_smoke` 死测试）裁决 = 删死码 + 堵 G1 + 补反向判据**（用户 2026-09-20 选定）。
> - **先取证再动手**：`BountyLedgerSmoke` 的 5 组断言**全部**是 `main()` / `validate()` 的**严格子集** ⇒ 「补跑活测试」= **+0 覆盖**（且会固化 F-18 刚消的双范式）⇒ **删**。
> - **探针另挖出真缺口 G1**：`len(cells) < 6 → continue` **静默丢弃**畸形行（5 列行实测 `rows=0` 且不报错），而输出仍称「结构合法」⇒ 改为**报错**（含行号 + 原文）；日期列可省 ⇒ 6 列合法。
> - **9 条判据 + 有效性证明**：同一套判据跑新旧两版 `parse_ledger` ⇒ **旧实现仅 R7/R8 红、其余 7 条一致**；R0b 为**反证的对照**（防 `_rejects` 恒真假绿）。
> - **遗留 G2（未动）**：空台账（表头在 · 零数据行）判 rc=0 ⇒ 「清空整表」理论可漂绿；是否定为非法属**策略** ⇒ 待裁。
> - **账本零变化**（448/3/18/469）· **不新增 smoke**（CI core 180 持平）· 棘轮不动 · 零 tol 放宽 · pyflakes F401 仍 201（`import unittest` 与死类同删 = 净零）。

> **📌 执行进度（2026-09-20 · 波次 2 已执行 · v0.9.113）**
> - **波次 2（v0.9.113）：本轮指令范围 = F-07 + F-18，主体完成。** 🔴 **T2.2（F-08 巨石拆分）未在本轮范围**（用户本轮只点 F-07 与 F-18；且审计列为「唯一触及判决路径入口的改动」）⇒ 仍按 **R3②** 待专项。T2.4（`scripts/registry_snapshot.py`）已在 v0.9.112 落地。
> - ✅ **T2.2（F-08 巨石拆分）已闭环（v0.9.117 · 专项单独发版）**：按原文件**顺序连续切片+ 基座提升**——`benchmarks.py` 6633→180 行（`benchmark_defs/part1-5` = 469 DEFS + `_vmm_overrides`）· `verification_adapters.py` 6789→57 行（`_adapter_core` + `_adapter_p1..p6`）。**四重独立验收全通过**：① 快照逐位一致（sha256 `6de7f0c4…` 与拆前完全相同）② 源码字节保真 530 条 0 缺失 ③ 501 个 code 对象语义级指令流等价 ④ 运行时冒烟（three_class 4/4 · golden_product 48/48 · count 11/11）。新增常驻门禁 `lda/run_adapter_shard_layout_smoke.py`（13 判据 · CI core 181→182），并经**三向反向突变**实测证明判据真会变红（换片序⇒仅 L6 红 · 片内遮蔽装饰器⇒L3/L4/L5/L7/L10/L11 红 · 少加载片⇒仅 L0 红）。**零锚改动 · 账本零变化**（448/3/18/469）。
> - **F-07（T2.1）**：审计数字「86 份」经 **AST 逐文件复测修正** —— 实为 **97 处 `def check(`（97 文件）/ 36 个结构类**。抽出 **50 个 smoke** 到 `lda_harness/smoke_kit.py`；**T1 红线守卫 `guard_t1_not_oracle` 6→1** 到 `lda_solver/redline.py`。余 **53 处不归一是刻意决定**（口径：HEAD 97 → 抽走 44 → 余 53 · 分布 31 个结构类；逐条实测理由见 CHANGELOG v0.9.113）。新增 **`run_helper_dup_ratchet_smoke.py`**（14 判据 · **只降不升**：`check` ≤53 / 重复组涉及文件 ≤30 / `smoke_kit` 接线 ≥69 + J7 单一来源运行时判据 + 5 条反向测试）⇒ **CI core 179→180**。
> - **F-18（T2.3）**：审计「19 个 smoke 用 unittest（84 test 方法）、其余用自研 `check()`」。本轮 **18 个** `unittest.main(verbosity=2)` 收尾统一为 `smoke_kit.run_unittest_suite(Cls)`（**报告与退出契约统一**，而非把 unittest 重写成第三套模板）；失败仍走标准 `FAIL:`/`ERROR:` 块 ⇒ CI 失败痕迹可判、不被误判 SKIP；整 suite 只跑一次保 `setUpClass` 语义。**1 个死测试特例单独判读**。
> - **验证**：① **Stage A = 逐文件字节级 stdout + rc 对照**（51 文件全量两轮：基线 608.9s → 对照 595.2s）⇒ **rc 全一致 · 45/51 逐字节一致 · IND=0 · OTH=0**，余 22 行为固有非确定（计时/浮点尾差/跨次累积）且**经同码双跑逐条证明**；② **Stage B = rc + `Ran N` + `FAILED(...)` + 逐用例**（输出契约本就要变）⇒ **19/19 全一致**；③ **pyflakes 棘轮实测=基线**（F401 仍 201，零变化）。
> - 🔴 **本轮血案与发现（全过程留痕，均属「验证副产品」）**：**① 硬编码输出常量** —— 迁移脚本把缩进 `"  "` 硬编码在分组表，而 6 个文件原文无缩进 ⇒ **rc 全 0、仅多 2 空格**（最易被「rc 绿=通过」蒙混）⇒ 改为**从原文推导 + 加断言**；**② F-18 采集器解析缺口** —— `unittest.main` 走 stderr + 带 docstring 用例输出**两行** ⇒ 基线漏计，报出 3 处**假差异**（`None -> ok`）⇒ 改用格式无关判据（`Ran N`）+ 行状态机解析；**③ 既有脆弱用例**（`run_tapeout_smoke`：**秒级**时间戳唯一 id + 跨进程持久化防重守卫 ⇒ **同秒重跑必假红**，已用 HEAD 与迁移版**同码双跑**证明与本次改动无因果）⇒ 本轮**不修**（属断言逻辑改造、非纯重构）· **待裁**。
> - **账本影响**：**零锚改动**（448/3/18/469 不变 · 棘轮 `MAX_SELF_CERTIFIED=18` 不动 · 零 tol 放宽 · **零判据改动**）；**CI core 179→180**（已同步 README 顶行 + `## 当前账本` 段 + CONTRIBUTING）。
> - **全量 CI core 180 实跑（2026-09-20）**：**180 PASS / 0 SKIP / 0 FAIL**（10 批 · 10 线程 · 批间冷却 12s · 5542.9s）。**预算余量体检 14 项全覆盖 · `low_margin` 空 · 最低 3.12×**：首轮体检 `run_splitter_readout_smoke` **201.46s/600s = 2.98×**（全表唯一 <3×，告警线 2× 未触）为该文件**不在 F-07 迁移面内**的负载抖动（同轮对照采集同类项抖动 ±15%）⇒ 按「余量底线 ≥3×」上调 **660s**，复算最低回升 3.12×。**纯耗时上限、判据一字未改**，且属**单调放宽** ⇒ 本轮全量结论对改后代码仍有效。
> - **待裁清单（v0.9.116 更新）**：① ✅ **已闭环（v0.9.114）** `run_bounty_ledger_smoke` 死测试（删死码 + 堵 G1 + 补 11 条判据）· ② ✅ **已闭环（v0.9.115）** G2 台账空表（判非法 + R9/R10 + 新旧对照证明）· ③ ✅ **已闭环（v0.9.116）** `run_tapeout_smoke` 秒级 id 假红（hermetic 化 + uuid4 + 补防重守卫正向断言 + 新增隔离棘轮）· ④ ✅ **已闭环（v0.9.117）** T2.2（F-08 巨石拆分）：按 R3① 执行（判据先落地 → 拆分 → 四重验收全通过）；新增常驻门禁 13 判据（CI core 181→182）；零锚改动 · 账本零变化 · ⑤ ✅ **已闭环（v0.9.117 收口 · 2026-09-21）** R1 误入库回退：`docs/B23_closing_report.html` 的补录提交 `ab85534` **已 `git revert`**（不回写公开历史），文件**保留在工作区未跟踪** ⇒ R1=② 原裁定语义恢复 · ⑥ ✅ **已闭环（2026-09-21）** **波次 3 / F-14 改判**：经六条取证（含真 ORACLE `tmm` 实跑 `run_demo`：v0 FAIL 0.4109 → v1 PASS 0.0326）判为**审计上下文误读** ⇒ **判定不做** · 波次 3 关闭 · **零代码零发版**。

> **📌 执行进度（2026-09-19 晚 · 波次 1 已执行）**
> - **波次 1（v0.9.112）：主体完成** —— F401 热点清 **41** 处（`lda_webui/app.py` 19 + `lda_harness/golden.py` 14 + 传递链 `benchmarks.py` 8）· F841 清 **59** 处 · F541 清 **25** 处 · F811 清 **4** 处。
> - **实测基线**：pyflakes 告警 **344 → 213**（F401 **244→201** · F841 **71→12** · F541 **25→0** · F811 **4→0** · F821 **0** 持平）。
> - **新增设施**：`lda/run_pyflakes_ratchet_smoke.py`（棘轮 · 5 判据含双向反向测试 · **进 CI core ⇒ 178→179**）+ `scripts/registry_snapshot.py`（R3 前置判据）。
> - **账本影响**：**零锚改动**（448/3/18/469 不变 · 棘轮 `MAX_SELF_CERTIFIED=18` 不动 · 零 tol 放宽）；**CI core 178→179**（已同步 README `## 当前账本` / CONTRIBUTING）。
> - **裁决落地**：R1=②（B23 html 保持不入库）· R2=②（只清两热点）· R3=②（已落 registry_snapshot）· R4=①（波次 4 待执行）· **R5=①（本文件入库**，与已入库的 `LDA_functional_code_audit_2026-09-19.md` 构成「审计 → 修复计划」闭环；4 份商业/战略类仍不入库）。**R1 补充执行（2026-09-21）**：`ab85534`（2026-09-20 误提交）已 `git revert`，文件回归未跟踪 ⇒ ② 的原裁定**已实质恢复**（详见 §4 R1 执行结果）。
> - 🔴 **首轮全量 CI core 179 实跑：178 PASS / 1 FAIL**（`run_webui_api_smoke.py`，rc=1，21.7s，**9 条路由 500**）。
> - **血案（F401 的第三条 re-export 通道 —— 前两条见棘轮文档）**：`lda_webui/routes.py` **不做 from-import**，而是 `sys.modules.get("__main__")` 反查 app 模块后**按属性取用**业务函数（既避循环导入、又避免「脚本 / 包」双实例产生两个独立 store）。故 `app.py` 里为它保留的 **12 个 `lda_pdk` 名**（`submit_device` / `submit_devices_batch` / `submit_benchmark_proposal` / `review_proposal` / `land_proposal` / `resubmit_proposal` / `review_proposals_batch` / `land_proposals_batch` / `publish_proposal` / `submit_measurement` / `review_measurement` / `land_measurement`）**看似未用、实为契约** ⇒ 被本轮 F401 清理连带删除 ⇒ 请求期 `AttributeError: module '__main__' has no attribute 'submit_device'`。
> - **修法**：`app.py` 恢复该 12 名，并以模块级显式元组 `_ROUTES_APP_CONTRACT` 标记为「已使用」⇒ pyflakes 不再判 F401，**棘轮基线 F401≤201 仍成立**（未放宽）。
> - **补防**：`run_webui_api_smoke` 新增**静态前置断言** `_check_app_attr_contract`（AST 精确收 `app.py` 顶层绑定 vs `routes.py` 全部 **103** 个 `_app.X` 引用，**启服前**按名报缺、秒级红）+ **反向自检** `_selftest_contract_negative`。已实证：AST 收集器与运行时 `hasattr` **逐名一致 103/103（零分歧）**；对"删掉 2 个契约名"的 app.py 副本精确报 **`3/103 名缺失：resubmit_proposal, review_proposal, submit_device`**（副本置 scratch，未触碰仓库本体）。
> - **修后复验**：`run_webui_api_smoke` **PASS=92 / INFO=80 / FAIL=0**（原 81P/9F）· `run_pyflakes_ratchet_smoke` 5/5 PASS 且实测与基线完全一致。
> - **二轮全量 CI core 179 实跑（修后）：177 PASS / 2 FAIL**（5739.8s · 10 批 · 10 线程）—— `run_d_criterion_smoke.py` **TIMEOUT**（180.0s）+ `run_ci_industrial_smoke.py` **FAIL**（rc=1 · 293.4s · 2/3）。✅ **`run_webui_api_smoke` 已 PASS（14.2s）⇒ 血案 1 的修复生效。**
> - 🔴 **血案 2（超时预算欠标定 · 复发类 · 一个根因两个红项）**：`run_d_criterion_smoke` 的内置预算 **180s** 严重欠标定（表内旧注释写「实测 ~15s」，是**独立候选还很少时**的数据）；本项 ③ 是**全部已接线严格独立候选的基线残差普查**（当前 **448 道**），**耗时随候选数线性增长** ⇒ 三次独立测量 **175.52s / 173.95s（standalone）/ 二轮 >180s 撞线** ⇒ 余量仅 **1.03×（≈6s）**。standalone 复跑 **rc=0 · 10 PASS / 0 FAIL** ⇒ **纯耗时问题，非数值/判据缺陷**。`run_ci_industrial_smoke` 的 FAIL 是**连带**（其 `_SUBSET_CONTRACT` 代表子集**含本项** ⇒ 子集非全 PASS ⇒ case 1 FAIL），**该文件本身无缺陷**。
> - **同族复发判读**：与 v0.9.111 §5.2 记录的 falsifiability / fuzz 假红**同族**（那次「只修踩到的两项、**未做全表审计**」）⇒ 本轮**全表审计 14 项**（用两轮全量实测耗时算余量），查出 **5 项不足**：`d_criterion` 180→**600**（1.03×→3.4×）· `fuzz` 2000→**4200**（1.46×→3.1×）· `falsifiability` 1800→**3600**（1.61×→3.2×）· `splitter_readout` 400→**600**（2.04×→3.1×）· `splitter_readout_cal` 400→**600**（2.31×→3.5×）。重标定后**全表最低余量 3.06×**。**判据一字未改**（TIMEOUT≠FAIL）。
> - **立规 + 机器体检**：表头写死纪律「进入本表的项，预算 ≥ 3× 该线程数实测耗时」；`scripts/ci_core_batched.py` 每次跑完输出「预算余量体检」（写入报告 `budget_audit`：<2× 或 TIMEOUT/CRASH 截断值即列名告警）。已正反验证 **18/18 PASS**（真数据 R1/修后等效 R2 均 0 项欠标定 · 合成 1.06× 被列 · 合成 3.6× 不列 · TIMEOUT 记录强制列出）。
> - **三轮全量 CI core 179 实跑（超时预算全表重标定后）：179 PASS / 0 SKIP / 0 FAIL**（**5600.4s** · 10 批 · 10 线程 · 批次 1–9 各 18 条、批次 10 为 17 条）—— ✅ **全绿** · d_criterion **PASS（174.2s，预算 600 ⇒ 余量 3.44×）** · ci_industrial **PASS（289.4s）** · perf/fuzz/falsifiability 全部 PASS。
> - ✅ **机器体检（新设施首跑即生效）**：报告 `budget_audit` = **14 项受覆盖项 · `low_margin` 为空（0 项欠标定）** · **全表最低余量 2.99×**（`run_fdtd2d_mmi_smoke` 133.9s / 400s；贴 3.0 目标线，远高于 2.0 告警线，**未列入告警**）。
> - **本轮收口三态**：三轮全量均**全量实跑取证**（非抽样）—— 首轮抓出血案 1（`_app` 契约）· 二轮抓出血案 2（超时预算欠标定）· 三轮 **179/179 全绿**（修后清零）。**两次血案的修复均经「假想→实测→反向验证」闭环，未触碰任何物理/数值判据**。
> - 🔴 **新发现（待排期）**：`lda/reports/verification_report.json` 的 **B157-B159 `candidate` 跨环境末位抖动**（第 6-7 位；`golden` / `tol` / `passed` 未变）—— 属 v0.9.75 报告确定性工作的**残留**（B-9 Hermite 梁 FEM 的 LAPACK 本征解受线程预算影响），**非本轮引入**（本轮未改任何数值核），建议单列专项根治。

---

## 1. 已闭环（v0.9.111）——不再排期

| 发现 | 级 | 落地 |
|---|---|---|
| F-01 `review.py` 缺 `_norm_params` | P1 | 补导入 + 4 条分支断言 + **反向测试**（撤掉导入 ⇒ rc=1、真报 `NameError`） |
| F-02 `lda_l3` 未声明打包 | P1 | `packages` 补 `lda_l3`，声明/磁盘**双向对账零缺漏** |
| F-03 pytest 无配置 | P2 | `norecursedirs`；**实证 rc=2 → rc=5**（收集错误 → 无测试可收集） |
| F-04 `v_t` 未定义 | P2 | `V_T` 修正 |
| F-05 `converged` 丢弃 | P2 | 保号修复 + `RuntimeWarning`（数值行为冻结） |
| F-06 3 处死 `rng` | P2 | 删死赋值 + docstring 如实改写 |
| F-09 9 处 typing 名缺失 | P2 | **F821 10 → 0** |
| F-10 根目录脚本入库 | P2 | 16 脚本 + 3 产物停跟踪 + `.gitignore`（**29 个根目录文件现已脱离跟踪**） |
| F-16 重复导入 | P3 | 删冗余行 |
| F-17 Tidy3D 许可误称 | P3 | 官方 LICENSE 实查 = **LGPL-2.1** 客户端 + Flexcompute 商业云 ⇒ 全仓 **7 处**订正 |
| N-1 Meep/Tidy3D 分级错写 A 级 | P3 | 按权威表改 **B 级** |
| N-2 CI 未钉哈希种子 | P3 | `PYTHONHASHSEED=0` |
| N-3 补 `origin` 远端 | — | **判定不做**（与 `sync_push.py` 双远端硬依赖冲突、无收益） |
| 超时表与线程标定脱钩 | — | v0.9.111：falsifiability 600→**1800**、新增 fuzz **2000**（**判据未改**）；🔴 **v0.9.112 全表审计后重标定 5 项**（d_criterion 180→600 · splitter_readout 400→600 · splitter_readout_cal 400→600 · falsifiability 1800→3600 · fuzz 2000→4200 ⇒ **全表最低余量 3.06×**，**判据一字未改**）+ 立规表头「预算 ≥3× 实测」+ 新增跑完自动「预算余量体检」（见波次 6 T6.1 根治设施） |

---

## 2. 待排期工作（波次 1 ✅ 已闭环 / 2 ✅ 已闭环 / **3 🚫 已关闭（2026-09-21）** / 4 ⏳ 未执行 / 5 🔒 外部 / 6 ⏳ 未执行）

### 波次 1 —— 静态卫生清理 + 棘轮固化 · 建议版本 **v0.9.112**

> 目标：把 pyflakes 三类计数压到 0，并**新增 CI 棘轮**防止回涨。**这是本计划性价比最高的一波。**

| # | 任务 | 对应发现 | 实测规模（受跟踪口径） | 做法要点 |
|---|---|---|---|---|
| T1.1 | **F401 定点清理（先 POC 后铺开）** | F-12 | **244 处 / 134 文件**（热点：`lda_webui/app.py` 19 · `lda_harness/golden.py` 16 · `lda_harness/benchmarks.py` 8 · `device_library.py` 5 · `shape_inverse.py` 5 · `run_spectrum_loop_smoke.py` 5） | 🔴 **必须先建 re-export 白名单**：AST 扫描「本模块符号被其它模块 `from X import y`」的集合，白名单内的 F401 **不是**死代码（已实证 `run_shelf_listing_smoke.py:154,273`、`run_shelf_taxonomy_smoke.py:141` 从 `lda_webui.app` 取 `shelf_status`）。建议**只做两个热点**（35 处），不做全量 —— 见 §4 R2 |
| T1.2 | F841 未用赋值清理 | F-11 | **71 处 / 50 文件** | 逐处判定「纯废弃」vs「**漏用**」——后者是 bug 信号（F-05 即此类）。判为漏用的**不许删，改注释/改逻辑** |
| T1.3 | F541 空 f-string 摘除 | F-13 | **25 处 / 19 文件** | `f"{{}}"` 类**须先解转义**再摘 `f`，否则改变输出语义 |
| T1.4 | F811 重复导入 | F-15 | **4 处 / 1 文件**（全在 `lda_l2/device_library.py`） | 🔴 **先证伪分支必要性**：疑似「契约 / live 双分支」各导一次同名符号 ⇒ 未证伪前**不动** |
| T1.5 | **新增 `run_pyflakes_ratchet_smoke.py`** | 本文新增 | — | 断言 `F401 ≤ 244 · F841 ≤ 71 · F541 ≤ 25 · F811 ≤ 4 · F821 = 0`，**只许降不许升**，纳入 CI core |

- **门禁**：全量 CI core（分批防掉电）+ 6 道账本护栏 + 新增棘轮护栏
- **风险**：**中低**。改动面大但机械；真风险只有两处 —— T1.1 的 re-export 误删、T1.2 的「假废弃真漏用」
- **为什么排第一**：机械、可逐项验证、且**收益能被棘轮固化**（否则下次扩基又长回来）

---

### 波次 2 —— 结构性重构 · **需专项** · 建议版本 **v0.9.113**

> 🔴 **本波是唯一触及判决路径入口的改动，必须单独发版。**

| # | 任务 | 对应发现 | 实测规模 | 做法要点 |
|---|---|---|---|---|
| T2.1 | 抽公共助手模块 | F-07 | `check()` **×86 文件** · `_http`/`_free_port` ×6 · **`guard_t1_not_oracle` ×6** | 抽到 `lda_harness/smoke_kit.py`。🔴 **`guard_t1_not_oracle` 是 T1 红线守卫**（T1 数值内核 vs 外部 oracle 的边界断言），复制 6 份 = 任一处漂移即削弱保护 ⇒ 抽一份 + 每处加「导入即自检」断言 |
| T2.2 | 巨石文件拆分 | F-08 | `lda_harness/verification_adapters.py` **6789 行** · `lda_harness/benchmarks.py` **6635 行** | 按批次族拆。🔴 **不是纯搬家**：`@_register_candidate` 装饰器**靠 import 顺序填充全局注册表** ⇒ 拆分必须保证注册表**逐位相等**。✅ **已完成（v0.9.117）**：连续切片 + 基座提升；四重验收全通过（快照逐位一致 / 源码字节保真 530 条 0 缺失 / 501 个 code 对象语义等价 / 布局契约 13 判据）；零锚改动 · 账本零变化 |
| T2.3 | 统一测试范式 | F-18 | 19 个 smoke 用 `unittest`（84 test 方法）· 172 个用自研 `check()` | 建议并入本波（同属机械改造面） |
| T2.4 | **新增 `scripts/registry_snapshot.py`（验收判据固化）** | 本文新增 | — | 导出 `BENCHMARK_ORDER` / `BENCHMARK_CANDIDATES` / `_PHYSICAL_LAW` **三份有序快照**；拆前拆后 `diff` **必须为空**。这是 T2.2 唯一可信的验收方式 |

- **门禁**：注册表快照零差异 + 全量 CI core + fuzz + 账本护栏
- **风险**：**高**。注册表是判决路径入口，静默漂移会**改变判决结果而不报错**（历史上 B-28 首版即因 candidate key 重名被静默覆盖）
- **建议**：**先只做 T2.4**（成本极低、零风险），拿到判据后再决定 T2.2 做不做 —— 见 §4 R3

---

### 波次 3 —— ~~数值工程~~ · 🔴 **已关闭（2026-09-21）** · 原建议版本 v0.9.114 已用于台账护栏补防

| # | 任务 | 对应发现 | 内容 | 纪律 |
|---|---|---|---|---|
| T3.1 | `solver_writer` 归一化 / 阻抗因子修复 | F-14 | 源码自述缺陷：**原始出射场幅平方当透射、无参考跑归一化、无 `nL/n0` 阻抗因子** ⇒ 需重做归一化链路 | 🔴 **不得为修它而放宽任何 tol**；若修完某锚余量上升，按 `lda-anchor-wiring` 走「升格 = 只收紧」流程 |

- **门禁**：判据 D（`1e-13 < 粗端残差 < tol` + 严格单调）+ 反向测试 + 全量 CI core
- **风险**：中（数值语义）；**它也是唯一可能影响独立率指标的一项**
- **注意**：这是 L3「AI 写内核」的 demo 路径 —— 修好会提升该路径可信度，但**不改变任何已定级锚**

> 🔴 **关闭理由（2026-09-21）**：**T3.1 的前提不成立。** 该 `# BUG` 注释位于 `_CANDIDATE_V0` **三引号模板字符串内部** —— 它是 demo 的**刻意错误示范**，紧邻 `_CANDIDATE_V1`（修复版）与之构成「**判 FAIL → 收重写 → 判 PASS**」的闭环演示；`build_offline_generator()` docstring 明写此意图。**改它 = 摧毁 L3「AI 写内核递归自举」thesis 的实物演示 + 打破 CI core 常驻门禁** `run_solver_writer_sandbox_smoke` 判据③（`run_ci_regression.py:458` · 实跑 8/8），且**零数值收益**（prod 求解核在 `lda_solver/`，该文件不在任何生产求解路径）。⇒ **T3.1 判定不做**，详见 §5 新增行与顶部「收口记录（2026-09-21）」。

---

### 波次 4 —— 补盲（审计未覆盖）· **独立于发版 · 可与波次 1/2 并行**

> 不改主仓库代码，产出是**报告**而非代码。

| # | 任务 | 审计出处 | 前置（已实测） |
|---|---|---|---|
| T4.1 | 依赖漏洞扫描 | §6.6 | `pip-audit` / `safety` **实测均不可用** ⇒ 需先在**隔离 venv** 安装（不污染系统环境） |
| T4.2 | WebUI 前端 JS lint | §6.3 | `index.html` 368 KB · `store.html` 75 KB 内联脚本；`esprima` **实测不可用** ⇒ 需在 **node 托管工作区**装 `eslint`（node 22 可用） |
| T4.3 | 性能 / 扩展性基线 | §6.5 | 仓库已有 `bench_router_scale.py` 等基准脚本但**无实测记录** ⇒ 跑出数字即可 |
| T4.4 | 锚数值交叉复核（抽查） | §6.2 | 历批已做判据 D；此处是**独立交叉复核**性质，抽 N 个锚重新复算 |

---

### 波次 5 —— 外部依赖 · **不由 AI 排期**

- **收官 9 锚**（B21 / E9 / E10 / E1 / E3-E7）：**9/9 依赖外部**（B21 已由四组只读实验裁决「不可内部升格」）
- **AI 侧可预备部分 = 已就绪**：验收流水线 + 账本脚本 + 部署流程（v0.9.111 已完整跑通一遍）
- **人类侧第 0 步**（≈0 成本 · 必先发）：A1 询价两封邮件 —— 缺收件人 / 预算 / NDA，**AI 不代发**

---

### 波次 6 —— 本轮全量实跑**新暴露**的缺陷（待排期 · 不属审计 18 条）

> 两轮全量 CI core 各抓出一处真缺陷，均与「扩基 / 判据」无关，属**验证副产品**。血案 1（`_app` 属性契约）已在 v0.9.112 内闭环（含护栏 + 反向测试）；血案 2（超时预算）已修 5 项，但**其根治设施**留待排期。

| # | 任务 | 出处 | 现状 / 做法要点 |
|---|---|---|---|
| T6.1 | **超时预算棘轮升格为核心 smoke** | 血案 2 | 本轮已把「预算余量体检」落在 `scripts/ci_core_batched.py`（跑完打印 + 写报告 `budget_audit`），但它只是**本地辅助脚本的报告**、**不是门禁**。升格做法：提交一份 `@10 线程` 实测基线表（时间戳 + 线程数 + 实测秒）⇒ 新增 core smoke 断言每个覆盖项 `budget ≥ 2× 基线`（目标 3×）⇒ 把「何时该重标定」变成**CI 会红**的机器纪律。🔴 **同类缺陷已复发两次**（v0.9.111 修 falsifiability/fuzz、v0.9.112 又查出 5 项）⇒ 优先级 **高**。代价：CI core 179→180 + 账本连锁 7 处 + 双向反向测试。**本次 v0.9.112 未做**（不破坏本版「不扩基 · 账本零变化」的口径）。 |
| T6.2 | `run_ci_industrial_smoke` 代表子集成员复评 | 血案 2（连带项） | 其 `_SUBSET_CONTRACT` 设计意图是「小的、固定、快速（<~30s）、**负载无关**」，但成员 `run_d_criterion_smoke` 因 448 道普查已达 **~174s** ⇒ 子集实为 ~176s、本文件 ~290s，且**继承该成员的超时预算风险**（本轮即因此连带 FAIL）。本次只**订正注释**、**未改构成**（改构成会变动该文件所验证的聚合契约覆盖面）。复评选项：① 换一个更快的「验证 harness」类 smoke 作代表 ② 保留并接受成本 ③ 缩小该成员自身的普查范围（🔴 须先论证不削弱判据 D ③ 的值域排除效力）。 |
| T6.3 | `reports/verification_report.json` 的 B157-B159 `candidate` 末位抖动 | 血案 1 轮次的副产品 | B-9 Hermite 梁 FEM 的 LAPACK 本征解受**线程预算**影响 ⇒ 第 6-7 位抖动（`golden` / `tol` / `passed` **不变**）。已证：**正确线程 env 下与 HEAD 逐字节一致** ⇒ 属「**手动**跑 harness 未注入线程 env」的**判读陷阱**，非仓库缺陷。根治选项：把 `thread_env_overrides()` 注入**下沉到 harness 自身**（而非只在 `run_ci_regression._child_env()` 包装层），使手动跑也确定。 |

---

## 3. 排序理由

1. **波次 1 先做** —— 机械、可逐项验证、收益能被棘轮固化；且它把「静态噪声」压到 0 后，后续任何真问题都不会被淹（审计已指出 360 条噪声会掩护真缺陷）。
2. **波次 2 靠后且独立发版** —— 它动的是**判决路径注册表**；与机械清理混批 = 出问题无法二分定位。
3. ~~**波次 3 独立** —— 数值语义与代码卫生正交；且它挂钩独立率，需单独走判据 D。~~ 🔴 **2026-09-21 关闭**：F-14 经六条取证判为**审计上下文误读**（详见 §2 波次 3 与 §5）⇒ 该项**不再存在**。
4. **波次 4 并行** —— 不动主仓库代码，随时可插空。
5. **波次 5 不排** —— 时序由外部决定。

---

## 4. 需你裁决（4 项）

| # | 事项 | 选项 | 我的建议 |
|---|---|---|---|
| **R1** | `docs/B23_closing_report.html` 未入库（B26 已入库） | ① 补入库 ② 保持不动 | **②保持不动** —— 已查证 `docs/index.html` 与全库**均无引用**，不构成断链；B26 入库更像当时顺手 ｜ ⚠️ **误执行与回退（2026-09-20 → 09-21）**：该裁定曾被误报为「v0.9.105 历史遗漏」并据此提交 `ab85534`（**隐性反向授权**：把已裁定「不动」项包装成新发现重提 ⇒ 被读作批准）⇒ 经你 2026-09-21 明确裁定 **(B)**，已 **`git revert ab85534`** 回退；文件**保留在工作区未跟踪** ⇒ R1=② **原裁定彻底恢复**。全仓引用复查：仓库内仅本行提及，无任何代码/文档引用 ⇒ 回退不构成断链（与本行原结论一致） |
| **R2** | F-12（F401 244 处 / 134 文件）做到什么程度 | ① 全量清 ② 只清两个热点 + 建棘轮 ③ 不做 | **②** —— 全量投入产出比差（134 文件改动面 vs 收益仅为静态噪声），且 re-export 误删风险随面扩大 |
| **R3** | F-08 巨石拆分是否投入 | ① 直接拆 ② 先做 `registry_snapshot.py` 再定 ③ 不做 | **②** —— 拆分本身不难，**难的是证明拆对了**；先把判据固化，成本极低且零风险 ｜ **执行结果（v0.9.117）**：① 已执行 —— 判据先落地（T2.4 · v0.9.112）→ 拆分（T2.2）→ 四重验收全通过，R3 决策链完整闭合 |
| **R4** | 是否允许装工具做波次 4 | ① 允许（隔离 venv 装 `pip-audit`；node 工作区装 `eslint`）② 不装 | **①** —— 都不进主仓库依赖，纯分析用途 |

> 另：本文件是否入库（技术/工程类惯例是入库，但这是**计划**非**结论**）—— 默认**暂不入库**，与 R1 一并由你定。

---

## 5. 判定不做（已证伪 / 无收益 · 不再提）

| 项 | 理由 |
|---|---|
| 冲 100% 独立率 | 数学不可达（需 ∞ 锚）；收官目标已定案 97.4% |
| 改造 S\* 为独立锚 | 摧毁校验的一方（S1↔S7、S3↔S8 是解析孪生/统计独立配对） |
| 宣称 B17 / B18 独立 | 物理不可接（同义反复 / regime 越界） |
| 删 12 terminal 缩分母 | 移球门 |
| 放宽 tol 升 E9 / E10 | 红线：tol 只收紧 |
| 补 `origin` 远端 | 与 `sync_push.py` 双远端硬依赖冲突、无实际收益 |
| **F-14 `solver_writer` 数值修复** | 🔴 **审计上下文误读（2026-09-21 证伪）**：`# BUG` 位于 `_CANDIDATE_V0` **三引号模板串内部**，是 demo 的刻意错误示范；`_CANDIDATE_V1` 即修复版，且该演示是 **CI 门禁** `run_solver_writer_sandbox_smoke`（`run_ci_regression.py:458`）判据③ 的依赖物（真 ORACLE `tmm` 实测 v0 FAIL→v1 PASS 与发布说明逐位一致）⇒ 改它 = **摧毁 L3 演示 + 破门禁**，零数值收益 |

---

## 6. 一页排期表

| 波次 | 内容 | 建议版本 | 风险 | 前置 | 预期账本影响 |
|---|---|---|---|---|---|
| **1** | 静态卫生清理 + 棘轮 | v0.9.112 | 中低 | 建 re-export 白名单 | 零变化 |
| **2** | 结构重构（助手抽取 + 巨石拆分 + 范式统一） | v0.9.113 | **高** | `registry_snapshot.py` 判据先落地 | 零变化 |
| **4** | 补盲（依赖扫描 / JS lint / 性能基线 / 数值抽查） | 不发版 | 低 | R4 批准装工具 | 零变化 |
| **5** | 收官 9 锚 | 外部决定 | — | 人类发函 / 预算 / NDA / MPW | 目标 457/0/12 = 97.44% |

> 🔴 **波次 3 已于 2026-09-21 关闭**（F-14 判定不做 · 见 §2 波次 3 与 §5）⇒ 上表**号段缺 3 为刻意保留**（波次编号是**标识**，不重排、不复用）。另：**波次 6**（全量实跑新暴露项 T6.1-T6.4 · 不属审计 18 条）**不在本表**，见 §2 波次 6。

---

## 7. 口径与诚实边界

- 本文数字均为 **2026-09-19 22:xx 实跑复测**（`pyflakes` API + 文件行数统计），**受跟踪文件口径（507 个 py）**。与审计报告 §3.2 的微小差异已定位：审计报 **F401 245 / F841 75**，本文实测 **244 / 71** —— 差异来自 v0.9.111 已删 1 处重复导入（F-16）+ 统计口径按「受跟踪」切分（未跟踪的 29 个根目录诊断脚本已从计数中剔除）。**F541 = 25、F811 = 4、F821 = 0 与审计一致。**
- 未纳入本计划的审计结论：§2.1 功能宣称真实性 ✅ 通过、§3.3 安全姿态 ✅ 干净、§3.4 确定性 ✅ 良好 —— **均无需行动**。
- 本计划**不重新论证**收官目标（97.4%）、不动棘轮常数、不动任何锚的定级。
