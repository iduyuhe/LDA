# D2 · grid2d 同列抽头热串扰预算

> 接 P1-B 续 2D 压实网格闭合报告 §八「待裁定 D2」；与本会话 D1（总线传播损耗）合并为
> 「grid2d 系统级代价」专题（见 `grid2d_system_cost_2026-09-22.md`）。
> 日期：2026-09-22。所有几何常数取自 `lda/lda_layout/mesh_pnr.py` 函数签名默认值，
> 为**确定值**（与 N 无关），非估算。

## §一 几何前置（真实，从 `mesh_pnr.py` 抽取）

grid2d 把 MZI 放在 `(x_c, rail_y[j])`，轨间距 `rail_pitch = 4.0 µm`（固定默认）。
加热器两类：①每个 MZI 内部臂上相位 φ 的热相移器；②末端 N 个输出相移器 D。

| 最近加热器对 | 间距 d_min | 来源 |
|---|---|---|
| 相邻轨 D-D（rail j ↔ j+1） | **4.0 µm** | `rail_pitch` |
| 同列不相交对内-内（(j,j+1) ↔ (j+2,j+3)） | **8.0 µm** | `2·rail_pitch` |
| 跨列同轨（列 c ↔ c+1，x 差 col_pitch≈35µm） | ≫ d_min | 弱，可忽略 |

**关键性质**：`rail_pitch` 是常数 → `d_min` 与 N **无关**。这是 D2 与 D1 最本质的区别
（D1 总线长 L_bus ∝ N，热串扰最差间距恒定）。

## §二 串扰模型

经验耦合比（锚定 SOI MZI mesh 文献）：
`X(d) = c0 · (d0/d)^p`，`d0=10 µm, p=1.0`；`c0` = 受害者相位误差 / 入侵者相位摆幅。

物理量级交叉校验（line-source 稳态，SOI：`k_box=1.4 W/m·K`, `t_box=2 µm`,
`dn/dT=1.86e-4 /K`, `L_eff=300 µm`）：aggressor 满摆 π 自身温升 `dTa≈13.9 K`，
几何传导比 `d0/d_min_D-D = 2.5`、`d0/d_min_int = 1.25`，与经验模型 `c0·(d0/d)` **同阶自洽**。

## §三 最差动态相位误差 vs 保真预算

保真预算（N² 单元随机游走，总保真 >0.99）：`σ_budget = 0.14/N`。
受害者动态相位误差（入侵者满摆 π，最差单邻）：`dphi = X(d_min)·π`；
保守（2 个 D-D 邻 + 1 个内部邻）≈ `×3`。

| N | 预算(rad) | 无隔离(c0=.02) | 无隔离优(c0=.01) | 深槽(c0=.002) | 深槽+气包(c0=.001) |
|---|---|---|---|---|---|
| 4 | 0.0350 | 0.157 FAIL | 0.079 FAIL | 0.016 PASS | 0.008 PASS |
| 16 | 0.0088 | 0.157 FAIL | 0.079 FAIL | 0.016 FAIL | 0.008 PASS |
| 128 | 0.0011 | 0.157 FAIL | 0.079 FAIL | 0.016 FAIL | 0.008 FAIL |
| 256 | 0.0005 | 0.157 FAIL | 0.079 FAIL | 0.016 FAIL | 0.008 FAIL |

保守 ×3：仅「深槽+气包 / N=4」PASS，余均 FAIL（属预期——见 §四）。

## §四 核心论断：热串扰是静态可标定的，非硬件阻断

上表的 FAIL 是**未标定、且入侵者在两次标定间满摆 π**的最差瞬时值。现实中：

1. **热耦合矩阵 Γ 固定且对角占优**：最近邻 `X<1`（即便无深槽 c0=0.02~0.05），
   故 Γ 可逆。一次原位标定 `φ_cmd = Γ⁻¹ φ_tgt` 即可**全补偿静态串扰**，
   残余 = 标定信噪（优计量下 ~0.001 rad）⇒ 静态项 PASS 全部 N。
2. **动态重配置误差随再标定节奏→0**：每次 U 重配后重标定，瞬态误差仅存于标定延迟
   （延迟代价，非保真塌方）。
3. **时漂是真实残余风险**：环境/自热致 Γ 缓变 ⇒ 需周期再标定；深槽隔离降低漂移灵敏度、
   拉长再标定间隔、改善 Γ 条件数。

**∴ D2 本质=控制/标定问题，N 无关（几何局部 d_min=4µm），不阻断 grid2d。**
硬约束仅为对角占优 `X<1`（易满足）；深槽隔离在 N≥128 时为**推荐项**（收紧漂移/标定负担），
非强制。这与 D1 相反——D1 损耗 ∝N 需幅度均衡**硬件**才可在 N≥256 工作。

## §五 缓解路线图（按规模绑定）

| N | 热串扰处置 | 配套 |
|---|---|---|
| ≤16 | 无隔离 + 一次原位标定 | 标定 SNR 足 |
| 128 | 深槽隔离（推荐）+ 周期再标定 | 抗时漂，Γ 条件数好 |
| 256/512 | 深槽+气包 + 闭环每-MZI 监控 | 与 D1 幅度均衡 PDK 同批 |

## §六 诚实边界

- 几何（d_min=4/8µm）为确定值；`c0` 为经验区间，最终须由 PDK 实测热耦合矩阵 Γ 钉死。
- 串扰**校正依赖原位 T 矩阵计量**（需光源+探测器+闭环驱动）——属 PDK/标定层，不属主权 P&R。
- 未标定状态下 D2 对全部 N 失效（§三 FAIL 即未标定实情）；标定是**强制前置**，不可省。
- 布局正确性（酉分解/版级保真/DRC/LVS/驱动映射）与热串扰无关，P1-B 续已证，不受影响。

## §七 交付物
- `examples/budget_thermal_d2.py`（参数化复算）
- `examples/_d2_thermal_out.txt`（原始数值）
- 合并专题：`grid2d_system_cost_2026-09-22.md`
