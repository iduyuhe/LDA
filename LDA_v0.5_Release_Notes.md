# LDA v0.5 Release Notes

> 版本：v0.5（2026-08-23）
> 里程碑定位：**系统级里程碑**——在 v0.4 真实化（真实版图 + 端口验收 + 伴随法逆设计）之上，补齐**系统级能力（M7）**、**护城河（标准 + 验证合约工业化）**与**逆设计纵深四阶**，形成"设计目标 → 已验证器件/系统/标准/逆设计"完整开源基线。
> 红线不变：**LLM 不进判决路径**，PASS/FAIL 由死标量比对（物理定律锚 + 确定性裁判）决定。

---

## 一、v0.5 相比 v0.4 新增（D-73~D-82）

### 1. M7 系统级三件（Track D）
| 交付 | 内容 | 实测 |
|---|---|---|
| **D-73 热光可调 WDM** | 静态 WDM 叠加每环热光相位 shifter，物理定律锚 Δλ/λ=(dn/dT)·R_th·P/n_eff（dn/dT=1.86e-4/K 材料常数，ORACLE 比对真实 Si 加热器斜率 [0.02,0.5] nm/mW） | S≈0.120 nm/mW、信道重分配 |P|≤P_max、|Δλ|≤FSR/2 无混叠 |
| **D-74 量子门/纠错拓扑** | 量子门库（11 门全幺正 + {H,T,CNOT} 通用性 T∉24元Clifford）+ rotated surface code（d² 数据比特全对易 + GF(2) 秩 k=1）+ cross-resonance 门（有效模型 + t_CR≤T2） | 门库全过、|g_CR| 命中实器件区间、阈值以下容错可达 |
| **D-75 大规模系统基准** | N≥8 WDM × N≥8 qubit 联合验收 + 性能/精度边界压测（容量自洽 / IL 级联余量 / qubit 间隔临界 / 标定网格分辨率） | 8×8 全过；容量自洽 8=8；IL 预算 7.3%；qubit 间隔余量 2.5×；网格分辨率 0.59%≤1%；总压测 0.056s |

### 2. 护城河两件（标准 + 验证合约）
- **D-76 L0 IR 开放标准 v0.3 定稿**：`docs/ir_spec.md`（LDA-STD-001 规范）+ `docs/ir_schema.json`（JSON Schema draft-07）+ 零漂移校验（文档↔Schema↔代码三方一致，9 kind）；顺带修复 `dsl.py` round-trip 丢 physics 物理锚的序列化缺陷。
- **D-77 验证合约工业化**：`run_ci_regression.py` 统一回归入口（自动发现 54+ smoke + harness B1-B13，core 集 **27 PASS / 0 FAIL**）+ `run_perf_bench.py` 求解器性能基准（greens numpy→numba **48.9×**、物理一致 rel=4.8e-16、基线漂移 ±30% 黄灯预警）+ CI `industrial-regression` job。**任何人跑一条命令看全量回归红绿。**

### 3. Track A 逆设计纵深四阶（D-80/D-81/D-82）
| 阶 | 交付 | 实测 |
|---|---|---|
| ① 谱形目标（D-80） | 目标从「收集场能」泛化：split_ratio 分束比（双监视器对数加权 FOM）/ spectrum 多波长（Σw_λ·FOM_λ）/ mode_match 模式匹配（场投影） | 50:50 分束比命中 0.574（err 0.074）；3 波长谱形 imp 11.7×；模式匹配 imp 8.6×；FD 对拍 ≤2e-4 |
| ② 形状逆设计（D-81） | voxel 拓扑 → 连续宽度曲线（K 控制点 + sigmoid 软边界，**可制造性内建**：宽度界 + 平滑 + DRC） | imp 6.6×；多目标 2 波长加权 5.6× + Pareto 前端 3 点 |
| ③ 形状+拓扑混合（D-82） | 分层表达：形状主干 ⊕ 拓扑微调带（**概率 OR 光滑组合**，处处可导） | 混合 imp 18.3× vs 纯形状 6.0×（**混合增益 3.06×**） |

### 4. 其它
- WebUI **四十三面板**（新增 ㊳大规模基准 / ㊴ IR 开放标准 / ㊵验证合约工业化 / ㊶谱形目标 / ㊷形状多目标 / ㊸混合逆设计）
- 全部交付三端同步（本地 / Gitee / GitHub，tree 级零差异 363/363）

## 二、v0.5 可对外宣称的能力
1. **真实化**（继承 v0.4）：foundry-ready GDS 基元（taper/Euler 弯/MMI/GC）+ 2D/3D FDTD 端口 S 参数验收（MMI/DC/Ring/GC 光栅方程 ORACLE）+ "设计→验证→版图"全链路闭环。
2. **系统级**（新增）：可调 WDM / 量子门纠错拓扑 / N≥8 大规模联合压测——"设计目标 → 已验证系统"。
3. **标准层**（新增）：L0 IR 开放标准（spec + JSON Schema + 零漂移）——社区共建起点。
4. **验证合约**（新增）：一键全量回归 + 性能基准 + 基线漂移监控——协作门槛降低。
5. **逆设计纵深**（新增）：谱形目标（分束/谱形/模式）→ 形状（可制造）→ 混合（分层表达）四阶能力链。

## 三、诚实边界（对外基线 v0.5）
- **求解器**：2D TEz 为主（3D 端口验收为小几何/窄窗；3D adjoint 形状归后续）；GPU fp64 消费卡阉割（生产默认 numba-cpu）。
- **模型**：系统级链路（WDM 级联 / 读出保真度）为解析物理模型（FSR 2D 有效折射率容差 30% 已知）；adjoint/形状/混合为 2D 逆设计（FOM 为收集场能，T>1 聚焦增益非功率透射）。
- **实证锚**：实证大数据锚（验证锚②）待发动期数据（D-62 暂缓）——物理定律锚（验证锚①）为当前唯一已落地地基。
- **不宣称**：签核级（tape-out signoff）、与商业 EDA 数值库逐点一致、量子门级仿真（CR 为有效模型非多能级数值）。

## 四、验证证据（全量）
- harness 13 题（B1-B13）物理定律锚全过；
- CI core 全量回归 27 PASS / 0 FAIL；
- 54+ smoke 全绿（D-01~D-82 各交付 smoke）；
- 三端 tree 级零差异同步（363/363）。

*LLM 不进判决路径：本 Release Notes 所有指标均为死标量比对实测产出。*
