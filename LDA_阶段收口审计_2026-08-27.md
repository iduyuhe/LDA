# LDA 阶段收口审计 + 后续准备资料（2026-08-27）

> 目的：把当前阶段（L2 芯片级闭环 + UI 收口，v0.8.28b）做扎实——审计注册完整性、确认 CI 全绿、四端同步无漂移；并先收集通往 tape-out 级的开放 PDK / MPW / 封装测试可行性资料，为后续工作准备。
>
> 诚实边界（不变）：当前为**原理验证级**，非 tape-out 级；须标注工艺前提。实证锚为公开文献量级语料；真实晶圆厂 NDA 实测仍属**发动期**。

---

## 一、阶段收口审计结论

### 1.1 引擎与基准注册完整性（代码级审计）
- **22 引擎（ENGINE_KINDS）全闭环 / 空壳 0**：15 器件引擎 verify 调 device_library 真实求解器（2D/1D FDTD、相位对角化、三模对角化），passed 基于真实数值死标量比对；5 loss 引擎接 `seed_empirical.json` 判 `|引擎输出−实测|≤tol`（语料带文献引用）；2 有源引擎调 active_models 真实公式（dn/dT、Pockels r）。Ring/Mzi 标 `analytic_only`（design 走 contract 解析契约，live FDTD 需 GPU 诚实 SKIP——非空壳，是算力诚实边界）。
- **45 锚（BENCHMARK_ORDER）全闭环 / 空壳 0**：B1-B27 物理定律 dispatch 判 `|c−g|≤tol`；E1-E7 实证锚 `EmpiricalAnchor.resolve` 读真实文献语料（9 条带 citation）判 `|c−measured|≤tol`；S1-S8 算术/蒙特卡洛真实；S9-S11 LVS 锚构真实版图案例（3 器件/多层/千器件链式），确定性几何集合比对，内置反例返回 0.0（非恒 1.0）。
- **结论：22 引擎 / 33 类 / 45 题全部真实闭环，无静默空壳。**

### 1.2 CI 工业门禁（全量 core 实跑）
- 运行 `run_ci_industrial_smoke.py --core`：**40 PASS / 0 SKIP / 0 FAIL**，总耗时 157.47s，全绿。
- 负例门禁生效：故意坏 smoke（`run_zz_bad_smoke.py`）被正确检出（fail=1），证明"静默通过"不可能。
- 性能基准：greens speedup 76.53×（Numba JIT 生效）。

### 1.3 四端同步状态
- 本地 HEAD = `50b3190`；Gitee ls-remote = `50b31909...`；GitHub ls-remote = `50b31909...` ——**三端 sha 完全一致**。
- 生产实测 `/api/health`：version=`0.8.28`（wheel 包版本号，50b3190 仅 HTML 结构修复，Python 包未升版本，功能无影响）、benchmarks=45、harness 字段已就位、uptime 4900s 稳定。
- 本地工作区：仅一个未跟踪的总结文档（正常新产物）；`lda/reports_chip_scale/` 有历史生成物但未阻塞（已在 .gitignore 或可接受）。
- **结论：四端同步无漂移，部署稳定。**

### 1.4 当前阶段定位（L2 芯片级闭环 · 版图签核）
已实现：千器件 GDS 导出 + 自写 DRC/LVS 双闸 + 四锚验收 + 五类锚判决（物理/实证/系统/统计/LVS）+ 提案编译器 + LLM 生成器（生成判决分离）。

---

## 二、后续准备资料：通往 tape-out 级的可行性

### 2.1 开放 PDK 现状（B 级可借，主权合规）
| PDK | 工艺 | 许可 | 光子/量子 | 备注 |
|---|---|---|---|---|
| **Sky130**（Google/SkyWater） | 130nm CMOS | Apache-2.0 | 有源/无源（非专用光子层） | 首个全开源可制造 PDK；MPW 经 Cadence/SkyWater/Efabless；alpha 非量产级 |
| **AIM Photonics Open PDK** | 硅光 300mm | 教育/学术协议 | 硅光专用 | 公开版缺部分专有器件模型，学术协议可得 |
| **SiEPIC PDK**（UBC） | IME 硅光 | 开放获取 | 硅光专用 | 学术课程广泛使用，与 KLayout/gdsfactory 集成 |
| **IHP Open PDK**（IHP-GmbH） | SiGe BiCMOS | 真开源 GitHub | 有限光子 | 全开源先例，光子弱 |
| **gf180mcu**（GlobalFoundries） | 180nm | Apache-2.0 | 通用 | open_pdks 支持，MPW 可选 |

**选型建议**：光子方向优先 **AIM Photonics / SiEPIC**（专用硅光层、与 gdsfactory 生态契合，属 B 级 MIT/BSD 友好）；CMOS 控制/读出方向可用 **Sky130**（MPW 成熟）。均不碰 A 级商业 NDA-PDK，主权合规。

### 2.2 MPW 多项目晶圆服务（送产线通道）
- **Sky130 MPW**：Cadence/SkyWater 联合 shuttle，设计块提交 $10,000–12,000（标准/ReRAM 两选项），2026 年初批次截止 1/16、die 交付目标 7/2026；接受 OASIS 终版（不限 EDA 工具）——**我们的 GDS 可直接提交**。
- **AIM Photonics / 学术 shuttle**：经大学/联盟通道，学术价更低。
- 关键：MPW 提交需 foundry 官方 DRC/LVS 规则集（我们自写判据不替代，但可作 pre-check 前置剪枝）。

### 2.3 封装测试可行性（L5 后道，独立域，不破红线）
- **软件层（现在就能做）**：封装寄生 RLGC（解析/矩量法）、测试向量与光 IO 测试规划（DFT→光子）、良率预测（基于 S7/S8 蒙特卡洛——已有地基）、封装-芯片协同仿真接口（封装寄生作黑箱 S 参数喂回链路）。
- **物理层（发动期资源）**：真实封装（wire bond/flip-chip/fiber attach）、ATE/探针台/光测试台、真实测量数据回流。
- **结论**：建模/规划/预测软件层技术上现在可做、不破"LLM 不进判决路径"红线；但实测卡物理资源，且属独立后道域、与核心护城河（设计-验证开放内核）关系弱 → 优先级低于 L2→L3/L4 缺口补强，建议放 P3 生态或更后。

### 2.4 从 L2 到真正 tape-out 的硬缺口（诚实清单）
1. **真实 PDK 模型**（B 级开源）：工艺角 SS/TT/FF 用 foundry 模型替代自写占位。
2. **foundry 官方 DRC/LVS 规则**：自写判据作 pre-check，终签核过官方 deck（Calibre/ICV/开源 KLayout deck）。
3. **掩模就绪**：OPC/RET/MRC/dummy fill（通常委托掩模厂，我们产出 clean GDS）。
4. **实测回流**：流片回来芯片实测数据回流比对——**结构性卡死在发动期**（无真实硅测量语料），AI 写代码绕不过。

---

## 三、下一步决策点（杜先生定）

| 选项 | 内容 | 优先级建议 |
|---|---|---|
| A（最务实） | 接一个 B 级开源 PDK（AIM/SiEPIC 或 Sky130），把工艺角管道从自写占位换成真实 PDK 模型驱动，让实测回流有真实比对基准 | 高 |
| B（独立后道） | 建封装测试软件层三件套（寄生/良率预测/协同仿真接口），接进现有判决框架 | 中（P3 后） |
| C（只出规划） | 把"LDA 通往 tape-out 路线图 + 开源 PDK 选型 + MPW 通道 + 封装测试可行性"写成正式文档，不动代码 | 当前可做 |
| 维持 | 维持现状，等发动期资源（院校/晶圆厂 PDK 外联）到位再谈 | — |

---

## 四、阶段收口签字（本日）

- [x] 22 引擎 / 45 锚注册完整性审计：全闭环，零空壳
- [x] CI core 全量实跑：40 PASS / 0 FAIL，负例门禁生效
- [x] 四端同步核对：本地/Gitee/GitHub 同 sha，生产部署稳定（v0.8.28，benchmarks=45）
- [x] 后续资料收集：开源 PDK 4+ 选型、MPW 通道、封装测试可行性（软件层可做/物理层发动期）
- [x] 诚实边界重申：原理验证级，非 tape-out 级

*本审计基于：代码级 Explore 审计（design_package/golden/benchmarks）+ 全量 CI 实跑 + 三端 ls-remote 核对 + 生产 /api/health/status 实测 + 开源 PDK/MPW 公开资料检索（2026-08-27）。*
