# LDA 研发规划与实施计划（2026-09-11 红线重排版）

> **文档性质**：执行视角。姊妹文档 `LDA_商业计划书与研发融资规划_修订版_2026-09-09.md`（BP 修订版）是融资/商业视角；本文把其中已定版的**研发五期（T0–T4）**细化为可落地的 WBS、里程碑、可证伪验收与近期行动清单。
>
> **本版依据**：在 `LDA_研发规划与实施计划_2026-09-09.md`（v0.9.63 定版）基础上，叠加 **2026-09-10 / 2026-09-11 杜先生终审裁定**——即 `docs/lda_active_device_redline_clarification_2026-09-10.md` §七（有源边界）/ §八（Foundry TCAD 分层解锁）/ §九（电域求解器分层解锁）。**本版核心变化 = 原「三不做」之「电域求解器」「Foundry TCAD」由终审锁改为 T1/T2 分层解锁**，研发边界与新可承接内容随之重排。
>
> **排除项（杜先生明确）**：「一人公司（OPC）模式」**仅讨论、不进规划文档**。
>
> **数字基线（README 顶行权威账本，v0.9.67 实测 + 本机 harness 实跑复核 2026-09-11）**：v0.9.67 · 22 引擎（光 15 + 量 7）+ 11 包 = 33 类 · 求解核 `lda_solver/` 40+ 模块 · **CI core 157 条** · **53 锚（28 严格独立 / 1 降级 / 24 自证桩）** · pdks=5 / layers=8 · 超市 **75 货架**（5 赛道 + 24 应用域）· **GC-* 48 条全 PASS** · 实证语料 30 条全 A 级 · 生产四赛道（A5/B4/C4/D4）17 任务全通。
> 🔴 账本订正：README 发布注写「24→23」系笔误；本机按 `candidate_class` 权威判序实跑 = **28/1/24/53**（B33 新增一道严格独立锚，自证桩 24 不变），以本机复核为准。

---

## 0. 一句话定位

**LDA = 光电接缝上的可证伪设计入口。** 不做整条链，只做「封装测试占 79.9% 成本的那一格」——把设计决策（耦合容差 / 间距 / 端口布局）对那 79.9% 成本池的影响，用物理定律锚 + 实证锚做成死标量判决。钱与价值都在接缝，不在一成多的芯片成本池。

**红线重排后的新增定位**：在「接缝设计入口」之内，原禁区「电域求解器 / Foundry TCAD」的 **T1 数值内核（载流子输运 PDE 求解）现已解锁**——LDA 可在主权可控（B 级 DEVSIM fork / C 级自研）前提下，做电-光器件的**载流子分布真求解**，但**输出不作真值 ORACLE**，且 T2 工艺真值 / 电-光耦合增益 / 封测产线 / 流片仍锁死。

---

## 1. 战略边界（研发必须守住的红线与不做清单）

### 1.1 分层边界：原「三不做」→「T1 可解锁 / T2 锁死 / 封测锁死」（🔴 本版核心修订）

> 缘起与裁定权威出处：`docs/lda_active_device_redline_clarification_2026-09-10.md` §七·§八·§九（杜先生 2026-09-10~11 拍板，终审锁定，防反复审议）。

| 原「三不做」项 | 新分层口径 | T1（可解锁） | T2（永久锁死） |
|---|---|---|---|
| **电域求解器** | 拆 T1/T2 | **T1 电学内核**：泊松方程（电势/电场）+ 漂移-扩散 + 连续性方程，求载流子浓度分布 N(x)。可基于开源 **DEVSIM（Apache-2.0，有限体积法，用户自定义方程，1D/2D/3D）fork 镜像冷备离线构建**，或完全自研 → 主权 **B 级借今踢后 / C 级自主**，**不破 A 级永不借**。 | 电-光耦合增益当功能真值（激光 L-I / SOA 增益 / 增益谱，需增益介质模型 + 行波耦合）、A 级商业电学求解器（Sentaurus DEVICE / Lumerical DEVICE / COMSOL 半导体）、foundry 工艺真值、封测实体产线、流片。 |
| **Foundry TCAD** | 拆 T1/T2 | **T1 数值内核**：漂移-扩散 Poisson + 连续性方程 + 载流子输运的 PDE 数值求解（与电域 T1 同一内核）。开源 DEVSIM fork 或自研 → **B 级 / C 级**，**不破 A 级**。 | foundry 专有工艺模型（implant/diffusion/etch/stress 校正）与经流片实测标定的参数 ORACLE。属 foundry 私有 IP，开源世界不可得；LDA **只消费 PDK 接口，不求解工艺物理**。 |
| **封测实体产线** | 维持原样 | — | **完全锁死**（C 期能力，资本开支重）。先委托测试、后自建；B 档跑完 MPW 后封测才是花钱大头。 |

**A 级永不借（维持不变）**：Lumerical / Ansys / Synopsys / Cadence 等美系商业求解器，直接不采用。DEVSIM（Apache-2.0）属 B 级，fork 镜像冷备即合规，**不破 A 级**。

**T1 解锁的 5 条铁律（须同时满足，否则破 IRONLAWS）**：
1. **成熟节点用途声明**（规避 EAR 744.23 先进节点最终用途管制）——声明仅用于成熟节点 / 非先进用途设计。
2. 仅用开源/自研内核 + 公开文献参数 / PDK 标定，**禁 import 美系 A 级商业**（Sentaurus / Lumerical DEVICE / COMSOL 半导体）。
3. 自主 T1 输出**不作真值 ORACLE**；判决仍落 foundry 实测 / 公开文献 / 标准背书（与 B33 走 PDK/文献锚同一逻辑），否则破 IRONLAWS。
4. 不流片、不抢制造；只做设计入口与行为仿真。
5. subprocess 调用（如 DEVSIM）仅回标量，不污染核心（沿用 Meep 隔离纪律）。

**🔴 出口管制诚实提醒（钉入红线，不可省）**：EAR 744.23(a)(2)(iii)（2024-12-02 新规）按「用途（先进节点集成电路）」管制 ECAD/TCAD，**非按代码许可**。开源 TCAD 在**技术主权/代码层面**不受美系商业绑定，但若**明知用于中国先进制程芯片设计**仍可能触线；成熟节点 / 非先进用途不在 744.23 范围。「开源 ≠ 完全不受 IP 影响」——此提醒为红线组成部分，每次 T1 内核对外演示/交付前必复核用途声明。

**影响范围（对原「三不做」的净放开）**：
- B 档中「载流子动力学（漂移-扩散真求解）」「探测器带宽真求解（渡越/暗电流/APD 倍增）」经 T1 内核**可做**。
- 激光 L-I / 增益谱 / SOA 仍受「增益介质模型 + A 级」约束**维持禁区**（除非后续单独裁定）。
- A 档（闭式/行为层有源）早已授权，且不依赖 T1 内核（详见 §3 T1-C）。

### 1.2 全生命周期只承担 3–5 段（责任边界）

8 阶段核对：0 商机 / 1 需求冻结 / 2 架构权衡 / 3 设计实现 / 4 版图签核 / 5 出带包 / 6 封测验证 / 7-8 量产收尾。
- **强项 3-4-5**（设计实现 / 版图签核 / 出带包）—— LDA 的承担区。T1 内核解锁后，**第 3 段（设计实现）扩到载流子分布级**——可在设计阶段真算载流子浓度 N(x) 对调制/探测/热调行为的影响，但仍不碰第 6 段（封测验证，委托伙伴）。
- **断点 1（需求冻结）与 6（封测验证）** —— 1 由 T0 覆盖；6 委托伙伴。
- **0 / 7 / 8 不做**（商机/量产） — 不承接也不负责。
- 🔴 **基线缺失 = 最隐蔽缺陷**：可证伪的前提是「证伪对象固定」；需求不冻结则 53 道锚无的放矢。⇒ **阶段 1 需求冻结是必补环节**。

### 1.3 AI 分工铁律：AI 出题、物理定律判卷

- ❌ **审核 = 判 PASS/FAIL** → LLM 进判决路径，**破最核心红线**，绝不可给。
- ✅ **审核 = 找可能错的地方（出题/攻击）** → 属生成侧，可以给，越多越好。
- 正确架构 = **蓝队生成 / 红队出题攻击 / 锚裁判（死标量）/ 人终审** 四层。判决权在锚，签字权在人。
- T1 内核的验证纪律同样适用：DEVSIM/自研 drift-diffusion 输出**不进死标量判决**，只作候选，死标量仍由物理定律锚 / foundry 实测 / 公开文献定。

### 1.4 生态开放四层光谱（定位 = 获客引擎，不是研发外包）

① **实测语料/对抗题**（已在做·风险最低·应加大）② **PDK 适配/器件库/文档**（判据我们写）③ **边缘求解模块+新增锚**（先判据后代码）④ **核心求解器/判决路径/锚库**（不开放代码，只开放对抗）。
- 🔴 **最大风险 = 假绿（自证桩批量生产）**：外部贡献「自己写代码 + 自己写 smoke 证明自己对」。
- 规避（核心机制）：**判据先于代码，且判据由 maintainer 写**。
- 🔴 **T1 内核主权扫描新增项**：若生态 PR 引入 DEVSIM 或任何电学/TCAD 依赖，PR 门禁 import+license 双扫必须确认其许可为 Apache-2.0（B 级）且**镜像冷备离线可构建**；A 级（Sentaurus/Lumerical/COMSOL）一经检出即拒。
- 🔴 **许可不可逆**：现在就加最轻量 DCO（`Signed-off-by`），成本近乎零，保住未来双许可/商业授权权。**待杜先生拍板是否启用。**

### 1.5 跨期工程铁律（落到行为复核，非标签）

- 🔴🔴 **标签≠行为、值≠行为**（已累计 8 次血案）：只读 docstring/注释/常量名会漏判。读代码判断「有没有某能力」必须 grep 实际调用点；加字段必须打到数据出口（端点）并真跑生产代码验证，不在 smoke 里另写同式复算（=重言式）。
- 🔴🔴 **新护栏必须反向测试会响**；**须测生产代码非内嵌副本**；**CORE 覆盖不到=没门禁**；SKIP 宁红不假绿。
- 🔴🔴 **「写了 smoke」≠「进了 CI」**：新 smoke 必须登记 `CORE_SMOKES`，<5s 且无重依赖者无权豁免（`run_ci_coverage_gate_smoke.py` 强制）。
- 扩库类改动同步**五处**：①条目库 ②CI 断言计数 ③定价/白名单派生表 ④回归集登记 ⑤对外账本计数。
- CI 解释器固定：`C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（3.13.14）。**绝不用 `lda_cuda_venv`**（3.11 不支持 PEP 701 ⇒ 假 FAIL）。
- 改后端（routes.py/store.py 等 import 期加载文件）后实测前**必须先重启被测实例**（「标签≠行为」第 8 次）。
- 🔴🔴 **T1 内核 EAR 744.23 合规铁律（新增）**：任何 T1 电学/TCAD 内核对外演示、交付、或写入文档前，必须附带「成熟节点 / 非先进用途」声明；代码仓须保留 DEVSIM fork 镜像冷备的离线构建证据（`requirements`/submodule 锁定 commit + 离线 wheel 清单）；缺用途声明或离线构建证据 → CI 红。

---

## 2. 研发五期总览（T0–T4 · T1 纳入解锁层）

| 期 | 目标 | 工期 | 我方研发投入 | 对外口径 | 核心交付 | 可证伪验收（节选） |
|---|---|---|---|---|---|---|
| **T0** 需求架构层 | 把「需求→架构」链做成交付起点 | 0–9 月 | 60 万 | 能力（非融资） | 升级 `proposal_compiler` 管线：多目标 Pareto / 自然语言入口 / S8 接入 / 红蓝队 | 改成本权重→Pareto 前沿位移；无锚证据者不入 ranked；域外候选必被剪枝 |
| **T1** 光电接口层 + 电学/TCAD 数值内核（T1 解锁层） | 网表→带参数紧凑模型 + 接 Cadence + **T1 载流子输运内核** + 主动器件扩锚 | 0–18 月 | **170–190 万**（原 110 + 内核 60–80） | **A 档订单 150–300 万** + T1 内核能力（非融资） | 紧凑模型 + DEVSIM fork/自研 drift-diffusion + **A 档主动锚 ~8 道** + **B 档载流子动力学/探测器带宽真求解** | 参数扰动 ±20% 时域响应必须变；T1 N(x) 与 DEVSIM 双向标定 Δ≤容差；A 档锚方法学独立；**T1 输出不作 ORACLE 守卫必响** |
| **T2** 真值链 | 自有实测回流，换掉自证桩（**T1 内核已做部分 B 档真求解，真值仍靠此期 MPW 回填**） | 6–24 月 | 240 万 | B 档主体 | 3–4 轮 MPW + 委托封测 + 实测回流规范（**T2 工艺真值仍锁死，只消费 PDK**） | ≥5 自证桩换真锚；工艺角由假设→实测分布；偏差公开标注 |
| **T3** 设计侧虚拟制造 2.0 | 工艺波动 + **可封装性** + **载流子分布→封装良率链路（T1-B 赋能）** | 12–30 月 | 研发人力承担（不单列） | 能力 | S13 良率锚 + P-CPO 间距下界 + 载流子分布驱动的设计反馈 | 设计改动→封装良率预测同向变化且可复现 |
| **T4** 标准位与生态 | L0 IR 推标准位 | 24–36 月 | 40 万 | 能力 | L0 IR / DesignPackage JSON 成为外部采用标准 | ≥2 家外部采用 + ≥1 家 foundry 签 PDK |

**资金对齐（36 月，原 1000 万框架 + T1 内核追加）**：
- 原框架：人力 450（含 T3）· T2 真值链 240 · T1 110 · T0 60 · 标杆客户 80 · 生态标准 40 · 算力 10 · 预备 10 = **1000 万**。
- **T1 内核解锁新增 ~60–80 万**：优先从 T1 研发（110→170~190）+ 预备金（10）调剂；若要做**完整 DEVSIM 自主 fork + 多器件标定**，需追加至约 **1070 万**（待杜先生拍板 T1 内核投入深度：轻量自研 vs DEVSIM 全 fork）。
- 🔴 诚实边界：450 万按 3 人编制（@50 万/人年）；**若要 5 人团队需 750 万，总额升至约 1300–1370 万**（待杜先生拍板 3 vs 5 人 + T1 内核深度）。

---

## 3. 逐期实施计划（WBS + 验收 + 风险）

### T0 需求架构层（0–9 月 · 60 万 · 升级非新建）— 基本完成

**已有资产**：`lda/lda_harness/proposal_compiler.py`（687 行，15 判据 smoke 已在 CI）。五段管线：编译 → 锚约束剪枝 → 域内生成 → 逐案锚验 → 确定性排序 → 人终审。6 条流水线：link / wdm_demux / quantum_fidelity / sensor_frontend / qkd_link / cpo_optical_io。

**已完成（2026-09-09~10 实测）**：WBS-T0-1 Pareto 排序（CORE 已登记）· WBS-T0-2 NLP 需求入口（4/4）· WBS-T0-3 S8 接入（4/4）· WBS-T0-4 红蓝队闭环（DeepSeek 蓝队 + GLM 红队 + 锚判卷死标量 + `/api/redteam_probe` + 30 分钟 cron 看门狗，CI 152）；WBS-T0-5 可封装性维度占位（T3 接入）。

**剩余**：人终审 UI 收口 + 对外演示物料。**验收**：① 成本权重变→Pareto 前沿位移 ② 无锚证据者不进 ranked ③ 域外候选必剪枝。

---

### T1 光电接口层 + 电学/TCAD 数值内核（T1 解锁层）（0–18 月 · 170–190 万研发 / A 档对外订单 150–300 万）

> 🔴 **本期为红线重排的最大变化**：在原 T1 光电接口层（紧凑模型/网表/Cadence）基础上，新增 **T1-B 电学/TCAD 数值内核（T1 解锁层）** 与 **T1-C 主动器件扩展**，三者并列。

#### T1-A 光电接口层（原 · 110 万研发 / A 档订单 150–300 万）
**范围**：网表从拓扑骨架升级为**带参数紧凑模型**：VπL / 3dB 带宽 / 结电容 / 响应度 / 暗电流 / 电极 RC。接进 Cadence（行为级模型 + 带参数网表）。
**已有基础**：`parasitic_rc.py` + B29 热光锚 + B33 探测器 RC 带宽锚（v0.9.67 已发）。
**明确不做**：SerDes / DSP / 眼图 / BER（电域求解器 T2 锁死项，见 §1.1）。

**WBS**：
1. 紧凑模型参数 schema 定义（VπL/带宽/结电容/响应度/暗电流/电极RC）。
2. 参数化网表生成器（SPICE 兼容）+ Cadence 导入适配。
3. 参数↔物理映射校准（接 B29 热光锚 + B30 读出保真度锚 + B33 RC 带宽锚做死标量比对）。
4. 反向护栏：参数扰动 ±20% 时域响应必须变。

**验收**：参数扰动 ±20% → 时域响应**必须变**（防常数假绿）；与 Cadence 导入往返一致。

#### T1-B 电学/TCAD 数值内核（T1 解锁层 · 🔴 新增）
**范围**：T1 电学内核 = 泊松方程（电势/电场）+ 漂移-扩散 + 连续性方程，求载流子浓度分布 N(x)；与 Foundry TCAD T1（载流子输运 PDE）**同一内核**。主权 B 级（DEVSIM Apache-2.0 fork 镜像冷备）/ C 级（自研）。
**解锁前提**：§1.1 的 5 条铁律 + EAR 744.23 成熟节点用途声明。

**WBS**：
1. **DEVSIM Apache-2.0 fork 镜像冷备**：锁定 commit + 离线 wheel 清单 + 离线构建验证脚本（PR 门禁跑通即证明不依赖外网/A 级）。主权扫描登记为 B 级。
2. **自研 drift-diffusion + 连续性 1D 内核**（C 级兜底）：泊松（Gummel / Newton 迭代求电势）→ 漂移-扩散求 N(x)/P(x) → 与 DEVSIM 同几何同边界**双向标定**（判据 D：残差随网格加密单调下降，N(x) 误差 ≤ 容差）。
3. **验证纪律护栏**：T1 输出**不作 ORACLE** 守卫（死标量仍由物理定律锚/文献/foundry 实测定）；反向测试——把 T1 输出当 ORACLE 喂判决回路必须被拒。
4. **成熟节点用途声明 + EAR 744.23 合规件**：写入 `docs/` + CI 门禁（缺声明/离线构建证据即红）。
5. **subprocess 隔离**：若调 DEVSIM 走子进程，仅回标量，沿用 Meep 隔离纪律（不污染核心求解链）。

**验收**：① N(x) 与 DEVSIM 双向标定 Δ≤容差且网格收敛单调 ② T1 输出不作 ORACLE 守卫必响 ③ 离线构建证据齐备 + 用途声明齐备 ④ import+license 双扫确认零 A 级。

**风险**：DEVSIM 方程自定义门槛高；自研 drift-diffusion 数值稳定性（Newton 不收敛/载流子守恒）。规避：先 1D p-n 结基准（Sze 教科书闭式 N(x) 作 golden），再扩 2D。

#### T1-C 主动器件扩展（A 档闭式 + B 档部分真求解）
**A 档（闭式/行为层，已授权，不依赖 T1-B）**：复用 B28/B29/B33 已验证范式，扩到 ~8 锚闭环。
- WBS-C1：Si 载流子色散相移（Soref-Bennett `Δn_eff=-k_n·N`，耗尽近似闭式给 N，不求解漂移-扩散）+ 独立交叉候选（与 B28 同性质）。
- WBS-C2：EAM-QCSE 位移闭式（Franz-Keldysh / QCSE 波长位移）+ 独立候选。
- WBS-C3：探测器带宽 τ=RC 闭式（B33 已立，扩到 APD 倍增闭式）。
- WBS-C4：激光器腔体 FSR / 布拉格波长 / 线宽闭式（复用 B15 布拉格范式，不碰 L-I/增益）。
- 每道按 VMM 走：`provenance=design_rule_anchor` 或 `independent_cross_check`；执行节奏：**先出锚题契约评审，再写码**。

**B 档部分真求解（🔴 经 T1-B 内核可做，原锁死项放开）**：
- WBS-C5：载流子动力学（漂移-扩散真求解 N(x,t) → 调制响应）+ 与 A 档闭式 N 交叉验证（闭式 N 作 golden 下限）。
- WBS-C6：探测器带宽真求解（渡越时间 / 暗电流 / APD 倍增经 T1-B 真算）→ 与 B33 闭式 τ=RC 交叉验证。
- **仍锁死**：激光 L-I / 增益谱 / SOA（需增益介质模型 + A 级，维持禁区，除非后续单独裁定）。

**验收**：A 档每锚方法学独立（判据 D 真数值收敛）；B 档真求解与闭式下限交叉验证误差公开；激光 L-I/增益类零代码（主权/红线守护断言必响）。

---

### T2 真值链（6–24 月 · 240 万）

**范围**：3–4 轮 MPW + 委托封测 + 实测回流规范。**先委托测试、后自建**（封测占近八成成本）。🔴 **T2 工艺真值（implant/diffusion/etch/stress / foundry 专有 ORACLE）仍永久锁死**，只消费 PDK 接口。

**WBS**：
1. MPW 排期与 foundry 对接（IMEC/CEA-Leti/TSMC/GF，按器件定）。
2. 委托封测协议（ficonTEC 类耦合设备年产能仅 30–40 台，交期 5–7 月，须早锁）。
3. 实测回流规范：把外部实测写入 `lda_pdk/empirical.py` 的 `review_measurement`。
4. **≥5 个自证桩换真锚**：优先选 24 个自证桩中物理上可有公开实测/自有 MPW 的。
5. 工艺角由假设分布 → 实测分布（S7 蒙特卡洛最坏情况 margin p5 改为实测支撑）。

**🔴 与 T1-B 的衔接（新增）**：B 档「载流子动力学/探测器带宽真求解」已在 T1-B 用 T1 内核算出，但其输出**不作真值 ORACLE**；T2 的 MPW 实测回流是这些真求解的**标定与验收来源**——T1-B 给行为预测，T2 给真值标定，二者互补不替代。

**验收**：≥5 自证桩换真锚；工艺角由假设变实测分布；**实测与仿真偏差公开标注**。

**风险**：MPW 排期长、封测设备交期长 ⇒ 必须 6 月启动询价与锁单。

---

### T3 设计侧虚拟制造 2.0（12–30 月 · 研发人力承担，不单列预算）

**🔴 战略修正（关键）**：不能只做工艺波动（光刻/刻蚀→CD 偏差→工艺角）。八成成本在封装与耦合，**真正值钱的设计优化是「可封装性」**——设计阶段就算清耦合容差、对准失配、封装良率。

**WBS**：
1. 工艺波动链路（巩固）：CD 偏差→工艺角→良率。
2. **可封装性链路（新建）**：耦合容差预测 / 对准失配预算 / 封装良率模型，接 S13 设计良率锚 + P-CPO 间距几何下界护栏。
3. **载流子分布→封装良率链路（T1-B 赋能，新增）**：用 T1-B 的 N(x) 预测调制/探测行为漂移 → 反推对耦合容差/对准失配预算的影响，给设计师「这版布局会让封测良率掉 X%」的实时反馈。
4. 设计决策→封装良率反向追溯。

**验收**：设计改动 → 封装良率预测**同向变化且可复现**；耦合容差/对准失配预测与实测对标误差公开。

---

### T4 标准位与生态（24–36 月 · 40 万）

**范围**：L0 IR / DesignPackage JSON 推标准位。🔑 「别人引用你的数」才是领袖，不是功能全。唯一有领袖相资产 = **L0 IR / DesignPackage JSON**。🔴 前提：标准是被采用出来的，当前 users=1。

**WBS**：
1. L0 IR schema 冻结 + 文档化（中英）。
2. 对外采用计划：≥2 家外部机构采用 + ≥1 家 foundry 签 PDK。
3. 生态开放治理三件套：判据先于代码 / 自动主权扫描（import+license 双扫，复用 `run_optional_import_guard_smoke.py` 的 ast 全包经验，**新增 DEVSIM/T1 内核许可扫描**）/ 贡献分级。

**验收**：≥2 家外部采用 + ≥1 家 foundry 签 PDK。

---

## 4. 融资节奏与里程碑解锁

**总额 1000 万（B 档真值+接口），T1 内核追加后约 1070 万（待拍板）**，拆两笔：

| 笔 | 金额 | 工期 | 解锁条件（三个可证伪里程碑，不达成不解锁下一笔） |
|---|---|---|---|
| **验证轮** | 100–150 万 | 12 月 | ① 2 轮 MPW 拿自有实测 ② ≥5 自证桩换真锚 ③ 1 个付费客户（A 档光电接口层订单优先） |
| **主力轮** | 800–1000 万（+T1 内核追加） | 24 月 | 以验证轮三个里程碑实际结果为锚 |

**🔑 估值纪律**：第一笔**不锁死估值**（可转债/SAFE 或小额天使）—— users=1、0 成交，估值无可验证支撑；第二笔以里程碑实际结果为锚。

**🔑 A 档（光电接口层 150–300 万）是「订单」不是融资** —— 客户已把范围划好，最可能落地首单，且能自造血。

**🔑 T1 内核对验证轮的增强（新增）**：T1-B 让 B 档「载流子动力学/探测器带宽真求解」从禁区变可演示——这是比原 09-09 版更强的「可证伪」素材，可在验证轮物料中作为**差异化技术资产**展示（但须诚实标注 T1 输出不作 ORACLE、T2 工艺真值仍锁死）。

---

## 5. 跨期治理与 CI 纪律

- **回归门禁**：发版前必跑 `--tag core` 全量回归（21–35min）看 `rc`；回归中不改被读文件；后台 `python -u`。
- **反向测试文化**：每个新护栏必须有反向测试证明会响；静态守卫 + 动态实跑两层都要。
- **判据先于代码**：无论是新锚、新求解器、还是生态 PR，先写死标量判据，再写实现。
- **主权扫描**：PR 门禁做 import + license 双扫（防 Meep=GPL v2+ 不可逆污染、防 B 级依赖「借今踢后」变「踢不掉」、**新增 DEVSIM/T1 内核 Apache-2.0 合规扫描**）。
- **🔴 EAR 744.23 合规门禁（新增）**：T1 内核相关任何交付/演示/文档缺「成熟节点用途声明 + 离线构建证据」→ CI 红。
- **三分类一致性**：README 账本 ≡ 本机 harness 推导 ≡ `/api/verification_ledger` 端点，任一漂移即红。

---

## 6. 实施组织与近期行动清单

> 假设编制 3 人（@50 万/人年），待杜先生拍板 3 vs 5 人 + T1 内核投入深度。以下 Sprint 1 已完项仅列结论，Sprint 2 为红线重排后立即可启动项。

### Sprint 1（0–90 天 · 已基本完成）
- [x] T0：Pareto / NLP 入口 / S8 接入 / 红蓝队闭环（CI 152）。
- [x] T1-A：紧凑模型 schema + SPICE 网表 + Cadence 适配（CI 146）。
- [x] T1-C（A 档首锚）：B33 探测器 RC 带宽锚（严格独立）发版 v0.9.67。
- [x] 验证轮融资物料草稿 + MPW 询价草案。

### Sprint 2（90–180 天 · 🔴 红线重排后新增/重排项）
**第 1 阶段（90–120 天）· T1 内核立项与主权合规**
- [ ] **T1-B-W1**：DEVSIM Apache-2.0 fork 镜像冷备 + 离线构建验证脚本（锁定 commit + 离线 wheel 清单）+ 主权扫描登记 B 级。
- [ ] **T1-B-W2**：自研 drift-diffusion + 连续性 1D 内核（Gummel/Newton 求电势 → N(x)）；Sze 教科书 p-n 结闭式 N(x) 作 golden。
- [ ] **T1-B-W3**：N(x) 与 DEVSIM 双向标定 + 判据 D 网格收敛 + T1 输出不作 ORACLE 守卫 + EAR 744.23 用途声明/合规件（写 `docs/` + CI 门禁）。
- [ ] **T1-C**：A 档扩展清单（Si 载流子色散 Vπ / EAM-QCSE / 探测器带宽 τ=RC / 激光器 FSR·布拉格·线宽）**锚题契约评审**（先评审后写码）。

**第 2 阶段（120–150 天）· T1-C 落地 + T2 启动**
- [ ] **T1-C-W1~W4**：A 档 4 类闭式锚各立物理定律锚 + 独立交叉候选，进 VMM（provenance 标 design_rule_anchor / independent_cross_check）。
- [ ] **T1-B-W4**：2D 载流子输运扩面（调制器截面 N(x,y)）+ 与 A 档闭式 N 交叉验证。
- [ ] **T2**：MPW 询价发出 + 封测设备交期锁单（最长 lead time 前置）。

**第 3 阶段（150–180 天）· B 档真求解 + 验证轮物料**
- [ ] **T1-C-W5/W6**：B 档载流子动力学 / 探测器带宽真求解（经 T1-B）+ 与闭式下限交叉验证，误差公开。
- [ ] **T2**：首轮 MPW 排期确认 + ≥5 自证桩换锚路线细化。
- [ ] 验证轮融资物料更新：加入 T1 内核差异化技术资产（诚实标注 ORACLE 纪律 + T2 工艺真值锁死）。

---

## 7. 外部事实与数字对齐（防旧值）

| 项 | 正确现值（2026-09-11 复核） | 作废旧值（勿复用） |
|---|---|---|
| 版本 | **v0.9.67** | v0.9.66 及更早 |
| CI core | **157** | 152 / 141 / 139 |
| 锚三分类 | **53（28 严格 / 1 降级 / 24 自证）** | 52（26/1/25）· 50（23/0/25） |
| 可被外部验货比例 | **28/53** | 26/52 · 25/50 |
| 货架 | **75**（5 赛道 + 24 应用域） | 58 · 65 |
| GC-* | **48 全 PASS** | — |
| 生产四赛道 | **A5/B4/C4/D4 共 17 任务** | 13 任务 |
| 融资口径 | **1000 万（B 档）两笔 + T1 内核追加 ~70 万** | 300–500 万 |
| 定位 | **光电接缝上的可证伪设计入口（+ T1 载流子输运内核解锁）** | 「光/量设计软件」 |
| 三不做 | **改为 T1 可解锁 / T2 锁死 / 封测锁死（分层边界）** | 电域求解器/Foundry TCAD 全不做 |
| proposal_compiler 判据 | **15**（实测） | 16（旧记忆/BP/手册三处均已订正） |

🔴 **核实外部事实用定位符（DOI/URL）为唯一入口**（v0.9.58 重大事故）：不可用作者名检索后否定定位符；下「记录错了」结论前先排除「存在另一篇同作者/同器件论文」。

---

## 8. 风险总表与规避

| 风险 | 等级 | 规避 |
|---|---|---|
| 自证桩假绿（24 个老病；外部贡献批量生产） | 🔴🔴 | 判据先于代码 + 判据由 maintainer 写 + 反向测试必响 |
| 标签≠行为（已 8 次血案） | 🔴🔴 | grep 实际调用点；加字段打到端点并真跑生产代码；不动内嵌副本 |
| 主权污染（Meep GPL v2+ / DEVSIM 误引 A 级） | 🔴 | import+license 双扫 PR 门禁（含 T1 内核 Apache-2.0 合规扫描）；B 级借今踢后 |
| **T1 内核破 ORACLE 纪律（输出当真值）** | 🔴🔴 | **T1 输出不作 ORACLE 守卫 + 反向测试必响**；死标量仍由物理定律锚/文献/foundry 实测定 |
| **EAR 744.23 出口管制（T1 内核用于先进节点）** | 🔴🔴 | **成熟节点用途声明 + 离线构建证据 + CI 门禁**；开源≠不受 IP 影响，明知先进用途即停 |
| 许可不可逆（DCO 缺失） | 🔴 | 现在加最轻量 DCO（待拍板） |
| 融资边界模糊（估值无支撑） | 🔴 | 第一笔不锁估值；里程碑解锁；A 档订单自造血 |
| 封测 lead time 堵真值回流 | 🔴 | T2 第 1 月即锁 MPW+封测设备 |
| 红队假独立（同源模型偏见） | 🔴 | 红蓝不同模型/提示/数据源；命中率=被锚确认缺陷÷出题数（死标量） |
| 一人公司流片级交付信任门槛 | ⚠️ | 可外部验货 + 责任边界清晰（仅担 3-5 段），不靠吹 |

---

## 附录：判据与缩写速查

- **判据 D**：扫候选自身离散参数恒 ~1e-16 ⇒ 假独立；须双向标定（含反向：参数扰动必须响应）。
- **S7 统计锚**：蒙特卡洛最坏情况 margin p5>0，固定种子确定性。
- **P-CPO / D-67**：间距几何下界对称夹逼。
- **S13 良率锚**：设计良率锚，T3 可封装性链路核心。
- **L0 IR / DesignPackage JSON**：光子界语义交换标准候选。
- **GC-\***：整芯片对标（48 条全 PASS）。**MPW**：多项目晶圆。**PDK**：工艺设计套件。
- **T1 / T2 分层**（本版新增）：T1=可解锁数值内核（载流子输运 PDE，B/C 级主权）；T2=锁死（工艺真值 / 电-光耦合增益 / A 级商业 / 封测产线 / 流片）。
- **DEVSIM**：Apache-2.0 有限体积 TCAD，用户自定义方程，本项目 T1 内核 B 级依赖（fork 镜像冷备）。
- **B 档/A 档/C 档**：融资三档（见 BP 修订版 §2.2）。
