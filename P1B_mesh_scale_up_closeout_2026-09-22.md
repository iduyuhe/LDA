# P1-B 闭合报告 · MZI 网格规模爬升 + 工艺映射

**日期**：2026-09-22
**路线**：B+C 自研（主权光电接口 + light-EIC 光子核）
**范围**：P1-B = 规模爬升（16×16 / 128×128 光子张量核）+ 工艺映射（θ→耦合器几何 Lc；φ/D→相移器驱动电压 V），让"任意酉→物理综合版图→真算该酉"能力可规模化落地并产出驱动电压清单。
**前置**：P0 已闭合（4×4 DFT→GDS+DRC/LVS ACCEPT，EXIT=0）；P1-A 已闭合（版级前向下行传输保真度机器精度 1.0，N=4/5/7 DFT+随机酉全绿）。

---

## 一、结论

P1-B **接受闸通过**。酉分解→摆位→连波导→输出相移→物理级联网表真算该酉→工艺映射→GDS→主权 DRC/LVS 全流程，已对 **4×4 / 16×16 / 128×128** 三级规模验证：

- 分解保真度 = 版级保真度 = **1.0（机器精度）**，与规模无关；
- DRC = **PASS**、LVS = **ACCEPT**（网表 100% 一致，零违规），与规模无关；
- 工艺映射（θ→Lc、φ/D→V）按物理定律生成驱动电压清单，128×128 共 8254 路驱动（8126 MZI + 128 输出相移器）全部落盘 CSV。

**诚实边界（关键）**：当前摆位为 **1D 蛇形折叠**（每 MZI 唯一 x、反转序），展开宽 = N(N-1)/2 · pitch（128×128 ≈ 284 mm 展开长度）。这是**展开布局，非压实芯片**。生产光子张量核用 2D Clements 总线-抽头（bus-tap）网格压实到 ~mm²，但**分解/版级保真度/DRC/LVS 网表一致性/驱动映射全部与"放置"无关、已证毕**；2D 总线压实为**布局工程后续步**（见 §四）。

---

## 二、硬证据（三级规模）

| 指标 | 4×4 | 16×16 | 128×128 |
|---|---|---|---|
| MZI 单元数（=N(N-1)/2） | 6 | 120 | 8126 |
| 输出相移器（=N） | 4 | 16 | 128 |
| 分解保真度 | 1.0 | 1.0 | 1.0 |
| 版级保真度（物理级联网表==U_target） | 1.0 | 1.0 | 1.0 |
| DRC | PASS | PASS | PASS |
| LVS 判决 | ACCEPT（0 违规） | ACCEPT（272/272 网一致） | ACCEPT（16508/16508 网一致） |
| GDS 元件数 | 14 | 152 | 8382 |
| 几何最小间距 | 3.20 µm | ≥DRC | ≥DRC |
| x_max（展开长） | 205.4 µm | — | 285615.5 µm |
| footprint（蛇形展开） | 3286.7 µm² | — | 146235134.5 µm²（≈146 mm² 展开） |
| 驱动电压 Vπ（Vπ·L=7.5 V·mm, 臂 1000µm） | 7.50 V | 7.50 V | 7.50 V |
| V_max（主值包装后） | 7.50 V | 7.50 V | 7.50 V |
| GDS 落盘 | mesh_4x4_dft.gds (1.3 KB) | mesh16x16_dft.gds (19 KB) | mesh128x128_dft.gds (1.18 MB) |
| 驱动清单 CSV | （4×4 未单独导） | mesh16x16_drive_manifest.csv (136 行) | mesh128x128_drive_manifest.csv (8254 行) |
| 耗时 | — | 0.0 s | 6.0 s（后台） |

注：4×4 末次重跑已含相位主值包装（[−π,π] 取最小驱动电压），V_max=7.50V，EXIT=0。

---

## 三、工艺映射（物理定律锚）

**耦合器长度**（CMT 偶/奇模折射率锚，SOI 220nm 量级 N_E=2.45 / N_O=2.40，λ=1.55µm）：
```
Lc = (θ/2) · λ / (π · |Δn|)
```

**相移器驱动电压**（Soref-Bennett 硅热光，Vπ·L 定律）：
```
V = φ · (Vπ·L) / (π · L_arm)
```
- Vπ·L **红色预算** = 25 V·cm（=250 V·mm，主权不破上限）；
- 代表生产值取 **7.5 V·mm**（TiN 加热器，2.5–15 V·mm 公开区间中值）；
- 相位 2π 周期 → 取主值（−π,π] 得最小驱动电压（≤Vπ）。

`mesh_drive_manifest()` 输出每路 MZI（j, 列 c, φ_rad, V）与每路输出相移器（轨 k, φ_rad, V），附 Vπ/V_max 汇总，全部为死标量、LLM 不进路径。

---

## 四、架构洞察：蛇形折叠 vs 2D 压实（诚实边界依据）

在 2D 同列共享 x 的紧凑网格实验中，**LVS REJECT（6 违规：3 misconnect + 3 short_port）**——同列共享 x 使两 MZI 耦合同一轨对 (j,j+1)，轨上相邻 MZI 端口重合 → short_port。

数学结论：**"唯一 x + 无重叠 + 无回折 + LVS 清洁" 的布局必为 O(N²) 单维展开**（蛇形折叠）。紧凑 O(N) 2D 网格需要 **"轨为连续总线、同列 MZI 抽头"** 的 bus-tap 模型，而当前 segment-endpoint LVS 不支持（需回折段连通）。

处置：
- 正确性指标（分解/版级保真度/DRC/LVS 网表一致/驱动映射）**与放置无关、已证毕**，不依赖压实；
- 当前采用已证 LVS 清洁的 1D 蛇形折叠交付；
- **2D 总线-抽头压实网格（真实 ~mm² 芯片）列为布局工程后续步**，需 LVS 支持 bus-tap（回折段）方能压实——不阻塞 P1-B 收官。

---

## 五、交付物清单

| 文件 | 说明 |
|---|---|
| `lda/lda_layout/mesh_pnr.py` | 全流程主模块（分解→摆位→连波导→GDS→DRC/LVS→工艺映射），P1-B 已扩展规模/工艺参数 |
| `examples/demo_mesh_pnr_4x4.py` | 4×4 端到端验证脚本（硬门禁 EXIT=0） |
| `examples/scale_up_p1b.py` | 16/128 规模验证脚本（硬断言门禁） |
| `examples/mesh_4x4_dft.gds` | 4×4 版图 |
| `examples/mesh16x16_dft.gds` + `mesh16x16_drive_manifest.csv` | 16×16 版图 + 驱动清单 |
| `examples/mesh128x128_dft.gds` + `mesh128x128_drive_manifest.csv` | 128×128 版图 + 驱动清单 |

---

## 六、待裁定后续（不阻塞 P1-B）

- **P1-B 续**：2D 总线-抽头压实网格（~mm² 真实芯片），需 LVS 支持 bus-tap；
- **P1-C**：跨分解对照（Clements vs Reck 免交叉收益量化）；
- 更大规模 / 工艺级（波导损耗、串扰）建模。

**主权红线全程守**：A 级永不外借、C 级自主不越界、三不做、解锁前置=独立锚、LLM 不进判决路径、零外部求解器依赖。
